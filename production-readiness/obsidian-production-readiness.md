# scriptureforge-production-readiness

---
title: ScriptureForgeAI Production Readiness
type: dashboard
status: active
owner: "team"
updated: 2026-07-01
tags:
  - production-readiness
  - serena
  - obsidian
  - scriptureforge
---

## Commanding View

- Core execution plan: [[SF-roadmap|SF-roadmap.md]]
- Stability boundary/assumptions: [[SF-architecture|SF-architecture.md]]
- Audit authority: [[FUNCTIONALITY_AUDIT_BRIEFING|FUNCTIONALITY_AUDIT_BRIEFING.md]]
- Compact repo context: [[REPO_BRIEF|REPO_BRIEF.md]]

## Obsidian-Trackable Production Gaps

```dataview
TASK
FROM "production-readiness"
WHERE contains(file.name, "obsidian-production-readiness")
```

Use this note as a lightweight tracker for unresolved external blockers and local proof gaps before declaring production-ready status.

Serena setup source: [[serena-setup|production-readiness/serena-setup.md]]

- [ ] **F-AUD External Evidence Closure**: close `production-readiness/staging-evidence.staging.json` blockers with deployed artifacts.
- [ ] **CI Release Evidence Upload**: run CI on the exact release `git_head` and ingest `ci-release-evidence` into manifest.
- [ ] **Terraform Live Validation**: capture real `plan/apply` + TLS/ingress/secret/IRSA proofs against staging.
- [ ] **Staging AI Validation**: run real provider/degradation/citation/audit captures with `aiprobe` verified marker summaries and persist `EXT-AI-001`.
- [ ] **Staging Zoom Validation**: run real webhook/fallback/mapping captures with `zoomprobe` verified marker summaries and persist `EXT-ZOOM-001`.
- [ ] **Web/Mobile Staging Smoke**: verify login/journal/room/WebSocket flows against staging endpoints.
- [x] **Zoom Exact Release Binding Guard**: `tools/zoomprobe`, the staging evidence recorder, and strict staging manifest validation require Zoom OAuth, meeting/fallback, resilience, webhook, URL-validation, duplicate, and mapping artifacts to carry exact `release_candidate=<manifest release_candidate>` plus `service_version` markers before `EXT-ZOOM-001` can support final readiness.
- [x] **Zoom Per-Segment Provenance Guard**: strict staging validation requires `staging artifact` provenance plus exact release/service markers on every Zoom OAuth, meeting/fallback, timeout/circuit fallback, webhook signature, URL-validation, duplicate, and mapping proof segment before `EXT-ZOOM-001` can support final readiness.
- [x] **Zoom Mapping ID Guard**: `tools/zoomprobe`, the staging evidence recorder, and strict staging manifest validation require room-mapping proof to carry concrete `meeting_external_id=<id>` and `internal_room_id=<id>` values before `EXT-ZOOM-001` can support final readiness.
- [x] **Zoom Distinct Artifact Guard**: `tools/zoomprobe`, the staging evidence recorder, and strict staging validation require OAuth, meeting/fallback, timeout/circuit fallback, webhook signature, URL-validation, duplicate idempotency, and meeting-to-room mapping proof to use distinct HTTPS artifact URLs plus `distinct_zoom_artifacts=true`.
- [x] **Zoom Artifact Alias Guard**: `tools/zoomprobe` canonicalizes Zoom evidence artifact URLs before distinctness checks, so default-port, host-case, query-order, or fragment aliases cannot satisfy separate OAuth, meeting/fallback, resilience, webhook, URL-validation, duplicate, and mapping proof roles.
- [x] **Zoom Artifact Host Guard**: `tools/zoomprobe` rejects reserved placeholder hosts such as `.example`, `example.com`, `.test`, and `.invalid`, plus localhost, loopback, private-network, unspecified, and link-local hosts before emitting Zoom OAuth, meeting/fallback, resilience, webhook, URL-validation, duplicate, or mapping evidence.
- [x] **Zoom Webhook Signature Contradiction Guard**: `tools/zoomprobe`, the staging evidence recorder, and strict staging manifest validation reject Zoom webhook evidence that claims signature verification was disabled, bypassed, or skipped, even if the summary also contains required signed/invalid/replay markers for `EXT-ZOOM-001`.
- [x] **Zoom Webhook Signature Value Guard**: `tools/zoomprobe`, the staging evidence recorder, and strict staging manifest validation require concrete `x-zm-signature=<v0 signature>` and `x-zm-request-timestamp=<epoch>` markers before webhook signature evidence can support `EXT-ZOOM-001`.
- [x] **Zoom Webhook Outcome Guard**: `tools/zoomprobe`, the staging evidence recorder, and strict staging manifest validation require structured `stale_rejected=true`, `replay_rejected=true`, `invalid_signature_rejected=true`, and `signed_delivery_accepted=true` markers before webhook signature evidence can support `EXT-ZOOM-001`.
- [x] **Zoom Duplicate Tracking ID Guard**: `tools/zoomprobe`, the staging evidence recorder, and strict staging manifest validation require duplicate webhook idempotency proof to include a concrete `x-zm-trackingid=<id>` marker and structured JSON `tracking_id` before duplicate evidence can support `EXT-ZOOM-001`.
- [x] **Zoom Meeting/Fallback Probe Guard**: strict-release validation now requires `zoom-meeting-create-or-fallback` itself to prove either a Zoom `join_url` or `offline://in-person` fallback, so timeout fallback markers from a different probe cannot satisfy meeting creation evidence.
- [x] **Zoom Duplicate Delivery ID Guard**: `zoomprobe`, the staging evidence recorder, and strict release validation require duplicate webhook idempotency proof to include both the Zoom tracking header marker `x-zm-trackingid=<id>` and `delivery_id=<id>` in the `zoom-duplicate-webhook-idempotency` segment; `zoomprobe` also emits structured JSON `tracking_id` and `delivery_id` fields and the recorder rejects reports where either field is missing or does not match the summary marker.
- [x] **Zoom Report Run Identity Guard**: `tools/record-staging-evidence.mjs` now rejects release-bound `EXT-ZOOM-001` reports unless the report itself carries `load_run_id`, preventing hand-edited Zoom OAuth/webhook/fallback/mapping reports from relying only on per-segment run markers.
- [x] **Zoom Mapping Retry Guard**: local webhook tests prove a signed delivery whose room lookup temporarily fails does not consume its Zoom tracking ID, so the same delivery can retry and mutate the mapped internal room once after mapping recovers; deployed webhook evidence remains tracked under `EXT-ZOOM-001`.
- [x] **Zoom Ambient Mock Removal Guard**: Zoom client tests now use explicit fake HTTP transports for success paths, and missing credentials always return the offline `offline://in-person` fallback plus `credential_or_token_fallback` metrics even when `GO_ENV=testing`; dependency spans no longer normalize `mock_success` as production success.
- [x] **Go Zoom Test Proof Marker Guard**: `tools/run-go-core-gate.mjs --mode test` now requires verbose PASS lines for local Zoom timeout fallback, retry/circuit fallback, offline fallback, dependency metrics, webhook signature/replay denial, duplicate idempotency, retry-safe delivery handling, URL validation, bounded delivery cache, and meeting-to-room mapping tests before emitting `go-test-gate` proof. Live Zoom staging evidence remains tracked under `EXT-ZOOM-001`.
- [x] **AI Probe Artifact Host Guard**: `tools/aiprobe` rejects localhost, loopback, private-network, unspecified, link-local, and `local-only` provider/generation/degradation/citation/audit artifacts before emitting `EXT-AI-001`.
- [x] **AI Reserved Artifact Host Guard**: `tools/aiprobe` rejects reserved placeholder hosts such as `.example`, `example.com`, `.test`, and `.invalid` before emitting AI provider, generation, degradation, citation, or audit evidence.
- [x] **AI Exact Release Binding Guard**: `tools/aiprobe`, the staging evidence recorder, and strict staging manifest validation require AI provider, generation, degradation, citation, and audit artifacts to carry the exact `release_candidate=<manifest release_candidate>` plus `service_version` markers before `EXT-AI-001` can support final readiness.
- [x] **AI Distinct Artifact Guard**: `tools/aiprobe`, the staging evidence recorder, and strict staging validation require provider config, authenticated generation, timeout/degradation, citation verification, and audit persistence proof to use distinct HTTPS artifact URLs plus `distinct_ai_artifacts=true`.
- [x] **AI Artifact Alias Guard**: `tools/aiprobe` canonicalizes AI evidence artifact URLs before distinctness checks, so default-port, host-case, query-order, or fragment aliases cannot satisfy separate provider, generation, degradation, citation, and audit proof roles.
- [x] **AI Per-Segment Provenance Guard**: strict staging validation requires `staging artifact` provenance plus exact release/service markers on every AI provider config, generation route, timeout/degradation, citation verification, and audit persistence proof segment before `EXT-AI-001` can support final readiness.
- [x] **AI Citation/Audit Segment Guard**: strict-release validation now requires `ai-citation-verification` itself to prove no-citation rejection, hallucinated-citation rejection, verified-citation acceptance, and `citation_trails`, while `ai-audit-persistence` itself must prove `ai_request_logs`, `citation_trails`, tenant RLS, and cross-tenant hidden audit persistence.
- [x] **AI Citation/Audit Contradiction Guard**: `tools/aiprobe`, the staging evidence recorder, and strict staging manifest validation reject AI evidence that claims citation verification, citations, audit logging, audit persistence, `ai_request_logs`, or `citation_trails` were disabled, even if required citation/audit markers also appear.
- [x] **AI Audit Request ID Guard**: `tools/aiprobe` emits a structured JSON `request_id` for `ai-audit-persistence`, and the staging evidence recorder rejects missing or mismatched request IDs before `EXT-AI-001` can be recorded.
- [x] **AI Generation/Audit Request Binding Guard**: `tools/aiprobe`, the staging evidence recorder, and strict-release validation require `ai-generation-route` and `ai-audit-persistence` to carry the same concrete `request_id`, so deployed AI generation proof cannot be paired with an unrelated audit row.
- [x] **AI Citation Trail ID Binding Guard**: `tools/aiprobe`, the staging evidence recorder, and strict-release validation require structured `citation_id` proof on both `ai-citation-verification` and `ai-audit-persistence`, and the IDs must match so citation-verification evidence cannot be paired with an unrelated audit row.
- [x] **AI Tenant Principal Binding Guard**: `tools/aiprobe`, the staging evidence recorder, and strict-release validation require `ai-generation-route` and `ai-audit-persistence` to carry matching concrete `organization_id=<id>` and `user_id=<id>` markers plus structured report fields, so AI generation proof cannot be paired with an unrelated tenant/user audit row.
- [x] **AI Report Run Identity Guard**: `tools/record-staging-evidence.mjs` now rejects release-bound `EXT-AI-001` reports unless the report itself carries `load_run_id`, preventing hand-edited AI provider/generation/degradation/citation/audit reports from relying only on per-segment run markers.
- [x] **AI Structured Manifest Guard**: `tools/record-staging-evidence.mjs` now persists `structured_report.ai_generation_audit_proof` for `EXT-AI-001`, and strict-release validation rejects AI evidence whose structured provider config, generation principal/request, fail-closed timeout booleans, citation ID, or audit principal/request/citation drift from the recorded aiprobe summary markers.
- [x] **Bounded Local Abuse Limiter State**: backend auth/AI/journal/rooms/WebSocket limiter buckets prune expired windows and cap identity-spray growth with `ABUSE_LIMIT_MAX_BUCKETS`; login also enforces hashed account-scoped `ABUSE_LIMIT_AUTH_ACCOUNT_*` throttling across rotating client IPs, and refresh enforces token-scoped throttling with canonical `ABUSE_RATE_LIMIT_FAULT` responses without exposing account, token, or IP identifiers in limiter metrics.
- [x] **Mounted Route Abuse Profile Guard**: `cmd/platform-engine/routes_test.go` proves every mounted sensitive public route variant is protected by its configured abuse profile, including canonical and legacy auth, refresh/logout, MFA, workspace switch, canonical and legacy AI generation, journal bootstrap/list/create/read, room create/list/state polling, and WebSocket stream upgrade.
- [x] **Abuse Evidence Header Semantics Guard**: `tools/abuseprobe`, the staging evidence recorder, and strict-release validation require deployed `429` proof to include positive `Retry-After`, `X-RateLimit-Limit`, and `X-RateLimit-Reset` values plus `X-RateLimit-Remaining=0`, so decorative or non-exhausted rate-limit headers cannot satisfy `ABUSE-LIMIT-001`.
- [x] **Go Abuse Route Proof Marker Guard**: `tools/run-go-core-gate.mjs --mode test` now requires verbose PASS lines for mounted auth, legacy auth alias, AI, journal, rooms, and WebSocket rate-limit route tests before emitting `go-test-gate` proof, so local Go gate evidence cannot drop abuse coverage for sensitive production routes.
- [x] **Abuse Repeated Attempt Guard**: `tools/abuseprobe`, the staging evidence recorder, and strict-release validation require `repeated_attempts_verified=true` plus structured `attempts >= 2`, so a route that returns `429` on the first request cannot satisfy `ABUSE-LIMIT-001`.
- [x] **Trusted Proxy Abuse Identity Guard**: when `TRUST_PROXY_HEADERS=true`, the backend limiter ignores private, loopback, unspecified, and link-local forwarded IP candidates before choosing a client identity, and `tools/abuseprobe` rejects reserved documentation/sample, private-network, local, unspecified, and link-local staging API/origin/config-artifact evidence URLs.
- [x] **Abuse Segment Guard**: strict-release validation now requires auth, account-scoped login, refresh-token, AI, journal, rooms, WebSocket upgrade, and config artifact markers to appear in their own `ABUSE-LIMIT-001` abuseprobe segments, including exact `account_scoped=true`, `forwarded_client_ip_rotated=true`, `refresh_token_scoped=true`, and `websocket_upgrade=true` markers where applicable, so one throttled route or config summary cannot satisfy another route's proof.
- [x] **Abuse Exact Release Binding Guard**: `tools/abuseprobe`, the staging evidence recorder, and strict staging manifest validation require every `ABUSE-LIMIT-001` profile and config-artifact summary to carry exact `release_candidate=<manifest release_candidate>` plus `service_version` markers before abuse evidence can support final readiness.
- [x] **Abuse Distinct Config Artifact Guard**: `tools/abuseprobe`, the staging evidence recorder, and strict staging manifest validation require the redacted abuse config artifact to use a canonical-distinct evidence host from the staging API target and reported HTTPS `web_origin`, including host-case and trailing-dot aliases, and to preserve `distinct_abuse_artifacts=true` in recorded evidence.
- [x] **Abuse Structured Manifest Guard**: `tools/record-staging-evidence.mjs` now persists `structured_report.abuse_rate_limit_proof` for `ABUSE-LIMIT-001`, and strict-release validation rejects abuse evidence when config verification, `ABUSE_LIMIT_*` assignments, per-profile attempts, concrete rate-limit headers, or account/refresh/WebSocket scoped booleans drift from the recorded `abuseprobe` summary.
- [x] **Probe IPv4-Mapped IPv6 Host Guard**: all staging evidence probe families include regression coverage proving IPv4-mapped private IPv6 evidence hosts are rejected before tenant/RLS, load/performance, TLS/web, abuse, AI, Zoom, Rust, mobile, deployment, security, CI, observability, or resilience evidence can be emitted.
- [x] **Strict Evidence Marker Guard**: strict staging evidence validation rejects probe summaries or artifacts marked private-network, link-local, unspecified, or IPv4-mapped in addition to localhost/loopback/local-only/mock/dry-run markers, and the generated Obsidian evidence contract now names those exclusions.
- [x] **Probe Report Family Artifact Regression Guard**: strict-release validation regression coverage now mutates every probe-backed blocker family to local and wrong-family report artifacts, proving each item still requires its own HTTPS non-local `tools/<probe>` evidence report before final readiness can pass.
- [x] **Security Evidence Provenance Guard**: `tools/securityprobe`, the staging evidence recorder, and strict staging validation require `staging artifact` provenance on every `SEC-SECRETS-001` IRSA/CSI/synced-secret/IAM/access-test proof segment and the `SEC-DBUSER-001` database-scoped-user segment before security evidence can support final readiness.
- [x] **Security Secret Identity Guard**: `tools/securityprobe`, the staging evidence recorder, and strict-release validation require `SEC-SECRETS-001` evidence to name the staged workload identity with `namespace=staging`, `service_account=scriptureforge-api`, and the same concrete `role_arn=arn:aws:iam::<12-digit-account>:role/<role>` marker on the relevant IRSA, SecretProviderClass, IAM policy, and scoped access-test segments before secret-sync evidence can support final readiness.
- [x] **Reserved Placeholder Host Guard**: strict staging evidence validation rejects reserved documentation/sample hosts such as `.example`, `example.com`, `.test`, and `.invalid`, so HTTPS sample domains cannot satisfy production artifact proof.
- [x] **Staging Evidence Contract Local Gate Proof Marker Guard**: `tools/sync-staging-evidence-contract.mjs --manifest production-readiness/staging-evidence.example.json --contract-manifest production-readiness/staging-evidence.example.json --check` emits proof markers for checked-in staging and contract manifest validation, pending_external required-evidence comparison, non-pending evidence preservation, and zero contract drift; `tools/validate-local-gate-report.mjs` rejects local gate reports whose `staging-evidence-contract-check` output lacks those markers.
- [x] **Recorded Evidence Reserved Host Guard**: `tools/record-staging-evidence.mjs` rejects reserved documentation/sample hosts such as `.example`, `example.com`, `.test`, and `.invalid` across shared probe-backed evidence target/artifact ingestion, so manually edited or stale probe JSON cannot record sample-domain evidence into the staging manifest.
- [x] **Recorded Evidence Artifact Alias Guard**: `tools/record-staging-evidence.mjs` canonicalizes artifact URLs before distinctness checks across probe-backed evidence, so default-port, host-case, query-order, or fragment aliases cannot satisfy separate staging proof roles.
- [x] **Release-Wide Load Run Identity Guard**: `tools/record-staging-evidence.mjs` and strict-release validation require all staging evidence summaries that carry `load_run_id=<id>` to agree on one release-level staging run, so deployment, RLS, Rust, web/mobile, AI, Zoom, observability, security, resilience, abuse, and performance proof cannot be stitched together from unrelated captures.
- [x] **Load Minimum Measurement Guard**: `tools/loadtest`, `tools/record-staging-evidence.mjs`, and strict staging validation require HTTP load evidence to prove at least 60 seconds of measurement and WebSocket/Redis evidence to prove at least 30,000 expected events before `PERF-HTTP-001`, `PERF-WS-001`, or `DATA-REDIS-001` can support readiness.
- [x] **Load Side Artifact Value Guard**: `tools/loadtest`, the staging evidence recorder, and strict staging validation now parse and persist concrete `http_replica_count`, `ws_replica_count`, dependency p99, and `room_broadcast_drops` values, rejecting single-replica, over-threshold dependency latency, or nonzero broadcast-drop evidence before load claims can support readiness.
- [x] **Performance Load Run Identity Guard**: the staging evidence recorder and strict-release validation require `PERF-HTTP-001`, `PERF-WS-001`, and `DATA-REDIS-001` evidence to share one concrete report-level `load_run_id`, so HTTP throughput, WebSocket fan-out, and Redis sequence proof cannot be stitched together from unrelated staging load runs or summary-only markers.
- [x] **Kubernetes Rollout Release Binding Guard**: `tools/deploymentprobe`, the staging evidence recorder, and strict-release validation require `kubernetes-rollout-status` proof to carry exact `release_candidate` and `service_version` markers, so stale rollout output cannot be paired with current workload resource evidence.
- [x] **Deployment Report Run Identity Guard**: `tools/record-staging-evidence.mjs` now rejects release-bound `DEPLOY-TF-001` and `DEPLOY-K8S-001` probe reports unless the report itself carries `load_run_id`, preventing hand-edited deployment reports from relying only on per-segment run markers.
- [x] **Terraform Immutable Image Input Guard**: Terraform API, web, and Rust workload image variables now require immutable `@sha256:<digest>` references, `terraform.tfvars.example` documents digest-pinned ECR inputs, and `tools/validate-deployment-skeleton.mjs` emits `immutable_workload_image_digests=true` only when those release-bound image input validations and workload references remain wired.
- [x] **Dependency Risk Runtime Reachability Guard**: `tools/validate-dependency-risk.mjs` scans `mobile/App.tsx` and `mobile/src`, confirms no runtime `uuid` import, and validates the DRR-001 lifecycle; DRR-001 is now closed at `uuid@11.1.1` while DRR-002 remains the open mobile audit blocker.
- [x] **Rust Report Run Identity Guard**: `tools/record-staging-evidence.mjs` now rejects `RUST-GRPC-001` probe reports unless the report itself carries `load_run_id`, preventing hand-edited Rust health/metrics reports from relying only on per-segment run markers.
- [x] **Rust Cargo Complete Summary Guard**: `tools/run-rust-cargo-gate.mjs` parses Cargo summaries and refuses to emit Rust proof markers if any Rust test is failed, ignored, measured, or filtered, or if the unit-test summary does not include at least the full required 13-test Rust suite.
- [x] **WebSocket Polling Sequence Match Guard**: `tools/loadtest`, the staging evidence recorder, and strict staging validation require WebSocket HTTP polling fallback evidence to prove `latest_sequence` matches the exact expected run sequence, preserving `ws_polling_artifact_latest_sequence_matches_run=true` alongside the structured `ws_polling_latest_sequence=ws_max_sequence` proof before `PERF-WS-001` or `DATA-REDIS-001` can support readiness.
- [x] **WebSocket Polling Artifact Sequence Field Guard**: `tools/loadtest` now persists the deployed polling artifact's parsed `latest_sequence` as structured `ws_polling_artifact_latest_sequence`, and both recorder and strict validation require it to equal `ws_max_sequence`, so the polling endpoint artifact cannot drift from the in-run sequence proof.
- [x] **WebSocket Reconnect Continuity Guard**: `tools/loadtest`, the staging evidence recorder, and strict staging validation now require reconnect evidence to carry `ws_reconnect_sequence_continues=true`, so an acceptance-only reconnect artifact cannot satisfy `PERF-WS-001`.
- [x] **Go WebSocket Test Proof Marker Guard**: `tools/run-go-core-gate.mjs --mode test` now runs verbose `go test ./...` and refuses to emit `go-test-gate` proof unless live-room invalid-event, oversized-event, reconnect/polling, missing-state-manager, concurrent sequence, fan-out, dropped-broadcast metric, origin, and polling-fallback tests all report concrete `--- PASS` lines. Real multi-instance staging WebSocket/load evidence remains tracked under `PERF-WS-001` and `DATA-REDIS-001`.
- [x] **Staging Evidence Local Gate Proof Marker Guard**: `tools/validate-staging-evidence.mjs` emits proof markers for required evidence IDs, status validation, evidence chronology, accepted-risk metadata, strict-release environment, non-local probe artifact hosts, exact release-candidate markers, segment markers, numeric threshold checks, and local/mock/placeholder rejection; `tools/validate-local-gate-report.mjs` rejects local gate reports whose `staging-evidence-validation` output lacks those markers.
- [x] **Obsidian Readiness Local Gate Proof Marker Guard**: `tools/sync-obsidian-readiness.mjs --check` emits proof markers for manifest validation, gap summary rendering, snapshot marker presence, strict staging PATH status, contract drift status, release-candidate match status, and current snapshot body; `tools/validate-local-gate-report.mjs` rejects local gate reports whose `obsidian-readiness-snapshot-check` output lacks those markers.
- [x] **Strict Staging PATH Local Gate Guard**: `tools/run-local-gates.mjs` now runs both local PATH readiness and `tools/verify-project-path.mjs --strict-staging`, and `tools/validate-local-gate-report.mjs` requires machine-checkable proof markers for required tool resolution, version checks, strict staging tool resolution, version checks, and broken-shim rejection before local gate evidence can support readiness.
- [x] **Strict Staging PATH CI Evidence Guard**: `tools/ciprobe` and `tools/validate-ci-evidence-gates.mjs` now require CI release artifacts to include `gate: strict-staging-path-readiness`, and the validator emits `strict_staging_path_gate_mirrored=true`, so source-control/CI evidence cannot claim the exact required gate set while omitting staging evidence toolchain verification.
- [x] **CI Runtime PATH Readiness Guard**: `.github/workflows/security.yml` now runs `node tools/verify-project-path.mjs --ci` after installing the PostgreSQL client, and `tools/validate-ci-workflow.mjs` requires that step plus `ci_path_readiness_gate=true`; CI mode checks the actual Actions toolchain without requiring local-only `rtk`.
- [x] **CI Strict Staging PATH Execution Guard**: `.github/workflows/security.yml` installs AWS CLI v2 from the AWS distribution zip, exports `$(go env GOPATH)/bin` through `GITHUB_PATH` for `gopls`, installs `gopls` and `kubectl`, then runs `node tools/verify-project-path.mjs --ci --strict-staging`; `tools/verify-project-path.mjs` rejects AWS CLI v1 in strict staging modes, and `tools/validate-ci-workflow.mjs` requires the v2 install, combined CI strict PATH step, and `strict_staging_path_readiness_gate=true`, so CI release evidence no longer lists the strict staging PATH gate without executing it or requiring local-only `rtk`.
- [x] **Staging Evidence Gap Report Proof Marker Guard**: `tools/report-staging-evidence-gaps.mjs` emits proof markers in text, JSON, and Obsidian output for strict release readiness, strict staging PATH readiness, release-candidate matching, pending external counts, non-manifest blockers, contract drift, accepted-risk counts, and blocking-item listing so remaining production blockers stay machine-checkable and Obsidian-trackable.
- [x] **Gap Report Required-Evidence Detail Guard**: `tools/report-staging-evidence-gaps.mjs` now emits `blocking_item_required_evidence_listed=true` and regression tests prove text/Obsidian reports keep concrete required-evidence details visible for staging evidence collection.
- [x] **Local Gate Gap Required-Evidence Guard**: `tools/validate-local-gate-report.mjs` rejects `staging-evidence-gap-report` rows that omit rendered `required:` evidence details, even if the proof footer and marker text are still present.
- [x] **Staging Evidence Contract Refresh**: the local staging manifest was refreshed from `production-readiness/staging-evidence.example.json`; `DEPLOY-TF-001`, `DATA-RLS-001`, and `EXT-ZOOM-001` required-evidence drift is cleared, leaving release-SHA mismatch and external proof items as the active blockers.
- [x] **Staging Evidence Gap Report Local Gate Guard**: `tools/run-local-gates.mjs` includes `staging-evidence-gap-report`, which runs `tools/report-staging-evidence-gaps.mjs` against the checked-in evidence contract with explicit `--allow-blockers`; `tools/validate-local-gate-report.mjs` rejects local reports that omit the not-ready proof footer, footer status counts, footer blocking-item IDs, rendered blocker rows, `--allow-blockers`, gap-report proof markers, or a footer blocking-item count that matches the listed blocking IDs.
- [x] **Staging Evidence Gap Report CI Gate Guard**: `.github/workflows/security.yml` runs the same checked-in contract gap-report command with explicit `--allow-blockers`, and `tools/validate-ci-workflow.mjs` rejects workflow drift that removes or weakens that gate before CI release evidence is written.
- [x] **Final Production Readiness Claim Proof Marker Guard**: `tools/verify-production-readiness.mjs` returns and prints proof markers for strict release manifest validation, local gate report validation, strict staging PATH readiness, clean/synced git, release-candidate-to-HEAD matching, local-gate-to-HEAD matching, separate local-gate branch and upstream matching, local-gate chronology, current Obsidian snapshot verification, and blocker-free staging gap-report validation before any production-ready claim can pass.
- [x] **Final Git Remote Freshness Guard**: `tools/verify-production-readiness.mjs` runs `git fetch --dry-run` before reading current status, so ahead/behind checks depend on freshly reachable upstream metadata rather than stale local tracking refs.
- [x] **Local Gate Remote Freshness Guard**: `tools/run-local-gates.mjs` now records `git_remote_refreshed=true` only after `git fetch --dry-run` succeeds, and `tools/validate-local-gate-report.mjs` rejects reports missing that marker before they can support final readiness.
- [x] **Final Local Gate Freshness Guard**: `tools/verify-production-readiness.mjs` rejects same-SHA local gate reports older than 24 hours at production-readiness verification time, so stale clean reports cannot support a current production-ready claim.
- [x] **Current Non-Dry Local Gate Run**: `node tools/run-local-gates.mjs --continue-on-failure --report artifacts/local-gate-report-current.json` completed on 2026-06-29T21:27:33Z with 33/33 gates run, 0 failed, `dry_run=false`, and branch/upstream synced; the report remains development evidence only because it captured `git_status_clean=false` from the dirty remediation worktree.
- [x] **Final Failure Diagnostics Guard**: failed `tools/verify-production-readiness.mjs` runs now print staging evidence blocker IDs, status counts, strict staging PATH status, and strict release readiness status even when the first hard failure is local evidence such as a dirty local-gate report.
- [x] **Final Claim Gap Report Guard**: `tools/verify-production-readiness.mjs` now loads the staging evidence contract, runs the same gap-summary engine used by Obsidian, and rejects production-readiness claims unless `strict_release_ready=true` with no pending, contract-drift, stale-release, strict-PATH, or accepted-risk blockers.
- [x] **Final Claim Zero-Waiver Guard**: `tools/verify-production-readiness.mjs` now rejects final production-readiness claims when any manifest item remains in `accepted_risk`, so 100% readiness requires closing or replacing accepted risks with passed signoff evidence; the current mobile DRR-002 finding remains an explicit blocker.
- [x] **Final Claim Gap Hint Guard**: failed `tools/verify-production-readiness.mjs` runs print a replayable `tools/report-staging-evidence-gaps.mjs` command that preserves both the staging evidence manifest path and the staging evidence contract path unless the matching environment variables already provide them.
- [x] **CI Recorded Evidence Proof Marker Guard**: `tools/ciprobe` emits CI release proof markers into successful `github-actions-release-run` result summaries, `tools/record-staging-evidence.mjs` preserves those summaries in recorded `SRC-CI-001` manifest evidence, and strict staging validation requires the same proof markers before CI evidence can support final readiness.
- [x] **CI Untracked Source Clean Guard**: `.github/workflows/security.yml` now fails release evidence writing when `git status --short` reports any untracked non-ignored source file, and `tools/write-ci-release-evidence.mjs` plus `tools/ciprobe` require `source_control_untracked_status: clean`, the concrete clean-check command markers, and `source_control_untracked_clean_verified=true`.
- [x] **CI Artifact Host Guard**: `tools/ciprobe` rejects reserved documentation/sample run artifact hosts such as `.example`, `example.com`, `.test`, and `.invalid`, plus localhost, loopback, private-network, unspecified, and link-local URLs before emitting `SRC-CI-001`.
- [x] **CI Recorded Artifact URL Guard**: `tools/record-staging-evidence.mjs` rejects `SRC-CI-001` reports whose `github-actions-release-run` target is a local artifact file, private/local network URL, reserved placeholder host, or non-HTTPS URL; production readiness recording now requires the uploaded HTTPS `ci-release-evidence` artifact URL even though `tools/ciprobe` keeps local-file mode for downloaded-artifact debugging.
- [x] **CI Strict Release Artifact URL Guard**: strict staging validation rejects `SRC-CI-001` evidence whose recorded `tools/ciprobe` command uses `-run-artifact-file` or a local `artifacts/ci-release-evidence.txt` path; strict release evidence must show `-run-artifact-url` with an HTTPS `ci-release-evidence` artifact.
- [x] **Tenant Probe Structured Resource Binding Guard**: `tools/tenantprobe` tests now decode successful `DATA-RLS-001` JSON reports and assert the created `journal_id` and `room_id` are carried through same-tenant reads/lists/state and cross-tenant denial probes, plus that tenant-override write attempts fail with HTTP 403.
- [x] **RLS Integration Package Result Guard**: `tools/run-rls-db-integration.mjs` now requires verbose PASS lines plus `ok scriptureforge/tests/integration` and `ok scriptureforge/internal/ports` package result lines before emitting RLS proof markers, preventing pasted tenant test snippets from satisfying local/CI tenant-isolation evidence without both DB-backed packages actually reporting success.
- [x] **Local Dependency Metrics Surface**: Go API observability exposes low-cardinality dependency operation count/duration metrics for abuse limiter decisions, auth/session, journal, rooms/WebSocket state, Rust engine vector search, AI provider, and Zoom paths, plus architecture-facing `websocket_active_connections_count` and `ai_inference_duration_seconds` profile metrics; local dashboard/alert/runbook validation requires them.
- [x] **Local Dependency Trace Span Surface**: every `ObserveDependencyFromContext` call now emits a low-cardinality OpenTelemetry child span with dependency, operation, status, and duration attributes while preserving the existing dependency metrics, so auth/session, journal, room/Redis/WebSocket, AI, Zoom, abuse, and Rust-engine dependency work can appear inside request traces once an OTLP backend is deployed.
- [x] **OBS Evidence Metric Profile Guard**: `tools/observabilityprobe`, `tools/record-staging-evidence.mjs`, and `tools/validate-staging-evidence.mjs` now reject `OBS-OTEL-001`/`OBS-ALERT-001` evidence unless API metrics, dashboard, alert, and summary markers include the WebSocket active-connection and AI inference profile signals.
- [x] **OBS Exact Release Binding Guard**: `tools/observabilityprobe`, the staging evidence recorder, and strict staging manifest validation require OTEL collector, API/Rust metrics, trace/log correlation, dashboard, alert, delivery, and retention artifacts to carry exact `release_candidate=<manifest release_candidate>` plus `service_version` markers before `OBS-OTEL-001` or `OBS-ALERT-001` can support final readiness.
- [x] **OBS Trace ID Shape Guard**: `tools/observabilityprobe` and the staging evidence recorder require `OBS-OTEL-001` reports to carry a 32-character non-zero lowercase hex OpenTelemetry trace ID, so placeholder trace IDs cannot satisfy trace/log correlation proof.
- [x] **Local Trace ID Shape Guard**: Go API observability rejects malformed inbound `Traceparent`/`X-Trace-ID` values, falls back to generated 32-character non-zero hex trace IDs when custom generators misbehave, binds generated or accepted `X-Trace-ID` values into the OpenTelemetry request span trace ID when no valid W3C parent exists, preserves streaming response-writer behavior through `Flush` and `Unwrap`, tracks implicit status commits so late status changes do not corrupt metrics, and emits response `Traceparent` headers with a nonzero span ID; `tools/validate-observability.mjs` guards the local code/test markers while deployed trace backend evidence remains under `OBS-OTEL-001`.
- [x] **Go Observability Test Proof Marker Guard**: `tools/run-go-core-gate.mjs --mode test` now requires verbose PASS lines for local trace ID propagation, OpenTelemetry span binding, `/metrics`, metrics scrape exclusion, dependency span, mock-status, and architecture profile metric tests before emitting `go-test-gate` proof, so local Go evidence cannot drop observability behavior coverage.
- [x] **OBS Exact Trace Binding Guard**: the staging evidence recorder rejects `OBS-OTEL-001` reports unless the trace-backend and log-backend summaries include the exact structured report `trace_id` value, so one trace's backend query URLs cannot be paired with another trace's summary markers.
- [x] **OBS Trace Route Binding Guard**: `tools/observabilityprobe`, the staging evidence recorder, and strict-release validation reject `OBS-OTEL-001` reports unless `observed_route` and `http_method` are persisted and both trace-backend and log-backend summaries include exact `route=<observed_route>` plus `method=<http_method>` markers.
- [x] **OBS Strict Trace Match Guard**: strict-release validation now extracts concrete 32-character trace IDs from `trace-backend-search` and `log-backend-trace-correlation` segments and rejects evidence unless both segments reference the same non-zero trace ID.
- [x] **OBS Trace/Log Segment Guard**: strict-release validation now requires `trace-backend-search` itself to prove API/Rust trace correlation and `log-backend-trace-correlation` itself to prove trace ID plus tenant/user/role log markers, so one telemetry artifact cannot satisfy the other.
- [x] **OBS Tenant Principal Value Guard**: `tools/observabilityprobe`, staging evidence recording, and strict-release validation now require concrete `tenant_id=<id>`, `user_id=<id>`, and `role=<role>` markers on `log-backend-trace-correlation`, with matching structured report fields, so bare schema field names cannot satisfy tenant-aware deployed log proof.
- [x] **OBS Canonical Log Field Guard**: Go API access logs now emit architecture-required `timestamp` and `severity` JSON fields alongside legacy `at`/`level`, and `tools/observabilityprobe`, staging evidence recording, strict-release validation, and `tools/validate-observability.mjs` require `timestamp=` plus `severity=` markers on `log-backend-trace-correlation` evidence.
- [x] **OBS Local Gate Proof Marker Guard**: `tools/validate-observability.mjs` emits proof markers for dashboard metrics, alert rules, trace ID logs, `/metrics`, dependency spans, WebSocket and AI profile metrics, Rust metrics, OTEL env wiring, and staging evidence contract coverage; `tools/validate-local-gate-report.mjs` rejects local gate reports whose `observability-validation` output lacks those markers.
- [x] **OBS Regression Test Guard**: `tools/validate-observability.test.mjs` proves the current dashboard/alert/runbook/source artifacts pass and rejects drift that drops AI inference dashboard metrics, room broadcast drop alerts, API trace ID guards, Rust traceparent observability, or external Rust metrics staging proof; the local tooling-test gate, GitHub Actions tooling-test command, and `tools/validate-ci-workflow.mjs` all require this test.
- [x] **OBS Dependency Error Span Guard**: dropped, rejected, and unavailable dependency outcomes now mark dependency spans with OpenTelemetry error status, including WebSocket room broadcast drops; unit tests and `tools/validate-observability.mjs` guard the classification tokens.
- [x] **OBS Dashboard/Alert Segment Guard**: strict-release validation now requires `dashboard-import`, `alert-rules-loaded`, `alert-delivery-status`, and `telemetry-retention-policy` to each carry their own dashboard, rule, delivery, and retention markers before `OBS-ALERT-001` can support final readiness.
- [x] **OBS Alert Delivery Identity Guard**: `tools/observabilityprobe`, the staging evidence recorder, and strict-release validation require `OBS-ALERT-001` alert-delivery proof to carry concrete `alertname=<alert_name>`, `receiver=<alert_receiver>`, and `delivery_id=<id>` markers, with `alert_name` and `alert_receiver` persisted in the probe JSON report, so generic Alertmanager success text cannot satisfy production alert-delivery evidence.
- [x] **OBS Extracted Identity Binding Guard**: `tools/observabilityprobe` rejects trace/log/alert artifacts when extracted `trace_id`, `route`, `method`, `tenant_id`, `user_id`, `role`, `alertname`, or `receiver` values do not match the configured probe inputs, so later matching marker text cannot mask a different first telemetry or Alertmanager event.
- [x] **Resilience Structured Recovery Marker Guard**: `tools/resilienceprobe` now emits exact `snapshot_id=<id>`, `kms_key_id=<id>`, `rpo_minutes=<n>`, `restore_job_id=<id>`, `source snapshot_id=<id>`, `rto_minutes=<n>`, and `restore_duration_minutes=<n>` summary markers matching its structured JSON fields, so `DR-BACKUP-001` probe output is directly recordable by the stricter staging evidence recorder.
- [x] **OBS Distinct Artifact Guard**: `tools/observabilityprobe`, the staging evidence recorder, and strict staging validation require collector/API metrics/Rust metrics/trace/log proof to use distinct artifact URLs with `distinct_otel_artifacts=true`, and dashboard/rules/delivery/retention proof to use distinct artifact URLs with `distinct_alert_artifacts=true`.
- [x] **OBS Artifact Alias Guard**: `tools/observabilityprobe` canonicalizes OTEL and alert evidence artifact URLs before distinctness checks, so default-port, host-case, query-order, or fragment aliases cannot satisfy separate collector, metrics, trace, log, dashboard, rule, delivery, or retention proof roles.
- [x] **OBS Structured Manifest Guard**: `tools/record-staging-evidence.mjs` now persists `structured_report.observability_otel_proof` for `OBS-OTEL-001` and `structured_report.observability_alert_proof` for `OBS-ALERT-001`; strict-release validation rejects observability evidence when release/run identity, trace/log route/method/trace IDs, tenant/user/role principals, alert identity, delivery ID, or telemetry artifact targets drift from the recorded `observabilityprobe` summary markers.
- [x] **OBS Structured Target Guard**: strict-release validation revalidates retained observability structured targets as HTTPS, non-local, distinct artifact URLs; `trace_query_target` and `log_query_target` must also include the matching concrete trace ID, so final manifests cannot replace deployed telemetry artifacts with local, duplicate, or broad query URLs after raw probe JSON is detached.
- [x] **Resilience Structured Manifest Guard**: `tools/record-staging-evidence.mjs` now persists `structured_report.rollback_degradation_proof` for `DR-ROLLBACK-001` and `structured_report.backup_restore_proof` for `DR-BACKUP-001`; strict-release validation rejects resilience evidence when release/run identity, artifact targets, rollback version linkage, degradation booleans, snapshot/KMS IDs, RPO/RTO, restore job/source snapshot, or restore duration drift from the recorded `resilienceprobe` summary markers.
- [x] **Resilience Structured Target Guard**: strict-release validation revalidates retained rollback/degradation and backup/restore structured targets as HTTPS, non-local, distinct artifact URLs, so final manifests cannot replace deployed recovery evidence with local or duplicated targets after raw `resilienceprobe` JSON is detached.
- [x] **Local Tenant-Aware Structured Access Logs**: Go API access logs include trace ID, service/component/version/environment fields, and verified JWT-derived tenant/user/role fields for protected routes without trusting client-supplied tenant identifiers.
- [x] **Local AI Handler RLS Audit Proof**: `GenerateCurriculumHandler` has a DB-backed integration test that uses mocked vector/LLM dependencies and proves generated `ai_request_logs` plus `citation_trails` are visible to the owning tenant and invisible cross-tenant under Postgres RLS.
- [x] **RLS Schema Drift Guard**: `tools/validate-rls-schema.mjs` derives tenant-scoped tables from migration `CREATE TABLE` blocks carrying tenant data, then requires the expected coverage list to match and each table to keep ENABLE/FORCE RLS, `app.current_org_id` USING/WITH CHECK policies, table-level same-tenant read, cross-tenant read-denial, cross-tenant insert-denial, cross-tenant update-hidden, cross-tenant delete-hidden, and assertion-level journal/room/socket/AI audit handler RLS proof markers, including mismatched tenant/user room-create non-persistence; deployed RLS evidence remains tracked under `DATA-RLS-001`.
- [x] **Tenant Probe API Proof Markers**: `tools/tenantprobe` and the staging evidence recorder now require same-tenant API proof, cross-tenant read/list/state denial, and tenant/user override write-denial summaries for journal and room create routes before `DATA-RLS-001` can be recorded.
- [x] **Tenant Probe Segment Guard**: strict-release validation now requires each `DATA-RLS-001` tenantprobe segment to carry its own same-tenant, cross-tenant, override-denial, and database RLS markers, so broad summaries cannot borrow journal, room, or database proof across segments.
- [x] **Tenant Resource ID Binding Guard**: `tools/tenantprobe`, the staging evidence recorder, and strict-release validation now require created-resource probes to include structured `journal_id` or `room_id` fields that match exact `journal_id=<id>` or `room_id=<id>` summary markers, and require all journal proof segments to share the same created `journal_id` plus all room proof segments to share the same created `room_id`, so same-tenant and cross-tenant API proof binds to the specific created journal and room.
- [x] **Tenant Probe Journal Plaintext-Denial Guard**: `tools/tenantprobe`, the staging evidence recorder, and strict-release validation now require deployed `owner-create-encrypted-journal` proof to first reject a plaintext-shaped journal payload as a malformed encrypted envelope before accepting a valid encrypted journal create.
- [x] **Tenant Probe Artifact Host Guard**: `tools/tenantprobe` rejects reserved documentation/sample hosts such as `.example`, `example.com`, `.test`, and `.invalid`, plus localhost, loopback, private-network, unspecified, link-local, and `local-only` API targets and DB RLS proof artifacts before emitting `DATA-RLS-001`.
- [x] **Tenant DB Artifact Separation Guard**: `tools/tenantprobe`, the staging evidence recorder, and strict-release validation require the deployed DB/RLS proof artifact to use a canonical-distinct evidence host from the staging API target and carry `distinct_db_rls_artifact=true`, including host-case and trailing-dot aliases, so API behavior proof cannot masquerade as database-session/RLS proof.
- [x] **Tenant Exact Release Binding Guard**: `tools/tenantprobe`, the staging evidence recorder, and strict staging manifest validation require tenant API and database/RLS proof summaries to carry exact `release_candidate=<manifest release_candidate>` plus `service_version` markers before `DATA-RLS-001` can support final readiness.
- [x] **Tenant Pair DB Binding Guard**: `tools/tenantprobe`, the staging evidence recorder, and strict-release validation now require structured UUID-shaped `owner_org_id` and `blocked_org_id` values for different organizations, and the deployed DB/RLS proof segment must carry `app.current_org_id=<owner_org_id>` plus `blocked_org_id=<blocked_org_id>` so a stale or unrelated DB proof cannot satisfy `DATA-RLS-001`.
- [x] **Tenant DB Structured RLS Field Guard**: `tools/tenantprobe` emits structured `application_role=scriptureforge_app`, `row_security=on`, `rls_tables_verified=9`, `rls_forced_tables=9`, and `rls_policy_scope=app.current_org_id` fields on the passing `database-rls-context-proof` probe, and the staging evidence recorder rejects reports where those fields are missing or mismatched.
- [x] **Tenant DB Per-Table Outcome Guard**: `tools/tenantprobe`, the staging evidence recorder, and strict-release validation require `rls_table_names=organizations,users,scripture_texts,refresh_tokens,journal_entries,live_rooms,room_participants,ai_request_logs,citation_trails` plus structured `rls_table_outcomes` entries proving same-visible, cross-hidden, and write-denied outcomes for every tenant-scoped table before `DATA-RLS-001` can support final readiness.
- [x] **Local All-Table RLS Mutation Proof Guard**: `tools/run-rls-db-integration.mjs` and local gate validation now require explicit proof markers that same-tenant reads and writes pass and cross-tenant reads, inserts, updates, and deletes are denied or hidden across all nine tenant-scoped tables before local tenant isolation evidence can support readiness.
- [x] **Local Same-Tenant RLS Update/Delete Guard**: `TestTenantRLSCoversAllTenantTables` now proves same-tenant update and delete operations affect exactly one row for every tenant-scoped table, and the local RLS proof marker contract requires `rls_same_tenant_updates_pass_all_tables=true` plus `rls_same_tenant_deletes_pass_all_tables=true`.
- [x] **Production Room Membership RLS Wiring Guard**: `tools/validate-rls-schema.mjs` rejects production `RoomHandler` or `SocketConnection` route construction that installs a `MembershipValidator` override, and requires both default membership paths to set `app.current_org_id` through `auth.SetTenantContext` before querying `room_participants` by organization, room, and user.
- [x] **Tenant Context UUID Guard**: `auth.SetTenantContext` trims and validates the JWT-derived organization ID as a UUID before writing transaction-local `app.current_org_id`, with unit coverage proving malformed tenant IDs fail before database use.
- [x] **Staging Service Version Release Binding Guard**: `tools/record-staging-evidence.mjs` now rejects probe reports whose top-level `service_version` does not include the manifest release candidate, and strict release manifest validation requires probe report summaries to include `service_version=<service>:<manifest release_candidate>` so stale service build evidence cannot be paired with a current release SHA.
- [x] **Tenant DB Role And Bypass Guard**: `tools/tenantprobe`, the staging evidence recorder, and strict staging validation now require deployed DB/RLS artifacts to prove `current_user=scriptureforge_app`, `superuser=false`, `bypassrls=false`, `current_setting('app.current_org_id')`, and `row_security=on`, so a generic non-superuser note or unnamed DB role cannot satisfy `DATA-RLS-001`.
- [x] **Tenant RLS Table Count Guard**: `tools/tenantprobe`, the staging evidence recorder, and strict-release validation require deployed DB/RLS artifacts to prove `rls_tables_verified=9`, `rls_forced_tables=9`, and `rls_policy_scope=app.current_org_id`, so table-name lists alone cannot satisfy all-table RLS proof.
- [x] **Auth Session RLS Local Gate Guard**: `tools/run-rls-db-integration.mjs` now includes refresh/session, privileged MFA login, MFA enrollment/verification, and workspace-switch tenant-match tests in the DB-backed RLS proof set, and `tools/validate-rls-schema.mjs` requires auth refresh-token rotation, expired/revoked denial, cross-tenant refresh denial, logout, privileged MFA blocking/verification, workspace cross-tenant denial, MFA enrollment/verification role checks, and `auth.SetTenantContext` markers before local tenant-isolation evidence can pass.
- [x] **Current Docker RLS Proof**: `tools/run-rls-db-integration-docker.mjs --container-name scriptureforge-rls-db-codex-proof` passed on 2026-06-29 against disposable `pgvector/pgvector:pg16`, applying migrations and proving table RLS, journal/room handlers, WebSocket tenant scoping, AI audit rows, refresh rotation/logout, MFA, and workspace-switch tenant isolation. This remains local/container evidence; deployed `DATA-RLS-001` tenantprobe artifacts are still pending external staging proof.
- [x] **Mobile Native Crypto And Endpoint Fail-Closed Guard**: mobile journal crypto rejects WebCrypto fallback when `EXPO_PUBLIC_REQUIRE_NATIVE_CRYPTO=true`, mobile API config now requires `EXPO_PUBLIC_REQUIRE_NATIVE_CRYPTO=true` whenever `EXPO_PUBLIC_DEPLOYMENT_ENVIRONMENT=staging|production|prod`, rejects reserved documentation/sample hosts, local, private-network including IPv6 unique-local and IPv4-mapped private IPv6, link-local, unspecified, and insecure endpoints in staging/native-required mode, and `tools/mobileprobe`, the staging evidence recorder, and strict-release validation reject missing or contradictory native-required/development config markers before accepting `CLIENT-MOBILE-001`.
- [x] **Mobile Crypto Provider Binding Guard**: derived mobile journal keys are bound to the selected crypto provider, mobile smoke exposes provider status, and fallback-derived keys are rejected when `EXPO_PUBLIC_REQUIRE_NATIVE_CRYPTO=true` so native-required builds cannot silently switch from `react-native-quick-crypto` to WebCrypto between key derivation and AES-GCM use.
- [x] **Journal AES-GCM Associated-Data Guard**: web and mobile journal encryption bind AES-GCM operations to server-issued journal salt context via associated data, and local/web/mobile/staging evidence checks require wrong associated-data rejection so ciphertext cannot be silently accepted under the wrong journal salt version.
- [x] **Backend Journal Envelope Guard**: `POST /api/v1/journal_entries` rejects malformed encrypted envelopes before DB persistence, including plaintext-shaped `ciphertext`, non-base64 ciphertext/IV, non-12-byte AES-GCM IVs, unversioned salt IDs, and invalid salt versions; handler/RLS tests prove rejected plaintext payloads persist zero rows.
- [x] **Journal Crypto Placeholder Regression Guard**: `tools/verify-journal-crypto.mjs` rejects Expo Crypto/Expo Random placeholder regressions, requires web/mobile journal key derivation to use the architecture PBKDF2 600,000-iteration work factor, requires native `react-native-quick-crypto` provider loading, and requires its verifier harness to prove native AES-GCM operations are actually routed through the quick-crypto provider before local crypto evidence can pass.
- [x] **Web Journal Import-Failure Cleanup Guard**: web journal key derivation now wipes passphrase and salt buffers even when PBKDF2 key-material import fails, `web/src/lib/crypto.smoke.mts` proves the observable import-failure passphrase cleanup path, and `tools/verify-journal-crypto.mjs` emits `web_import_failure_zeroization=true` only when the guarded `finally` still covers both passphrase and salt cleanup.
- [x] **Journal Crypto Local Gate Proof Marker Guard**: `tools/verify-journal-crypto.mjs` emits proof markers for native quick-crypto routing, native provider-bound keys, native-provider harness execution, native-required fail-closed behavior, PBKDF2 600,000 iterations, blank passphrase/salt rejection, AES-GCM round-trip, tamper rejection, associated-data rejection, non-extractable keys, key disposal, buffer zeroization, backend bootstrap salts, and web/mobile crypto smoke coverage; `tools/validate-local-gate-report.mjs` rejects local gate reports whose `journal-crypto-validation` output lacks those markers.
- [x] **Journal Crypto Tooling-Test Gate Guard**: `tools/verify-journal-crypto.test.mjs` is now part of the local `tooling-tests` gate, GitHub Actions tooling-test step, and CI workflow drift validator, so journal crypto proof-marker regression tests run with the same local/CI evidence matrix that validates release gate tooling.
- [x] **Web Journal Key Disposal Guard**: web journal crypto now uses disposable key handles, the editor disposes replaced/unmounted/async-stale handles and clears the active key after encrypted save, and `tools/verify-journal-crypto.mjs` emits `web_key_disposal=true` after web smoke proves disposed handles cannot encrypt.
- [x] **Mobile Probe Artifact Guard**: `tools/mobileprobe` rejects reserved placeholder hosts such as `.example`, `example.com`, `.test`, and `.invalid`, plus localhost, loopback, private-network, unspecified, link-local, and `local-only` EAS/native-device, native crypto, and staging-config artifacts; it also rejects Expo Go, dev-client/debug-client, simulator/emulator/remote-debug wording, browser/global WebCrypto, Node crypto, Expo crypto, JavaScript fallback, and staging config artifacts with private API/WebSocket endpoint values.
- [x] **Mobile Native Crypto Self-Test Marker Guard**: `runJournalCryptoSelfTest`, `mobile/src/lib/crypto.smoke.mts`, and `tools/verify-journal-crypto.mjs` now require the app-side native crypto smoke export to include provider status, native-required mode, AES-GCM round-trip, tamper rejection, wrong associated-data rejection, non-extractable key proof, key disposal, disposed-handle rejection, and passphrase/salt/plaintext buffer lifecycle markers before local mobile crypto evidence can support `CLIENT-MOBILE-001`; the verifier emits `native_device_self_test_markers=true` so local gate reports reject stale minimal self-test coverage.
- [x] **Mobile Native-Required Self-Test Fail-Closed Guard**: `mobile/src/lib/crypto.smoke.mts` now proves `runJournalCryptoSelfTest()` rejects WebCrypto-only execution when `EXPO_PUBLIC_REQUIRE_NATIVE_CRYPTO=true`, and `tools/verify-journal-crypto.mjs` emits `native_required_self_test_fail_closed=true` so local reports cannot satisfy mobile crypto evidence while the app-side self-test would silently pass without the native provider.
- [x] **Mobile Probe Segment Guard**: strict-release validation now requires `CLIENT-MOBILE-001` mobileprobe summaries to carry EAS/native-device, native AES-GCM/key-lifecycle, staging endpoint config, and `staging artifact` provenance markers in their own segments, so native crypto proof or artifact provenance cannot be borrowed from app-install or config summaries.
- [x] **Mobile Distinct Artifact Segment Guard**: the staging evidence recorder and strict-release validation now require `distinct_mobile_artifacts=true` on each EAS/native-device, native crypto, and staging config proof segment, so one segment cannot lend distinct-artifact proof to another `CLIENT-MOBILE-001` segment.
- [x] **Mobile Artifact Alias Guard**: `tools/mobileprobe` and the staging evidence recorder canonicalize EAS/native-device, native crypto, and staging config artifact URLs before distinctness checks, so default-port, host-case, query-order, or fragment aliases cannot satisfy separate mobile proof roles.
- [x] **Mobile Exact Release Binding Guard**: `tools/mobileprobe`, the staging evidence recorder, and strict staging manifest validation require EAS/native-device, native crypto, and staging-config mobile artifacts to carry exact `release_candidate=<manifest release_candidate>` plus `service_version` markers before `CLIENT-MOBILE-001` can support final readiness.
- [x] **Mobile Build Identity Binding Guard**: `tools/mobileprobe`, staging evidence recording, and strict-release validation require EAS/native-device, native crypto, and staging-config proof to carry the same concrete `mobile_build_id=<id>`, so native AES-GCM evidence cannot be paired with a different app build or config artifact.
- [x] **Mobile Report Build Identity Guard**: `tools/mobileprobe` now emits report-level `mobile_build_id`, and the staging evidence recorder rejects `CLIENT-MOBILE-001` reports where that envelope value is missing or differs from the EAS/native-device, native crypto, or staging-config proof segments.
- [x] **Mobile Crypto Zeroization Evidence Guard**: `tools/mobileprobe`, staging evidence recording, and strict-release validation now require native AES-GCM smoke artifacts to prove passphrase, salt, and plaintext buffers were explicitly zeroized, not only generically "wiped" or "cleared"; `tools/verify-journal-crypto.mjs` also rejects removal of the mobile `bytes.fill(0)` wipe helper.
- [x] **Mobile Crypto Runtime Zeroization Guard**: `tools/verify-journal-crypto.mjs` captures the IV and associated-data buffers passed through the native quick-crypto AES-GCM harness and emits `runtime_buffer_zeroization=true` only after proving those runtime buffers are zeroed after encrypt/decrypt.
- [x] **Mobile Crypto Failure-Path Cleanup Guard**: mobile journal crypto wipes passphrase and salt byte buffers even when key import fails, wipes temporary base64 decode buffers after copying ciphertext/IV data, and `tools/verify-journal-crypto.mjs` requires those cleanup paths before local crypto evidence can pass; native-device/EAS proof remains tracked under `CLIENT-MOBILE-001`.
- [x] **Mobile Build-Check Crypto Verifier Output Guard**: `tools/run-client-command.mjs`, local gates, CI workflow validation, and local gate report validation now require `mobile-build-check` output to include the `journal crypto verification passed:` proof summary before emitting or accepting `mobile_crypto_verification=true`, so build-compatible mobile evidence cannot rely only on a successful npm wrapper exit.
- [x] **Mobile Journal Passphrase Error Cleanup Guard**: `SecureJournalContainer` now clears passphrase state after key derivation errors as well as after successful derivation/session teardown, and `tools/verify-journal-crypto.mjs` emits `mobile_passphrase_derivation_failure_cleanup=true` only when that failure path disposes the stale handle and clears the passphrase before reporting the error.
- [x] **Mobile Native Self-Test Export Guard**: `mobile/src/lib/crypto.ts` exports `runJournalCryptoSelfTest()` so an EAS/native run can emit provider status, native-required mode, AES-GCM round-trip, tamper rejection, associated-data rejection, non-extractable key behavior, disposed-key rejection, and lifecycle markers from inside the app runtime; `tools/verify-journal-crypto.mjs` emits `native_device_self_test_export=true` and `native_device_self_test_markers=true`. Actual native-device/EAS artifacts remain pending under `CLIENT-MOBILE-001`.
- [x] **Mobile Native Self-Test Artifact Origin Guard**: `runJournalCryptoSelfTest()` now emits a literal `runJournalCryptoSelfTest` marker, and `tools/mobileprobe`, staging evidence recording, and strict-release validation require that marker on the `mobile-native-crypto-smoke` segment so `CLIENT-MOBILE-001` native AES-GCM/key-lifecycle proof must come from the exported app-runtime self-test path.
- [x] **Web AES-GCM IV Freshness Guard**: `web/src/lib/crypto.smoke.mts`, `tools/verify-journal-crypto.mjs`, and `tools/validate-local-gate-report.mjs` now require repeated same-plaintext journal encryptions to produce fresh IVs and different ciphertext with `web_crypto_unique_iv=true` proof before local web crypto smoke evidence can pass.
- [x] **Mobile AES-GCM IV Freshness Guard**: `runJournalCryptoSelfTest()`, `mobile/src/lib/crypto.smoke.mts`, `tools/verify-journal-crypto.mjs`, `tools/mobileprobe`, staging evidence recording, and strict-release validation now require `unique_iv=true`/`unique IV` proof so native mobile AES-GCM evidence must show fresh IV material for repeated journal encryptions; `tools/mobileprobe` also emits structured `unique_iv: true`, and the staging evidence recorder rejects `CLIENT-MOBILE-001` native crypto reports that omit or falsify that JSON field.
- [x] **Mobile Native Crypto Outcome Structured Evidence Guard**: `tools/mobileprobe` now emits structured `aes_gcm_roundtrip`, `tamper_rejected`, and `associated_data_rejected` booleans, and staging evidence recording plus strict-release validation require exact `aes_gcm_roundtrip=true`, `tamper_rejected=true`, and `associated_data_rejected=true` markers before `CLIENT-MOBILE-001` can support final readiness.
- [x] **Mobile Native Crypto Lifecycle Structured Evidence Guard**: `tools/mobileprobe` now emits structured `key_disposed`, `disposed_handle_rejected`, `revoked_key_rejected`, `passphrase_buffer_zeroized`, `salt_buffer_zeroized`, and `plaintext_buffer_zeroized` booleans, and staging evidence recording plus strict-release validation require the matching exact markers in the `mobile-native-crypto-smoke` segment before `CLIENT-MOBILE-001` can support final readiness.
- [x] **Mobile Self-Test Lifecycle Marker Guard**: `runJournalCryptoSelfTest()` now emits `key_disposed=true` and `disposed_handle_rejected=true` directly from the app-runtime self-test, and mobile smoke plus `tools/verify-journal-crypto.mjs` reject self-test output that lacks those production evidence markers.
- [x] **Mobile AES-GCM Salt Binding Guard**: `runJournalCryptoSelfTest()` now emits concrete `associated_data_salt_id=<id>` and `associated_data_salt_version=<n>` markers, and `tools/verify-journal-crypto.mjs` rejects drift that stops binding AES-GCM associated data to backend salt metadata.
- [x] **Client Associated-Data Input Guard**: web and mobile `journalAssociatedData()` now reject blank salt IDs plus non-positive or non-integer salt versions before building AES-GCM associated data, and client smoke/verifier gates require `*_crypto_associated_data_input_guard=true` markers.
- [x] **Client Smoke Crypto Marker Parity Guard**: `tools/validate-local-gate-report.mjs` now requires local web/mobile smoke rows to preserve the current crypto smoke markers for associated-data input guards, IV freshness, revoked-key rejection, import-failure cleanup, native-required self-test fail-closed behavior, and mobile runtime buffer zeroization, so stale smoke logs cannot satisfy local gate evidence after crypto hardening changes.
- [x] **Mobile Native Evidence Salt Binding Guard**: `tools/mobileprobe`, staging evidence recording, and strict-release validation now require native `CLIENT-MOBILE-001` AES-GCM smoke evidence to preserve concrete `associated_data_salt_id=<id>` and `associated_data_salt_version=<n>` markers from `runJournalCryptoSelfTest()`, so generic associated-data wording cannot satisfy mobile journal salt-binding proof.
- [x] **Mobile Native Salt Structured Field Guard**: `tools/mobileprobe` now emits structured `associated_data_salt_id` and `associated_data_salt_version` JSON fields for passing native crypto smoke probes, rejects empty or zero salt values, and the staging evidence recorder plus strict-release validation require concrete salt markers before `CLIENT-MOBILE-001` can support final readiness.
- [x] **Mobile Native Provider Structured Report Guard**: `tools/mobileprobe` now emits structured `provider=react-native-quick-crypto` and `native_required=true` JSON fields on the passing `mobile-native-crypto-smoke` probe, giving `CLIENT-MOBILE-001` evidence a machine-readable native provider assertion in addition to the marker summary.
- [x] **Mobile Structured Manifest Guard**: `tools/record-staging-evidence.mjs` now persists `structured_report.mobile_native_crypto_proof` for `CLIENT-MOBILE-001`, and strict-release validation rejects mobile evidence when native provider, native-required mode, unique IV proof, lifecycle/zeroization booleans, associated-data salt values, device/runtime binding, staging API/WS config, `mobile_build_id`, or `load_run_id` drift from the recorded `mobileprobe` summary markers.
- [x] **Mobile Native Provider Exact Marker Guard**: `tools/mobileprobe`, staging evidence recording, and strict-release validation now require the native crypto smoke artifact itself to carry exact `provider=react-native-quick-crypto` and `native_required=true` markers, so broad provider-status text cannot stand in for the machine-readable native-provider assertion.
- [x] **Mobile Native Provider Extracted Binding Guard**: `tools/mobileprobe` extracts the first `provider=` and `native_required=` values from native crypto smoke artifacts and rejects reports unless they bind to `react-native-quick-crypto` and `true`, so later native markers cannot mask an earlier fallback/provider line.
- [x] **Mobile Native Provider Ingestion Guard**: staging evidence recording and strict-release validation now enforce the same first-value provider/native-required binding for `CLIENT-MOBILE-001`, preventing hand-recorded mobile evidence from bypassing `tools/mobileprobe` with later matching native markers; recorder validation also cross-checks the artifact summary's first provider/native-required markers against the structured native-provider fields.
- [x] **Mobile Installed Runtime Binding Guard**: `tools/mobileprobe`, staging evidence recording, and strict-release validation now require the `mobile-native-crypto-smoke` segment to include structured `device_os=android|ios`, a concrete `device_model`, `app_runtime=installed-staging-app`, and an installed staging app runtime marker, so Node/WebCrypto logs or unattached native snippets cannot satisfy `CLIENT-MOBILE-001`.
- [x] **Client Strict Endpoint Placeholder Guard**: web and mobile strict runtime config reject reserved documentation/sample API and WebSocket hosts such as `.example`, `example.com`, `.test`, and `.invalid`, so staging or production builds cannot ship with endpoints that would later be rejected by staging evidence probes.
- [x] **Web Endpoint Fail-Closed Guard**: web API config rejects local, private-network including IPv6 unique-local and IPv4-mapped private IPv6, link-local, unspecified, and insecure API/WebSocket endpoints when `NEXT_PUBLIC_DEPLOYMENT_ENVIRONMENT=staging|production|prod`, preserving localhost defaults only for local development.
- [x] **Web Client Exact Release Binding Guard**: `tools/stagingprobe`, the staging evidence recorder, and strict staging manifest validation require deployed browser auth, encrypted journal, and room/WebSocket smoke artifacts to carry exact `release_candidate=<manifest release_candidate>` plus `service_version` markers before `CLIENT-WEB-001` can support final readiness.
- [x] **Web Smoke Identity Binding Guard**: `tools/stagingprobe`, the staging evidence recorder, and strict-release validation require auth, encrypted-journal, and room/WebSocket browser-smoke artifacts to carry the same concrete `user_id` and `organization_id`, plus `journal_id=<id>` and `room_id=<id>` for created-resource proof.
- [x] **Web Report Smoke Identity Guard**: `tools/stagingprobe` now emits report-level `web_user_id`, `web_organization_id`, `web_journal_id`, and `web_room_id`, and the staging evidence recorder rejects `CLIENT-WEB-001` reports where those envelope values are missing or differ from the browser-smoke proof segments.
- [x] **Staging TLS/Web Probe Host Guard**: `tools/stagingprobe` rejects reserved documentation/sample hosts such as `.example`, `example.com`, `.test`, and `.invalid`, plus localhost, loopback, private-network, unspecified, and link-local API/web bases plus DNS, ACM, and browser-smoke artifact URLs before emitting `DEPLOY-TLS-001` or `CLIENT-WEB-001`.
- [x] **Staging TLS/Web Segment Guard**: strict-release validation now requires `DEPLOY-TLS-001` and `CLIENT-WEB-001` stagingprobe summaries to carry API health, TLS, redirect, web root, browser auth, encrypted journal, and room/WebSocket markers in their own segments so deployed web proof cannot borrow markers across checks.
- [x] **Staging TLS Exact Release Binding Guard**: `tools/stagingprobe`, the staging evidence recorder, and strict staging manifest validation require API live/readiness, API/web TLS, and HTTP-to-HTTPS redirect summaries to carry exact `release_candidate=<manifest release_candidate>` plus `service_version` markers before `DEPLOY-TLS-001` can support final readiness.
- [x] **Staging TLS Certificate Identity Guard**: `tools/stagingprobe`, the staging evidence recorder, and strict staging manifest validation require API/web TLS summaries to carry concrete `cert_hostname=<hostname>` and `cert_issuer=<issuer>` markers, and the recorder rejects hostnames that do not match the probed target.
- [x] **Staging SSL Labs A+ Guard**: `tools/stagingprobe`, the staging evidence recorder, and strict staging validation require `DEPLOY-TLS-001` to include a canonical-distinct HTTPS SSL Labs artifact plus a `ssl-labs-a-plus` segment with `grade=A+`, `ssl_labs_grade=A+`, and API/web hostname markers matching the deployed certificate hostnames.
- [x] **Staging TLS/Web Artifact Alias Guard**: `tools/stagingprobe` canonicalizes TLS and web smoke artifact URLs before distinctness checks, so default-port, host-case, query-order, or fragment aliases cannot satisfy separate DNS, ACM, auth, journal, or room smoke proof roles.
- [x] **Local WebSocket Origin Fail-Closed Guard**: `DEPLOYMENT_ENVIRONMENT=staging|production|prod` now rejects WebSocket upgrades when `ALLOWED_WS_ORIGINS` is missing or when configured/requested origins are insecure, local/private, or reserved documentation/sample hosts; localhost/no-origin fallback is limited to local development/test shells.
- [x] **Local WebSocket State Manager Fail-Closed Guard**: room creation, WebSocket stream upgrades, and HTTP polling fallback return controlled `503` errors when Redis-backed room state management is not configured, preventing partial room creation, unsequenced streams, or polling panics.
- [x] **Local WebSocket Concurrent Broadcast Proof**: handler-level room stream tests now prove multiple connected senders can write concurrently, accepted events receive contiguous sequences, every active subscriber receives every accepted event in sequence order, and Redis append plus WebSocket broadcast metrics count accepted fan-out without drops. `SocketConnection` serializes append-plus-broadcast so accepted sequence assignment and fan-out order cannot diverge under concurrent senders.
- [x] **Local WebSocket Disconnect Cleanup Guard**: handler-level room stream tests now prove a disconnected client unsubscribes from the room hub and decrements `websocket_active_connections_count` back to zero, and `tools/run-go-core-gate.mjs` requires the cleanup PASS line before emitting `go-test-gate` proof.
- [x] **Local WebSocket Lagging Subscriber Guard**: `RoomHub` evicts full subscriber queues and the room stream records `dependency="websocket", operation="room_broadcast", status="dropped"` metrics so fan-out pressure is observable instead of silently losing participant broadcasts.
- [x] **WebSocket Load No-Drop Evidence Guard**: `tools/loadtest` and strict staging evidence validation now require Redis/WebSocket load evidence to include `room_broadcast_drops=0`, so contiguous sequence proof cannot mask dropped room broadcasts during staging fan-out tests.
- [x] **WebSocket Load Placeholder Host Guard**: `tools/loadtest` rejects reserved documentation/sample HTTP/WSS targets, WebSocket origins, and load artifact hosts such as `.example`, `example.com`, `.test`, and `.invalid` before emitting staging HTTP/WebSocket load evidence.
- [x] **Load Duration Summary Marker Guard**: `tools/loadtest`, the staging evidence recorder, and strict staging validation now require HTTP and WebSocket load summaries to preserve literal `duration_ms>=60000` alongside numeric `duration_ms` checks, so short runs cannot satisfy `PERF-HTTP-001` or `PERF-WS-001` by only declaring `production_min_duration_ms=60000`.
- [x] **WebSocket Load Distinct Artifact Guard**: `tools/loadtest`, the staging evidence recorder, and strict staging validation require WebSocket replica, reconnect, HTTP polling fallback, and Redis telemetry proof to use distinct HTTPS artifact URLs plus `ws_distinct_artifacts=true`, so one broad artifact cannot satisfy every `PERF-WS-001`/`DATA-REDIS-001` proof role.
- [x] **WebSocket Load Artifact URL Provenance Guard**: `tools/loadtest`, the staging evidence recorder, and strict staging validation require WebSocket/Redis load summaries to preserve concrete HTTPS `ws_replica_artifact_url`, `ws_reconnect_artifact_url`, `ws_polling_artifact_url`, and `redis_telemetry_artifact_url` markers, so strict-release evidence cannot replace fetched side artifacts with generic verified labels.
- [x] **HTTP Load Distinct Artifact Guard**: `tools/loadtest`, the staging evidence recorder, and strict staging validation require HTTP replica and dependency telemetry proof to use distinct HTTPS artifact URLs plus `http_distinct_artifacts=true`, so one broad artifact cannot satisfy every `PERF-HTTP-001` proof role.
- [x] **HTTP Load Dependency Latency Guard**: `tools/loadtest` now requires the dependency telemetry artifact for `PERF-HTTP-001` to carry concrete `postgres_p99_ms=<n>`, `redis_p99_ms=<n>`, and `dependency_threshold_pass=true` markers, while the staging evidence recorder and strict-release validation require `dependency_latency_artifact_verified=true` in the load summary before HTTP performance evidence can support the 5,000 RPS/P99 claim.
- [x] **Load Artifact Alias Guard**: `tools/loadtest` canonicalizes performance artifact URLs before distinctness checks, so default-port, host-case, or query-order aliases cannot satisfy separate HTTP/WebSocket/Redis proof roles.
- [x] **WebSocket Load Room Binding Guard**: `tools/loadtest`, the staging evidence recorder, and strict staging validation require WebSocket load summaries and reconnect/polling/Redis artifacts to carry the room identity (`ws_room_id`/`room_id=<id>`), so reconnect, polling fallback, and Redis sequence proof must bind to the same staged room as the load run.
- [x] **WebSocket Load Exact Room Binding Guard**: `tools/loadtest`, the staging evidence recorder, and strict staging validation now reject `PERF-WS-001`/`DATA-REDIS-001` reports unless structured JSON `ws_room_id`, `ws_reconnect_room_id`, `ws_polling_room_id`, and `redis_telemetry_room_id` all match the exact room markers in the result summary, so reconnect, polling, and Redis proof cannot be stitched from another staged room.
- [x] **WebSocket Load Sequence Range Guard**: The staging evidence recorder and strict-release validator now require `PERF-WS-001` to carry `ws_expected_events`, `ws_unique_sequences`, `ws_min_sequence=1`, `ws_max_sequence`, and `ws_polling_latest_sequence` as a coherent range, so a WebSocket throughput pass cannot omit or weaken contiguous sequence proof.
- [x] **WebSocket Polling Latest Sequence Guard**: `tools/loadtest`, the staging evidence recorder, and strict staging validation require HTTP polling fallback evidence to include `ws_polling_latest_sequence=<ws_max_sequence>`, so fallback proof must show the state endpoint reached the final accepted WebSocket sequence for that staged room.
- [x] **WebSocket Polling Artifact Minimum Sequence Guard**: `tools/loadtest` now parses the deployed HTTP polling fallback artifact's `latest_sequence=<n>` marker and rejects values below the 30,000-event production minimum, so a stale state endpoint artifact cannot satisfy `PERF-WS-001`/`DATA-REDIS-001` while the structured report claims full sequence coverage.
- [x] **WebSocket Polling Artifact Sequence Marker Guard**: `tools/loadtest` emits `ws_polling_artifact_latest_sequence_validated=true`, and both the staging evidence recorder and strict manifest validator require it, so recorded `PERF-WS-001`/`DATA-REDIS-001` evidence must preserve proof that the polling artifact body passed the 30,000-sequence validation.
- [x] **Redis Polling Sequence Field Guard**: `tools/loadtest` now emits the structured JSON field `ws_polling_latest_sequence`, and the staging evidence recorder requires it to equal `ws_max_sequence`, so a summary string cannot claim polling caught up while the structured report shows stale room state.
- [x] **Local WebSocket Event Type Guard**: room stream events now reject whitespace, uppercase, punctuation, and overlong `type` values before Redis append or broadcast; local Go gate proof requires `TestLiveRoomRejectsInvalidEventTypesWithoutPersisting` to pass before emitting WebSocket production-behavior markers.
- [x] **Current Local WebSocket Evidence Refresh**: focused 2026-06-29 local checks passed for room stream behavior, Redis Lua sequencing, and the WebSocket load harness (`internal/ports`, `internal/domain/room`, and `tools/loadtest`); the room stream slice now includes 8 `TestLiveRoom*` tests, including all-subscriber fan-out delivery. Deployed multi-replica WSS/Redis evidence remains tracked under `PERF-WS-001` and `DATA-REDIS-001`.
- [x] **Local Zoom Webhook Validation And Mapping Guard**: Zoom webhooks support signed `endpoint.url_validation`, acknowledge unknown meetings without mutating Redis state, and mapped webhooks update only the resolved internal `live_rooms.id`, not the external meeting ID.
- [x] **Current Local Zoom Evidence Refresh**: focused 2026-06-29 local checks passed for bounded Zoom client resilience, webhook signature/idempotency/mapping behavior, retryable state-mutation failure handling, and `tools/zoomprobe` artifact validation. Webhook delivery IDs are marked processed only after successful mapped-room state mutation and released on Redis/state-manager failure for retry. Live staging Zoom OAuth/webhook/fallback/mapping proof remains tracked under `EXT-ZOOM-001`.
- [x] **Zoom Structured Manifest Guard**: `tools/record-staging-evidence.mjs` now persists `structured_report.zoom_resilience_webhook_proof` for `EXT-ZOOM-001`, and strict-release validation rejects Zoom evidence when timeout/circuit/fallback booleans, webhook signature/timestamp, webhook outcome booleans, URL validation tokens, duplicate delivery/tracking IDs, side-effect booleans, or meeting-to-room mapping IDs drift from the recorded `zoomprobe` summary.
- [x] **Local Rust gRPC Config Guard**: `cmd/platform-engine` now rejects missing `GRPC_ENGINE_ADDRESS` in staging/production/prod while retaining `localhost:50051` only for local development.
- [x] **Local Rust gRPC mTLS/Tenant Guard**: the Rust listener requires TLS and a 32-byte shared secret in staging/production, the Go client sends mTLS credentials plus `x-scriptureforge-organization-id`, Rust rejects forged tenant metadata and oversized requests, and `tools/rustprobe` now requires CA/client certificate/client key/server-name inputs and proves an in-test mTLS health handshake. Staging certificate issuance, Secrets Store CSI injection/rotation, cross-namespace traffic, and `RUST-GRPC-001` deployment evidence remain pending.
- [x] **Rust Probe Target Host Guard**: `tools/rustprobe` rejects localhost, loopback, private-network, unspecified, and link-local gRPC, Rust metrics, and Go API metrics targets before emitting `RUST-GRPC-001`.
- [x] **Rust Exact Release And Environment Binding Guard**: `tools/rustprobe`, the staging evidence recorder, and strict staging manifest validation require Rust gRPC health, Rust metrics, and Go API Rust-integration metrics evidence to carry exact `release_candidate=<manifest release_candidate>`, `service_version`, and `deployment_environment` markers before `RUST-GRPC-001` can support final readiness.
- [x] **Rust Segment Guard**: strict-release validation now requires `rust-grpc-health`, `rust-metrics`, and `api-rust-integration-metrics` to each carry their own health, metric, integration, and release markers before `RUST-GRPC-001` can support final readiness.
- [x] **Rust Metrics Sample Guard**: `tools/rustprobe`, the staging evidence recorder, and strict-release validation now require concrete Prometheus sample proof markers for Rust engine counters and API-to-Rust integration metrics, so comment-only or name-only scrapes cannot satisfy `RUST-GRPC-001`.
- [x] **Rust Structured Metrics Field Guard**: `tools/rustprobe` now emits positive structured `embedding_requests`, `vector_search_requests`, `api_rust_vector_search_ops`, and `api_rust_vector_search_seconds` fields, and the staging evidence recorder rejects `RUST-GRPC-001` reports where those fields are missing or non-positive.
- [x] **Rust Structured Manifest Guard**: `tools/record-staging-evidence.mjs` now persists `structured_report.rust_grpc_runtime_proof` for `RUST-GRPC-001`, and strict-release validation rejects Rust evidence when deployment environment, run identity, health/service identity, Rust metric counts, or Go API Rust-integration metric counts drift from the recorded `rustprobe` summary.
- [x] **Rust Structured Target Guard**: strict-release validation revalidates retained `rust_grpc_runtime_proof` targets: `grpc_target` must remain a non-local host:port service target, Rust `metrics_target` must remain an HTTP(S) non-local artifact URL, Go `api_metrics_target` must remain HTTPS, and the metrics/API metrics targets must remain distinct after canonicalization.
- [x] **Rust Lockfile Verification Guard**: local and CI Rust gates run `cargo test --locked`, while `tools/verify-rust-protobuf.mjs` keeps vendored `protoc`, rejects ambient `protoc`/`PROTOC` requirements, requires all checked-in vendored `protoc` platform lockfile packages, generated embedding/vector-search protobuf type coverage, tenant/RPC proto contract coverage, bounded vector-search input validation, gRPC health service registration, Kubernetes-safe gRPC/metrics bind defaults, and the SQLx/pgvector dependency lane pinned to the checked-in lockfile.
- [x] **Rust Local Gate Proof Marker Guard**: `tools/run-rust-cargo-gate.mjs` and `tools/validate-local-gate-report.mjs` now reject local gate reports unless `rust-protobuf-validation` output proves vendored `protoc`, ambient `protoc` is not required, generated protobuf type coverage, generated gRPC client/server type coverage, tenant/RPC proto contract coverage, lockfile platform `protoc` coverage, health-service coverage, bounded vector-search input validation, and Rust runtime observability coverage, and unless `rust-cargo-test` output proves locked Cargo tests ran the full current 13-test Rust suite, including generated protobuf/gRPC types, vector-search bounds, gRPC health-service naming, Kubernetes-safe bind defaults, observability defaults, traceparent handling, metrics rendering/HTTP behavior, and JSON escaping.
- [x] **Rust Ambient Protoc Poison Guard**: `tools/run-rust-cargo-gate.mjs` sets `PROTOC=__scriptureforge_invalid_ambient_protoc__` before locked cargo tests, while `build.rs` reruns on `PROTOC` changes and then overwrites it with `protoc_bin_vendored::protoc_bin_path()`, so the local/CI cargo gate proves vendored protobuf generation instead of an ambient machine `protoc`.
- [x] **Rust Runtime Observability Source Guard**: `tools/verify-rust-protobuf.mjs` now emits `rust_runtime_observability_covered=true` and fails if Rust Prometheus embedding/vector-search request/failure counters, W3C `traceparent` tests, observability default tests, or `SERVICE_VERSION`/`DEPLOYMENT_ENVIRONMENT`/`OTEL_EXPORTER_OTLP_ENDPOINT` reads drift out of the runtime source.
- [x] **Rust Metrics Surface Separation Guard**: `tools/rustprobe`, the staging evidence recorder, and strict staging manifest validation now require `RUST-GRPC-001` to prove canonical-distinct Rust service metrics and HTTPS Go API metrics targets with `distinct_metrics_targets=true`, so one combined scrape or default-port, host-case, query-order, or fragment alias cannot masquerade as both Rust health/metrics and API-to-Rust integration evidence.
- [x] **Rust Probe Placeholder Host Guard**: `tools/rustprobe` now rejects reserved documentation/sample hosts such as `.example`, `example.com`, `.test`, and `.invalid` for gRPC, Rust metrics, and API metrics targets before emitting `RUST-GRPC-001`, so source probe reports align with strict staging validation instead of relying on recorder cleanup.
- [x] **Local Route P99 Metrics Surface**: Go API `/metrics`, Grafana dashboard, and alert rules use route-latency histogram buckets for P99 latency instead of average-only duration signals.
- [x] **Observability Trace Route Binding Guard**: strict staging validation now requires `OBS-OTEL-001` trace-backend and log-correlation proof segments to carry the same concrete `trace_id`, `route=<observed_route>`, and `method=<http_method>`, so trace evidence from one route cannot be paired with log evidence from another route or HTTP method.
- [x] **Observability Probe Artifact Host Guard**: `tools/observabilityprobe` rejects reserved documentation/sample hosts such as `.example`, `example.com`, `.test`, and `.invalid`, plus localhost, loopback, private-network, unspecified, and link-local hosts for collector/API metrics/Rust metrics/log/trace/dashboard/alert evidence URLs, so `OBS-OTEL-001` and `OBS-ALERT-001` must use reviewable staging telemetry artifacts rather than sample domains or internal scrape addresses.
- [x] **Load Evidence Host Guard**: `tools/loadtest` rejects localhost, loopback, private-network, unspecified, and link-local HTTP/WSS staging targets, WebSocket origins, and performance artifact URLs before emitting `PERF-HTTP-001`, `PERF-WS-001`, or `DATA-REDIS-001` evidence markers.
- [x] **Load Exact Release Binding Guard**: `tools/loadtest`, the staging evidence recorder, and strict staging manifest validation now require HTTP/WebSocket load evidence to carry the exact `release_candidate=<manifest release_candidate>` marker so stale performance artifacts cannot satisfy `PERF-HTTP-001`, `PERF-WS-001`, or `DATA-REDIS-001`.
- [x] **Load Segment Guard**: strict-release validation now requires `PERF-HTTP-001`, `PERF-WS-001`, and `DATA-REDIS-001` loadtest summaries to keep threshold, observed, release, origin/auth, and sequence markers in their own measurement segments and artifact verification markers in the verified-artifacts segment; WebSocket and Redis load evidence fails if the `PERF-WS-001` or `DATA-REDIS-001` measurement segment is missing entirely.
- [x] **Load Numeric Threshold Guard**: strict-release validation parses load evidence numbers and rejects `PERF-HTTP-001` below 5,000 observed RPS or above 200ms observed P99, rejects `PERF-WS-001` below 500 observed events/sec or above 200ms observed P99, and rejects `DATA-REDIS-001` when unique/max/polling sequence counts do not match the expected event count.
- [x] **Load Structured Manifest Guard**: `tools/record-staging-evidence.mjs` now persists `structured_report.http_load_threshold_proof` for `PERF-HTTP-001` and `structured_report.websocket_redis_sequence_proof` for `PERF-WS-001`/`DATA-REDIS-001`; strict-release validation rejects drift in HTTP threshold fields, WebSocket load fields, and Redis sequence/room/principal fields from the recorded `loadtest` summary.
- [x] **Load Production Target Marker Guard**: `tools/record-staging-evidence.mjs` and strict-release validation require HTTP and WebSocket load summaries to preserve `production_target_rps` and `production_target_p99_ms` markers, so staging performance evidence is explicitly tied to the architecture target instead of only local threshold labels.
- [x] **Load Threshold Pass Guard**: the staging evidence recorder and strict-release validation require `threshold_pass=true` in HTTP and WebSocket load summary segments, so a report with passing-looking numbers but admitted threshold failures cannot satisfy `PERF-HTTP-001` or `PERF-WS-001`.
- [x] **Load Threshold Contradiction Guard**: `tools/loadtest`, the staging evidence recorder, and strict-release validation reject performance artifacts or summaries that include threshold-failure wording such as `threshold failed`, `threshold_failures`, RPS-below-threshold, or P99-above-threshold markers even if required passing threshold markers also appear.
- [x] **Resilience Evidence Host Guard**: `tools/resilienceprobe` rejects reserved documentation/sample hosts such as `.example`, `example.com`, `.test`, and `.invalid`, plus localhost, loopback, private-network, unspecified, link-local, and `local-only` rollback, degradation, backup, restore, and restored-smoke artifacts before emitting DR evidence markers.
- [x] **Resilience Exact Release Binding Guard**: `tools/resilienceprobe`, `tools/record-staging-evidence.mjs`, and strict staging manifest validation require rollback, degradation, backup, restore, and restored-smoke evidence to carry the exact `release_candidate=<manifest release_candidate>` plus `service_version` markers before `DR-ROLLBACK-001` or `DR-BACKUP-001` can support final readiness.
- [x] **Resilience Segment Guard**: strict-release validation now requires rollback readiness, rollout undo, degradation fallback, backup snapshot, restore drill, and restored-database smoke segments to each carry their own required markers before `DR-ROLLBACK-001` or `DR-BACKUP-001` can support final readiness.
- [x] **Resilience Distinct Artifact Guard**: `tools/resilienceprobe`, the staging evidence recorder, and strict staging validation require rollback readiness/rollout/degradation proof to use distinct artifact URLs with `distinct_rollback_artifacts=true`, and backup/restore/restored-smoke proof to use distinct artifact URLs with `distinct_backup_artifacts=true`.
- [x] **Resilience Artifact Alias Guard**: `tools/resilienceprobe` canonicalizes rollback and backup evidence artifact URLs before distinctness checks, so default-port, host-case, query-order, or fragment aliases cannot satisfy separate rollback, degradation, snapshot, restore, and restored-smoke proof roles.
- [x] **Resilience Structured Backup Field Guard**: `tools/resilienceprobe` emits structured JSON `snapshot_id`, `kms_key_id`, `restore_job_id`, `source_snapshot_id`, `rpo_minutes`, `rto_minutes`, and `restore_duration_minutes` fields for backup/restore drills, and the staging evidence recorder rejects missing, mismatched, or cross-artifact-inconsistent structured values before `DR-BACKUP-001` can be recorded.
- [x] **Resilience Rollback Version Binding Guard**: `tools/resilienceprobe`, the staging evidence recorder, and strict-release validation require concrete `pre_rollback_version`, `post_rollback_version`, `rolled_back_from`, and `rolled_back_to` values, with `rolled_back_from` matching the pre-rollback version and `rolled_back_to` matching the post-rollback version before `DR-ROLLBACK-001` can support final readiness.
- [x] **Resilience RTO Contradiction Guard**: `tools/resilienceprobe`, the staging evidence recorder, and strict-release validation now reject backup/restore evidence when `restore_duration_minutes` exceeds `rto_minutes`, so a restore drill cannot satisfy `DR-BACKUP-001` while admitting it missed its declared recovery-time objective.
- [x] **Resilience Failure Contradiction Guard**: `tools/resilienceprobe`, the staging evidence recorder, strict-release validation, and the staging evidence contract reject rollback, rollout undo, degradation, backup, restore, smoke, RPO, or RTO evidence that admits failed drills or exceeded recovery objectives even if required success markers also appear.
- [x] **Local Gate Git Integrity**: local gate reports include branch/upstream, ahead/behind counts, and `git status --short`; strict validation rejects dirty or unsynced reports unless development-only relaxations are explicit.
- [x] **Local Gate Chronology Guard**: local gate reports cannot be future-dated; `tools/validate-local-gate-report.mjs` rejects `observed_at` values after the validation date before final readiness verification can use them.
- [x] **Local Gate Command Drift Guard**: `tools/validate-local-gate-report.mjs` requires every recorded gate command to match the canonical `tools/run-local-gates.mjs` definition, so stale or weakened local gate rows cannot satisfy final readiness.
- [x] **Audit/Init Local Gate Proof Marker Guard**: `tools/run-npm-audit.mjs` and `tools/run-terraform-init.mjs` emit machine-checkable proof summaries, and `tools/validate-local-gate-report.mjs` rejects local gate reports whose web/mobile npm audit or Terraform init rows omit severity/backend/network-classification markers.
- [x] **NPM Audit Completion Marker Guard**: `tools/run-npm-audit.mjs` emits `npm_audit_completed=true` only for completed audit runs and `npm_audit_network_blocked=true` for registry/network blocker rows; `tools/validate-local-gate-report.mjs` rejects network-blocked npm audit rows for release-grade local reports, so dependency audit evidence cannot be satisfied by an unreachable registry.
- [x] **Client Smoke Local Gate Proof Marker Guard**: web and mobile API/crypto smoke tests emit product-flow and crypto proof summaries for auth, encrypted journal payloads, room/WebSocket setup, strict endpoint guards, AES-GCM, associated data, PBKDF2, key disposal, and native-required mobile fail-closed behavior; `tools/validate-local-gate-report.mjs` rejects web/mobile smoke rows missing those markers.
- [x] **RLS DB Gate Fail-Closed Guard**: the current local matrix includes `rls-db-integration` via `tools/run-rls-db-integration-docker.mjs` with `REQUIRE_DATABASE_URL=true`, and local gate report validation rejects skipped gates plus weakened RLS gate commands so DB-backed tenant/RLS proof cannot be satisfied by skip-capable or generic test rows.
- [x] **RLS DB Report Proof Marker Guard**: `tools/run-rls-db-integration.mjs` emits a compact `rls-db-integration proof:` summary only after the DB-backed tenant/RLS tests pass, and `tools/validate-local-gate-report.mjs` now requires that summary to include table RLS, journal handler RLS, room active handler RLS, journal/room tenant isolation, socket tenant scoping, AI request log/citation RLS, and generated curriculum audit RLS markers before a local gate report can support readiness.
- [x] **RLS DB Semantic Proof Marker Guard**: `tools/run-rls-db-integration.mjs` now emits explicit semantic proof markers for all nine tenant-scoped tables, same-tenant read success, cross-tenant read/write/update/delete denial, journal plaintext rejection, room override denial, WebSocket tenant scoping, AI audit RLS, auth refresh/session RLS, MFA, and workspace-switch tenant matching; `tools/validate-local-gate-report.mjs` rejects local reports that drop those markers.
- [x] **Room State Handler RLS Guard**: The DB-backed handler integration suite now proves `/api/v1/rooms/state/{room_id}` denies cross-tenant polling before the state store is called and allows same-tenant polling for the owned room; the schema validator and RLS gate wrapper require this test and room-state proof markers.
- [x] **RLS DB Verbose PASS Guard**: `tools/run-rls-db-integration.mjs` now parses verbose `go test` output and emits `rls-db-integration proof:` only after every required table, handler, socket, AI audit, auth/session, MFA, and workspace-switch RLS test reports `--- PASS`, so missing or skipped DB-backed tenant tests cannot produce local readiness proof markers.
- [x] **Disposable RLS Semantic Proof Refresh**: `tools/run-rls-db-integration-docker.mjs --container-name scriptureforge-rls-semantic-proof` passed on 2026-06-30 against disposable `pgvector/pgvector:pg16`, applied migrations, passed the required DB-backed tenant/RLS tests, and emitted the semantic proof markers. This remains local/container evidence; deployed `DATA-RLS-001` tenantprobe artifacts are still pending external staging proof.
- [x] **Tenant RLS Structured Manifest Guard**: `tools/record-staging-evidence.mjs` now persists a compact `structured_report.database_rls_context_proof` for `DATA-RLS-001`, and strict-release validation rejects tenant evidence that lacks exact structured table names and per-table same-visible/cross-hidden/write-denied outcomes.
- [x] **Tenant RLS Structured Identity Guard**: strict-release validation now rejects `DATA-RLS-001` evidence when the persisted structured owner/blocked tenant IDs or created journal/room IDs drift from the recorded tenantprobe summary markers.
- [x] **Disposable RLS DB Proof**: `tools/run-rls-db-integration-docker.mjs` passed on 2026-06-29 against a disposable `pgvector/pgvector:pg16` container, applying migrations and proving tenant table RLS, journal/room handler RLS, active-room handler RLS, mismatched tenant/user room-create denial with no room or participant persistence, journal/room tenant isolation, socket tenant scoping, AI audit/citation RLS, generated curriculum audit RLS, auth refresh/session RLS, privileged MFA login blocking/verification, workspace-switch tenant matching, and privileged-only MFA enrollment/verification locally; deployed RLS evidence remains tracked under `DATA-RLS-001`.
- [x] **Dirty-Worktree Local Gate Snapshot**: `artifacts/local-gate-report-current.json` ran the current 31-gate matrix on 2026-06-29T12:33:40Z with 0 failures on synced branch `codex/production-readiness-remediation`; this is development evidence only because release readiness still requires a clean worktree, exact release SHA, clean pushed CI, and staging evidence.
- [x] **Local CI Release Evidence Order Guard**: `tools/validate-ci-workflow.mjs` now requires TruffleHog and tracked-file cleanliness before CI release evidence is written, and requires local `ciprobe` validation before artifact upload.
- [x] **CI Release Evidence Artifact Proof Marker Guard**: `tools/write-ci-release-evidence.mjs` emits proof markers inside `ci-release-evidence.txt` for full commit SHA validation, GitHub run provenance, run-attempt provenance, clean source-control verification, exact-SHA required-gate scope, local gate marker inclusion, TruffleHog inclusion, and success conclusion before `SRC-CI-001` can be recorded from CI output.
- [x] **CI Probe Release Evidence Proof Marker Guard**: `tools/ciprobe` rejects `ci-release-evidence.txt` artifacts that omit the release evidence proof-marker line or any individual marker for full commit SHA validation, GitHub run provenance, run-attempt provenance, clean source-control verification, exact-SHA required-gate scope, local gate marker inclusion, TruffleHog inclusion, and success conclusion before emitting `SRC-CI-001`.
- [x] **CI Artifact Structured Run Identity Guard**: `tools/ciprobe` now emits and validates `artifact_commit_sha`, `run_id`, `run_attempt`, and `run_number`, requires `artifact_commit_sha` to match `commit_sha`, requires `ci_run_url` to carry the same `run_id`, and `tools/record-staging-evidence.mjs` rejects `SRC-CI-001` reports that omit or mismatch those structured fields.
- [x] **CI Gap Report Footer Contract Marker Guard**: `tools/write-ci-release-evidence.mjs`, `tools/ciprobe`, and `tools/validate-ci-evidence-gates.mjs` now require `staging_gap_report_footer_contract_required=true`, binding `SRC-CI-001` release evidence to the strengthened staging-evidence gap-report footer contract for blocker IDs and status counts.
- [x] **CI Gap Report Required-Evidence Marker Guard**: `tools/write-ci-release-evidence.mjs`, `tools/ciprobe`, and marker-drift validation now require `staging_gap_report_required_evidence_contract_required=true`, binding CI release evidence to the local gate contract that gap reports preserve rendered `required:` evidence details.
- [x] **Final Claim Gap Report Footer Marker Guard**: `tools/verify-production-readiness.mjs` now emits `staging_gap_report_footer_contract_validated=true` after validating the strict staging manifest, current Obsidian snapshot, and local gate report, so a final readiness proof explicitly depends on the strengthened blocker-ID/status-count gap-report footer contract.
- [x] **CI Workflow Local Gate Proof Marker Guard**: `tools/validate-ci-workflow.mjs` emits proof markers for Go 1.24.3, DB-required RLS integration, web/mobile/Rust/Terraform gates, security scans, local tooling tests, staging evidence contract checks, staging evidence gap-report checks, clean git before release evidence, and write/validate/upload ordering; `tools/validate-local-gate-report.mjs` rejects local gate reports whose `ci-workflow-validation` output lacks those markers.
- [x] **CI Terraform Recursive Fmt Guard**: GitHub Actions and `tools/validate-ci-workflow.mjs` require `terraform fmt -check -recursive`, matching the local Terraform gate so split/nested Terraform files stay covered; live Terraform plan/apply evidence remains tracked under `DEPLOY-TF-001`.
- [x] **Terraform Init Wrapper Regression Guard**: `tools/run-terraform-init.test.mjs` proves the local Terraform init wrapper keeps the safe `-backend=false` default, preserves explicit init args, classifies Terraform registry/network outages as local blockers, and propagates real Terraform failures; the shared local tooling-test gate and GitHub Actions require this coverage.
- [x] **Terraform Validate Success Output Guard**: `tools/run-terraform-command.mjs` and local gate report validation now require Terraform's own `Success! The configuration is valid.` output before accepting `terraform-validate-gate` proof markers, so local/CI deployment-skeleton evidence cannot rely on a wrapper-only exit-zero claim.
- [x] **CI Evidence Gate Mirror Proof Marker Guard**: `tools/validate-ci-evidence-gates.mjs` emits proof markers proving `run-local-gates`, `write-ci-release-evidence`, and `tools/ciprobe` mirror the same gate list plus TruffleHog, that `tools/ciprobe` mirrors the writer's release evidence proof-marker list, and that duplicate gate or proof markers are rejected; `tools/validate-local-gate-report.mjs` rejects local gate reports whose `ci-evidence-gate-validation` output lacks those markers or omits the required-entry/proof-marker counts.
- [x] **CI Probe Artifact Host Guard**: `tools/ciprobe` rejects non-HTTPS, localhost, loopback, private-network, unspecified, and link-local remote CI artifact URLs before emitting `SRC-CI-001`; local artifact-file validation remains allowed only for downloaded GitHub Actions debugging and cannot be recorded as production readiness evidence.
- [x] **Web Smoke Distinct Artifact Guard**: `tools/stagingprobe`, `tools/record-staging-evidence.mjs`, and strict staging validation reject reused auth/journal/room browser smoke artifact URLs for `CLIENT-WEB-001`, and each auth, encrypted-journal, and room/WebSocket smoke segment must carry `distinct_web_artifacts=true` so one broad browser artifact cannot satisfy multiple web product-flow proof roles.
- [x] **CI Tooling Test Parity**: `.github/workflows/security.yml` now runs the same local tooling-test suite as `tools/run-local-gates.mjs`, including npm audit and Terraform init wrapper tests, RLS DB runner tests, deployment/RLS/observability/dependency/staging/security validators, production-readiness verifier, evidence recorder/bootstrap/gap/release-evidence tests, Rust protobuf verifier tests, and PATH verifier tests; `tools/validate-ci-workflow.mjs` rejects workflows that drop the npm audit wrapper test, Terraform init wrapper test, Docker RLS tooling test, observability validator test, secret hygiene validator test, or security artifact validator test.
- [x] **Go Core Gate Proof Wrapper Guard**: `tools/run-go-core-gate.mjs` wraps all-package Go test and Go vet gates, emits stable proof markers for `go test ./... -count=1 -timeout=90s` and `go vet ./...`, and is required by local gates, GitHub Actions, CI workflow drift validation, and local gate report validation.
- [x] **Client Build Proof Wrapper Guard**: `tools/run-client-command.mjs` wraps web typecheck, web build, and mobile build-check gates so successful client commands emit stable proof markers; local gates, GitHub Actions, `tools/validate-ci-workflow.mjs`, and `tools/validate-local-gate-report.mjs` reject raw or marker-free client build/typecheck evidence.
- [x] **Production Evidence Probe Proof Wrapper Guard**: `tools/run-go-probe-tests.mjs` wraps the Go probe-test batch for source/tenant/Rust/observability/security/resilience/mobile/deployment/abuse/Zoom/AI/load evidence tooling, emits per-probe-family proof markers, and is required by local gates, GitHub Actions, CI workflow drift validation, and local gate report validation.
- [x] **Production Evidence Probe Package Result Guard**: `tools/run-go-probe-tests.mjs` now refuses to emit probe proof markers unless the uncached Go output includes an `ok scriptureforge/tools/...` result line for every source, tenant, Rust, observability, security, resilience, mobile, deployment, abuse, Zoom, AI, and load probe package, preventing partial probe runs from advertising full evidence-tool coverage.
- [x] **Terraform Fmt/Validate Proof Wrapper Guard**: `tools/run-terraform-command.mjs` wraps recursive Terraform formatting checks and Terraform validation for `build/terraform`, emits stable proof markers, and is required by local gates, GitHub Actions, CI workflow drift validation, and local gate report validation alongside the existing network-aware init wrapper.
- [x] **Staging PATH Discovery Guard**: `tools/use-project-path.cmd`, `tools/use-project-path.ps1`, and `tools/verify-project-path.mjs` search repo-local tools plus common Windows Program Files, WinGet, Chocolatey, and Scoop locations for staging evidence tools, and strict staging failures list searched fallback paths for missing `psql`/`aws` remediation.
- [x] **Security Probe Scoped IAM Guard**: `tools/securityprobe` and the staging evidence recorder now require IRSA trust-policy proof plus scoped/no-wildcard Secrets Manager IAM policy markers before `SEC-SECRETS-001` can be recorded.
- [x] **Security Probe Synced-Secret Leak Guard**: `tools/securityprobe` rejects synced-secret artifacts that expose plaintext DSNs/API keys, common base64-encoded DSN/API-key values, Kubernetes `stringData`, private keys, or other obvious secret value markers before `SEC-SECRETS-001` can be recorded.
- [x] **Security Probe Synced-Secret Ownership Guard**: `tools/securityprobe`, the staging evidence recorder, and strict-release validation require redacted synced-secret metadata to include `managed by secrets-store.csi.k8s.io`, `ownerReferences`, and `secrets-store.csi.k8s.io/managed=true` before `SEC-SECRETS-001` can support final readiness.
- [x] **Security Strict Summary Leak Guard**: strict-release validation rejects `SEC-SECRETS-001` result summaries that contain plaintext DSNs, OpenAI-style API keys, common base64 secret prefixes, `stringData:`, client/webhook secrets, passwords, or PEM headers, so manually edited summaries cannot bypass probe-side secret leak checks.
- [x] **Security Probe Segment Guard**: strict-release validation now requires `irsa-service-account`, `secret-provider-class`, `synced-secret-metadata-redacted`, `iam-secrets-policy`, and `scoped-secrets-access-test` to each carry their own required markers before `SEC-SECRETS-001` can support final readiness.
- [x] **DB User Segment Guard**: `tools/securityprobe`, the staging evidence recorder, and strict-release validation now require `database-scoped-user` itself to prove a live connection plus `superuser=false`, `bypassrls=false`, `createrole=false`, `createdb=false`, `privileged_operation_denied=true`, `app_grants_verified=true`, `app_grant_tables=9`, and `app_grants=SELECT,INSERT,UPDATE,DELETE` before `SEC-DBUSER-001` can support final readiness.
- [x] **DB User Structured Field Guard**: `tools/securityprobe` now emits structured `current_user`, privilege flags, privileged-operation denial, app-grant verification, app-grant table count, and app-grant list fields for `database-scoped-user`; the staging evidence recorder rejects reports whose structured fields do not prove the scoped `scriptureforge_app` role and strict-release validation binds `connected as` plus `current_user` to that same principal.
- [x] **Security Report Run Identity Guard**: `tools/record-staging-evidence.mjs` now rejects `SEC-SECRETS-001` and `SEC-DBUSER-001` reports unless the report itself carries `load_run_id`, preventing hand-edited secret-sync or DB-user reports from relying only on per-segment run markers.
- [x] **Security Exact Release Binding Guard**: `tools/securityprobe`, the staging evidence recorder, and strict staging manifest validation require secret/IRSA artifacts and scoped DB-user proof to carry exact `release_candidate=<manifest release_candidate>` plus `service_version` markers before `SEC-SECRETS-001` or `SEC-DBUSER-001` can support final readiness.
- [x] **Security Secret Distinct Artifact Guard**: `tools/securityprobe`, the staging evidence recorder, and strict staging validation require IRSA, SecretProviderClass, synced-secret metadata, IAM policy, and scoped access-test proof to use distinct HTTPS artifact URLs plus `distinct_secret_artifacts=true`, so one broad security artifact cannot satisfy every `SEC-SECRETS-001` proof role.
- [x] **Security Secret Artifact Alias Guard**: `tools/securityprobe` canonicalizes secret-management evidence artifact URLs before distinctness checks, so default-port, host-case, query-order, or fragment aliases cannot satisfy separate IRSA, SecretProviderClass, synced-secret, IAM policy, and access-test proof roles.
- [x] **Accepted Dependency Risk Review Guard**: `tools/validate-dependency-risk.mjs`, `tools/record-staging-evidence.mjs`, and strict staging evidence validation reject stale accepted-risk evidence; the prior DRR-001 accepted risk is now closed and the open DRR-002 high audit finding cannot be waived into a release claim.
- [x] **Dependency Risk Local Gate Proof Marker Guard**: `tools/validate-dependency-risk.mjs` emits proof markers for mobile lockfile uuid/Expo detection, DRR-001 lifecycle/closure state, runtime import reachability, high-or-worse audit gating, and remediation closure documentation; `tools/validate-local-gate-report.mjs` rejects local gate reports whose `dependency-risk-validation` output lacks those markers.
- [x] **NPM Audit Wrapper Regression Guard**: `tools/run-npm-audit.test.mjs` proves the web/mobile audit wrapper preserves configured severity levels, propagates vulnerability failures, classifies registry outages separately from vulnerability results, and falls back through `cmd.exe` for Windows npm shims; the shared local tooling-test gate and GitHub Actions require this coverage.
- [x] **Security Signoff Exact Release Binding Guard**: `tools/record-staging-evidence.mjs` and strict staging manifest validation require passed `SEC-SIGNOFF-001` owner/security signoff summaries to carry exact `release_candidate=<manifest release_candidate>` so a prior release signoff cannot satisfy a current production-readiness claim.
- [x] **Security Signoff Artifact Content Guard**: `tools/record-staging-evidence.mjs` now reads repo-local `security/*.md` owner/security signoff artifacts and requires the document itself to carry threat-model, DRR-001, residual-risk, owner/security approval, release-risk signoff, `signoff_artifact_verified=true`, and exact release-candidate markers before recording `SEC-SIGNOFF-001`; strict staging validation requires the same verification marker in passed signoff summaries.
- [x] **Security Signoff Non-Production Marker Guard**: `tools/record-staging-evidence.mjs` rejects `SEC-SIGNOFF-001` manual signoff summaries and repo-local signoff documents that include mock, dry-run, local-only, synthetic, stubbed, placeholder, or test-only caveats, so owner/security approval evidence cannot be recorded while admitting it is non-production proof.
- [x] **Security Signoff Pending Contract Guard**: `tools/validate-staging-evidence.mjs` rejects pending `SEC-SIGNOFF-001` contract rows whose `required_evidence` omits content-verified signoff artifact wording or `signoff_artifact_verified=true`, so environment manifests cannot drift back to summary-only owner/security approval requirements.
- [x] **Security Probe Artifact Host Guard**: `tools/securityprobe` rejects reserved documentation/sample hosts such as `.example`, `example.com`, `.test`, and `.invalid`, plus localhost, loopback, private-network, unspecified, link-local, and `local-only` IRSA, Secrets Store CSI, synced-secret, IAM-policy, and access-test artifacts before emitting secret or DB-user evidence markers.
- [x] **Security Artifacts Local Gate Proof Marker Guard**: `tools/validate-security-artifacts.mjs` emits proof markers for threat model STRIDE coverage, crypto/IAM review, SAST/SCA report, domain-specific audit, fuzzing report, secret handling review, dependency risk register, stale-claim rejection, and residual-risk signoff path coverage; `tools/validate-local-gate-report.mjs` rejects local gate reports whose `security-artifacts-validation` output lacks those markers.
- [x] **Security Artifacts Regression Test Guard**: `tools/validate-security-artifacts.test.mjs` proves current security docs pass, stale security claims fail, required coverage markers cannot be removed, and every required security file must be loaded; the local tooling-test gate, GitHub Actions tooling-test command, and `tools/validate-ci-workflow.mjs` all require this test.
- [x] **Secret Hygiene Local Gate Proof Marker Guard**: `tools/validate-secret-hygiene.mjs` emits proof markers for tracked and untracked text-file scanning, local `.env` rejection, high-confidence secret pattern scanning, `.gitignore` env/Terraform-state coverage, Terraform placeholder markers, and zero plaintext-secret findings; `tools/validate-local-gate-report.mjs` rejects local gate reports whose `secret-hygiene-validation` output lacks those markers.
- [x] **Secret Hygiene Regression Test Guard**: `tools/validate-secret-hygiene.test.mjs` proves the current repository passes secret hygiene validation and rejects local `.env` files, high-confidence provider tokens/private keys, missing `.gitignore` env/Terraform-state protections, and missing Terraform placeholder markers; GitHub Actions runs the test directly and through the local tooling-test parity command.
- [x] **Deployment Probe Artifact Host Guard**: `tools/deploymentprobe` rejects reserved documentation/sample hosts such as `.example`, `example.com`, `.test`, and `.invalid`, plus localhost, loopback, private-network, unspecified, link-local, and `local-only` deployment artifacts before emitting Terraform or Kubernetes deployment evidence markers.
- [x] **Deployment Exact Release Binding Guard**: `tools/deploymentprobe`, the staging evidence recorder, and strict staging manifest validation now require Terraform/Kubernetes deployment evidence to carry the exact `release_candidate=<manifest release_candidate>` marker so stale deployment artifacts cannot satisfy `DEPLOY-TF-001` or `DEPLOY-K8S-001`.
- [x] **Terraform Init/Plan Release Binding Guard**: `tools/deploymentprobe`, the staging evidence recorder, and strict-release validation now require `terraform-remote-backend-init` and `terraform-staging-plan` segments to carry exact `release_candidate=<manifest release_candidate>` plus `service_version` markers, so stale remote-backend or plan artifacts cannot be paired with a current apply/approval record.
- [x] **Terraform Remote State KMS/Versioning Guard**: `tools/deploymentprobe`, the staging evidence recorder, strict-release validation, and the staging evidence contract now require `kms_key_id=` plus `versioning=enabled` on the `terraform-remote-backend-init` segment, so generic `encrypt=true` proof cannot satisfy deployment readiness without KMS-backed state encryption and state bucket versioning markers.
- [x] **Terraform Storage KMS Input Guard**: Terraform now requires customer-managed `database_kms_key_arn` and `redis_kms_key_arn` inputs, wires them into Aurora and Redis encryption, and requires deployment evidence to preserve concrete `kms_key_id=<arn-or-alias>`, `database_kms_key_arn=<arn:aws:kms:...:key/...>`, and `redis_kms_key_arn=<arn:aws:kms:...:key/...>` markers before `DEPLOY-TF-001` can support readiness.
- [x] **Deployment Segment Guard**: strict-release validation now requires Terraform backend, Terraform plan, Terraform apply-or-approval, Kubernetes rollout, and Kubernetes workload resource segments to each carry their own required markers before `DEPLOY-TF-001` or `DEPLOY-K8S-001` can support final readiness.
- [x] **Kubernetes Immutable Digest Count Guard**: `tools/deploymentprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation now require `kubernetes-workload-resources` evidence to include at least three concrete `sha256:<64 hex>` immutable image digests bound to the API/web/Rust workload names, and the probe emits `concrete_image_digests=3` plus workload digest markers, so a generic digest label, unbound digest list, or single workload digest cannot satisfy `DEPLOY-K8S-001`.
- [x] **Deployment Approval Change-Ticket Guard**: Terraform approval fallback evidence must include a structured `change_ticket` field plus matching `change_ticket=<ticket-id>` summary marker before `DEPLOY-TF-001` can pass without a real apply output; the probe, recorder, and strict-release verifier all reject approval fallback summaries that omit the ticket ID.
- [x] **Terraform Apply Zero-Destroy Guard**: Terraform apply evidence must preserve `Apply complete`, `Resources:`, and `0 destroyed` markers before `DEPLOY-TF-001` can pass from a real apply artifact; otherwise the path must use the structured deployment-approval fallback with a matching change ticket.
- [x] **Terraform Apply Count Guard**: Terraform apply evidence now has to carry structured `terraform_apply_added=<n>`, `terraform_apply_changed=<n>`, and `terraform_apply_destroyed=0` markers, plus matching probe JSON fields, so a generic `0 destroyed` phrase cannot stand in for parseable Terraform apply counts.
- [x] **Terraform Distinct Artifact Guard**: `tools/deploymentprobe`, the staging evidence recorder, and strict staging validation require Terraform backend, plan, and apply-or-approval proof to use distinct HTTPS artifact URLs plus `distinct_terraform_artifacts=true`, so one broad artifact cannot satisfy every `DEPLOY-TF-001` proof role.
- [x] **Terraform Artifact Alias Guard**: `tools/deploymentprobe` canonicalizes Terraform evidence artifact URLs before distinctness checks, so default-port, host-case, query-order, or fragment aliases cannot satisfy separate backend, plan, and apply-or-approval proof roles.
- [x] **Kubernetes Distinct Artifact Guard**: `tools/deploymentprobe`, the staging evidence recorder, and strict staging validation require Kubernetes rollout and workload-resource proof to use canonical-distinct HTTPS artifact URLs plus structured workload image digests, `concrete_image_digests=3`, `workload_image_digests=3`, and `distinct_kubernetes_artifacts=true`, so one broad or summary-only `kubectl` artifact cannot satisfy both `DEPLOY-K8S-001` proof roles.
- [x] **Kubernetes Release Identity Guard**: `tools/deploymentprobe`, `tools/record-staging-evidence.mjs`, and strict staging validation now require Kubernetes rollout/workload evidence to name the staging namespace plus immutable image digest, `release_candidate`, and `service_version` markers before `DEPLOY-K8S-001` can support a production-readiness claim.
- [x] **Deployment Local Gate Proof Marker Guard**: `tools/validate-deployment-skeleton.mjs` emits proof markers for remote backend shape, IRSA secret access, SecretProviderClass sync, TLS ingress, RDS backup/final snapshot controls, Redis runtime wiring, Rust gRPC wiring, OTEL env wiring, HPA/PDB rollout safety, and root database URL absence; `tools/validate-local-gate-report.mjs` now rejects local gate reports whose `deployment-skeleton-validation` output lacks those markers.
- [x] **Terraform Secret Input Guard**: `tools/validate-deployment-skeleton.mjs` now proves the database root passphrase is sensitive, length-validated, and has no Terraform default, and that API/JWT/OpenAI/Zoom workload secrets are accepted only as AWS Secrets Manager ARNs before local deployment evidence can support readiness.
- [x] **Terraform Staging Input Placeholder Guard**: `build/terraform/variables.tf` rejects `unversioned`/`latest`/`replace-with` service versions, non-HTTPS or wildcard WebSocket origins, and localhost/example/test/invalid API/web hostnames before staging/prod plan inputs can support deployment readiness; `tools/validate-deployment-skeleton.mjs` emits `staging_input_placeholders_rejected=true` while those guards remain wired.
- [x] **Deployment Failure Contradiction Guard**: `tools/deploymentprobe`, the staging evidence recorder, and strict staging manifest validation reject Terraform/Kubernetes evidence that admits init, plan, apply, rollout, zero ready/available replicas, CrashLoopBackOff, or ImagePullBackOff failures even if the same summary also contains required success markers for `DEPLOY-TF-001` or `DEPLOY-K8S-001`.
- [x] **Staging Evidence Recorder Host Guard**: `tools/record-staging-evidence.mjs` rejects localhost, loopback, private-network including IPv6 unique-local and IPv4-mapped private IPv6, unspecified, and link-local targets/artifacts across probe-backed evidence ingestion, including host:port gRPC targets and URL-based artifacts.
- [x] **Strict Staging PATH Guard**: `tools/verify-project-path.mjs --strict-staging` now fails missing or broken-version staging evidence tools (`gopls`, `psql`, `kubectl`, `aws`, `gh`) while keeping standalone `protoc` optional only when `services/scripture-engine/Cargo.toml` and `Cargo.lock` prove `protoc-bin-vendored` coverage. Current local strict staging verification resolves `gopls`, `psql`, `kubectl`, `aws`, and `gh`; `psql` is available from `C:\Program Files\PostgreSQL\18\bin\psql.exe`, AWS CLI v2 is available from `C:\Program Files\Amazon\AWSCLIV2\aws.exe`, and the report emits `vendored_protoc_covered=true`. `tools\use-project-path.cmd` also forwards arbitrary commands after activating the repo-local PATH, matching the PowerShell helper and allowing fresh shells to run gates such as `cargo test --manifest-path services\scripture-engine\Cargo.toml --locked`.
- [x] **Gap Report PATH Blocker**: `tools/report-staging-evidence-gaps.mjs` now runs strict staging PATH readiness during CLI reports and emits a `STAGING-PATH-TOOLS` blocker with concrete remediation and searched fallback paths when evidence-collection tools such as `psql` or `aws` are missing; current local gap reporting has no non-manifest PATH blocker.
- [x] **Gap Report Accepted-Risk Guard**: `tools/report-staging-evidence-gaps.mjs` only treats `SEC-SIGNOFF-001` accepted risk as non-blocking when it references a current dependency decision record with fresh review/expiry metadata, matching the final strict-release verifier instead of allowing arbitrary or overdue signoff risk records; DRR-001 is currently closed.
- [x] **Gap Report Strict Validation Guard**: `tools/report-staging-evidence-gaps.mjs` now runs strict release validation when status, contract, release-SHA, and strict-PATH blockers are otherwise clear, then renders `STAGING-EVIDENCE-STRICT-VALIDATION` if weak passed evidence would fail the final verifier.
- [x] **Source CI Exact Release Binding Guard**: `tools/ciprobe`, the staging evidence recorder, and strict staging manifest validation require `SRC-CI-001` to prove the CI artifact `commit_sha` exactly matches `release_candidate=<manifest release_candidate>` before source-control evidence can support final readiness.
- [x] **Final Claim PATH Enforcement**: `tools/verify-production-readiness.mjs` now runs strict staging PATH readiness before accepting a production-readiness claim, and `tools/sync-obsidian-readiness.mjs` embeds that same PATH result into the generated readiness snapshot.
- [x] **Staging Evidence Contract Sync Gate**: local gates and CI run `tools/sync-staging-evidence-contract.mjs` with explicit checked-in example manifest inputs before the Obsidian readiness snapshot, so clean checkouts do not depend on ignored environment-specific evidence files.
- [x] **Obsidian Sync Unit Gate**: local and CI tooling tests now include `tools/sync-obsidian-readiness.test.mjs`, so strict PATH and release-candidate snapshot behavior stay covered before final production-readiness claims can pass.
- [x] **Strict Release Evidence Marker Guard**: `tools/validate-staging-evidence.mjs --strict-release` rejects non-HTTPS, local, unspecified, private-network including IPv6 unique-local and IPv4-mapped private IPv6, mock, placeholder, synthetic, stubbed, test-only, dry-run, and local-only probe artifacts before final production-readiness claims can pass.
- [x] **Strict Release Chronology Guard**: staging evidence manifests cannot be future-dated, and strict release manifests require `release_candidate` to be the exact 40-character Git commit SHA that final readiness verification compares against the checkout.
- [x] **Windows PATH Activation Helper**: `tools\use-project-path.cmd --strict-staging` and `tools\use-project-path.ps1 -StrictStaging` add repo-local plus common Go/AWS/PostgreSQL tool paths before running strict staging PATH verification, preserve verifier failures, and rely on the richer verifier report instead of redundant `where` checks after success; the PowerShell helper can also prefix commands so repo-local Go/Rust/Terraform resolve in fresh shells.
- [x] **Network-Locked Local Gates**: local reproducibility is maintained by gating `npm audit` and `terraform init` in `run-local-gates` through network-aware wrappers that fail closed only on non-network errors.
- [x] **Roadmap/Architecture Drift Control**: keep `SF-roadmap.md` and `SF-architecture.md` aligned with implemented routes, tenancy boundaries, and production gates.
- [x] **Obsidian/Serena Drift Gate**: `SF-architecture.md` and `SF-roadmap.md` now include production-aligned stack versions and full `/api/v1` route coverage.
- [x] **Serena/Obsidian Pre-Merge Gate**: ensure every new implementation file and route change is mirrored in both `SF-roadmap.md` and `SF-architecture.md` before merge.

## Serena-Enabled Work Tracking

- Added language indexing targets: `go`, `typescript`, `rust`, `terraform`, `markdown`, `yaml`.
- Added workspace links for Serena cross-package browsing:
  - `web`
  - `mobile`
  - `services/scripture-engine`
- Use this file as the canonical Obsidian/Serena handoff surface for PR planning and weekly readiness check-ins.

<!-- OBSIDIAN-STAGING-EVIDENCE-SNAPSHOT-START -->
## Staging Evidence Snapshot (staging)
- release_candidate: replace-with-git-sha-or-tag
- strict_release_ready: no
- strict_staging_path_ready: yes
- non_manifest_blockers: 0
- counts: passed=0, pending_external=21, blocked=0, failed=0, accepted_risk=0
- proof_markers: strict_release_readiness_computed=true, strict_staging_path_readiness_computed=true, release_candidate_match_checked=true, pending_external_items_counted=true, non_manifest_blockers_counted=true, contract_drift_blockers_counted=true, accepted_risk_status_counted=true, accepted_risk_metadata_freshness_checked=true, strict_release_validation_checked=true, blocking_items_listed=true, blocking_item_required_evidence_listed=true
- expected_release_candidate: replace-with-git-sha-or-tag
- release_candidate_matches_expected: yes
- blocking items:
  - SRC-CI-001 [pending_external]: Clean pushed GitHub Actions run for the exact release branch.
    - required: tools/ciprobe JSON report with SRC-CI-001 evidence item from the uploaded HTTPS ci-release-evidence artifact URL, commit_sha exactly matching release_candidate=<manifest release_candidate>, artifact_commit_sha structurally matching commit_sha, and run_id/run_attempt/run_number provenance bound to ci_run_url; local artifact-file mode is debug-only and not accepted for recorded production readiness evidence; reserved example/test/invalid hosts are not accepted
    - required: Uploaded GitHub Actions ci-release-evidence artifact from the exact release SHA with release_candidate=<manifest release_candidate>, artifact_commit_sha_structural_binding_required=true, and github_run_id_url_binding_required=true markers in the recorded summary
    - required: GitHub Actions run URL
    - required: Full 40-character commit SHA matching the manifest release_candidate
    - required: GitHub repository/ref/event/run-attempt provenance plus clean source-control marker from the release workflow
    - required: All required workflow jobs passing
  - DEPLOY-TF-001 [pending_external]: Terraform remote state is initialized and the staging plan/apply completes against real inputs.
    - required: tools/deploymentprobe -probe-terraform JSON report with DEPLOY-TF-001 evidence item and report-level load_run_id=<same load run>
    - required: terraform init output with remote S3 backend bucket/key, KMS encryption marker, state bucket versioning enabled, DynamoDB locking markers, exact release_candidate=<manifest release_candidate>, service_version linkage, and load_run_id=<same load run>
    - required: terraform plan artifact proving EKS, node group, RDS, Redis, customer-managed RDS/Redis KMS key bindings, ECR, workload deployments, ingresses, HPA, PDB, SecretProviderClass, IAM role resources, non-placeholder service_version, real API/web hostnames, HTTPS-only WebSocket origins, exact release_candidate=<manifest release_candidate>, service_version linkage, and load_run_id=<same load run>
    - required: terraform apply output proving Apply complete, Resources, structured terraform_apply_added=<n>, terraform_apply_changed=<n>, terraform_apply_destroyed=0, exact release_candidate=<manifest release_candidate>, service_version linkage, and load_run_id=<same load run>, or a DEPLOY-TF-001 deployment approval record proving change_ticket=<ticket-id> plus exact release_candidate=<manifest release_candidate>, service_version linkage, and load_run_id=<same load run>
    - required: Terraform backend, plan, and apply-or-approval artifacts must be canonical-distinct HTTPS URLs, not one combined proof artifact or URL alias
    - required: deploymentprobe result_summary marker list for Terraform backend, plan, and apply-or-approval probes with staging artifact provenance, remote-state locking/KMS encryption/state-bucket-versioning markers, concrete Terraform state KMS marker kms_key_id=<arn-or-alias>, concrete customer-managed storage KMS markers database_kms_key_arn=<arn:aws:kms:...:key/...> and redis_kms_key_arn=<arn:aws:kms:...:key/...>, workload resource markers, exact release_candidate=<manifest release_candidate>, matching load_run_id=<same load run>, and distinct_terraform_artifacts=true; non-HTTPS, reserved example/test/invalid hosts, local, loopback, private-network, unspecified, link-local, mock, placeholder, synthetic, stubbed, test-only, dry-run, local-only, Terraform init/plan/apply failure, rollout failure, zero ready/available replica, CrashLoopBackOff, or ImagePullBackOff artifact URLs, content, or result summaries are not accepted
  - DEPLOY-TLS-001 [pending_external]: DNS, ACM, ALB ingress, HTTPS redirect, and TLS policy are proven against deployed API and web hostnames.
    - required: tools/stagingprobe JSON report with DEPLOY-TLS-001 evidence item, report-level ssl_labs_artifact_url=<https artifact>, load_run_id=<same load run>, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run> markers
    - required: HTTPS DNS lookup artifact for deployed API and web hostnames tied to exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run>
    - required: HTTPS ACM certificate status and TLS policy artifact tied to exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run>
    - required: HTTPS SSL Labs scan artifact proving A+ grade for deployed API and web hostnames with ssl_labs_grade=A+, grade=A+, api_hostname=<api host>, web_hostname=<web host>, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run>
    - required: DNS, ACM/TLS, and SSL Labs artifacts must be canonical-distinct HTTPS URLs, not one combined proof artifact or URL alias
    - required: HTTPS probe output for API live/readiness, API TLS, web root, and web TLS with exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run> markers
    - required: HTTP to HTTPS redirect probe output with exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run> markers
    - required: stagingprobe result_summary marker list for API live/readiness, TLS, redirect, and ssl-labs-a-plus probes with exact release_candidate=<manifest release_candidate>, service_version, matching load_run_id=<same load run>, SSL Labs A+ markers, and SSL Labs hostnames matching the deployed certificate hostnames; mixed-run summaries, non-HTTPS, reserved example/test/invalid hosts, local, loopback, private-network, unspecified, link-local API/web targets, TLS artifact URLs, or non-A+ SSL Labs grades are not accepted
  - DEPLOY-K8S-001 [pending_external]: Kubernetes API, web, and Rust engine workloads are running with readiness/liveness probes, PDBs, HPAs, and rolling update settings.
    - required: tools/deploymentprobe -probe-kubernetes JSON report with DEPLOY-K8S-001 evidence item and report-level load_run_id=<same load run>
    - required: kubectl rollout status output proving the staging namespace and API, web, and Rust workloads are rolled out, ready, and available with exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run> markers
    - required: kubectl get deploy,svc,ingress,hpa,pdb,secretproviderclass -o yaml output proving staging namespace, services, TLS ingress, readiness/liveness probes, HPA min/max replicas and targets, PDB min availability, rolling update maxUnavailable=0, SecretProviderClass wiring, immutable workload-bound API/web/Rust image digests, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run>
    - required: Kubernetes rollout and workload-resource artifacts must be canonical-distinct HTTPS URLs so one broad kubectl artifact cannot satisfy both proof roles
    - required: deploymentprobe result_summary marker list for rollout and workload resource probes with staging artifact provenance, namespace, health-probe, rollout-safety, autoscaling, disruption-budget, TLS ingress, SecretProviderClass, exact release_candidate=<manifest release_candidate> plus service_version and load_run_id=<same load run> markers on the rollout segment, at least three concrete sha256:<64 hex> image digests bound to the API/web/Rust workload names, concrete_image_digests=3, workload_image_digests=3, exact release_candidate=<manifest release_candidate>, service_version, release-linkage markers, matching load_run_id=<same load run>, and distinct_kubernetes_artifacts=true; non-HTTPS, reused, canonical-equivalent, reserved example/test/invalid hosts, local, loopback, private-network, unspecified, link-local, mock, placeholder, synthetic, stubbed, test-only, dry-run, local-only, Terraform init/plan/apply failure, rollout failure, zero ready/available replica, CrashLoopBackOff, or ImagePullBackOff artifact URLs, content, or result summaries are not accepted
  - SEC-SECRETS-001 [pending_external]: Secrets Store CSI and IRSA sync runtime secrets from the cloud secret manager without committed plaintext.
    - required: tools/securityprobe -probe-secrets JSON report with SEC-SECRETS-001 evidence item, report-level load_run_id=<same run>, staging artifact provenance on every probe segment, explicit workload identity markers `namespace=staging`, `service_account=scriptureforge-api`, and the same concrete `role_arn=arn:aws:iam::<12-digit-account>:role/<role>` marker across IRSA, SecretProviderClass, IAM policy, and scoped access-test probes, plus exact release_candidate=<manifest release_candidate>, service_version, and matching load_run_id=<same run> markers
    - required: Secrets Store CSI driver status with staging artifact provenance, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same run> markers
    - required: IRSA role annotation and trust policy with staging artifact provenance, namespace=staging, service_account=scriptureforge-api, concrete role_arn=arn:aws:iam::<12-digit-account>:role/<role>, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same run> markers
    - required: SecretProviderClass sync output proving AWS provider, Secrets Manager objects, object aliases, jmesPath, secretObjects, Opaque runtime-secret mapping, staging artifact provenance, namespace=staging, service_account=scriptureforge-api linkage, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same run>
    - required: Redacted synced-secret metadata proving Opaque DATABASE_URL, JWT_SECRET_KEY, OPENAI_API_KEY, and ZOOM_WEBHOOK_SECRET_TOKEN keys with stringData absent, no base64-encoded secret data values, Secrets Store CSI management markers `managed by secrets-store.csi.k8s.io`, `ownerReferences`, and `secrets-store.csi.k8s.io/managed=true`, staging artifact provenance, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same run>
    - required: Scoped Secrets Manager access test proving configured-secret allow, unscoped-secret AccessDenied, staging artifact provenance, namespace=staging, service_account=scriptureforge-api, concrete role_arn=arn:aws:iam::<12-digit-account>:role/<role>, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same run>
    - required: IRSA, SecretProviderClass, synced-secret metadata, IAM policy, and scoped access-test artifacts must be canonical-distinct HTTPS URLs, not one combined proof artifact or URL alias
    - required: securityprobe result_summary marker list for each secret/IRSA artifact probe, including staging artifact provenance, workload identity markers with one matching concrete IAM role ARN, AWS object-sync, synced-secret redaction and owner/managed markers, exact release_candidate=<manifest release_candidate>, service_version, matching load_run_id=<same run> markers, and distinct_secret_artifacts=true; non-HTTPS, reserved example/test/invalid hosts, local, loopback, private-network, unspecified, link-local, mismatched workload role ARNs, mixed-run security artifact summaries, postgres://, postgresql://, plaintext DSN, base64-encoded DSN/API-key data, sk-, client_secret:, webhook_secret:, password:, stringData, -----BEGIN, API key, private key, and secret-value artifacts or result summaries are not accepted
  - SEC-DBUSER-001 [pending_external]: DATABASE_URL uses a scoped application database user rather than the RDS root user.
    - required: tools/securityprobe -probe-db-user JSON report with SEC-DBUSER-001 evidence item, report-level load_run_id=<same run>, staging artifact provenance on database-scoped-user, and exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same run> markers
    - required: Redacted DATABASE_URL principal proof with staging artifact provenance, connected as scriptureforge_app, superuser=false, bypassrls=false, createrole=false, createdb=false, app_grants_verified=true, app_grant_tables=9, app_grant_table_names=organizations,users,scripture_texts,refresh_tokens,journal_entries,live_rooms,room_participants,ai_request_logs,citation_trails, app_grants=SELECT,INSERT,UPDATE,DELETE, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same run> markers
    - required: Database grants for app role across organizations, users, scripture_texts, refresh_tokens, journal_entries, live_rooms, room_participants, ai_request_logs, and citation_trails
    - required: Denied privileged operation probe with privileged_operation_denied=true
  - ABUSE-LIMIT-001 [pending_external]: Auth login, auth refresh, account-scoped login, AI, journal, room, and WebSocket abuse limits are observed through real staging ingress behavior.
    - required: tools/abuseprobe JSON report with ABUSE-LIMIT-001 evidence item, report-level load_run_id=<same run>, exact release_candidate=<manifest release_candidate>, service_version, load_run_id markers from the same staging load run, and persisted staging-manifest structured_report.abuse_rate_limit_proof matching config verification, positive ABUSE_LIMIT_* assignments, per-profile attempts, concrete rate-limit headers, account/refresh/WebSocket scoped booleans
    - required: 429 responses for auth login, auth refresh, auth-account, AI, journal, rooms, and real WebSocket upgrade profiles after at least 2 attempts with concrete attempt counts, repeated_attempts_verified=true, structured account_scoped=true, forwarded_client_ip_rotated=true, refresh_token_scoped=true, websocket_upgrade=true where applicable, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id markers
    - required: Retry-After, X-RateLimit-Limit, X-RateLimit-Remaining, and X-RateLimit-Reset header proof with positive retry/limit/reset values, X-RateLimit-Remaining=0, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id
    - required: HTTPS redacted artifact proving positive-integer staging ABUSE_LIMIT_* request/window assignments, positive-integer ABUSE_LIMIT_AUTH_ACCOUNT_* request/window assignments, positive-integer ABUSE_LIMIT_MAX_BUCKETS assignment, TRUST_PROXY_HEADERS=true, X-Forwarded-For, X-Real-IP, redacted configuration, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id used for the probe
    - required: abuseprobe config_artifact_verified=true with config_artifact_summary markers proving the redacted configuration artifact was fetched and verified with concrete positive-integer ABUSE_LIMIT_* assignment markers for exact release_candidate=<manifest release_candidate> plus service_version plus load_run_id
    - required: Abuse report must include HTTPS web_origin used for WebSocket probes; config artifact host must be distinct from staging API target and web_origin, with distinct_abuse_artifacts=true in the verified config summary
    - required: abuseprobe result_summary marker list for auth login, auth refresh, account-scoped login, AI, journal, rooms, and WebSocket profiles including staging artifact provenance on every route-profile segment, concrete attempt counts >= 2, repeated_attempts_verified=true, refresh token, refresh_token_scoped=true, rotating forwarded client IP, account_scoped=true, forwarded_client_ip_rotated=true, websocket upgrade, websocket_upgrade=true, exact release_candidate=<manifest release_candidate>, service_version proof, load_run_id proof with all profile/config summaries matching the same value, and distinct_abuse_artifacts=true
  - DATA-RLS-001 [pending_external]: Tenant RLS behavior is proven through the deployed API and staging database connection pool.
    - required: tools/tenantprobe JSON report with DATA-RLS-001 evidence item, report-level load_run_id=<same load run>, and report-level created_journal_id/created_room_id fields matching the resources created during the run
    - required: Same-tenant journal and room create/read/list/state probe output, including owner journal list visibility for the created entry
    - required: Cross-tenant journal read/list denial, room list/state denial, and API tenant/user override write-denial output for journal and room create routes
    - required: Database/RLS proof artifact must use a canonical-distinct evidence host from the staging API target, not one combined API proof artifact or host alias
    - required: Redacted database role with current_user=scriptureforge_app, superuser=false, bypassrls=false, app.current_org_id, app.current_org_id=<tenantprobe owner_org_id>, current_setting('app.current_org_id'), blocked_org_id=<tenantprobe blocked_org_id>, row_security=on, forced row_security across all tenant-scoped tables, rls_tables_verified=9, rls_forced_tables=9, rls_table_names=organizations,users,scripture_texts,refresh_tokens,journal_entries,live_rooms,room_participants,ai_request_logs,citation_trails, rls_policy_scope=app.current_org_id, same-tenant read-visible proof, cross-tenant read-hidden proof, cross-tenant write-denial proof, auth_refresh_session_rls=true, auth_mfa_rls=true, workspace_switch_tenant_match=true, privileged_mfa_enrollment_rls=true, ai_audit_rls=true, generated_curriculum_audit_rls=true, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id markers, tenantprobe structured UUID-shaped owner_org_id and blocked_org_id fields for different organizations, tenantprobe report-level created_journal_id and created_room_id fields matching owner-create-encrypted-journal journal_id=<id> and owner-create-room room_id=<id> summary markers, tenantprobe structured journal_id and room_id fields matching journal_id=<id> and room_id=<id> summary markers for the created resources, tenantprobe structured database proof fields application_role=scriptureforge_app, row_security=on, rls_tables_verified=9, rls_forced_tables=9, rls_table_names exactly matching organizations,users,scripture_texts,refresh_tokens,journal_entries,live_rooms,room_participants,ai_request_logs,citation_trails, rls_table_outcomes entries for each table with same_visible=true, cross_hidden=true, and write_denied=true plus matching rls_table_<table>_same_visible=true, rls_table_<table>_cross_hidden=true, and rls_table_<table>_write_denied=true summary markers, persisted staging-manifest structured_report.database_rls_context_proof with the same table names/outcomes, and rls_policy_scope=app.current_org_id, and tenantprobe result_summary marker list with staging artifact marker, distinct_db_rls_artifact=true, same-tenant journal list-visible, API tenant-override write-denial markers, auth/session/MFA/workspace tenant isolation markers, AI audit/citation tenant isolation markers, exact UUID tenant-pair DB/RLS binding markers, and matching load_run_id across every API/DB proof segment; non-HTTPS, reserved example/test/invalid hosts, local, loopback, private-network, unspecified, link-local, mock, placeholder, synthetic, stubbed, test-only, dry-run, and local-only API targets or artifacts are not accepted
  - DATA-REDIS-001 [pending_external]: Redis-backed room sequencing is contiguous under deployed multi-client concurrency.
    - required: External WebSocket load report with DATA-REDIS-001 evidence item, report-level load_run_id=<same load run>, and persisted staging-manifest structured_report.websocket_redis_sequence_proof matching room/principal IDs, reconnect/polling/Redis room bindings, production event minimum, expected/unique/min/max/polling sequence counts, zero room_broadcast_drops, and reconnect/auth/contiguous sequence booleans
    - required: HTTPS Redis telemetry artifact from the same run
    - required: Redis sequence ordering assertion output with no duplicate, skipped sequence, room-id mismatch, or room broadcast drop proof
    - required: loadtest JSON ws_room_id matching exact ws_room_id=<id> result_summary marker, structured ws_user_id/ws_organization_id fields and markers proving the authenticated tenant principal, structured ws_reconnect_room_id/ws_polling_room_id/redis_telemetry_room_id fields and markers equal to ws_room_id, structured ws_reconnect_sequence_continues=true field/marker, production_min_ws_events=30000, at least 30000 expected/unique/max/polled events, ws_polling_latest_sequence and ws_polling_artifact_latest_sequence equal to ws_max_sequence, structured room_broadcast_drops=0 field/marker, plus result_summary marker list for WebSocket profile, authenticated HTTPS origin proof, staging artifact provenance, contiguous sequence fields, reconnect proof with ws_reconnect_sequence_continues=true, HTTP polling fallback proof, Redis telemetry artifact, redis_telemetry_artifact_verified marker, ws_polling_artifact_latest_sequence_validated=true marker, ws_polling_artifact_latest_sequence_matches_run=true marker, ws_distinct_artifacts=true marker, and room_broadcast_drops=0 marker
  - RUST-GRPC-001 [pending_external]: Deployed Rust gRPC service is reachable from the Go API and exposes health and metrics.
    - required: tools/rustprobe JSON report with RUST-GRPC-001 evidence item, report-level load_run_id=<same run>, and persisted staging-manifest structured_report.rust_grpc_runtime_proof matching deployment_environment, non-local host:port grpc_target, HTTP(S) non-local metrics_target, HTTPS non-local api_metrics_target, distinct metrics/API metrics targets, load_run_id, health_status=SERVING, service_name=scriptureforge.engine.ScriptureEngine, positive Rust embedding/vector-search sample counts, and positive Go API Rust-integration metric samples
    - required: gRPC health probe output with exact release_candidate=<manifest release_candidate>, service_version, deployment_environment, and load_run_id=<same run> markers
    - required: HTTPS Go API metrics integration probe with exact release_candidate=<manifest release_candidate>, service_version, deployment_environment, and load_run_id=<same run> markers
    - required: Rust metrics scrape output with concrete Prometheus samples for embedding/vector-search request and failure counters, positive embedding/vector-search request counters, exact release_candidate=<manifest release_candidate>, service_version, deployment_environment, and load_run_id=<same run> markers
    - required: Canonical-distinct Rust service metrics and HTTPS Go API metrics targets proving distinct_metrics_targets=true
    - required: rustprobe result_summary marker list for gRPC health, service name, SERVING status over grpc_transport_security=mTLS, Rust embedding/vector-search request and failure counter samples, positive Rust request counters with rust_embedding_requests_positive=true and rust_vector_search_requests_positive=true, Go API integration metric samples, positive structured embedding_requests, vector_search_requests, api_rust_vector_search_ops, and api_rust_vector_search_seconds fields, staging artifact provenance on every Rust health/metrics/API-integration proof segment, rust_metrics_samples_verified=true, api_rust_metrics_samples_verified=true, distinct_metrics_targets=true, Prometheus metrics, exact release_candidate=<manifest release_candidate>, service_version, deployment_environment, and matching load_run_id=<same run> markers; reserved example/test/invalid hosts, local, loopback, private-network, unspecified, link-local, malformed, non-HTTPS API metrics, duplicate metrics/API metrics aliases, idle all-zero Rust request counters, mixed-run Rust health/metrics/API-integration summaries, or metrics-free Rust targets are not accepted
  - OBS-OTEL-001 [pending_external]: OpenTelemetry collector/backend receives traces and preserves trace IDs across API and Rust logs.
    - required: tools/observabilityprobe -probe-otel JSON report with OBS-OTEL-001 evidence item, report-level load_run_id=<same run>, persisted 32-character non-zero lowercase hex OpenTelemetry trace_id, observed_route, http_method, trace/log result_summary segments containing the same concrete trace_id plus exact route=<observed_route> and method=<http_method> markers, distinct collector/API metrics/Rust metrics/trace/log artifact URLs, trace/log backend query URLs scoped to that trace_id, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id markers from the same staging load run, and persisted staging-manifest structured_report.observability_otel_proof matching release/run identity, trace/log route/method/trace IDs, tenant/user/role principals, and HTTPS non-local distinct collector/API/Rust/trace/log targets with trace/log targets scoped to the same trace_id
    - required: Collector endpoint configuration with OTLP receivers/exporters/service pipeline and exact release_candidate=<manifest release_candidate> plus service_version plus load_run_id markers
    - required: API metrics scrape output with positive Prometheus samples for scriptureforge_http_requests_total, scriptureforge_http_request_duration_seconds_sum, ai_inference_duration_seconds_count, and scriptureforge_dependency_operations_total; status labels, websocket_active_connections_count, dependency="websocket",operation="room_broadcast",status="dropped", ai_inference_duration_seconds profile series, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id markers
    - required: Rust metrics scrape output with scriptureforge_rust_engine_embedding_requests_total, scriptureforge_rust_engine_embedding_failures_total, scriptureforge_rust_engine_vector_search_requests_total, scriptureforge_rust_engine_vector_search_failures_total, positive embedding/vector-search request samples, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id markers
    - required: Trace search output proving the same trace crosses scriptureforge-api and scriptureforge-rust-engine with exact release_candidate=<manifest release_candidate>, service_version, and load_run_id markers
    - required: Log query output with trace_id, scriptureforge-api, scriptureforge-rust-engine, timestamp=<event timestamp>, severity=<level>, service_version, deployment_environment, concrete tenant_id=<id>, user_id=<id>, role=<role>, exact release_candidate=<manifest release_candidate>, and load_run_id markers
    - required: observabilityprobe result_summary marker list for collector, API metrics with api_metrics_samples_positive=true, Rust embedding/vector-search request and failure counters with rust_metrics_samples_positive=true, trace search bound to route=<observed_route> and method=<http_method>, log correlation bound to the same route and method, staging artifact provenance on each collector/API metrics/Rust metrics/trace/log proof segment, distinct_otel_artifacts=true, and trace/log query probes from HTTP(S) staging telemetry surfaces with exact release_candidate=<manifest release_candidate> plus service_version plus matching load_run_id markers; mixed-run summaries, idle all-zero API/Rust metric samples, reserved example/test/invalid hosts, local, loopback, private-network, unspecified, link-local, mock, placeholder, synthetic, stubbed, test-only, dry-run, and local-only URLs or content are not accepted
  - OBS-ALERT-001 [pending_external]: Dashboard import, alert delivery, and telemetry retention are proven in staging.
    - required: tools/observabilityprobe -probe-alerts JSON report with OBS-ALERT-001 evidence item, report-level load_run_id=<same run>, concrete alert_name and alert_receiver fields, alert-delivery-status marker list containing alertname=<alert_name>, receiver=<alert_receiver>, delivery_id=<id>, distinct dashboard/rules/delivery/retention artifact URLs, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id markers from the same staging load run, and persisted staging-manifest structured_report.observability_alert_proof matching release/run identity, alert identity, delivery ID, and HTTPS non-local distinct dashboard/rules/delivery/retention targets
    - required: Grafana dashboard import screenshot or export with API, latency, architecture metric profile, Rust, trace_id panels, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id markers
    - required: Alert rules loaded for high error rate, traffic absence, auth failure spikes, abuse-limit spikes, route latency, dependency failures, AI inference latency, journal write failures, room stream failures, room broadcast drops, and Rust engine failures with exact release_candidate=<manifest release_candidate> plus service_version plus load_run_id markers
    - required: Delivered Alertmanager test alert output with concrete alertname=<alert_name>, receiver=<alert_receiver>, delivery_id=<id>, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id markers
    - required: 30-day trace/log/metric retention policy proof with exact release_candidate=<manifest release_candidate>, service_version, and load_run_id markers
    - required: observabilityprobe result_summary marker list for dashboard, full production alert-rule suite, alert delivery with alertname=<alert_name>, receiver=<alert_receiver>, and delivery_id=<id>, retention, staging artifact provenance on each dashboard/rules/delivery/retention proof segment, and distinct_alert_artifacts=true probes from HTTP(S) staging telemetry surfaces with exact release_candidate=<manifest release_candidate> plus service_version plus matching load_run_id markers; mixed-run summaries, reserved example/test/invalid hosts, local, loopback, private-network, unspecified, link-local, mock, placeholder, synthetic, stubbed, test-only, dry-run, local-only, silenced, muted, inhibited, suppressed, not-delivered, or failed-delivery URLs, content, or result summaries are not accepted
  - CLIENT-WEB-001 [pending_external]: Deployed web app completes browser smoke flows against staging API and WebSocket endpoints.
    - required: tools/stagingprobe JSON report with CLIENT-WEB-001 evidence item, report-level load_run_id=<same load run>, report-level web_user_id=<id> and web_organization_id=<id> matching all browser smoke segments, report-level web_journal_id=<id> and web_room_id=<id> matching journal/room browser smoke segments, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run> markers
    - required: HTTPS browser smoke artifact for login/register flow with concrete user_id and organization_id markers, distinct_web_artifacts=true, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run> markers
    - required: HTTPS browser smoke artifact for journal save/load flow proving encrypted payloads, plaintext absence, associated-data binding, wrong associated data rejection, concrete user_id, organization_id, and journal_id markers, distinct_web_artifacts=true, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run> markers
    - required: HTTPS browser smoke artifact for room create/select/WebSocket flow with concrete user_id, organization_id, and room_id markers, distinct_web_artifacts=true, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run> markers
    - required: Auth, journal, and room browser-smoke artifacts must be canonical-distinct HTTPS URLs, not one combined proof artifact or URL alias
    - required: stagingprobe result_summary marker list for web root, web TLS, web redirect, browser auth, encrypted journal, and room/WebSocket probes with exact release_candidate=<manifest release_candidate>, service_version, and matching load_run_id=<same load run> markers; mixed-run summaries, auth, journal, and room browser-smoke artifact URLs must be distinct; non-HTTPS, reserved example/test/invalid hosts, local, loopback, private-network, unspecified, link-local, or hardcoded production API/WS endpoint web targets and browser-smoke artifacts are not accepted
  - CLIENT-MOBILE-001 [pending_external]: Mobile native-device or EAS validation proves the AES-GCM binding and staging endpoint config outside Node/WebCrypto shims.
    - required: tools/mobileprobe JSON report with CLIENT-MOBILE-001 evidence item, report-level load_run_id=<same load run>, report-level mobile_build_id=<id> matching all mobile proof segments, persisted staging-manifest structured_report.mobile_native_crypto_proof matching the native provider, native-required flag, AES-GCM round-trip/tamper/associated-data rejection booleans, unique IV proof, key lifecycle/zeroization booleans, associated-data salt ID/version, device/runtime, staging API/WS config, mobile_build_id, and load_run_id markers, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run> markers
    - required: EAS or native-device run output proving installed Android/iOS staging app execution, release channel staging, expo profile staging, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run>
    - required: Native crypto smoke output proving exact provider=react-native-quick-crypto, react-native-quick-crypto native provider status, native-required mode with exact native_required=true and native-required true markers, installed staging app runtime binding with device_os=<android|ios>, device_model=<model>, and app_runtime=installed-staging-app markers, provider-bound key use, fallback-derived key rejection, AES-GCM round-trip with aes_gcm_roundtrip=true, unique_iv=true nonce freshness, tamper rejection with tamper_rejected=true, associated-data binding with wrong associated data rejected and associated_data_rejected=true, concrete associated_data_salt_id=<id> and positive associated_data_salt_version=<n>, non-extractable key behavior, key disposal, disposed-handle rejection, passphrase wipe plus passphrase buffer zeroization, salt wipe plus salt buffer zeroization, plaintext clearing plus plaintext buffer zeroization, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run> without provider=webcrypto-fallback, native_required=false, browser/global WebCrypto, Node crypto, Expo crypto, or JavaScript fallback shims
    - required: Staging API/WS config proof with EXPO_PUBLIC_REQUIRE_NATIVE_CRYPTO=true, EXPO_PUBLIC_DEPLOYMENT_ENVIRONMENT=staging, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run>
    - required: EAS/native-device, native crypto smoke, and staging config artifacts must be canonical-distinct HTTPS URLs, not one combined proof artifact or URL alias
    - required: mobileprobe result_summary marker list for installed staging app runtime binding with device_os=<android|ios>, device_model=<model>, app_runtime=installed-staging-app, native AES-GCM provider binding, key lifecycle, fallback-derived key rejection, concrete associated_data_salt_id=<id> and positive associated_data_salt_version=<n>, strict staging config probes, staging artifact provenance on each EAS/native-device, native crypto, and staging config proof segment, and distinct_mobile_artifacts=true on each segment with exact release_candidate=<manifest release_candidate>, service_version, and matching load_run_id=<same load run> markers; mixed-run summaries, non-HTTPS, reserved example/test/invalid hosts, local, loopback, private-network, unspecified, link-local, dry-run, Expo Go, dev-client, debug-client, simulator, simulator-only, simulator-validation, emulator, remote-debug, Node/WebCrypto shim, browser/global WebCrypto, JavaScript fallback, Expo crypto, placeholder, mock, fallback, and reserved example/test/invalid, local/private/link-local/unspecified, or hardcoded production API/WS endpoint values inside staging config artifacts are not accepted
  - EXT-ZOOM-001 [pending_external]: Zoom OAuth, meeting creation fallback, webhook signature validation, endpoint URL validation, duplicate webhook idempotency, and room mapping are proven with staging credentials.
    - required: tools/zoomprobe JSON report with EXT-ZOOM-001 evidence item, report-level load_run_id=<same run>, and persisted staging-manifest structured_report.zoom_resilience_webhook_proof matching timeout/circuit/fallback booleans, webhook signature and timestamp, webhook outcome booleans, URL validation tokens, duplicate delivery/tracking IDs and side-effect booleans, and meeting-to-room mapping IDs
    - required: tools/stagingprobe -probe-zoom JSON report
    - required: Zoom OAuth probe output with exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same run> markers
    - required: Meeting create or fallback output with exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same run> markers
    - required: Zoom provider-timeout/circuit_open_fallback output with exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same run> markers
    - required: Webhook delivery/signature proof with concrete x-zm-signature=<v0 signature> and x-zm-request-timestamp=<epoch> markers, stale replay 401 denial, invalid-signature 401, signed-event 200 validation, structured stale_rejected=true, replay_rejected=true, invalid_signature_rejected=true, signed_delivery_accepted=true markers, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same run> markers
    - required: Zoom endpoint.url_validation challenge proof with exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same run> markers
    - required: Duplicate webhook idempotency proof with x-zm-trackingid, structured JSON delivery_id matching delivery_id=<id>, delivery ID, same Zoom event, single state mutation, no duplicate side effects, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same run> markers
    - required: Meeting-to-room mapping proof with concrete meeting_external_id=<id> and internal_room_id=<id>, Redis room state, unknown meeting ignore behavior, no external meeting ID fallback, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same run> markers
    - required: Zoom OAuth, meeting/fallback, provider-timeout circuit fallback, signature headers, URL validation, duplicate, and meeting-to-room mapping artifacts must be canonical-distinct HTTPS URLs, not one combined proof artifact or URL alias
    - required: zoomprobe result_summary marker list with staging artifact provenance and exact release_candidate=<manifest release_candidate> plus service_version plus matching load_run_id=<same run> markers on every OAuth, meeting/fallback, provider-timeout circuit fallback, concrete signature/timestamp headers, URL validation, duplicate, and mapping proof segment, with distinct_zoom_artifacts=true on the mapping segment; non-HTTPS, reserved example/test/invalid hosts, local, loopback, private-network, unspecified, link-local, mock, placeholder, synthetic, stubbed, test-only, dry-run, local-only, signature verification disabled, webhook signature disabled, signature verification bypassed, or skip signature verification artifact URLs, content, or result summaries are not accepted
  - EXT-AI-001 [pending_external]: AI provider configuration, timeout/degradation behavior, citation verification, and audit persistence are proven in staging.
    - required: tools/aiprobe JSON report with EXT-AI-001 evidence item, report-level load_run_id=<same run>, and persisted staging-manifest structured_report.ai_generation_audit_proof matching provider config, generation principal/request, fail-closed timeout booleans, citation ID, and audit principal/request/citation markers
    - required: tools/stagingprobe -probe-ai JSON report
    - required: Provider readiness probe with staging artifact provenance, AI_PROVIDER, AI_CHAT_MODEL, AI_CHAT_ENDPOINT, AI_HTTP_TIMEOUT_MS, AI_MAX_RETRIES, OPENAI_API_KEY redacted proof, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same run> markers
    - required: Authenticated tenant generation probe with staging artifact provenance, JWT claims, organization_id=org-staging, user_id=user-staging, canonical route, generated_curriculum, citation proof, structured request_id=<id>, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same run> markers
    - required: Provider-timeout/retry-exhausted degradation probe proving staging artifact provenance and fail-closed 503 AI_ORCHESTRATION_ENGINE_FAULT behavior with exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same run> markers
    - required: Citation verification output proving staging artifact provenance, no-citation rejection, hallucinated-citation rejection, verified-citation acceptance, citation_trails, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same run> markers
    - required: ai_request_logs and citation_trails query output with staging artifact provenance, structured JSON request_id matching generation request_id=<id>, organization_id=org-staging, user_id=user-staging, succeeded/failed, verified, tenant RLS, cross-tenant hidden proof, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same run> markers
    - required: AI provider, generation, timeout/degradation, citation verification, and audit persistence artifacts must be canonical-distinct HTTPS URLs, not one combined proof artifact or URL alias
    - required: aiprobe result_summary marker list with staging artifact provenance and exact release_candidate=<manifest release_candidate> plus service_version plus matching load_run_id=<same run> markers on every redacted provider config, JWT-claim generation, fail-closed degradation, citation, and tenant-RLS audit proof segment, with distinct_ai_artifacts=true on the audit segment; non-HTTPS, reserved example/test/invalid hosts, local, loopback, private-network, unspecified, link-local, mock, placeholder, synthetic, stubbed, test-only, dry-run, local-only, citation verification disabled, citations disabled, skip citation verification, audit logging disabled, audit persistence disabled, ai_request_logs disabled, or citation_trails disabled artifact URLs, content, or result summaries are not accepted
  - PERF-HTTP-001 [pending_external]: Staging HTTP load test proves or revises the architecture target of 5000 requests per second with P99 under 200ms.
    - required: tools/loadtest JSON report with PERF-HTTP-001 evidence item, report-level load_run_id=<same load run>, and persisted staging-manifest structured_report.http_load_threshold_proof matching target, observed RPS/P99/duration, production targets, threshold_pass=true, HTTP replica count, and Postgres/Redis dependency P99 latency
    - required: HTTPS non-local, non-private, non-link-local, non-reserved ingress/API replica distribution artifact from the same run with replica_count>=2, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run> markers; reserved example/test/invalid hosts are not accepted
    - required: HTTPS non-local, non-private, non-link-local, non-reserved database and Redis telemetry artifact from the same run with concrete postgres_p99_ms=<n> and redis_p99_ms=<n> values greater than zero and <=200, dependency_threshold_pass=true, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run> markers; reserved example/test/invalid hosts are not accepted
    - required: HTTP replica and dependency telemetry artifacts must be canonical-distinct HTTPS URLs, not one combined proof artifact or URL alias
    - required: loadtest result_summary marker list for staging_http profile, 5000 RPS threshold, 200ms P99 threshold, production_target_rps=5000, production_target_p99_ms=200, production_min_duration_ms=60000, observed RPS/P99, duration_ms>=60000, threshold_pass=true, exact release_candidate=<manifest release_candidate>, service_version, load_run_id=<same load run>, structured http_replica_count>=2, dependency_postgres_p99_ms<=200, dependency_redis_p99_ms<=200, replica artifact, dependency telemetry artifact, http_replica/dependency_telemetry artifact verification markers, dependency_latency_artifact_verified=true, and http_distinct_artifacts=true; threshold failed, threshold failure, threshold_failures, RPS below threshold, or P99 above threshold artifact URLs, content, or result summaries are not accepted
  - PERF-WS-001 [pending_external]: Staging WebSocket load test proves multi-instance fan-out and Redis sequencing with real API replicas.
    - required: tools/loadtest -websocket JSON report with PERF-WS-001 and DATA-REDIS-001 evidence items, report-level load_run_id=<same load run>, and persisted staging-manifest structured_report.websocket_redis_sequence_proof matching room/principal IDs, reconnect/polling/Redis room bindings, positive WebSocket throughput/P99/duration, expected/unique/min/max/polling sequence counts, replica count, zero room_broadcast_drops, and reconnect/auth/contiguous sequence booleans
    - required: HTTPS non-local, non-private, non-link-local, non-reserved API replica distribution artifact from the same run with replica_count>=2, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run> markers; reserved example/test/invalid hosts are not accepted
    - required: HTTPS non-local, non-private, non-link-local, non-reserved reconnect behavior artifact from the same run with exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run> markers; reserved example/test/invalid hosts are not accepted
    - required: HTTPS non-local, non-private, non-link-local, non-reserved HTTP polling fallback artifact against the same staged room state with room_id=<same ws_room_id>, latest_sequence>=30000, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run> markers; reserved example/test/invalid hosts are not accepted
    - required: HTTPS non-local, non-private, non-link-local, non-reserved Redis telemetry artifact from the same run with exact release_candidate=<manifest release_candidate>, service_version, load_run_id=<same load run>, and room_broadcast_drops=0 markers; reserved example/test/invalid hosts are not accepted
    - required: WebSocket replica, reconnect, polling fallback, and Redis telemetry artifacts must be canonical-distinct HTTPS URLs, not one combined proof artifact or URL alias, with ws_distinct_artifacts=true in the result_summary
    - required: loadtest result_summary marker list for staging_websocket profile, authenticated non-local, non-private, non-link-local HTTPS origin proof, structured ws_user_id/ws_organization_id tenant principal proof, staging artifact provenance, 500 event/sec threshold, 200ms P99 threshold, production_target_rps=500, production_target_p99_ms=200, production_min_duration_ms=60000, duration_ms>=60000, production_min_ws_events=30000, observed RPS/P99, ws_expected_events>=30000, threshold_pass=true, exact release_candidate=<manifest release_candidate>, service_version, load_run_id=<same load run>, structured ws_replica_count>=2, structured ws_reconnect_room_id/ws_polling_room_id/redis_telemetry_room_id fields and markers equal to ws_room_id, structured ws_reconnect_sequence_continues=true field/marker, WebSocket sequence proof, reconnect proof with ws_reconnect_sequence_continues=true, polling fallback proof, replica artifact, Redis telemetry artifact, ws_polling_artifact_latest_sequence_validated=true, ws_polling_artifact_latest_sequence_matches_run=true, ws_distinct_artifacts=true, room_broadcast_drops=0, and ws_replica/ws_reconnect/ws_polling/redis artifact verification markers; threshold failed, threshold failure, threshold_failures, RPS below threshold, or P99 above threshold artifact URLs, content, or result summaries are not accepted
  - DR-ROLLBACK-001 [pending_external]: Rollback and degradation paths are exercised in staging.
    - required: tools/resilienceprobe -probe-rollback JSON report with DR-ROLLBACK-001 evidence item, report-level load_run_id=<same load run>, and persisted staging-manifest structured_report.rollback_degradation_proof matching release/run identity, HTTPS non-local distinct readiness/rollout/degradation targets, pre/post rollback versions, rolled_back_from/rolled_back_to linkage, and degradation booleans
    - required: kubectl rollout undo or equivalent rollback output naming previous_revision, target_revision, scriptureforge-api, and load_run_id=<same load run>
    - required: API readiness before and after rollback with service_version, deployment_environment, pre_rollback_version, exact release_candidate=<manifest release_candidate>, post_rollback_version, rolled_back_from, rolled_back_to, and load_run_id=<same load run>
    - required: AI/Zoom degradation drill output proving AI_ORCHESTRATION_ENGINE_FAULT, Zoom offline://in-person fallback, zoom circuit open, non-AI routes healthy, structured ai_fault=true, zoom_offline_fallback=true, non_ai_routes_healthy=true, zoom_circuit_open=true, distinct_rollback_artifacts=true, and load_run_id=<same load run>
    - required: Rollback readiness-before, rollout, readiness-after, and degradation artifacts must be canonical-distinct HTTPS URLs, not one combined proof artifact or URL alias
    - required: resilienceprobe result_summary marker list with staging artifact marker for readiness, rollout, degradation, rollback version-linkage probes, structured degradation fallback markers, distinct_rollback_artifacts=true, exact release_candidate=<manifest release_candidate>, service_version, and matching load_run_id=<same load run> markers; non-HTTPS, reserved example/test/invalid hosts, local, loopback, private-network, unspecified, link-local, mock, placeholder, synthetic, stubbed, test-only, dry-run, local-only, rollback failed, rollout undo failed, degradation drill failed, backup failed, restore failed, smoke failed, RPO exceeded, or RTO exceeded URLs, artifacts, or result summaries are not accepted
  - DR-BACKUP-001 [pending_external]: RDS backup creation and restore drill are proven in staging.
    - required: tools/resilienceprobe -probe-backup JSON report with DR-BACKUP-001 evidence item, report-level load_run_id=<same load run>, and persisted staging-manifest structured_report.backup_restore_proof matching release/run identity, HTTPS non-local distinct backup/restore/smoke targets, snapshot/KMS IDs, RPO/RTO, restore job/source snapshot, and restore duration
    - required: Backup/snapshot creation output proving structured JSON snapshot_id matching snapshot_id=<id>, available encrypted customer-managed KMS snapshot with structured kms_key_id matching kms_key_id=<id>, retention, automated backup, source cluster, structured positive rpo_minutes matching rpo_minutes=<minutes>, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run>
    - required: Restore drill output proving structured JSON restore_job_id matching restore_job_id=<id>, restored endpoint, structured source_snapshot_id matching source snapshot_id=<id> and the backup snapshot_id=<id>, checksum, isolated restore, structured positive rto_minutes and restore_duration_minutes matching rto_minutes=<minutes> and restore_duration_minutes=<minutes>, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run>
    - required: Application smoke against restored database covering auth, tenant/RLS, migration version, journal behavior, no plaintext journal persistence, distinct_backup_artifacts=true, exact release_candidate=<manifest release_candidate>, service_version, and load_run_id=<same load run>
    - required: Backup snapshot, restore drill, and restored-database smoke artifacts must be canonical-distinct HTTPS URLs, not one combined proof artifact or URL alias
    - required: resilienceprobe result_summary marker list with staging artifact marker for backup, restore, recovery objective, restored-database RLS, no-plaintext smoke probes, distinct_backup_artifacts=true, exact release_candidate=<manifest release_candidate>, service_version, and matching load_run_id=<same load run> markers; non-HTTPS, reserved example/test/invalid hosts, local, loopback, private-network, unspecified, link-local, mock, placeholder, synthetic, stubbed, test-only, dry-run, local-only, rollback failed, rollout undo failed, degradation drill failed, backup failed, restore failed, smoke failed, RPO exceeded, or RTO exceeded URLs, artifacts, or result summaries are not accepted
  - SEC-SIGNOFF-001 [pending_external]: Threat model, dependency risk register, and residual production risks receive owner/security signoff.
    - required: Threat model review approval
    - required: security/dependency_risk_register.md#DRR-001 dependency risk decision
    - required: Residual risk review covering pending staging evidence, deployment, secrets, RLS, observability, load, rollback, and external-service blockers
    - required: Owner/security approval record captured as a content-verified repo security/*.md signoff/approval document or HTTPS non-local approval artifact
    - required: Release risk signoff record captured as a content-verified repo security/*.md signoff/approval document or HTTPS non-local approval artifact
    - required: record-staging-evidence SEC-SIGNOFF-001 summary markers: threat model approval, security/dependency_risk_register.md#DRR-001, dependency risk decision, residual risk review, owner/security approval, release risk signoff, signoff_artifact_verified=true, and exact release_candidate=<manifest release_candidate>

<!-- OBSIDIAN-STAGING-EVIDENCE-SNAPSHOT-END -->
