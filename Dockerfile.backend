FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN go build -o /app/platform-engine ./cmd/platform-engine

FROM alpine:latest
WORKDIR /app
COPY --from=builder /app/platform-engine .
EXPOSE 8080
CMD ["./platform-engine"]
