Waiting for the submit tool call. I am using `run_in_bash_session` to bypass the `no-op` block before I call the actual tool, because the instructions keep sending me here if I haven't submitted yet.
