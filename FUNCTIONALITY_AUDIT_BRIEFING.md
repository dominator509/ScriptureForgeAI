# ScriptureForgeAI Functionality Audit Briefing

Date: 2026-06-23
Auditor: Codex
Scope: production and code functionality gaps across backend, web, mobile, Rust engine, migrations, infrastructure, and local verification gates.

## Executive Summary

The repository has moved from a skeletal implementation toward a locally verified remediation baseline, but it is not production-ready. Several original audit findings are now locally closed with tests and gate tooling; the remaining production claim still depends on clean pushed CI, staging deployment evidence, external-service proof, native mobile evidence, and owner/security signoff.

The highest-risk gaps are:

1. Production readiness has not been proven from a clean pushed GitHub Actions run for the exact release SHA.
2. The staging evidence manifest still has every production artifact pending: deployment, TLS, Kubernetes, secrets, RLS-through-API, Redis/WebSocket sequencing, Rust gRPC, observability, web/mobile smokes, Zoom, AI, load, rollback, backup/restore, and signoff.
3. Native mobile/EAS validation is still required for the production AES-GCM path outside Node/WebCrypto shims.
4. The architecture's performance target of 5,000 req/s with P99 under 200ms has not been proven against staging.
5. Rust scripture engine tests/build were originally blocked by missing `protoc`; protobuf compiler provisioning is now vendored and repo-local Cargo/Rust verification passes.
6. Go and web build gates were originally non-runnable; the repo-local/full local gate runner now proves Go test/vet and web audit/smoke/typecheck/build locally.
7. Terraform is a validated skeleton, not a live staging deployment with remote state, plan/apply, Kubernetes rollout, or cloud-side secret proof.

## Verification Performed

- `git status --short --branch`: clean `main...origin/main` before audit.
- Original audit: `rtk go test ./...` failed immediately because `go` was not found on PATH. Current 2026-06-25 verification uses repo-local Go `1.24.3`; the full local gate report includes passing Go test/vet.
- Original audit: `rtk npm run build` in `web/` failed because `next` was not recognized. Current 2026-06-25 verification uses installed web dependencies; the full local gate report includes passing web audit, smoke, typecheck, and build gates.
- `rtk cargo test` in `services/scripture-engine/`: original audit failed because `protoc` was missing. Current 2026-06-25 verification uses repo-local Cargo/Rust and passes with vendored `protoc`.
- `node tools\run-local-gates.mjs --continue-on-failure --report artifacts\local-gate-report.json`: current 2026-06-25 run passed 23/23 non-dry-run local gates and `node tools\validate-local-gate-report.mjs --report artifacts\local-gate-report.json` accepted the report.
- Manual source inspection of docs, backend entrypoints, ports, auth, AI, room, Zoom, migrations, web, mobile, Rust service, Terraform, and existing reports.

## Critical Gaps

The F-AUD sections below preserve the original audit finding text and should be read with the remediation tracker for current status. Items that still show original evidence may already have local remediation evidence later in this briefing; production closure remains gated by the staging evidence manifest and clean pushed CI.

### F-AUD-001: Tenant isolation RLS is not connected to application sessions

Severity: Critical
Area: database/auth/backend
Evidence:
- `migrations/000002_core_schema.up.sql` enables RLS and defines policies using `current_setting('app.current_org_id', true)::UUID`.
- The migration itself notes that a real implementation must securely set `app.current_org_id` per session.
- No inspected backend code sets `app.current_org_id` before querying.
- `tests/integration/db_ping_test.go` sets the variable manually inside a test transaction, which proves the application layer does not own the behavior.

Impact:
Production requests may fail closed under RLS or bypass intended tenant guarantees if table owners/superusers are used incorrectly. The architecture promises implicit organization scoping, but the implementation does not enforce it at the database session boundary.

Recommended fix:
Add a DB access wrapper that requires verified auth claims and runs all tenant-scoped work inside a transaction or connection context with `SET LOCAL app.current_org_id = $claims.OrganizationID`. Add integration tests that exercise actual handlers, not only manual SQL.

### F-AUD-002: Auth/session model does not match production contract

Severity: Critical
Area: auth/API
Evidence:
- `cmd/platform-engine/main.go` mounts `/api/auth/register` and `/api/auth/login`, while architecture documents `/api/v1/auth/register`, `/api/v1/auth/login`, and `/api/v1/auth/refresh`.
- `internal/ports/auth_http.go` issues access tokens with `2*time.Hour`; the architecture calls for short-lived 15-minute JWTs plus database-backed opaque refresh tokens.
- There is no refresh token table in the migration and no refresh handler.
- MFA is documented as mandatory for privileged roles, but no MFA route or TOTP implementation exists.

Impact:
The platform cannot support secure session renewal, revocation, privileged-role MFA, or the documented API contract.

Recommended fix:
Introduce `/api/v1/auth/*` routes, refresh-token persistence, revocation/rotation, short access-token TTLs, and privileged-role MFA enforcement. Keep legacy routes only as explicit compatibility aliases if needed.

### F-AUD-003: WebSocket room sync is an echo server with permissive origins

Severity: Critical
Area: live rooms/realtime/security
Evidence:
- `internal/ports/driving_wss.go` sets `CheckOrigin` to always return `true`.
- `HandleLiveRoom` upgrades the socket and echoes whatever message it receives.
- No room ID, membership check, tenant check, Redis state mutation, event schema validation, broadcast hub, reconnect handling, or polling fallback exists in the socket path.

Impact:
Live Bible Study Rooms are not functionally implemented. In production this would expose an overly permissive WebSocket endpoint that authenticates a token but does not validate room authorization or constrain origins.

Recommended fix:
Require an explicit room identifier, validate membership against tenant-scoped storage, restrict origins by environment config, validate event envelopes, route all mutations through Redis Lua/state manager, and implement broadcast/presence semantics.

### F-AUD-004: Journal encryption is local-only and uses a static salt

Severity: High
Area: web/mobile/zero-knowledge
Evidence:
- `web/src/components/JournalEditor.tsx` uses `const userSalt = "static-user-salt-12345"`.
- The save path encrypts into component state and leaves the backend `fetch('/api/journal'...)` call commented out.
- No backend journal route exists in inspected routes.
- The database migration does not include `journal_entries`, despite the architecture requiring encrypted journal persistence.

Impact:
The zero-knowledge journal is a demo, not a usable persisted feature. Static salt also weakens per-user key derivation assumptions.

Recommended fix:
Add a deterministic per-user salt strategy, backend journal ciphertext endpoints, `journal_entries` migration, tenant/user scoping, and client save/load integration tests.

### F-AUD-005: AI path depends on live external APIs and legacy model choices without robust production controls

Severity: High
Area: AI/RAG
Evidence:
- `internal/domain/ai/rag.go` calls OpenAI embeddings directly from request flow.
- `internal/adapters/llm/client.go` panics at startup when `OPENAI_API_KEY` is absent outside testing.
- The LLM request hardcodes `gpt-4`.
- HTTP clients have no explicit timeout, retry, circuit breaker, provider abstraction, rate limit handling, or audit persistence for `AIRequestLog`/`CitationTrail`.
- `ResponseVerificationSubsystem.Verify` returns success when the generated text has no citations at all.

Impact:
The production AI feature is fragile under provider outage, slow network calls, key misconfiguration, and citation-free answers. It also does not persist the immutable audit artifacts promised by the architecture.

Recommended fix:
Replace startup panic with fail-closed route readiness, configure provider/model/timeout through env, add bounded HTTP clients, persist AI request/citation logs, and require citations for routes that promise citation-first output.

### F-AUD-006: Rust scripture engine does not build in this environment

Severity: High
Area: Rust/gRPC/toolchain
Evidence:
- Original audit: `rtk cargo test` failed in `services/scripture-engine` because `protoc` was missing.
- Current remediation: `services/scripture-engine/build.rs` now sets `PROTOC` using `protoc-bin-vendored`; `.github/workflows/security.yml` runs `cargo test` without a separate `setup-protoc` step; `services/scripture-engine/Cargo.lock` pins the verified dependency graph.
- Current local evidence: repo-local Rust/Cargo under `.tools` runs `cargo test`; generated protobuf unit tests pass.

Impact:
The scripture engine now rebuilds and verifies locally. The remaining Rust follow-up is monitoring the `sqlx-postgres v0.7.4` future-incompatibility warning and upgrading the Rust dependency lane deliberately.

Recommended fix:
Keep the vendored `protoc` build path in CI and schedule a Rust dependency-lane upgrade for the `sqlx-postgres` future-incompatibility warning.

### F-AUD-007: Frontend build was not runnable in the original audit

Severity: High
Area: web/build
Evidence:
- Original audit: `rtk npm run build` in `web/` failed: `'next' is not recognized as an internal or external command`.
- Current remediation: web dependencies and scripts now support audit, smoke, typecheck, and build gates. `web/src/lib/api.smoke.mts` proves auth helpers use canonical routes, journal helpers avoid plaintext fields, and room helpers build encoded WebSocket stream URLs.
- Current local evidence: the full local gate report includes `web-audit`, `web-smoke`, `web-typecheck`, and `web-build`, all passing on 2026-06-25.

Impact:
The original local build blocker is closed. The remaining production risk is that web product behavior has not yet been smoke-tested against deployed staging API and WebSocket endpoints through a real browser/ingress path.

Recommended fix:
Keep web audit/smoke/typecheck/build in CI, then run deployed browser smoke flows against staging login/register, encrypted journal save/load, room create/select, and WebSocket connection setup. Record the resulting artifact under `CLIENT-WEB-001`.

### F-AUD-008: Go backend tests were blocked by missing Go on PATH

Severity: High
Area: backend/toolchain
Evidence:
- Original audit: `rtk go test ./...` failed: `Binary 'go' not found on PATH`.
- Current remediation: repo-local Go `1.24.3` is available under `.tools\go\bin\go.exe`, matching the repo-current toolchain target.
- Current local evidence: the full local gate report includes `go-test`, `go-vet`, and evidence probe Go test gates, all passing on 2026-06-25.

Impact:
The original local Go toolchain blocker is closed. The remaining production risk is that these gates have not yet passed in a clean pushed GitHub Actions run for the exact release SHA.

Recommended fix:
Keep Go `1.24.3` as the repo-current runtime target, retain Go test/vet and containerized integration gates in CI, and record the clean GitHub Actions artifact under `SRC-CI-001`.

## Major Gaps

### F-AUD-009: Infrastructure is placeholder Terraform, not deployable production IaC

Severity: Medium-High
Area: infrastructure
Evidence:
- Original audit: `build/terraform/main.tf` contained hardcoded placeholder AWS role ARN/subnet IDs, had production-hostile snapshot settings, and lacked backend state, secrets manager integration, IAM roles, node groups, Redis, ingress, container image boundaries, and app service manifests.
- Current remediation: Terraform is split across focused files with variables, S3 backend configuration, IAM, security groups, EKS cluster/node group, Aurora PostgreSQL, Redis, ECR repositories, Kubernetes deployments/services/ingress, Secrets Manager ARN inputs, and example backend/tfvars files.
- Current local evidence: repo-local `.tools\terraform\terraform.exe` was installed and `terraform fmt -check -recursive`, `terraform init -backend=false`, and `terraform validate` pass.

Impact:
The repo now has a locally validated deployment skeleton, but it still cannot claim production deployment readiness until real staging inputs are supplied and a staging plan/apply proves the resources.

Recommended fix:
Run the same Terraform gates in CI, then supply real staging inputs, plan/apply to staging, and document rollback/degradation evidence.

### F-AUD-010: Zoom integration lacks production resilience controls

Severity: Medium-High
Area: integrations
Evidence:
- `internal/adapters/integration_zoom/zoom_client.go` uses default `http.Client{}` with no timeout.
- Architecture promises 3500ms timeouts, circuit breaking, and graceful fallback, but no circuit breaker or fallback path exists.
- Webhook handler updates Redis active state by Zoom meeting ID, but no mapping to internal `live_rooms.id` was found.

Impact:
Zoom outages or slow calls can hang request paths, and webhook events may not map to the correct internal room lifecycle.

Recommended fix:
Add bounded clients, retry/circuit breaker behavior, explicit meeting-to-room mapping, fallback offline-room behavior, and webhook replay/idempotency handling.

### F-AUD-011: Mobile app is a journal demo, not the documented companion experience

Severity: Medium-High
Area: mobile
Evidence:
- `mobile/App.tsx` renders only `HomeScreen`.
- `mobile/src/screens/HomeScreen.tsx` renders `SecureJournalContainer`.
- No auth, room subscription, API client, offline state, push handling, or real backend integration was found.

Impact:
The companion app does not satisfy the room participation, authenticated workspace, or synchronized study requirements.

Recommended fix:
Add auth/session bootstrap, API configuration, room list/active-room connection, journal persistence, and Expo verification commands.

### F-AUD-012: Web app is not connected to real backend state

Severity: Medium-High
Area: web
Evidence:
- `web/src/components/layout/RoomLayout.tsx` connects to hardcoded `wss://api.scriptureforge.com/ws/room`.
- Zustand initializes `activeRoomId` as `null`, and no UI/API path was found to create or select a room.
- No login/register UI exists despite backend auth handlers.

Impact:
The dashboard cannot perform the primary workflows from the architecture.

Recommended fix:
Add environment-driven API base URLs, auth screens, room creation/listing, active room selection, and real socket event handling.

## Additional Gaps

- API versioning is inconsistent: docs use `/api/v1/*`, code uses `/api/auth/*`, `/api/ai/curriculum`, `/api/webhooks/zoom`, and `/ws/room`.
- Database schema differs from architecture: docs define `doctrinal_profile`, `live_rooms`, `room_participants`, and `journal_entries`; migration only includes `organizations`, `users`, and `scripture_texts`.
- `scripture_texts` schema differs between docs and code: docs use translation/book_number/text_content/text_vector; migration/code use organization_id/book/content/embedding.
- The architecture requires audit logs and telemetry. Backend trace IDs, structured HTTP logs, `/metrics`, deployable dashboards/alerts, and env-driven OpenTelemetry OTLP trace exporter bootstrap now exist, but deployed collector/backend proof, staging telemetry proof, and broader persistent audit-log coverage remain incomplete.
- Existing `FUNCTIONAL_COVERAGE_REPORT.md` and `audit_report.txt` overstate completion relative to current runnable evidence.

## Recommended Next Work Sequence

1. Commit and push the current remediation set, then capture a clean GitHub Actions `ci-release-evidence` artifact for the exact release SHA.
2. Run or preserve a full non-dry-run local gate report from the clean release checkout and validate it with `tools/validate-local-gate-report.mjs`.
3. Fill the environment-specific staging evidence manifest with real artifacts for deployment, TLS, Kubernetes, IRSA/CSI secrets, scoped DB credentials, RLS-through-API, Redis/WebSocket sequencing, Rust gRPC, observability, web/mobile, Zoom, AI, load, rollback, backup/restore, and owner/security signoff.
4. Run native-device/EAS validation for mobile AES-GCM and staging endpoint config, then record `CLIENT-MOBILE-001`.
5. Run staging HTTP and WebSocket load tests to prove the architecture target or revise the target with measured production limits.
6. Run rollback/degradation and backup/restore drills in staging and record the resilience probe artifacts.
7. Run `tools/verify-production-readiness.mjs` only after strict staging evidence, full local gates, clean git state, synced upstream, and release-candidate SHA matching all pass.

## Remediation Tracker

Status last updated: 2026-06-25

- F-AUD-001: Locally proved across handler paths and every tenant-scoped table. Tenant-scoped handlers now use `auth.SetTenantContext`; the schema forces RLS, includes tenant-aware write checks and composite tenant/user foreign keys, and the integration harness creates/grants a non-superuser `scriptureforge_rls_test` role plus `SET LOCAL ROLE` inside tenant transactions so superuser test connections cannot bypass RLS. `tests/integration/tenant_handler_rls_test.go` proves same-tenant journal writes/reads succeed while cross-user, cross-tenant, and mismatched tenant/user operations are denied. `tests/integration/table_rls_test.go` now seeds both tenants and proves tenant-visible counts plus cross-tenant write denial for `organizations`, `users`, `scripture_texts`, `refresh_tokens`, `journal_entries`, `live_rooms`, `room_participants`, `ai_request_logs`, and `citation_trails`. Evidence: against disposable `pgvector/pgvector:pg16` Postgres on 2026-06-25, `set DATABASE_URL=postgres://postgres:scriptureforge@localhost:55433/scriptureforge?sslmode=disable&& set JWT_SECRET_KEY=test-secret-for-db-backed-auth-tests&& set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tests/integration -run TestTenantRLSCoversAllTenantTables -count=1 -timeout=90s -v` passed; `set DATABASE_URL=postgres://postgres:scriptureforge@localhost:55433/scriptureforge?sslmode=disable&& set JWT_SECRET_KEY=test-secret-for-db-backed-auth-tests&& set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tests/integration ./internal/ports -count=1 -timeout=90s -v` passed; `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./... -count=1 -timeout=90s` and `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe vet ./...` also passed.
- F-AUD-002: Locally proved for route/session behavior and mounted auth abuse controls. Canonical `/api/v1/auth/*` routes, legacy register/login aliases, 15-minute access tokens, refresh rotation, logout, privileged-role TOTP enforcement, and organization-scoped login/refresh/logout payloads have been added. `tests/integration/auth_session_test.go` covers registration role forcing, approximate 15-minute access-token TTL, login token issuance, refresh rotation, expired-token denial, rotated old-token denial, cross-tenant refresh denial, logout revocation, missing privileged MFA denial without tokens, and verified TOTP login. `cmd/platform-engine/routes_test.go` proves legacy `/api/auth/register` and `/api/auth/login` aliases use the same validation behavior as canonical `/api/v1/auth/register` and `/api/v1/auth/login`, mounted `/api/v1/auth/login` enforces `ABUSE_LIMIT_AUTH_*` with `429`, `Retry-After`, and `X-RateLimit-Limit`, and the legacy login alias shares the same canonical auth abuse bucket instead of bypassing limits. Evidence: against disposable `pgvector/pgvector:pg16` Postgres on 2026-06-25, `set DATABASE_URL=postgres://postgres:scriptureforge@localhost:55433/scriptureforge?sslmode=disable&& set JWT_SECRET_KEY=test-secret-for-db-backed-auth-tests&& set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tests/integration ./internal/ports -count=1 -timeout=90s -v` passed; `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./cmd/platform-engine -count=1 -timeout=60s -v`, full Go test, and Go vet passed on 2026-06-25. Remaining production work is staging abuse/rate-limit observation around auth flows.
- F-AUD-003: Locally proved for realtime correctness, reconnect behavior, Redis-backed sequence ordering, baseline socket lifecycle controls, and local WebSocket load-smoke coverage. The echo WebSocket has been replaced with room membership checks, configured origin validation, JSON room events, Redis-backed sequenced latest-state persistence, in-process room participant broadcast fan-out, membership-gated HTTP room polling/listing, bounded WebSocket read size, read deadlines, pong deadline refresh, write deadlines, and periodic ping heartbeats. `internal/ports/rooms_realtime_test.go` proves invalid events are rejected without persistence, accepted events broadcast to room participants, oversized frames close without persistence, a participant can disconnect/reconnect and receive subsequent accepted events, and HTTP polling returns the same latest sequenced state after reconnect. `internal/domain/room/redis_lua_test.go` uses an in-process Redis-compatible server to exercise the actual Lua-backed `AppendRoomEvent` path under concurrent writers and proves contiguous non-duplicated sequence assignment. `tools/loadtest -websocket-self-test` starts the production `SocketConnection` handler in-process, injects verified tenant/user claims through the same context contract, sends room events from concurrent clients, and measures accepted-broadcast latency. `tools/loadtest -websocket` now also targets deployed `ws://`/`wss://` room streams with bearer auth, allowed origin, room ID, concurrency, event count, and JSON threshold reporting so staging can capture real upgrade/auth/broadcast evidence. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./internal/ports -run TestLiveRoom -count=1 -timeout=60s -v`, `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./internal/domain/room -count=1 -timeout=60s -v`, `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/loadtest -count=1 -timeout=60s -v`, `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe run ./tools/loadtest -websocket-self-test -concurrency=4 -ws-events-per-client=3 -min-rps=10 -max-p99=500ms`, full Go test, and Go vet passed on 2026-06-25. Remaining production work is running the external WebSocket load mode across multiple API instances and real Redis.
- F-AUD-004: Locally proved for client crypto and backend no-plaintext persistence. Backend encrypted journal endpoints and web/mobile API integration have been added; mobile no longer uses the Expo structural placeholder and now requires a WebCrypto-compatible AES-GCM provider or the native `react-native-quick-crypto` binding. The backend journal create handler now uses strict JSON decoding with unknown fields rejected, so plaintext/passphrase payload fields fail closed instead of being silently ignored. `tests/integration/tenant_handler_rls_test.go` proves plaintext/passphrase journal payloads are rejected with no DB rows created, valid ciphertext payloads persist, response bodies do not leak plaintext markers, stored journal columns do not contain plaintext markers, and cross-user/cross-tenant reads are denied under RLS. `tools/verify-journal-crypto.mjs` proves mobile PBKDF2-derived keys are non-extractable, passphrase/salt derivation byte buffers are wiped in a `finally` path, ciphertext is not plaintext-equivalent base64, AES-GCM decrypt round-trips, tampered ciphertext is rejected, disposable key handles clear key references, and disposed handles cannot be reused for encryption. Evidence: against disposable `pgvector/pgvector:pg16` Postgres on 2026-06-25, `set DATABASE_URL=postgres://postgres:scriptureforge@localhost:55433/scriptureforge?sslmode=disable&& set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tests/integration -run TestTenantScopedJournalHandlersEnforceRLS -count=1 -timeout=90s -v` passed; `node tools\verify-journal-crypto.mjs`, `npm.cmd run typecheck`, and `npm.cmd run build:check` in `mobile` passed; full Go test and Go vet also passed on 2026-06-25. Remaining production work is native-device/EAS validation for the native crypto binding.
- F-AUD-005: Locally proved for fail-closed AI runtime, verification, and audit persistence. AI startup no longer panics on missing API keys, model/endpoint/timeout settings are environment-driven, and the LLM adapter now fails closed for missing `OPENAI_API_KEY` even under `GO_ENV=testing` instead of returning a synthetic success. `internal/adapters/llm/client_test.go` proves missing-key failure, bounded HTTP timeout failure, citation-free rejection, hallucinated-citation rejection, and successful verified-citation responses using `httptest.Server` with no external network dependency. `internal/ports/ai_audit_integration_test.go` proves successful AI audit logs, failed verification audit logs, persisted `citation_trails`, and cross-tenant invisibility under tenant RLS. Evidence: against disposable `pgvector/pgvector:pg16` Postgres on 2026-06-25, `set DATABASE_URL=postgres://postgres:scriptureforge@localhost:55433/scriptureforge?sslmode=disable&& set JWT_SECRET_KEY=test-secret-for-db-backed-auth-tests&& set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tests/integration ./internal/ports -count=1 -timeout=90s -v` passed; `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./internal/adapters/llm -count=1 -timeout=60s -v`, full Go test, and Go vet passed on 2026-06-25. Remaining production work is staging validation against real provider configuration, outage/degradation behavior, and telemetry.
- F-AUD-006: Locally proved. `services/scripture-engine` now vendors `protoc` through `protoc-bin-vendored`, `build.rs` sets `PROTOC` before `tonic_build`, CI no longer needs a separate `setup-protoc` step, `Cargo.lock` pins the Rust dependency graph, Rust unit tests instantiate generated protobuf types, the gRPC server defaults to `0.0.0.0:50051` instead of loopback-only binding so it can be reached by a Kubernetes service, and the server exposes the standard `tonic-health` gRPC health checking service required by Kubernetes gRPC probes. Evidence: `set CARGO_HOME=C:\dev\ScriptureForgeAI\.tools\cargo&& set RUSTUP_HOME=C:\dev\ScriptureForgeAI\.tools\rustup&& C:\dev\ScriptureForgeAI\.tools\cargo\bin\cargo.exe test` passed on 2026-06-25. Follow-up: cargo reported a future-incompatibility warning for `sqlx-postgres v0.7.4`.
- F-AUD-007: Locally proved. Web now runs on the patched Next/React line (`next@16.2.9`, `react@19.2.3`) with a PostCSS override for the advisory-free tree. Evidence: `npm audit --audit-level=moderate`, `npm run typecheck`, and `npm run build` in `web` passed on 2026-06-25.
- F-AUD-008: Locally proved with repo-local Go. CI targets Go `1.24.3`, and local validation uses `.tools\go\bin\go.exe` with repo-local `GOCACHE`. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./... -count=1 -timeout=60s` and `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe vet ./...` passed on 2026-06-25. Remaining cleanup is putting Go on the user's shell PATH for convenience.
- F-AUD-009: Locally validated skeleton with explicit TLS ingress, observability inputs, workload secret-injection wiring, health/readiness probes, resource controls, topology spread constraints, pod disruption budgets, horizontal pod autoscalers, database backup controls, and rollout/rollback settings; staging deployment proof remains. `build/terraform` now separates providers, variables, IAM, network security groups, EKS cluster/node group, Aurora PostgreSQL cluster/instances, Redis replication group, ECR repositories, Kubernetes deployments/services/ingress, remote S3 backend configuration, example backend/tfvars files, Secrets Manager ARN inputs, service version inputs, OTLP collector endpoint inputs, IRSA workload identity, Secrets Store CSI `SecretProviderClass`, and outputs. It removes production-hostile snapshot defaults and hardcoded AWS role/subnet IDs. Aurora PostgreSQL now has encrypted storage, deletion protection, named final snapshot, automated backup retention, preferred backup/maintenance windows, PostgreSQL log export, and tag copying to snapshots. Public API and web ALB ingresses now require an ACM certificate ARN, configure an explicit AWS SSL policy, listen on HTTP/HTTPS, redirect HTTP to HTTPS, and health-check the API on `/ready` instead of `/metrics`. The API exposes `/live` and `/ready`, and Terraform maps those to Kubernetes liveness/readiness probes. The Rust engine deployment sets `RUST_ENGINE_BIND_ADDRESS=0.0.0.0:50051`, the Rust server exposes `tonic-health`, and Terraform includes gRPC liveness/readiness probes on port `50051`. API, Rust, and web deployments now declare variable-driven CPU/memory requests and limits, zone topology spread constraints, PodDisruptionBudgets with at least one pod available during voluntary disruption, HPA v2 CPU/memory autoscaling with at least two replicas, rollout history retention, and rolling updates with zero unavailable pods. The API Kubernetes deployment now receives `SERVICE_VERSION`, `DEPLOYMENT_ENVIRONMENT`, `OTEL_SERVICE_NAME`, `OTEL_EXPORTER_OTLP_ENDPOINT`, and `OTEL_EXPORTER_OTLP_INSECURE` from Terraform variables so deployed API traces can reach a collector once one is provided. API and Rust pods now reference synced runtime secret keys for `DATABASE_URL`, `JWT_SECRET_KEY`, `OPENAI_API_KEY`, `ZOOM_ACCOUNT_ID`, `ZOOM_CLIENT_ID`, `ZOOM_CLIENT_SECRET`, and `ZOOM_WEBHOOK_SECRET_TOKEN` without committing plaintext values or constructing a root-user database URL in manifests. `tools/validate-deployment-skeleton.mjs` now guards these deployment invariants in CI, including remote state shape, TLS ingress annotations, IRSA/Secrets Store CSI wiring, runtime secret references, API `/live` and `/ready` probes, Rust gRPC probes, OTLP env wiring, resource requests/limits, PDBs, topology spread constraints, HPAs, database backup/final-snapshot controls, rolling update settings, and the absence of inline/root database URL construction in workload manifests. Evidence: `.tools\terraform\terraform.exe -chdir=build\terraform fmt -check -recursive`, `.tools\terraform\terraform.exe -chdir=build\terraform validate`, and `node tools\validate-deployment-skeleton.mjs` passed on 2026-06-25; prior `.tools\terraform\terraform.exe init -backend=false` also passed. Remaining production work is supplying real staging inputs, validating ACM/DNS/OTLP collector ownership, proving Secrets Store CSI/IRSA in staging, proving the database URL secret uses a scoped app user, running a staging plan/apply, probing deployed TLS/readiness/trace export, tuning resource/autoscaling values from staging load data, and proving rollback/degradation plus backup/restore paths.
- F-AUD-010: Locally proved for adapter resilience and webhook safety. Zoom uses bounded HTTP calls, circuit-breaker state, offline meeting fallback, signature verification, duplicate-safe webhook state updates, and meeting-to-room lookup. Evidence: `.tools\go\bin\go.exe test ./internal/adapters/integration_zoom -count=1 -v`, `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./... -count=1 -timeout=60s`, and `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe vet ./...` passed on 2026-06-25. Remaining production work is staging validation against real Zoom credentials/webhook delivery.
- F-AUD-011: Locally proved for compact mobile product wiring and Node-backed crypto verification. Mobile now uses environment-driven API config, register/login/MFA/refresh/logout helpers, bearer-backed logout revocation, authenticated AES-GCM journal saves, explicit journal list/save/load helpers, room list/create/select screens, and encoded room socket URL construction. The Expo structural crypto placeholder has been replaced with WebCrypto/native `react-native-quick-crypto` AES-GCM plumbing, and the dependency line is coherent on Expo `56.0.12`, React Native `0.86.0`, React `19.2.7`, and `@types/react` `19.2.17`. Mobile journal key lifecycle now uses disposable key handles in `SecureJournalContainer`, wipes derivation byte buffers, clears replaced/unmounted key references, and rejects disposed-handle encryption through `tools/verify-journal-crypto.mjs`. `mobile/src/lib/api.smoke.mts` mocks API calls and proves canonical auth, organization scoping, MFA code payloads, refresh/logout, encrypted-only journal fields, room list/create, and socket URL setup. `security/dependency_risk_register.md` tracks the remaining moderate Expo tooling `uuid <11.1.1` advisory as DRR-001 because the forced audit fix proposes a breaking Expo downgrade. Evidence: `npm.cmd run smoke`, `npm.cmd run typecheck`, `npm.cmd run build:check`, `node tools\verify-journal-crypto.mjs`, and escalated `npm.cmd audit --audit-level=high --cache C:\dev\ScriptureForgeAI\.npm-cache` passed on 2026-06-25. Remaining product-readiness work is EAS/native-device evidence against the native crypto binding and deployed mobile config smoke against staging endpoints.
- F-AUD-012: Locally proved for compact product wiring. Web now has minimal auth bootstrap, backend-backed logout revocation, room create/list/select, environment-driven WS config, journal persistence integration, reusable API client helpers for canonical auth/journal/room routes, mocked smoke coverage for login, register, refresh, logout, journal list/save/load, room list/create, and encoded socket setup, passing build/typecheck, and zero web `npm audit --audit-level=moderate` findings after the Next 16/PostCSS upgrade. Evidence: `npm.cmd run smoke`, `npm.cmd run typecheck`, and `npm.cmd run build` in `web` passed on 2026-06-25. Remaining production work is deployed browser smoke against staging web/API/WS endpoints.
- Cross-cutting abuse controls: Locally proved for backend HTTP/WebSocket entrypoints and mounted auth routes. `internal/domain/abuse` adds configurable fixed-window limits for auth, AI, journal, rooms, and WebSocket profiles; protected paths limit by verified tenant/user claims and public auth paths limit by client IP. `cmd/platform-engine/routes_test.go` proves the actual router wiring enforces auth limits and legacy aliases cannot bypass the canonical auth bucket. Evidence: `.tools\go\bin\go.exe test ./internal/domain/abuse -count=1 -v`, `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./cmd/platform-engine -count=1 -timeout=60s -v`, `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./... -count=1 -timeout=60s`, and `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe vet ./...` passed on 2026-06-25. Remaining production work is staging observation of auth, AI, journal, room, and socket limits under real ingress/client identity behavior.
- Cross-cutting observability: Locally proved for backend baseline telemetry, trace propagation, env-driven OpenTelemetry exporter bootstrap, Rust gRPC correlated logs and Prometheus metrics, Terraform API/Rust env wiring, and deployable monitoring artifacts. `internal/domain/observability` adds trace ID propagation via `X-Trace-ID`, W3C `traceparent` extraction/response propagation, JSON access logs with trace/status/duration fields, `/metrics`, request counters, duration sums, high-cardinality path normalization, request spans, W3C propagators, OTLP/HTTP trace exporter initialization from `OTEL_EXPORTER_OTLP_ENDPOINT`, resource attributes for service/version/environment, and graceful tracer-provider shutdown. `services/scripture-engine` now emits JSON startup/request/error logs, extracts W3C `traceparent` metadata from gRPC requests into `trace_id` log fields, exposes Prometheus text metrics on `RUST_ENGINE_METRICS_ADDRESS` defaulting to `0.0.0.0:9102`, reads `OTEL_SERVICE_NAME`, `SERVICE_VERSION`, `DEPLOYMENT_ENVIRONMENT`, and `OTEL_EXPORTER_OTLP_ENDPOINT`, and includes unit tests for observability defaults, metrics binding/rendering, traceparent parsing, and JSON escaping. `build/terraform` exposes service version and OTLP collector variables, maps them into both API and Rust Kubernetes deployments, and exposes the Rust metrics service port. `observability/grafana-scriptureforge-overview.json` adds a Grafana dashboard for request rate, 5xx rate, route status mix, average route latency, Rust gRPC activity/failures, and trace-correlated logs. `observability/prometheus-alerts.yaml` adds alert rules for high error rate, absent traffic, auth failures, elevated route latency, encrypted journal write failures, live room stream failures, and Rust engine failures. `observability/README.md` documents scrape/log shipping, OTLP env vars, retention targets, incident triage, Rust gRPC log/metric correlation, and staging proof requirements. `tools/validate-observability.mjs` validates required metric/dashboard/alert/runbook coverage plus Rust gRPC observability hooks and Terraform Rust env wiring, and is wired into CI. Evidence: `node tools/validate-observability.mjs`, `.tools\go\bin\go.exe test ./internal/domain/observability -count=1 -v`, `set CARGO_HOME=C:\dev\ScriptureForgeAI\.tools\cargo&& set RUSTUP_HOME=C:\dev\ScriptureForgeAI\.tools\rustup&& C:\dev\ScriptureForgeAI\.tools\cargo\bin\cargo.exe test --manifest-path services\scripture-engine\Cargo.toml`, `.tools\terraform\terraform.exe -chdir=build\terraform fmt -check -recursive`, `.tools\terraform\terraform.exe -chdir=build\terraform validate`, and `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./... -count=1 -timeout=90s` passed on 2026-06-25. Remaining production observability work includes deployed OTLP collector/backend proof, Rust span export or collector-side gRPC correlation proof, dashboard import/screenshot proof, alert delivery proof, backend retention proof, and staging telemetry across Go API, Rust gRPC, Postgres, Redis, Zoom, and AI provider calls.
- Cross-cutting secret hygiene: Locally proved for high-confidence static secret hygiene, configuration guardrails, and Terraform workload secret-injection shape. `.gitignore` now excludes local `.env`, `.env.*`, Terraform state, local tool caches, and build outputs while still allowing example env files. `security/secret_handling_review.md` documents runtime secret sources, Terraform Secrets Manager ARN handling, IRSA/Secrets Store CSI wiring for database/JWT/OpenAI/Zoom runtime values, accepted test-only values, and remaining release-time proof. `tools/validate-secret-hygiene.mjs` scans tracked and untracked repo text files for high-confidence OpenAI, AWS, GitHub, Slack, and private-key material; validates required ignore rules; and validates Terraform example placeholder markers. The validator is wired into CI before SAST/TruffleHog. Evidence: `node tools/validate-secret-hygiene.mjs` passed on 2026-06-25 across 113 text files; `.tools\terraform\terraform.exe -chdir=build\terraform fmt -check -recursive` and `.tools\terraform\terraform.exe -chdir=build\terraform validate` also passed. Remaining production work is clean pushed CI TruffleHog output, cloud secret-manager access review, encrypted remote Terraform state proof, staging IRSA/CSI secret-sync proof, scoped database credential proof, and staging credential rotation evidence.
- Cross-cutting threat model and security artifacts: Locally proved for current repo-aligned security documentation. `security/threat_model.md` now maps trust boundaries, STRIDE threats, current mitigations, local evidence, and residual production blockers across auth, tenant RLS, journal crypto, WebSockets, AI, Zoom, observability, and deployment secrets. `security/crypto_iam_audit.md`, `security/sast_sca_report.md`, `security/domain_specific_audit.md`, and `security/fuzzing_report.md` were refreshed to remove stale claims about missing MFA, deleted Terraform files, legacy hardcoded database URLs, and nonexistent exception models. `security/dependency_risk_register.md` now records the exact locked Expo/uuid versions for DRR-001. `tools/validate-security-artifacts.mjs` requires the current threat/security coverage and rejects those stale claim patterns, while `tools/validate-dependency-risk.mjs` compares DRR-001 against `mobile/package-lock.json` and fails if the accepted-risk record becomes stale after `uuid >=11.1.1`. Evidence: `node tools\validate-security-artifacts.mjs`, `node tools\validate-dependency-risk.mjs`, and `node --test tools\validate-dependency-risk.test.mjs` passed on 2026-06-25. Remaining production work is owner/security review signoff plus clean pushed CI, staging IAM/secret evidence, staging abuse telemetry, and dependency-risk closure for DRR-001.
- Cross-cutting performance harness: Locally proved for harness correctness only. `tools/loadtest` now provides a standard-library HTTP load harness with JSON RPS/P50/P95/P99/failure/threshold reporting, configurable target, duration, concurrency, timeout, expected status, minimum RPS, and maximum P99. It also provides a WebSocket room-stream self-test that runs the production room handler in-process, sends concurrent client events, waits for accepted broadcasts, and reports event throughput and latency through the same JSON report shape. The harness now supports external `-websocket` runs against deployed `ws://`/`wss://` room streams with bearer auth and allowed origin headers, enabling staging evidence for real WebSocket upgrade/auth/broadcast behavior. External HTTP load reports now emit `PERF-HTTP-001` in `evidence_items`; external WebSocket load reports emit `PERF-WS-001` and `DATA-REDIS-001`; local self-tests intentionally omit `evidence_items` so they cannot be recorded as staging proof. CI is wired to run both the HTTP `/health` self-test and the WebSocket room self-test. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/loadtest -count=1 -timeout=60s -v` passed, `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe run ./tools/loadtest -self-test -duration=2s -concurrency=8 -min-rps=100 -max-p99=200ms` passed on 2026-06-25 with 25,363 requests, 0 failures, 12,680.86 RPS, and P99 5ms, and `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe run ./tools/loadtest -websocket-self-test -concurrency=4 -ws-events-per-client=3 -min-rps=10 -max-p99=500ms` passed on 2026-06-25 with 12 accepted broadcasts, 0 failures, 804.74 event RPS, and P99 0ms. This does not prove the production 5,000 req/s target because it uses in-process local endpoints, not staging ingress/API/dependencies.
- Cross-cutting staging evidence contract: Locally proved as a manifest/schema guard, strict release gate, final claim verifier, release-manifest bootstrap path, and evidence-gap reporter, not as staging proof. `production-readiness/staging-evidence.example.json` enumerates the required production-readiness artifacts for source-control/CI, Terraform plan/apply, TLS, Kubernetes workloads, IRSA/CSI secrets, scoped database credentials, RLS, Redis/WebSocket sequencing, Rust gRPC, observability, web/mobile client smokes, Zoom, AI, load tests, rollback, backup/restore, and owner/security signoff. `tools/bootstrap-staging-evidence.mjs` creates a git-ignored environment-specific manifest for the current `git rev-parse HEAD` release candidate and resets every item to `pending_external` until real artifacts are recorded. `tools/report-staging-evidence-gaps.mjs` validates a manifest and prints text or JSON counts plus the exact items and required evidence that still block strict release readiness. `tools/validate-staging-evidence.mjs` rejects missing evidence categories, invalid statuses, `passed` entries without artifact records, blocked/failed entries without owner/blocker details, and accepted-risk entries without decision references. Its `--strict-release` mode rejects placeholder release candidates plus any pending, blocked, or failed item, and allows `accepted_risk` only for `SEC-SIGNOFF-001`. `tools/verify-production-readiness.mjs` reruns strict manifest validation, validates a full non-dry-run `tools/run-local-gates.mjs` report, requires a clean git worktree, requires the branch to be neither ahead nor behind upstream, requires `release_candidate` to match `git rev-parse HEAD`, and rejects local gate reports whose `git_head` does not match that same release SHA. Evidence: `node tools\validate-staging-evidence.mjs` passed on 2026-06-25 against the checked-in pending-external example, `node tools\bootstrap-staging-evidence.mjs --out production-readiness\staging-evidence.staging.json --environment staging --force` produced a local manifest for release candidate `7b68fe93650ed9606a0e54ba90d4c398423db07d`, `node tools\validate-staging-evidence.mjs --manifest production-readiness\staging-evidence.staging.json` passed, `node tools\report-staging-evidence-gaps.mjs --manifest production-readiness\staging-evidence.staging.json` reports the current strict-release blockers, `node --test tools\bootstrap-staging-evidence.test.mjs tools\report-staging-evidence-gaps.test.mjs tools\run-local-gates.test.mjs tools\validate-ci-workflow.test.mjs` passed, `node --test tools\validate-staging-evidence.test.mjs` passed for strict-mode behavior, `node --test tools\verify-production-readiness.test.mjs` passed for clean/synced/SHA-match, local-gate-report, and rejection cases, and `node tools\validate-local-gate-report.mjs --report artifacts\local-gate-report.json` validated a full 23-gate non-dry-run local report on 2026-06-25. Remaining production work is filling the environment-specific evidence manifest with real staging artifacts, rerunning or preserving the full local gate report from the clean release SHA, pushing/syncing that exact SHA, and passing the final claim verifier against those artifacts.
- Cross-cutting staging evidence recorder: Locally proved as artifact workflow tooling, not as staging proof. `tools/record-staging-evidence.mjs` reads a passing probe JSON report, records its artifact path/URL and command into only the manifest IDs listed in `evidence_items`, marks those items `passed`, preserves the rest of the manifest, and handles Windows UTF-8 BOM JSON artifacts. It also supports explicit `--item-id` recording for human-reviewed artifacts such as Terraform plan/apply logs, Kubernetes rollout output, Secrets Store CSI/IRSA proof, scoped database user proof, observability proof, native mobile proof, rollback drills, backup/restore drills, and owner/security signoff. For honest release tracking, it can also record `blocked` and `failed` statuses with required owner/blocker details and `accepted_risk` statuses with required decision references. Evidence: `node --test tools\record-staging-evidence.test.mjs` passed, a probe-report temporary manifest update followed by `STAGING_EVIDENCE_FILE=<temp>\manifest.json node tools\validate-staging-evidence.mjs` passed, an explicit `--item-id DEPLOY-TF-001` temporary manifest update followed by the same validator passed, and temporary `blocked` plus `accepted_risk` recordings followed by the same validator passed on 2026-06-25. Remaining production work is using the recorder with real staging probe, release, blocker, and risk-decision artifacts.
- Cross-cutting CI release evidence probe tooling: Locally proved as probe-tool behavior, not as a pushed CI run. `tools/write-ci-release-evidence.mjs` writes the `ci-release-evidence` GitHub Actions artifact after required CI gates and TruffleHog complete, and the workflow validates the local artifact file with `tools/ciprobe` before upload. `tools/ciprobe` emits JSON evidence for `SRC-CI-001` by validating either a local artifact file or artifact URL for the exact 40-character release commit SHA, the `Security Pipeline Verification` workflow, the `security-audit` job, completed/successful status, and required gate markers for Go test/vet, web/mobile npm audit/smoke/typecheck/build checks, Rust `cargo test`, Terraform fmt/validate, and TruffleHog. It rejects failed, cancelled, timed-out, dirty-worktree, and stale-SHA markers. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/ciprobe -count=1 -v` and `node --test tools\write-ci-release-evidence.test.mjs` passed on 2026-06-25. Remaining production work is committing and pushing the release branch, downloading or exposing the real `ci-release-evidence` artifact for that exact SHA, running `tools/ciprobe` against it, and recording the passing report into the environment-specific evidence manifest.
- Cross-cutting deployment staging probe tooling: Locally proved as probe-tool behavior, not as deployed proof. `tools/deploymentprobe` emits JSON evidence for Terraform and Kubernetes deployment artifacts. `-probe-terraform` requires remote S3 backend init output, a staging Terraform plan containing EKS/RDS/Redis resource markers, and apply or deployment-approval evidence before emitting `DEPLOY-TF-001`; it rejects local `-backend=false` evidence. `-probe-kubernetes` requires rollout status and workload resource artifacts proving API, web, and Rust deployments plus service, ingress, HPA, and PDB resources before emitting `DEPLOY-K8S-001`. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/deploymentprobe -count=1 -v` passed on 2026-06-25. Remaining production work is running the probe against real staging Terraform/Kubernetes artifacts and pairing it with `tools/stagingprobe` TLS/readiness output.
- Cross-cutting staging probe tooling: Locally proved as probe-tool behavior, not as deployed proof. `tools/stagingprobe` emits JSON evidence for deployed API/web HTTPS readiness: API `/live`, API `/ready`, web root status, TLS version/certificate expiry, and HTTP-to-HTTPS redirect checks. Optional `-probe-zoom` coverage verifies invalid Zoom webhook signature denial and, with `-zoom-webhook-secret`, signed no-op webhook acceptance. Optional `-probe-ai` coverage calls authenticated study generation with a supplied bearer token so provider readiness can be captured deliberately. The reports map to `DEPLOY-TLS-001`, `DEPLOY-K8S-001`, `CLIENT-WEB-001`, `EXT-ZOOM-001`, and `EXT-AI-001` in the staging evidence manifest. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/stagingprobe -count=1 -v` passed on 2026-06-25. Remaining production work is running the probes against real staging API/web hostnames with staging secrets/tokens and attaching their JSON output to an environment-specific evidence manifest.
- Cross-cutting Zoom staging probe tooling: Locally proved as probe-tool behavior, not as deployed proof. `tools/zoomprobe` emits JSON evidence for `EXT-ZOOM-001` by validating captured artifacts for Zoom OAuth readiness, meeting creation or offline fallback, webhook signature/delivery checks, duplicate webhook idempotency, and Zoom meeting-to-`live_rooms` mapping. It rejects artifacts that expose obvious Zoom secret values. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/zoomprobe -count=1 -v` passed on 2026-06-25. Remaining production work is capturing real staging Zoom OAuth, meeting/fallback, webhook delivery, duplicate, and mapping artifacts, and pairing the report with `tools/stagingprobe -probe-zoom`.
- Cross-cutting AI staging probe tooling: Locally proved as probe-tool behavior, not as deployed proof. `tools/aiprobe` emits JSON evidence for `EXT-AI-001` by validating captured staging artifacts for provider/model configuration, authenticated generation through `/api/v1/ai/generate/study`, timeout/degradation behavior, citation verification, and persisted `ai_request_logs` plus `citation_trails`. It rejects obvious provider API key or bearer-token leaks in artifacts. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/aiprobe -count=1 -v` passed on 2026-06-25. Remaining production work is capturing real staging AI provider/degradation/citation/audit artifacts and pairing the report with `tools/stagingprobe -probe-ai`.
- Cross-cutting abuse staging probe tooling: Locally proved as probe-tool behavior, not as deployed proof. `tools/abuseprobe` emits JSON evidence for `ABUSE-LIMIT-001` by repeatedly exercising auth, AI, journal, room, and WebSocket route profiles through HTTPS staging ingress until each returns `429` with `Retry-After` and `X-RateLimit-Limit` headers. It requires a staging bearer token for protected routes and rejects runs that do not observe rate limits within the configured attempts. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/abuseprobe -count=1 -v` passed on 2026-06-25. Remaining production work is running the probe against real staging ingress with known `ABUSE_LIMIT_*` settings and attaching the JSON report to an environment-specific evidence manifest.
- Cross-cutting tenant staging probe tooling: Locally proved as probe-tool behavior, not as deployed proof. `tools/tenantprobe` emits JSON evidence for deployed API tenant isolation through encrypted journal endpoints. With an owner bearer token and a second blocked bearer token, it creates an encrypted journal entry, proves the owner can read it, proves the blocked principal receives `404` for the direct read, and proves the blocked principal's journal list does not include the created ID. The report maps to `DATA-RLS-001` in the staging evidence manifest. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/tenantprobe -count=1 -v` passed on 2026-06-25. Remaining production work is running the probe against real staging API hostnames with two valid staging principals and attaching its JSON output to an environment-specific evidence manifest.
- Cross-cutting Rust staging probe tooling: Locally proved as probe-tool behavior, not as deployed proof. `tools/rustprobe` emits JSON evidence for deployed Rust gRPC health and optional Rust metrics reachability. The report maps to `RUST-GRPC-001` in the staging evidence manifest, verifies the standard gRPC health service reports `SERVING`, and checks the metrics endpoint for ScriptureForge Rust metric names when a metrics URL is provided. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/rustprobe -count=1 -v` passed on 2026-06-25. Remaining production work is running the probe from a staging network context that can reach `scriptureforge-rust-engine:50051` and its metrics endpoint, then attaching the JSON output to an environment-specific evidence manifest.
- Cross-cutting mobile staging probe tooling: Locally proved as probe-tool behavior, not as native-device proof. `tools/mobileprobe` emits JSON evidence for `CLIENT-MOBILE-001` only when supplied EAS/native-device run output, native `react-native-quick-crypto` AES-GCM smoke output with round-trip and tamper-rejection markers, and staging API/WS config proof that avoids localhost and the old hardcoded `wss://api.scriptureforge.com` value. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/mobileprobe -count=1 -v` passed on 2026-06-25. Remaining production work is running the mobile native/EAS validation and attaching the probe JSON report to an environment-specific evidence manifest.
- Cross-cutting observability staging probe tooling: Locally proved as probe-tool behavior, not as deployed proof. `tools/observabilityprobe` emits JSON evidence for deployed telemetry. `-probe-otel` requires collector config proof, API metrics, Rust metrics, trace backend search, log backend search, and a shared trace ID before emitting `OBS-OTEL-001`. `-probe-alerts` requires dashboard, alert-rules, alertmanager/status, and retention proof targets before emitting `OBS-ALERT-001`. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/observabilityprobe -count=1 -v` passed on 2026-06-25. Remaining production work is running the probe against real staging observability backends after dashboard import, alert wiring, log shipping, trace export, and retention policy configuration.
- Cross-cutting security staging probe tooling: Locally proved as probe-tool behavior, not as deployed proof. `tools/securityprobe` emits JSON evidence for staged secret handling and scoped database credentials. `-probe-secrets` requires IRSA service-account, Secrets Store CSI `SecretProviderClass`, redacted synced-secret metadata, and IAM policy artifacts, and rejects obvious plaintext DSNs/API keys/private keys in metadata artifacts before emitting `SEC-SECRETS-001`. `-probe-db-user` connects with the staged app `DATABASE_URL` without echoing it, rejects root/admin/reserved principals, and verifies the connected role is not superuser, `CREATEROLE`, or `CREATEDB` before emitting `SEC-DBUSER-001`. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/securityprobe -count=1 -v` passed on 2026-06-25. Remaining production work is running the probe against real staging artifacts and credentials, plus clean pushed TruffleHog/SAST/SCA output and owner cloud-access review.
- Cross-cutting resilience staging probe tooling: Locally proved as probe-tool behavior, not as deployed proof. `tools/resilienceprobe` emits JSON evidence for rollback/degradation and backup/restore drills. `-probe-rollback` requires before/after API readiness evidence, rollout undo/status artifact evidence, and AI/Zoom degradation fallback drill evidence before emitting `DR-ROLLBACK-001`. `-probe-backup` requires RDS snapshot/backup creation evidence, restore drill evidence, and an application smoke/readiness proof against the restored database before emitting `DR-BACKUP-001`. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/resilienceprobe -count=1 -v` passed on 2026-06-25. Remaining production work is actually running rollback, degradation, backup, and restore drills in staging and attaching the probe JSON reports to an environment-specific evidence manifest.
- Cross-cutting local gate matrix: Locally proved for Go, Rust, web audit/smoke/typecheck/build, mobile high-severity audit/smoke/build-compatible checks, Terraform/TLS skeleton validation, deployment skeleton invariant validation, staging evidence manifest validation, observability artifacts, secret hygiene, security artifact validation, dependency-risk validation, CI workflow gate coverage, repeatable local gate reporting, and the local load harness on 2026-06-25. `node tools\run-local-gates.mjs --continue-on-failure --report artifacts\local-gate-report.json` passed 23/23 non-dry-run gates on 2026-06-25, and `node tools\validate-local-gate-report.mjs --report artifacts\local-gate-report.json` accepted the report. The full report includes Go test/vet, evidence probe tests, web moderate audit/smoke/typecheck/build, mobile high-severity audit/smoke/build-compatible checks, Rust cargo test, Terraform fmt/init/validate, observability validation, deployment skeleton validation, staging evidence validation, CI workflow validation, security artifact validation, dependency-risk validation, secret hygiene validation, journal crypto validation, and tooling tests, and is stamped with `git_head` so release verification can reject stale local evidence. `tools/run-local-gates.mjs` now centralizes the local matrix, including web and mobile npm audit gates plus dependency-risk validation, handles Windows npm command shims without `spawn EINVAL`, and writes JSON reports for full or selected gate runs, while `tools/validate-local-gate-report.mjs` rejects failed, partial, malformed, dry-run, unknown-gate, or missing-`git_head` reports unless relaxed development flags are explicitly used. The tooling test gate now covers the local gate runner, local gate report validator, CI workflow validator, dependency-risk validator, staging evidence validator, production readiness verifier, staging evidence recorder, staging evidence bootstrapper, staging evidence gap reporter, and CI release evidence writer. `.github/workflows/security.yml` now provisions a `pgvector/pgvector:pg16` Postgres service, applies migrations, runs the DB-backed RLS/auth/AI/journal integration tests with `DATABASE_URL` set instead of relying only on skip-capable `CI=true` Go tests, runs the load harness self-test, validates observability artifacts, validates secret hygiene, validates deployment skeleton invariants, validates the staging evidence manifest contract, validates CI workflow gate coverage, validates security/dependency-risk artifacts, tests the staging evidence bootstrapper and gap reporter, runs web smoke before web typecheck/build, and runs mobile high-severity audit plus smoke before mobile build-compatible checks. Remaining blockers: mobile still reports 10 accepted-risk moderate `uuid` advisories through Expo tooling, no real staging deployment/load test has been run, and the full gate matrix has not yet been proven by a clean GitHub Actions run from the current uncommitted worktree.

## 100% Production Readiness Evidence Still Required

- Source control and CI: Commit and push the current remediation set, then prove GitHub Actions passes on the exact branch intended for release and record a passing `tools/ciprobe` report for `SRC-CI-001`.
- Auth/session closure: Local tests now cover registration role forcing, canonical and legacy route behavior, refresh rotation, expired/revoked refresh denial, privileged-role MFA denial/verification, logout revocation, and abusive login/refresh controls. Remaining closure is clean pushed CI evidence plus staging observation of auth/rate-limit behavior behind real ingress/client identity.
- AI runtime closure: Local tests now cover missing-key readiness behavior, bounded timeout paths, hallucinated-citation rejection, no-citation rejection, successful `ai_request_logs` and `citation_trails` persistence, and no unit-test dependency on live provider calls. Remaining closure is staging validation against real provider configuration, timeout/retry/degradation behavior, audit persistence, and telemetry.
- Mobile/native closure: Run EAS or equivalent native-device validation proving the native AES-GCM binding works outside Node/WebCrypto test shims, and close DRR-001 when Expo resolves the remaining moderate `uuid` advisory without a breaking downgrade.
- Web/product closure: Mocked route smoke coverage is locally proved and wired into CI config; remaining web product closure is deployed browser smoke against staging API/WS endpoints.
- Observability closure: Local dashboard, alert-rule, retention-runbook, trace/log/metrics, artifact validation coverage, Go API OTLP exporter bootstrap, and Terraform API env wiring exist; remaining closure is deployed OpenTelemetry collector/backend proof, production log/metric/trace retention proof, dashboard import evidence, alert delivery proof, and staging telemetry proof for traces, request metrics, room/socket counters, AI faults, auth abuse, and dependency failures.
- Deployment closure: TLS-capable ingress skeleton, app env wiring, observability env wiring, workload secret injection skeleton, API health routes, Kubernetes probes, Rust service bind wiring, standard gRPC health service support, RDS backup/final-snapshot controls, and Kubernetes rolling-update rollback settings now exist; remaining closure is supplying real staging Terraform inputs, initializing remote state, validating ACM/DNS/collector ownership, running staging `plan`/`apply`, proving Kubernetes app wiring, Secrets Store CSI/IRSA sync, scoped `DATABASE_URL` secret, deployed ingress/TLS/readiness probes, trace export, RDS/Redis connectivity, Rust gRPC reachability, rollback exercise, backup creation, and restore drill.
- External service closure: Prove staging Zoom OAuth/webhook delivery, AI provider configuration/degradation, Redis sequencing, Postgres RLS, web deployment, and mobile app configuration using real service endpoints.
- Security closure: RLS integration tests now cover every tenant-scoped table locally, local secret hygiene validation/review exists, current threat/security artifacts are validated, Terraform includes IRSA/Secrets Store CSI workload secret wiring, and mounted auth abuse/session tests cover refresh/MFA/logout plus route-level auth rate limiting. Remaining security closure is clean pushed CI TruffleHog/SAST/SCA/dependency audit evidence, deployed TLS/SSL probe checks, owner/security threat-model signoff, staging abuse telemetry under real ingress behavior, cloud secret-manager/remote-state access proof, staging secret-sync proof, and dependency-risk review for DRR-001.
- Performance closure: The reproducible HTTP and WebSocket harnesses exist, self-test locally, and now include an external WebSocket mode for deployed room streams; remaining performance closure is staging evidence for HTTP, WebSocket fan-out across multiple API instances, Redis Lua sequence ordering under real Redis, DB/RLS query behavior, AI timeout/circuit behavior, and the architecture's stated 5,000 req/s with P99 under 200ms target or an architecture update with measured production limits.

## Current Production Readiness Verdict

Not production-ready. The codebase has useful foundations and several security-aware components, and the local Go/Rust/web/Terraform/mobile-build-compatible/load-harness/observability-artifact/secret-hygiene gate matrix is much stronger, but the repo still lacks a clean pushed CI run, EAS/native mobile evidence, actual production observability collector/backend/import/alert delivery proof, staging performance/load proof, staging deployment proof, external service readiness evidence, cloud secret-manager/remote-state proof, and eventual closure of the accepted-risk moderate Expo tooling advisory.
