# ScriptureForgeAI Functionality Audit Briefing

Date: 2026-06-23
Auditor: Codex
Scope: production and code functionality gaps across backend, web, mobile, Rust engine, migrations, infrastructure, and local verification gates.

## Executive Summary

The repository has moved from a skeletal implementation toward a locally verified remediation baseline, but it is not production-ready. Several original audit findings are now locally closed with tests and gate tooling; the remaining production claim still depends on clean pushed CI, staging deployment evidence, external-service proof, native mobile evidence, and owner/security signoff.

The highest-risk gaps are:

Current-state note (2026-08-28): the branch tip is now `7e52902ba92032e05bebf27b5293b6cb61c5a7ca`. Hosted push run `33159869107` and pull-request run `33159877417` completed successfully for this exact SHA, including PostgreSQL/RLS integration, web/mobile checks, Rust protobuf/tests, Terraform validation, secret scanning, source-control cleanliness, and uploaded CI release evidence. The exact-SHA local matrix at `2026-08-28T09:46:40Z` executed 35/35 gates with 34 passing; its only failure was the fail-closed Docker-backed RLS gate because the local Docker server endpoint was unreachable.

1. A prior clean pushed GitHub Actions run and exact-release `tools/ciprobe` proof exist for commit `610efd0e50cb1f949e4a0cff2f8a66e5bbb83eab` (run `32001756732`); the current branch tip `006559ea68d0cd56679d54af0728e306ffb2b40f` has focused deployment and documentation validation, while its push run `33071077208` failed before workflow steps started (`runner_id=0`, `steps=[]`). The full local matrix rerun was blocked at the Docker-backed RLS gate because the Docker daemon/config is access-denied. Final readiness still requires hosted CI recovery and recording the uploaded HTTPS artifact in the environment-specific staging manifest.
2. The staging evidence manifest still has every production artifact pending: deployment, TLS, Kubernetes, secrets, RLS-through-API, Redis/WebSocket sequencing, Rust gRPC, observability, web/mobile smokes, Zoom, AI, load, rollback, backup/restore, and signoff.
3. Local mobile crypto verification now covers AES-GCM, the architecture PBKDF2 600,000-iteration work factor, blank passphrase/salt rejection, key lifecycle cleanup, native-required fail-closed behavior, and the `react-native-quick-crypto` provider-selection path with a local provider harness; native mobile/EAS validation is still required for production AES-GCM proof outside Node/WebCrypto shims.
4. The architecture's performance target of 5,000 req/s with P99 under 200ms has not been proven against staging.
5. Rust scripture engine tests/build were originally blocked by missing `protoc`; protobuf compiler provisioning is now vendored and repo-local Cargo/Rust verification passes.
6. Go and web build gates were originally non-runnable; the repo-local/full local gate runner now proves Go test/vet and web audit/smoke/typecheck/build locally.
7. Terraform is a validated skeleton, not a live staging deployment with remote state, plan/apply, Kubernetes rollout, or cloud-side secret proof.

## Verification Performed

- Current exact-SHA verification (2026-08-28): pushed branch tip `7e52902ba92032e05bebf27b5293b6cb61c5a7ca` passed GitHub Actions push run `33159869107` and pull-request run `33159877417`; all workflow steps completed successfully, including the real PostgreSQL/RLS suite and CI release-evidence upload. The exact-SHA local matrix executed 35/35 gates with 34 passing, `git_remote_refreshed=true`, a clean worktree, and 0/0 divergence; only the Docker-backed `rls-db-integration` gate failed because the local Docker server endpoint was unreachable.
- `git status --short --branch`: clean `main...origin/main` before audit.
- Original audit: `rtk go test ./...` failed immediately because `go` was not found on PATH. Current 2026-06-27 verification uses repo-local Go `1.24.3`; the full local gate report includes passing Go test/vet.
- Original audit: `rtk npm run build` in `web/` failed because `next` was not recognized. Current 2026-06-27 verification uses installed web dependencies; the full local gate report includes passing web audit, smoke, typecheck, and build gates.
- `rtk cargo test` in `services/scripture-engine/`: original audit failed because `protoc` was missing. Current verification uses repo-local Cargo/Rust, passes with vendored `protoc`, and the local/CI gate path now runs `cargo test --locked`.
- `node tools\run-local-gates.mjs --continue-on-failure --report artifacts\local-gate-report-current.json`: current 2026-06-29 dirty-worktree development evidence passed the earlier 31/31 non-dry-run local gates at 2026-06-29T12:33:40Z, including `rls-db-integration`, `rls-schema-validation`, `ci-evidence-gate-validation`, `staging-evidence-contract-check`, and the Obsidian readiness snapshot check. The local matrix now has 32 gates after adding `staging-evidence-gap-report`, which runs the gap reporter against the checked-in evidence contract with explicit `--allow-blockers` and validates the marker-rich not-ready blocker list. The local RLS DB gate runs `tools/run-rls-db-integration-docker.mjs` with `REQUIRE_DATABASE_URL=true`, applies migrations into a disposable `pgvector/pgvector:pg16` container, and fails if DB-backed tenant/RLS proof cannot run. This is not release-grade evidence because strict validation still requires a clean worktree, clean pushed CI, matching release SHA, and staging evidence.
- `node tools\run-local-gates.mjs`: current 2026-08-17 clean-release evidence passed `34/34` gates with `dry_run=false`, `git_head=610efd0e50cb1f949e4a0cff2f8a66e5bbb83eab`, `git_status_clean=true`, `git_ahead=0`, `git_behind=0`, and `git_remote_refreshed=true` at `2026-08-17T06:38:22Z`. The report is locally validated evidence for the exact pushed release, while staging deployment and external-service proof remain separate requirements.
- 2026-08-17 exact-release CI evidence: GitHub Actions run `32001756732` completed successfully for `610efd0e50cb1f949e4a0cff2f8a66e5bbb83eab`; the release artifact was accepted by `tools/ciprobe` as `SRC-CI-001`, including clean tracked/untracked source-control markers and the required gate set. The environment-specific staging manifest still requires the uploaded HTTPS artifact URL rather than a local downloaded-file path before final readiness can pass.
- `cmd /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& rtk go test ./cmd/platform-engine ./internal/domain/abuse -count=1 -timeout=60s"`: current focused local abuse-control evidence passed 51 route/limiter tests. `cmd/platform-engine/routes_test.go` now proves every mounted sensitive public route variant enforces its configured abuse profile before a second same-identity request can reach normal handler behavior: canonical and legacy auth routes, refresh/logout, MFA, workspace switch, canonical and legacy AI generation, journal bootstrap/list/create/read, room create/list/state polling, and WebSocket stream upgrade. This is still local mounted-route proof; staging ingress evidence remains tracked under `ABUSE-LIMIT-001`.
- 2026-06-30 update: `ABUSE-LIMIT-001` staging config evidence now rejects zero-valued abuse-limit assignments. `tools/abuseprobe`, the staging evidence recorder, and strict-release validation require positive integers for every `ABUSE_LIMIT_*` request/window assignment, `ABUSE_LIMIT_AUTH_ACCOUNT_*` request/window assignment, and `ABUSE_LIMIT_MAX_BUCKETS`, so a redacted config artifact cannot satisfy production readiness with effectively disabled or unusable limiter values.
- 2026-06-30 update: `ABUSE-LIMIT-001` route-profile evidence now requires structured scoped/protocol proof in addition to marker summaries. `tools/abuseprobe` emits `account_scoped=true`, `forwarded_client_ip_rotated=true`, `refresh_token_scoped=true`, and `websocket_upgrade=true` fields where applicable; the staging evidence recorder rejects reports that omit those booleans; and strict-release validation requires the matching exact markers in each abuse profile segment. This remains evidence-contract hardening only; real staging ingress proof is still pending external evidence.
- 2026-06-30 update: `ABUSE-LIMIT-001` and `DATA-RLS-001` staging evidence now require `load_run_id` binding. `tools/abuseprobe`, `tools/tenantprobe`, the staging evidence recorder, and strict-release validation reject missing or mixed `load_run_id` values across route/config/API/DB proof segments, so rate-limit and tenant/RLS evidence cannot be assembled from unrelated staging runs. This remains evidence-contract hardening only; real staging ingress and deployed API/database proof are still pending external evidence.
- 2026-07-01 update: `ABUSE-LIMIT-001` and `DATA-RLS-001` reports now require structured top-level `load_run_id` in addition to summary markers. `tools/record-staging-evidence.mjs` rejects abuse-control and tenant/RLS reports that only carry run identity in `config_artifact_summary`, route summaries, API summaries, or database proof summaries, so hand-edited evidence cannot rely on prose markers for the report run identity. Real staging ingress and deployed API/database proof remain pending external evidence.
- 2026-07-01 update: `ABUSE-LIMIT-001` final-release validation now requires the recorded staging manifest to retain `structured_report.abuse_rate_limit_proof` with config verification, positive `ABUSE_LIMIT_*` assignments, per-profile attempts, concrete rate-limit headers, and account/refresh/WebSocket scoped booleans bound back to the `abuseprobe` summary markers. This prevents marker-only or hand-edited abuse evidence from satisfying rate-limit readiness after raw probe JSON is no longer attached locally. Real staging ingress abuse proof remains pending external evidence.
- 2026-07-01 update: `DATA-RLS-001` tenant reports now require report-level `created_journal_id` and `created_room_id` fields matching the owner create probe resource IDs. `tools/tenantprobe` emits those fields, and `tools/record-staging-evidence.mjs` rejects reports that omit or mismatch them, so deployed tenant evidence cannot assemble read/list/state proof around resources that differ from the resources created during the same run. Real deployed API/database proof remains pending external evidence.
- 2026-07-01 update: deployed tenant DB/RLS evidence now requires per-table outcome proof, not just all-table counts. `tools/tenantprobe` requires `rls_table_names=organizations,users,scripture_texts,refresh_tokens,journal_entries,live_rooms,room_participants,ai_request_logs,citation_trails` plus `rls_table_<table>_same_visible=true`, `rls_table_<table>_cross_hidden=true`, and `rls_table_<table>_write_denied=true` markers for every tenant-scoped table, emits structured `rls_table_outcomes`, and the staging evidence recorder plus strict-release validation reject missing or partial table outcome proof. Real deployed API/database proof remains pending external evidence.
- 2026-07-01 update: `DATA-RLS-001` final-release validation now requires the recorded staging manifest to retain a compact `structured_report.database_rls_context_proof` with exact tenant table names and per-table same-visible/cross-hidden/write-denied booleans. This prevents a marker-only or hand-edited manifest from satisfying tenant isolation after the original `tenantprobe` JSON report is no longer attached locally. Real deployed API/database proof remains pending external evidence.
- 2026-07-01 update: `DATA-RLS-001` strict validation now also binds the persisted `structured_report.database_rls_context_proof` identity fields back to the recorded summary markers: `owner_org_id` must match the database `app.current_org_id`, `blocked_org_id` must match the database blocked-tenant marker, and created journal/room IDs must match the owner create probes. This prevents a hand-edited structured report from describing a different tenant pair or resource than the attached probe summary. Real deployed API/database proof remains pending external evidence.
- 2026-07-01 update: `EXT-AI-001` final-release validation now requires the recorded staging manifest to retain `structured_report.ai_generation_audit_proof` with provider configuration, generation principal/request, fail-closed timeout booleans, citation ID, and audit principal/request/citation fields bound back to the `aiprobe` summary markers. This prevents a marker-only or hand-edited AI manifest from satisfying provider/degradation/citation/audit readiness after the raw probe JSON is no longer attached locally. Real deployed AI provider/degradation/citation/audit proof remains pending external evidence.
- 2026-07-01 update: `auth.SetTenantContext` now trims and validates `organization_id` as a UUID before setting transaction-local `app.current_org_id`, and unit coverage proves malformed tenant IDs fail closed before any database call. This strengthens local tenant-boundary enforcement; deployed API/database proof remains pending under `DATA-RLS-001`.
- 2026-06-30 update: `SEC-DBUSER-001` database-principal evidence now has to prove the staged connection is specifically `scriptureforge_app`. `tools/securityprobe` already compares `current_user` with the redacted DATABASE_URL principal and rejects reserved/admin usernames; the staging evidence recorder and strict-release validation now also require the `database-scoped-user` summary to include `connected as scriptureforge_app`, preventing a generic non-superuser account from satisfying the scoped application-user proof.
- 2026-06-30 update: staging load/performance side artifacts now have to be bound to a concrete load run. `tools/loadtest` requires `-load-run-id` or `STAGING_LOAD_RUN_ID` for staging HTTP and WebSocket evidence; side artifacts must include matching `load_run_id=<same load run>` markers; and the recorder plus strict-release validation require the same marker in `PERF-HTTP-001`, `PERF-WS-001`, and `DATA-REDIS-001` summaries. This prevents same-release but different-run replica, telemetry, reconnect, polling, or Redis artifacts from satisfying load readiness. Real staging load proof remains pending external evidence.
- 2026-07-01 update: staging load/performance reports now require a structured top-level `load_run_id` in addition to summary markers. `tools/record-staging-evidence.mjs` rejects `PERF-HTTP-001`, `PERF-WS-001`, and `DATA-REDIS-001` reports that only carry `load_run_id=<same load run>` in `result_summary`, so hand-edited load evidence cannot rely on prose markers for the report run identity. Real staging load proof remains pending external evidence.
- 2026-07-01 update: `PERF-HTTP-001`, `PERF-WS-001`, and `DATA-REDIS-001` final-release validation now requires the recorded staging manifest to retain structured load proofs: `structured_report.http_load_threshold_proof` for HTTP throughput/latency/dependency evidence and `structured_report.websocket_redis_sequence_proof` for WebSocket/Redis room, principal, reconnect, polling, sequence, replica, and dropped-broadcast evidence. This prevents marker-only or hand-edited load summaries from satisfying performance/WebSocket/Redis readiness after raw `loadtest` JSON is no longer attached locally. Real staging load proof remains pending external evidence.
- 2026-06-30 update: source loadtest reports now emit exact production target markers in their verified marker lists. HTTP staging evidence must preserve `production_target_rps=5000`, `production_target_p99_ms=200`, and `production_min_duration_ms=60000`; WebSocket staging evidence must preserve `production_target_rps=500`, `production_target_p99_ms=200`, `production_min_duration_ms=60000`, and `production_min_ws_events=30000`; and `tools/loadtest/main_test.go` locks those markers into producer tests. Real staging HTTP/WebSocket/Redis load proof remains pending under `PERF-HTTP-001`, `PERF-WS-001`, and `DATA-REDIS-001`.
- 2026-06-30 update: external-service staging evidence now has the same run-identity binding. `tools/aiprobe` requires `-load-run-id` or `STAGING_LOAD_RUN_ID` and rejects AI provider/generation/degradation/citation/audit artifacts that omit the matching `load_run_id=<same run>` marker; `tools/zoomprobe` now requires the same run identity and rejects Zoom OAuth, meeting/fallback, timeout/circuit, webhook delivery, URL validation, duplicate-idempotency, and room-mapping artifacts that omit the matching marker; `tools/observabilityprobe` now requires the same run identity and rejects OTEL collector/API metrics/Rust metrics/trace/log artifacts plus dashboard/rules/alert/retention artifacts that omit the matching marker; `EXT-ZOOM-001`, `EXT-AI-001`, `OBS-OTEL-001`, and `OBS-ALERT-001` recorder validation require every probe summary to include the same run ID and reject mixed probe run IDs; strict-release validation now requires `load_run_id=` inside the corresponding Zoom, AI, OTEL, and alert summary segments. This prevents same-release but different-run external-service artifacts from satisfying production readiness. Real staging Zoom, AI, and observability proof remains pending external evidence.
- 2026-07-01 update: `OBS-OTEL-001` and `OBS-ALERT-001` now require a structured top-level `load_run_id` in addition to per-segment markers. The staging evidence recorder rejects observability reports that only carry summary-level run markers, strict-release validation requires `load_run_id` in the observability family summaries, and the staging evidence contract now documents same-run binding for collector, metrics, trace/log, dashboard, alert, delivery, and retention artifacts. Real staging observability proof remains pending external evidence.
- 2026-07-01 update: `OBS-OTEL-001` and `OBS-ALERT-001` final-release validation now requires the recorded staging manifest to retain `structured_report.observability_otel_proof` and `structured_report.observability_alert_proof`, binding release/run identity, trace/log route/method/trace IDs, tenant/user/role principals, alert identity, delivery ID, and telemetry artifact targets back to the `observabilityprobe` summary markers. This prevents marker-only or hand-edited observability manifests from satisfying trace, dashboard, alert, or retention readiness after the raw probe JSON is no longer attached locally. Real deployed observability proof remains pending external evidence.
- 2026-07-01 update: strict-release validation now also revalidates retained observability structured targets. `structured_report.observability_otel_proof` targets and `structured_report.observability_alert_proof` targets must remain HTTPS, non-local, distinct artifact URLs, and trace/log query targets must include the matching concrete trace ID. This prevents a final staging manifest from retaining strong marker summaries while drifting structured telemetry artifact targets to local, duplicate, or broad query URLs. Real deployed observability proof remains pending external evidence.
- 2026-07-01 update: `DEPLOY-TLS-001`, `CLIENT-WEB-001`, and `CLIENT-MOBILE-001` reports now require structured top-level `load_run_id` in addition to per-probe summary markers. `tools/record-staging-evidence.mjs` rejects TLS, browser-smoke, and native mobile smoke reports that only carry run identity in probe summaries, so hand-edited client/deployment evidence cannot rely on prose markers for the report run identity. Real staging web/mobile/TLS proof remains pending external evidence.
- 2026-07-01 update: `CLIENT-WEB-001` reports now require report-level `web_user_id`, `web_organization_id`, `web_journal_id`, and `web_room_id` fields matching the browser auth, journal, and room smoke proof segments. `tools/stagingprobe` emits these envelope fields only after the smoke segments agree, and `tools/record-staging-evidence.mjs` rejects missing or mismatched values so deployed web evidence cannot pair auth, encrypted-journal, or WebSocket room proof from different users/resources. Real deployed browser smoke proof remains pending external evidence.
- 2026-07-01 update: `CLIENT-MOBILE-001` reports now also require a structured report-level `mobile_build_id` matching the EAS/native-device, native crypto, and staging-config proof segments. `tools/mobileprobe` emits the envelope field after proving all three artifacts share one build ID, and `tools/record-staging-evidence.mjs` rejects missing or mismatched report values so mobile production evidence cannot stitch native crypto proof to a different installed app/config build. Real EAS/native-device proof remains pending external evidence.
- 2026-07-01 update: native mobile crypto staging evidence now carries AES-GCM IV freshness as structured JSON. `tools/mobileprobe` extracts `unique_iv=true`, emits `unique_iv: true` on the `mobile-native-crypto-smoke` probe, rejects missing or false IV freshness markers, and `tools/record-staging-evidence.mjs` rejects hand-recorded `CLIENT-MOBILE-001` reports that omit the structured field even if the prose summary still contains `unique_iv=true`. The recorder also checks the artifact summary's first `provider=` and `native_required=` bindings against the structured native-provider fields. Real EAS/native-device proof remains pending external evidence.
- 2026-07-01 update: native mobile crypto outcome evidence now has to be machine-readable as well as prose-readable. `tools/mobileprobe` extracts and emits `aes_gcm_roundtrip`, `tamper_rejected`, and `associated_data_rejected` booleans for `mobile-native-crypto-smoke`; `tools/record-staging-evidence.mjs`, strict-release validation, and the staging evidence contract require `aes_gcm_roundtrip=true`, `tamper_rejected=true`, and `associated_data_rejected=true` before `CLIENT-MOBILE-001` can support final readiness. Real EAS/native-device proof remains pending external evidence.
- 2026-07-01 update: native mobile crypto lifecycle evidence now has to be machine-readable, not only prose. `tools/mobileprobe` extracts and emits `key_disposed`, `disposed_handle_rejected`, `revoked_key_rejected`, `passphrase_buffer_zeroized`, `salt_buffer_zeroized`, and `plaintext_buffer_zeroized` boolean fields for `mobile-native-crypto-smoke`, and `tools/record-staging-evidence.mjs` plus strict-release validation require the corresponding `*_true` markers in that same segment before `CLIENT-MOBILE-001` can support final readiness. Real EAS/native-device proof remains pending external evidence.
- 2026-07-01 update: the app-runtime `runJournalCryptoSelfTest()` now emits the exact `key_disposed=true` and `disposed_handle_rejected=true` lifecycle markers required by `CLIENT-MOBILE-001`, while keeping the legacy `key_disposal=true` verifier marker for local gate continuity. Mobile smoke tests and `tools/verify-journal-crypto.mjs` now reject self-test output that omits those production evidence markers. Real EAS/native-device execution remains pending external proof.
- 2026-07-01 update: strict-release manifest validation now has cross-family regression coverage proving every probe-backed blocker rejects both local report artifacts and HTTPS artifacts from the wrong evidence family. This locks the shared `tools/<probe>` non-local report requirement across CI, deployment, TLS/web, security, abuse, tenant/RLS, Redis/load, Rust, observability, mobile, Zoom, AI, and resilience evidence. This is evidence-contract hardening only; real staging artifacts remain pending external proof.
- 2026-07-01 update: `SEC-SIGNOFF-001` manual evidence now requires a durable signoff artifact, either a repo `security/*.md` signoff/approval document or a non-local HTTPS approval artifact. Repo-local signoff documents must be read, non-empty, and contain the same threat-model, DRR-001, residual-risk, owner/security approval, release-risk signoff, `signoff_artifact_verified=true`, and exact `release_candidate=<manifest release_candidate>` markers before `tools/record-staging-evidence.mjs` records them; strict-release validation requires the verification marker too. Scratch `artifacts/` paths and summary-only approvals are rejected, so owner/security approval cannot be closed by a throwaway local file or a decorative filename. Real owner/security signoff remains pending external approval.
- 2026-07-01 update: `SEC-SIGNOFF-001` manual evidence now also rejects explicit non-production caveats before recording. `tools/record-staging-evidence.mjs` fails owner/security signoff summaries and repo-local signoff document bodies that include mock, dry-run, local-only, synthetic, stubbed, placeholder, or test-only markers, so a signoff cannot be recorded as passed while admitting it is rehearsal evidence. Real owner/security signoff remains pending external approval.
- 2026-07-01 update: `SRC-CI-001` CI release evidence now requires structured run identity, not just prose markers. `tools/ciprobe` emits and validates `artifact_commit_sha`, `run_id`, `run_attempt`, and `run_number`, rejects artifacts whose `commit:` line differs from `commit_sha` even if the target SHA appears elsewhere, rejects `run_url` values not bound to `run_id`, and `tools/record-staging-evidence.mjs` rejects hand-recorded CI reports that omit or mismatch those fields. This strengthens clean pushed CI evidence only; remaining production closure still requires recording the uploaded HTTPS `ci-release-evidence` artifact into the staging manifest for the exact release SHA.
- 2026-06-30 update: deployment staging evidence now requires a single run identity across Terraform and Kubernetes artifacts. `tools/deploymentprobe` requires `-load-run-id` or `STAGING_LOAD_RUN_ID` and rejects deployment artifacts that omit the matching `load_run_id=<same run>` marker; `DEPLOY-TF-001` and `DEPLOY-K8S-001` recorder validation require every probe summary to include the same run ID and reject mixed probe run IDs; strict-release validation also requires `load_run_id=` inside the remote backend, plan, apply/approval, rollout, and workload-resource summary segments. This prevents same-release but different-run deployment artifacts from satisfying readiness. Real Terraform apply and Kubernetes rollout proof remain pending external evidence.
- 2026-07-01 update: `DEPLOY-TF-001` Terraform evidence now requires concrete KMS identifiers, not marker names alone. `tools/deploymentprobe` rejects remote-backend artifacts without `kms_key_id=<arn-or-alias>`, rejects plan artifacts without concrete `kms_key_id=<arn-or-alias>`, `database_kms_key_arn=<arn:aws:kms:...:key/...>`, and `redis_kms_key_arn=<arn:aws:kms:...:key/...>` markers, emits those values structurally, and the staging evidence recorder plus strict-release validation reject summaries that omit concrete KMS values. Real Terraform apply and cloud-side KMS proof remain pending external evidence.
- 2026-06-30 update: Zoom webhook idempotency now guards duplicate deliveries before database room-mapping work, not only before Redis state mutation. `internal/adapters/integration_zoom/zoom_webhook_test.go` proves two concurrent deliveries with the same `x-zm-trackingid` perform only one room lookup and one mapped active-state update while the duplicate returns 200. This reduces duplicate delivery stampede risk; live Zoom staging evidence remains pending under `EXT-ZOOM-001`.
- 2026-06-30 update: Zoom duplicate-webhook staging evidence now requires structured side-effect proof, not only prose markers. `tools/zoomprobe` emits `single_state_mutation=true` and `no_duplicate_side_effects=true` for `zoom-duplicate-webhook-idempotency`, and both `tools/record-staging-evidence.mjs` and strict-release validation reject reports or manifests missing those booleans and matching summary markers. Real Zoom staging evidence remains pending under `EXT-ZOOM-001`.
- 2026-07-01 update: Zoom no longer has an ambient `GO_ENV=testing` mock-meeting success path. Missing Zoom credentials now always produce the same offline meeting fallback and `credential_or_token_fallback` dependency metric, even in test-mode shells, while successful Zoom tests use explicit fake HTTP transports. Observability also treats `mock_success` dependency statuses as error-like instead of production success. Real Zoom OAuth/webhook/fallback/mapping staging proof remains pending under `EXT-ZOOM-001`.
- 2026-07-01 update: `tools/zoomprobe` now requires `staging artifact` provenance in each fetched Zoom artifact body before emitting `EXT-ZOOM-001`, instead of adding the marker only in passing summaries. Regression coverage proves an otherwise complete OAuth artifact without staging provenance fails; real staging Zoom evidence remains pending external proof.
- 2026-07-01 update: Zoom webhook signature staging proof now requires structured outcome markers, not only prose. `tools/zoomprobe` emits `stale_rejected=true`, `replay_rejected=true`, `invalid_signature_rejected=true`, and `signed_delivery_accepted=true` on `zoom-webhook-signature-delivery`; the staging evidence recorder and strict-release validation reject reports or manifests missing those booleans even when signature/timestamp and stale/replay/signed text is present. Real Zoom OAuth/webhook/fallback/mapping staging proof remains pending external evidence.
- 2026-07-01 update: `EXT-ZOOM-001` final-release validation now requires the recorded staging manifest to retain `structured_report.zoom_resilience_webhook_proof` with timeout/circuit/fallback booleans, webhook signature/timestamp, webhook outcome booleans, URL-validation tokens, duplicate delivery/tracking IDs, side-effect booleans, and meeting-to-room mapping IDs bound back to the `zoomprobe` summary markers. This prevents marker-only or hand-edited Zoom evidence from satisfying webhook/resilience readiness after raw probe JSON is no longer attached locally. Real Zoom OAuth/webhook/fallback/mapping staging proof remains pending external evidence.
- 2026-06-30 update: WebSocket fan-out now fail-safes when `SocketConnection.Hub` is not preconfigured. The handler lazily initializes one shared hub on the socket instance instead of creating a private per-connection hub, and `internal/ports/rooms_realtime_test.go` proves nil-hub room subscribers still receive the same accepted broadcast sequence. This closes a local wiring regression path; deployed multi-replica fan-out/load proof remains pending under `PERF-WS-001` and `DATA-REDIS-001`.
- 2026-08-16 WebSocket event-envelope hardening: room stream ingress now uses strict JSON decoding and rejects unknown fields, missing/null payloads, and client-supplied sequence values before Redis append or broadcast. Focused tests prove invalid envelopes are rejected while server-assigned sequences remain valid; deployed multi-instance fan-out/load proof remains external under `PERF-WS-001` and `DATA-REDIS-001`.
- 2026-06-30 update: mobile journal passphrase lifetime is shorter after local key derivation. `SecureJournalContainer` now clears passphrase state after a key is successfully derived while preserving the installed disposable key handle, and `tools/verify-journal-crypto.mjs` requires that preservation guard so future changes cannot reintroduce long-lived passphrase state. Local evidence: `cmd /c "call tools\use-project-path.cmd >nul && cd mobile && rtk npm run build:check"` passed mobile typecheck, API/crypto smoke tests, and journal crypto verifier. Native-device/EAS proof remains pending under `CLIENT-MOBILE-001`.
- 2026-07-01 update: mobile journal passphrase cleanup now also covers key derivation errors. `SecureJournalContainer` clears passphrase state after disposing the failed/replaced key handle in the derivation `.catch` path, and `tools/verify-journal-crypto.mjs` emits `mobile_passphrase_derivation_failure_cleanup=true` only when that failure-path cleanup is present. This is local lifecycle hardening; native-device/EAS AES-GCM proof remains pending under `CLIENT-MOBILE-001`.
- 2026-07-01 update: mobile crypto smoke coverage now uses an instrumented WebCrypto-compatible provider to prove passphrase, salt, plaintext, ciphertext, IV, associated-data, and decoded plaintext buffers are zeroized after derivation/encrypt/decrypt operations. `tools/verify-journal-crypto.mjs` requires this runtime smoke marker before emitting journal crypto proof. Native-device/EAS AES-GCM proof remains pending under `CLIENT-MOBILE-001`.
- 2026-07-01 update: web journal key derivation now keeps passphrase/salt cleanup inside the PBKDF2 key-material import failure boundary. `web/src/lib/crypto.smoke.mts` proves the observable `importKey` failure passphrase cleanup path, and `tools/verify-journal-crypto.mjs` emits `web_import_failure_zeroization=true` only when the guarded `finally` still covers both passphrase and salt buffers. This is local web-client lifecycle hardening; deployed browser smoke and native-device/EAS proof remain pending external evidence.
- 2026-06-30 update: abuse limiter identity scoping now rejects incomplete authenticated claim material for rate-limit identity construction. Middleware only uses tenant/user scoped buckets when both trimmed `organization_id` and `user_id` are present; malformed or incomplete claim contexts fall back to client/proxy identity instead of sharing a blank authenticated bucket. Local evidence: `cmd /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& rtk go test ./internal/domain/abuse -count=1 -timeout=90s"` passed 19 abuse limiter tests, including the new incomplete-claims fallback regression. Staging ingress abuse proof remains pending under `ABUSE-LIMIT-001`.
- 2026-06-28 update: passed `SEC-SIGNOFF-001` manual signoff evidence now requires exact `release_candidate=<manifest release_candidate>` markers in both `tools/record-staging-evidence.mjs` and strict staging validation, closing the stale prior-release owner/security signoff reuse path. Accepted-risk signoff remains limited to `security/dependency_risk_register.md#DRR-001` with non-overdue review/expiry metadata.
- 2026-06-28 update: web and mobile journal AES-GCM now bind encryption/decryption to server-issued journal salt context using associated data, and local/web/mobile/staging evidence checks reject missing or wrong associated-data proof so ciphertext cannot be silently accepted under the wrong journal salt version.
- Manual source inspection of docs, backend entrypoints, ports, auth, AI, room, Zoom, migrations, web, mobile, Rust service, Terraform, and existing reports.

## Critical Gaps

The F-AUD sections below preserve the original audit finding text and should be read with the remediation tracker for current status. Items that still show original evidence may already have local remediation evidence later in this briefing; production closure remains gated by the staging evidence manifest and clean pushed CI.

### F-AUD-001: Tenant isolation RLS is not connected to application sessions

Severity: Critical
Area: database/auth/backend
Evidence:
- `migrations/000002_core_schema.up.sql` enables RLS and defines policies using `current_setting('app.current_org_id', true)::UUID`.
- The migration itself notes that a real implementation must securely set `app.current_org_id` per session.
- No inspected backend code sets `app.current_org_id` before querying.
- `tests/integration/db_ping_test.go` sets the variable manually inside a test transaction, which proves the application layer does not own the behavior.

Impact:
Production requests may fail closed under RLS or bypass intended tenant guarantees if table owners/superusers are used incorrectly. The architecture promises implicit organization scoping, but the implementation does not enforce it at the database session boundary.

Recommended fix:
Add a DB access wrapper that requires verified auth claims and runs all tenant-scoped work inside a transaction or connection context with `SET LOCAL app.current_org_id = $claims.OrganizationID`. Add integration tests that exercise actual handlers, not only manual SQL.

### F-AUD-002: Auth/session model does not match production contract

Severity: Critical
Area: auth/API
Evidence:
- `cmd/platform-engine/main.go` mounts `/api/auth/register` and `/api/auth/login`, while architecture documents `/api/v1/auth/register`, `/api/v1/auth/login`, and `/api/v1/auth/refresh`.
- `internal/ports/auth_http.go` issues access tokens with `2*time.Hour`; the architecture calls for short-lived 15-minute JWTs plus database-backed opaque refresh tokens.
- There is no refresh token table in the migration and no refresh handler.
- MFA is documented as mandatory for privileged roles, but no MFA route or TOTP implementation exists.

Impact:
The platform cannot support secure session renewal, revocation, privileged-role MFA, or the documented API contract.

Recommended fix:
Introduce `/api/v1/auth/*` routes, refresh-token persistence, revocation/rotation, short access-token TTLs, and privileged-role MFA enforcement. Keep legacy routes only as explicit compatibility aliases if needed.

### F-AUD-003: WebSocket room sync is an echo server with permissive origins

Severity: Critical
Area: live rooms/realtime/security
Evidence:
- `internal/ports/driving_wss.go` sets `CheckOrigin` to always return `true`.
- `HandleLiveRoom` upgrades the socket and echoes whatever message it receives.
- No room ID, membership check, tenant check, Redis state mutation, event schema validation, broadcast hub, reconnect handling, or polling fallback exists in the socket path.

Impact:
Live Bible Study Rooms are not functionally implemented. In production this would expose an overly permissive WebSocket endpoint that authenticates a token but does not validate room authorization or constrain origins.

Recommended fix:
Require an explicit room identifier, validate membership against tenant-scoped storage, restrict origins by environment config, validate event envelopes, route all mutations through Redis Lua/state manager, and implement broadcast/presence semantics.

### F-AUD-004: Journal encryption is local-only and uses a static salt

Severity: High
Area: web/mobile/zero-knowledge
Evidence:
- `web/src/components/JournalEditor.tsx` uses `const userSalt = "static-user-salt-12345"`.
- The save path encrypts into component state and leaves the backend `fetch('/api/journal'...)` call commented out.
- No backend journal route exists in inspected routes.
- The database migration does not include `journal_entries`, despite the architecture requiring encrypted journal persistence.

Impact:
The zero-knowledge journal is a demo, not a usable persisted feature. Static salt also weakens per-user key derivation assumptions.

Recommended fix:
Add a deterministic per-user salt strategy, backend journal ciphertext endpoints, `journal_entries` migration, tenant/user scoping, and client save/load integration tests.

### F-AUD-005: AI path depends on live external APIs and legacy model choices without robust production controls

Severity: High
Area: AI/RAG
Evidence:
- `internal/domain/ai/rag.go` calls OpenAI embeddings directly from request flow.
- `internal/adapters/llm/client.go` panics at startup when `OPENAI_API_KEY` is absent outside testing.
- The LLM request hardcodes `gpt-4`.
- HTTP clients have no explicit timeout, retry, circuit breaker, provider abstraction, rate limit handling, or audit persistence for `AIRequestLog`/`CitationTrail`.
- `ResponseVerificationSubsystem.Verify` returns success when the generated text has no citations at all.

Impact:
The production AI feature is fragile under provider outage, slow network calls, key misconfiguration, and citation-free answers. It also does not persist the immutable audit artifacts promised by the architecture.

Recommended fix:
Replace startup panic with fail-closed route readiness, configure provider/model/timeout through env, add bounded HTTP clients, persist AI request/citation logs, and require citations for routes that promise citation-first output.

### F-AUD-006: Rust scripture engine does not build in this environment

Severity: High
Area: Rust/gRPC/toolchain
Evidence:
- Original audit: `rtk cargo test` failed in `services/scripture-engine` because `protoc` was missing.
- Current remediation: `services/scripture-engine/build.rs` now sets `PROTOC` using `protoc-bin-vendored`; `.github/workflows/security.yml` runs `cargo test --locked` without a separate `setup-protoc` step; `services/scripture-engine/Cargo.lock` pins the verified dependency graph; the Rust dependency lane has moved to `pgvector 0.4.2` and `sqlx-postgres 0.9.0`.
- Current local evidence: repo-local Rust/Cargo under `.tools` runs `cargo test --locked`; generated protobuf unit tests pass; `tools/verify-rust-protobuf.mjs` guards the vendored `protoc` path, all checked-in vendored `protoc` platform lockfile packages, generated protobuf coverage for embedding and vector-search response/result types, the tenant/RPC `proto/scripture.proto` contract, the SQLx 0.9/pgvector 0.4 lane, static SQL query safety, and Rust runtime observability wiring for metrics, traceparent extraction, and release/deployment metadata.

Impact:
The scripture engine now rebuilds and verifies locally. The earlier `sqlx-postgres v0.7.4` future-incompatibility warning is closed by the SQLx 0.9/pgvector 0.4 dependency-lane upgrade.

Recommended fix:
Keep the vendored `protoc` build path, SQLx 0.9/pgvector 0.4 lane, and static SQL query checks in CI while staging proves deployed Rust gRPC health and metrics.

### F-AUD-007: Frontend build was not runnable in the original audit

Severity: High
Area: web/build
Evidence:
- Original audit: `rtk npm run build` in `web/` failed: `'next' is not recognized as an internal or external command`.
- Current remediation: web dependencies and scripts now support audit, smoke, typecheck, and build gates. `web/src/lib/api.smoke.mts` proves auth helpers use canonical routes, journal helpers avoid plaintext fields, and room helpers build encoded WebSocket stream URLs.
- Current local evidence: the full local gate report includes `web-audit`, `web-smoke`, `web-typecheck`, and `web-build`, all passing on 2026-06-25.

Impact:
The original local build blocker is closed. The remaining production risk is that web product behavior has not yet been smoke-tested against deployed staging API and WebSocket endpoints through a real browser/ingress path.

Recommended fix:
Keep web audit/smoke/typecheck/build in CI, then run deployed browser smoke flows against staging login/register, encrypted journal save/load, room create/select, and WebSocket connection setup. Browser smoke artifacts must carry exact `release_candidate=<manifest release_candidate>` and `service_version` markers before `CLIENT-WEB-001` can support final readiness.

### F-AUD-008: Go backend tests were blocked by missing Go on PATH

Severity: High
Area: backend/toolchain
Evidence:
- Original audit: `rtk go test ./...` failed: `Binary 'go' not found on PATH`.
- Current remediation: repo-local Go `1.24.3` is available under `.tools\go\bin\go.exe`, matching the repo-current toolchain target.
- Current local evidence: the full local gate report includes `go-test`, `go-vet`, and evidence probe Go test gates, all passing on 2026-06-25.

Impact:
The original local Go toolchain blocker is closed. The remaining production risk is that these gates have not yet passed in a clean pushed GitHub Actions run for the exact release SHA.

Recommended fix:
Keep Go `1.24.3` as the repo-current runtime target, retain Go test/vet and containerized integration gates in CI, and record the clean GitHub Actions artifact under `SRC-CI-001`.

## Major Gaps

### F-AUD-009: Infrastructure is placeholder Terraform, not deployable production IaC

Severity: Medium-High
Area: infrastructure
Evidence:
- Original audit: `build/terraform/main.tf` contained hardcoded placeholder AWS role ARN/subnet IDs, had production-hostile snapshot settings, and lacked backend state, secrets manager integration, IAM roles, node groups, Redis, ingress, container image boundaries, and app service manifests.
- Current remediation: Terraform is split across focused files with variables, S3 backend configuration, IAM, security groups, EKS cluster/node group, Aurora PostgreSQL, Redis, ECR repositories, Kubernetes deployments/services/ingress, Secrets Manager ARN inputs, and example backend/tfvars files.
- Current local evidence: repo-local `.tools\terraform\terraform.exe` was installed and `terraform fmt -check -recursive`, `terraform init -backend=false`, and `terraform validate` pass.

Impact:
The repo now has a locally validated deployment skeleton, but it still cannot claim production deployment readiness until real staging inputs are supplied and a staging plan/apply proves the resources.

Recommended fix:
Run the same Terraform gates in CI, then supply real staging inputs, plan/apply to staging, and document rollback/degradation evidence.

### F-AUD-010: Zoom integration lacks production resilience controls

Severity: Medium-High
Area: integrations
Evidence:
- `internal/adapters/integration_zoom/zoom_client.go` uses default `http.Client{}` with no timeout.
- Architecture promises 3500ms timeouts, circuit breaking, and graceful fallback, but no circuit breaker or fallback path exists.
- Webhook handler updates Redis active state by Zoom meeting ID, but no mapping to internal `live_rooms.id` was found.

Impact:
Zoom outages or slow calls can hang request paths, and webhook events may not map to the correct internal room lifecycle.

Recommended fix:
Add bounded clients, retry/circuit breaker behavior, explicit meeting-to-room mapping, fallback offline-room behavior, and webhook replay/idempotency handling.

### F-AUD-011: Mobile app is a journal demo, not the documented companion experience

Severity: Medium-High
Area: mobile
Evidence:
- `mobile/App.tsx` renders only `HomeScreen`.
- `mobile/src/screens/HomeScreen.tsx` renders `SecureJournalContainer`.
- No auth, room subscription, API client, offline state, push handling, or real backend integration was found.

Impact:
The companion app does not satisfy the room participation, authenticated workspace, or synchronized study requirements.

Recommended fix:
Add auth/session bootstrap, API configuration, room list/active-room connection, journal persistence, and Expo verification commands.

### F-AUD-012: Web app is not connected to real backend state

Severity: Medium-High
Area: web
Evidence:
- `web/src/components/layout/RoomLayout.tsx` connects to hardcoded `wss://api.scriptureforge.com/ws/room`.
- Zustand initializes `activeRoomId` as `null`, and no UI/API path was found to create or select a room.
- No login/register UI exists despite backend auth handlers.

Impact:
The dashboard cannot perform the primary workflows from the architecture.

Recommended fix:
Add environment-driven API base URLs, auth screens, room creation/listing, active room selection, and real socket event handling.

## Additional Gaps

- API versioning is inconsistent: docs use `/api/v1/*`, code uses `/api/auth/*`, `/api/ai/curriculum`, `/api/webhooks/zoom`, and `/ws/room`.
- Database schema differs from architecture: docs define `doctrinal_profile`, `live_rooms`, `room_participants`, and `journal_entries`; migration only includes `organizations`, `users`, and `scripture_texts`.
- `scripture_texts` schema differs between docs and code: docs use translation/book_number/text_content/text_vector; migration/code use organization_id/book/content/embedding.
- The architecture requires audit logs and telemetry. Backend trace IDs, structured HTTP logs, `/metrics`, deployable dashboards/alerts, and env-driven OpenTelemetry OTLP trace exporter bootstrap now exist, but deployed collector/backend proof, staging telemetry proof, and broader persistent audit-log coverage remain incomplete.
- Existing `FUNCTIONAL_COVERAGE_REPORT.md` and `audit_report.txt` overstate completion relative to current runnable evidence.

## Recommended Next Work Sequence

1. Complete for the current remediation set: commit/push, capture the exact-SHA GitHub Actions `ci-release-evidence` artifact, and validate it with `tools/ciprobe`; record its uploaded HTTPS URL in the environment-specific staging manifest when staging collection begins.
2. Complete for the current release: preserve and validate the clean non-dry-run local gate report from the exact release checkout with `tools/validate-local-gate-report.mjs`.
3. Fill the environment-specific staging evidence manifest with real artifacts for deployment, TLS, Kubernetes, IRSA/CSI secrets, scoped DB credentials, RLS-through-API, Redis/WebSocket sequencing, Rust gRPC, observability, web/mobile, Zoom, AI, load, rollback, backup/restore, and owner/security signoff.
4. Run native-device/EAS validation for mobile AES-GCM and staging endpoint config, then record `CLIENT-MOBILE-001`.
5. Run staging HTTP and WebSocket load tests to prove the architecture target or revise the target with measured production limits.
6. Run rollback/degradation and backup/restore drills in staging and record the resilience probe artifacts.
7. Run `tools/verify-production-readiness.mjs` only after strict staging evidence, full local gates, clean git state, synced upstream, and release-candidate SHA matching all pass.

## Remediation Tracker

Status last updated: 2026-08-28

2026-08-27 current evidence update: `node tools/run-local-gates.mjs --continue-on-failure` completed against pushed commit `9a3d90ba6e3c25b72fdfc32e9f243674c05d7727` with `34/34` gates, `0` failures, `dry_run=false`, refreshed upstream metadata, a clean worktree, and `0/0` ahead/behind. GitHub Actions runs `33066720997` (push) and `33066729082` (pull request) for that commit failed before workflow steps started (`runner_id=0`, `steps=[]`), so hosted CI remains an external blocker; the prior successful run `32001756732` belongs to commit `610efd0e50cb1f949e4a0cff2f8a66e5bbb83eab`.
2026-08-27 post-image-commit evidence update: focused deployment skeleton tests (20/20), local-gate report tests (34/34), Serena/Obsidian synchronization, roadmap artifact validation, and whitespace checks passed for `006559ea68d0cd56679d54af0728e306ffb2b40f`. The attempted full matrix could not complete because Docker client access to `C:\Users\domin\.docker` and `npipe:////./pipe/docker_engine` was denied; no full-matrix pass is claimed for this tip. GitHub Actions push run `33071077208` also failed before steps (`runner_id=0`, `steps=[]`), so hosted CI and Docker-backed RLS remain external blockers.

- F-AUD-001: Locally proved across handler paths and every tenant-scoped table. Tenant-scoped handlers now use `auth.SetTenantContext`; the schema forces RLS, includes tenant-aware write checks and composite tenant/user foreign keys, and the integration harness creates/grants a non-superuser `scriptureforge_rls_test` role plus `SET LOCAL ROLE` inside tenant transactions so superuser test connections cannot bypass RLS. `tests/integration/tenant_handler_rls_test.go` proves same-tenant journal writes/reads succeed while cross-user, cross-tenant, and mismatched tenant/user operations are denied. `tests/integration/table_rls_test.go` now seeds both tenants and proves tenant-visible counts, known same-tenant row visibility, known cross-tenant row invisibility, cross-tenant insert denial, and cross-tenant update/delete invisibility for `organizations`, `users`, `scripture_texts`, `refresh_tokens`, `journal_entries`, `live_rooms`, `room_participants`, `ai_request_logs`, and `citation_trails`. Evidence: against disposable `pgvector/pgvector:pg16` Postgres on 2026-06-25, `set DATABASE_URL=postgres://postgres:scriptureforge@localhost:55433/scriptureforge?sslmode=disable&& set JWT_SECRET_KEY=test-secret-for-db-backed-auth-tests&& set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tests/integration -run TestTenantRLSCoversAllTenantTables -count=1 -timeout=90s -v` passed; `set DATABASE_URL=postgres://postgres:scriptureforge@localhost:55433/scriptureforge?sslmode=disable&& set JWT_SECRET_KEY=test-secret-for-db-backed-auth-tests&& set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tests/integration ./internal/ports -count=1 -timeout=90s -v` passed; `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./... -count=1 -timeout=90s` and `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe vet ./...` also passed. Current focused evidence: `cmd /c "call tools\use-project-path.cmd >nul && rtk node tools\run-rls-db-integration-docker.mjs"` passed on 2026-06-29 against a disposable `pgvector/pgvector:pg16` container, applying migrations and passing tenant table RLS, journal/room handler RLS, socket tenant scoping, auth/session RLS, and AI audit/citation RLS tests. In this shell without `DATABASE_URL`, the focused integration command compiles and skips DB execution as expected.
- F-AUD-001 drift guard: `tools/validate-rls-schema.mjs` now fail-closes tenant schema drift by requiring every tenant-scoped table in `migrations/000002_core_schema.up.sql` to keep ENABLE/FORCE RLS, `app.current_org_id` USING/WITH CHECK policies, table-level same-tenant read/cross-tenant read-denial/cross-tenant insert-denial/update-hidden/delete-hidden proof markers, legacy handler-level RLS proof markers, stronger ports-level tenant isolation proofs for journal handlers, room handlers, room tenant-override rejection, WebSocket stream rejection, and cross-tenant write blocking, plus AI audit-log/citation RLS proof markers for `GenerateCurriculumHandler`, `ai_request_logs`, and `citation_trails`. It also requires auth-session proof markers for refresh rotation/revocation, privileged MFA login blocking/verification, workspace-switch cross-tenant denial, and privileged-only MFA enrollment/verification behavior. Evidence: `cmd /c "call tools\use-project-path.cmd >nul && node tools\validate-rls-schema.mjs"` and `cmd /c "call tools\use-project-path.cmd >nul && node --test tools\validate-rls-schema.test.mjs"` passed on 2026-06-29; `cmd /c "call tools\use-project-path.cmd >nul && rtk node --test tools\validate-rls-schema.test.mjs tools\run-rls-db-integration.test.mjs tools\run-rls-db-integration-docker.test.mjs"` passed 17/17 tests on 2026-06-27. Remaining production work is deployed API/database RLS proof under `DATA-RLS-001`.
- 2026-06-28 F-AUD-001 guard hardening: `tools/validate-rls-schema.mjs` now requires assertion-level markers for plaintext journal rejection, same-tenant journal create/list success, cross-user and cross-tenant journal denial, same-tenant and mismatched room active-list behavior, direct ports-level cross-tenant journal/list denial, cross-tenant room/state denial, same-tenant room/state success, socket rejection without event append, and same-tenant/cross-tenant AI audit-log and citation counts. This keeps the local RLS proof from regressing to name-only tests while deployed API/database proof remains pending under `DATA-RLS-001`.
- 2026-06-28 F-AUD-001 tenant-table discovery hardening: `tools/validate-rls-schema.mjs` now derives tenant-scoped tables directly from migration `CREATE TABLE` blocks carrying tenant data and fails if that discovered list differs from the RLS coverage list. This prevents a new `organization_id` table from being added without explicit RLS policy and integration-test coverage. Deployed API/database proof remains pending under `DATA-RLS-001`.
- 2026-06-29 F-AUD-001 local Docker proof refresh: `cmd /c "call tools\use-project-path.cmd >nul && rtk node tools\run-rls-db-integration-docker.mjs"` passed against a disposable `pgvector/pgvector:pg16` container after applying migrations, proving table RLS, journal handler RLS, active-room handler RLS, journal/room tenant isolation, WebSocket tenant scoping, AI audit/citation tenant RLS, generated-curriculum audit persistence, auth refresh/session RLS, privileged MFA login blocking/verification, workspace-switch tenant matching, and privileged-only MFA enrollment/verification locally. `tools/run-rls-db-integration.test.mjs` asserts every claimed proof marker is present in the selected DB-backed Go test pattern. Deployed API/database proof remains pending under `DATA-RLS-001`.
- 2026-06-28 F-AUD-001 room handler mismatch hardening: `internal/ports/tenant_isolation_integration_test.go` now proves a request carrying a tenant B organization claim with a tenant A user ID cannot create a live room and persists zero `live_rooms` or `room_participants` rows for that forged pairing. `tools/validate-rls-schema.mjs` requires those assertion markers, and the refreshed Docker-backed RLS run passed after the regression was added. Deployed API/database proof remains pending under `DATA-RLS-001`.
- 2026-06-28 F-AUD-001 staging proof separation hardening: `tools/tenantprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation now reject `DATA-RLS-001` evidence when the redacted database/RLS proof artifact is hosted on the same canonical evidence host as the staging API target, including host-case or trailing-dot aliases, and require the DB/RLS proof segment to carry `distinct_db_rls_artifact=true`. This keeps deployed API behavior proof separate from database-session/RLS proof while the real staging run remains pending.
- 2026-06-29 F-AUD-001 tenant-pair DB binding hardening: `tools/tenantprobe` now requires structured `owner_org_id` and `blocked_org_id` values for different staging organizations and requires the DB/RLS proof artifact to carry `app.current_org_id=<owner_org_id>` plus `blocked_org_id=<blocked_org_id>`. The staging evidence recorder and strict-release validation reject `DATA-RLS-001` reports that omit those tenant-pair bindings, preventing stale or unrelated DB/RLS artifacts from satisfying deployed tenant-isolation proof.
- 2026-06-30 F-AUD-001 structured tenantprobe report hardening: `tools/tenantprobe` regression tests now decode the successful `DATA-RLS-001` JSON report and assert that the created journal ID is present on owner read/list and blocked read/list probes, the created room ID is present on owner list/state and blocked list/state probes, and blocked journal/room tenant-override writes prove HTTP 403 denial. Deployed API/database proof remains pending under `DATA-RLS-001`.
- 2026-06-29 F-AUD-001/service-version release binding hardening: `tools/record-staging-evidence.mjs` now rejects probe reports whose top-level `service_version` does not include the manifest `release_candidate`, and strict release manifest validation requires required probe-report summaries to carry `service_version=<service>:<manifest release_candidate>` in addition to exact `release_candidate=<manifest release_candidate>` markers. This prevents stale deployed service artifacts from being paired with a current release SHA while real staging evidence remains pending.
- F-AUD-002: Locally proved for route/session behavior and mounted auth abuse controls. Canonical `/api/v1/auth/*` routes, legacy register/login aliases, 15-minute access tokens, refresh rotation, logout, privileged-role TOTP enforcement, and organization-scoped login/refresh/logout payloads have been added. `tests/integration/auth_session_test.go` covers registration role forcing, approximate 15-minute access-token TTL, login token issuance, refresh rotation, expired-token denial, rotated old-token denial, cross-tenant refresh denial, logout revocation, missing privileged MFA denial without tokens, and verified TOTP login. `cmd/platform-engine/routes_test.go` proves legacy `/api/auth/register` and `/api/auth/login` aliases use the same validation behavior as canonical `/api/v1/auth/register` and `/api/v1/auth/login`, mounted `/api/v1/auth/login` enforces `ABUSE_LIMIT_AUTH_*` with `429`, `Retry-After`, and `X-RateLimit-Limit`, and the legacy login alias shares the same canonical auth abuse bucket instead of bypassing limits. `internal/ports/auth_http_test.go` now proves login enforces an `ABUSE_LIMIT_AUTH_ACCOUNT_*` bucket keyed by a hash of normalized organization ID plus email, refresh enforces a token-scoped bucket keyed by normalized organization ID plus hashed refresh token, both reject repeated attempts across rotating client IPs with canonical `ABUSE_RATE_LIMIT_FAULT` responses, and metrics keep account/token/IP identifiers out of abuse-limiter labels. Evidence: against disposable `pgvector/pgvector:pg16` Postgres on 2026-06-25, `set DATABASE_URL=postgres://postgres:scriptureforge@localhost:55433/scriptureforge?sslmode=disable&& set JWT_SECRET_KEY=test-secret-for-db-backed-auth-tests&& set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tests/integration ./internal/ports -count=1 -timeout=90s -v` passed; `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./cmd/platform-engine -count=1 -timeout=60s -v`, full Go test, and Go vet passed on 2026-06-25. Focused 2026-06-29 evidence for this account/refresh-throttle hardening: `cmd /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& rtk go test ./internal/ports ./cmd/platform-engine ./internal/domain/abuse -run \"TestAuthAccountLimit|TestRefreshTokenLimit|TestMounted.*Abuse|TestLegacyAuthAliasSharesCanonicalAbuseBucket|TestLimiter\" -count=1 -timeout=90s -v"` passed 23 tests across three packages. Remaining production work is staging abuse/rate-limit observation around auth flows.
- F-AUD-003: Locally proved for realtime correctness, reconnect behavior, Redis-backed sequence ordering, baseline socket lifecycle controls, production-like origin fail-closed behavior, handler-level concurrent broadcast behavior, and local WebSocket load-smoke coverage. The echo WebSocket has been replaced with room membership checks, configured origin validation, JSON room events, Redis-backed sequenced latest-state persistence, in-process room participant broadcast fan-out, membership-gated HTTP room polling/listing, bounded WebSocket read size, read deadlines, pong deadline refresh, write deadlines, periodic ping heartbeats, and policy-violation close behavior for malformed or wrong-room event envelopes. `internal/ports/rooms_realtime_test.go` proves invalid events close with WebSocket policy violation without echo or persistence, accepted events broadcast to room participants, oversized frames close without persistence, a participant can disconnect/reconnect and receive subsequent accepted events, HTTP polling returns the same latest sequenced state after reconnect, configured bad origins are rejected, `DEPLOYMENT_ENVIRONMENT=staging|production|prod` rejects WebSocket upgrades when `ALLOWED_WS_ORIGINS` is missing while local development still allows localhost/no-origin fallback, and concurrent connected senders receive contiguous accepted sequences plus Redis append metrics for every accepted event. `internal/domain/room/redis_lua_test.go` uses an in-process Redis-compatible server to exercise the actual Lua-backed `AppendRoomEvent` path under concurrent writers and proves contiguous non-duplicated sequence assignment. `tools/loadtest -websocket-self-test` starts the production `SocketConnection` handler in-process, injects verified tenant/user claims through the same context contract, sends room events from concurrent clients, and measures accepted-broadcast latency. `tools/loadtest -websocket` targets deployed room streams with bearer auth, HTTPS allowed origin, room ID, concurrency, event count, and JSON threshold reporting so staging can capture real upgrade/auth/broadcast evidence; for WSS staging targets it now requires `-ws-token`, rejects local/insecure `-ws-origin`, requires HTTPS artifacts for API replica distribution, reconnect behavior, HTTP polling fallback, and Redis telemetry, requires release_candidate/service_version linkage in those artifacts, persists `ws_authenticated=true` plus `ws_origin`, and the staging evidence recorder rejects `PERF-WS-001` reports that omit those proofs. Evidence: `cmd /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& go test ./internal/ports -run TestLiveRoom -count=1 -timeout=60s -v"`, `cmd /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& go test ./internal/domain/room -count=1 -timeout=60s -v"`, and `cmd /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& go test ./tools/loadtest -count=1 -timeout=60s -v"` passed on 2026-06-27; earlier WebSocket self-test, full Go test, and Go vet evidence remains in the local gate matrix. Remaining production work is running the external WebSocket load mode across multiple API instances and real Redis.
- F-AUD-004: Locally proved for client crypto and backend no-plaintext persistence. Backend encrypted journal endpoints and web/mobile API integration have been added; web and mobile now use the architecture PBKDF2 600,000-iteration work factor for journal key derivation, bind AES-GCM encryption/decryption to server-issued journal salt context with associated data, and reject decryption when the associated data does not match. Mobile no longer uses the Expo structural placeholder and now prefers the native `react-native-quick-crypto` binding before falling back to a WebCrypto-compatible AES-GCM provider for local/test-compatible runtimes. Production/staging mobile builds must set `EXPO_PUBLIC_REQUIRE_NATIVE_CRYPTO=true`, fail closed if `react-native-quick-crypto` is unavailable, reject fallback-derived keys in native-required mode, reject staging/production config that omits the native-required flag, and reject local or insecure API/WS endpoints whenever native crypto or `EXPO_PUBLIC_DEPLOYMENT_ENVIRONMENT=staging|production|prod` makes the build strict; `tools/mobileprobe` requires that native-required config marker before accepting `CLIENT-MOBILE-001`. The backend journal create handler now uses strict JSON decoding with unknown fields rejected, so plaintext/passphrase payload fields fail closed instead of being silently ignored. `tests/integration/tenant_handler_rls_test.go` proves plaintext/passphrase journal payloads are rejected with no DB rows created, valid ciphertext payloads persist, response bodies do not leak plaintext markers, stored journal columns do not contain plaintext markers, and cross-user/cross-tenant reads are denied under RLS. `tools/verify-journal-crypto.mjs` proves mobile PBKDF2-derived keys use the 600,000-iteration work factor and are non-extractable, passphrase/salt derivation byte buffers are wiped in a `finally` path, ciphertext is not plaintext-equivalent base64, AES-GCM decrypt round-trips, tampered ciphertext is rejected, wrong associated data is rejected, disposable key handles clear key references, disposed handles revoke stale raw key references, untracked externally imported keys are rejected, the mobile helper loads and prefers `react-native-quick-crypto` before fallback WebCrypto, the verifier rejects Expo Crypto/Expo Random placeholder regressions, the native-provider harness proves AES-GCM operations are routed through quick-crypto, the native-required flag rejects WebCrypto fallback when `react-native-quick-crypto` is unavailable, mobile strict runtime config rejects staging/production builds without `EXPO_PUBLIC_REQUIRE_NATIVE_CRYPTO=true`, mobile strict runtime config rejects local/insecure endpoints, stale mobile async-derived key handles are disposed if derivation resolves after cleanup, mobile plaintext/passphrase/key state is cleared on sign-out, mobile plaintext state is cleared after encrypted save, web journal salts come from authenticated backend bootstrap, stale web `CryptoKey` state is cleared during passphrase/salt lifecycle changes, web disposable handles revoke stale raw key references, untracked web keys are rejected, and web plaintext state is cleared immediately after encryption. `mobile/src/lib/crypto.smoke.mts` now makes the mobile smoke gate itself cover AES-GCM round-trip, PBKDF2 work-factor drift, tamper rejection, wrong associated-data rejection, non-extractable keys, key-handle disposal, stale raw-key revocation, untracked-key rejection, and native-required fail-closed behavior; `web/src/lib/crypto.smoke.mts` covers web AES-GCM round-trip, tamper rejection, wrong associated-data rejection, PBKDF2 work-factor drift, non-extractable keys, stale raw-key revocation, and untracked-key rejection; and the verifier rejects removal of both smoke paths. Current 2026-06-28 strict-release validation also segment-guards `CLIENT-MOBILE-001` and `CLIENT-WEB-001` so EAS/native-device install proof, native AES-GCM/key-lifecycle/associated-data proof, HTTPS/WSS staging config proof, and deployed web journal associated-data proof must appear in their own probe segments rather than borrowing markers from one another. Evidence: against disposable `pgvector/pgvector:pg16` Postgres on 2026-06-25, `set DATABASE_URL=postgres://postgres:scriptureforge@localhost:55433/scriptureforge?sslmode=disable&& set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tests/integration -run TestTenantScopedJournalHandlersEnforceRLS -count=1 -timeout=90s -v` passed; `cmd /c "call tools\use-project-path.cmd >nul && node tools\verify-journal-crypto.mjs"`, `cmd /c "call tools\use-project-path.cmd >nul && cd mobile && npm run smoke"`, and `cmd /c "call tools\use-project-path.cmd >nul && cd mobile && npm run build:check"` passed on 2026-06-27; current focused evidence `cmd /c "call tools\use-project-path.cmd >nul && rtk node tools\verify-journal-crypto.mjs"` passed, `cmd /c "call tools\use-project-path.cmd >nul && cd mobile && rtk npm run build:check"` passed with typecheck, API/crypto smoke, and journal crypto verifier, and `cmd /c "call tools\use-project-path.cmd >nul && cd web && rtk npm run smoke"` plus `cmd /c "call tools\use-project-path.cmd >nul && cd web && rtk npm run typecheck"` passed with smoke tests including `crypto.smoke.mts`. Remaining production work is native-device/EAS validation for the native crypto binding with `EXPO_PUBLIC_REQUIRE_NATIVE_CRYPTO=true`, deployed web/mobile HTTPS/WSS staging endpoint smoke artifacts, and production evidence manifest recording.

2026-06-29 update: `runJournalCryptoSelfTest` now exports the richer native-device evidence vocabulary expected by `CLIENT-MOBILE-001`, including provider status, native-required mode, AES-GCM round-trip, tamper rejection, wrong associated-data rejection, non-extractable key proof, key disposal, disposed-handle rejection, and passphrase/salt/plaintext buffer lifecycle markers. `mobile/src/lib/crypto.smoke.mts` and `tools/verify-journal-crypto.mjs` now require those markers so the app-side native smoke export cannot regress to a minimal placeholder result, and the verifier emits `native_device_self_test_markers=true` so local gate reports reject stale self-test marker coverage. Native-device/EAS execution of that self-test remains pending external production evidence.

2026-06-29 update: `mobile/src/lib/crypto.smoke.mts` now also proves `runJournalCryptoSelfTest()` fails closed when `EXPO_PUBLIC_REQUIRE_NATIVE_CRYPTO=true` and only the WebCrypto fallback is available. `tools/verify-journal-crypto.mjs` emits `native_required_self_test_fail_closed=true` and rejects removal of that smoke assertion, tightening local evidence for native-required mobile builds while the real EAS/native-device crypto artifact remains pending under `CLIENT-MOBILE-001`.

2026-06-29 update: `tools/verify-journal-crypto.test.mjs` is now wired into the local `tooling-tests` gate and the GitHub Actions tooling-test step, and `tools/validate-ci-workflow.test.mjs` rejects workflows that drop it. This moves journal crypto proof-marker regression coverage from a focused local check into the standard local/CI evidence matrix; native-device/EAS proof remains pending under `CLIENT-MOBILE-001`.

2026-06-29 update: `CLIENT-MOBILE-001` native crypto evidence now must prove it came from the exported app-runtime `runJournalCryptoSelfTest()` path. `runJournalCryptoSelfTest()` includes a literal `runJournalCryptoSelfTest` marker, and `tools/mobileprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation require that marker on the `mobile-native-crypto-smoke` segment before accepting native AES-GCM/key-lifecycle proof. Focused evidence passed: `node tools\verify-journal-crypto.mjs`, `go test ./tools/mobileprobe -count=1 -timeout=90s` with repo-local `GOCACHE`, mobile `npm run smoke`, mobile `npm run build:check`, and the staging evidence regression tests for record/validate/final readiness. Real EAS/native-device execution remains pending under `CLIENT-MOBILE-001`.

2026-06-30 update: mobile journal crypto self-test and production evidence now require AES-GCM IV freshness proof. `runJournalCryptoSelfTest()` encrypts the same plaintext twice with the same key and associated data, fails if the IV or ciphertext repeats, and emits `unique_iv=true` plus `unique IV`; `mobile/src/lib/crypto.smoke.mts`, `tools/verify-journal-crypto.mjs`, `tools/mobileprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation require those markers before `CLIENT-MOBILE-001` can support final readiness. Native-device/EAS execution remains pending external proof.

2026-06-30 update: mobile journal crypto self-test now records the concrete backend salt metadata used for AES-GCM associated data. `runJournalCryptoSelfTest()` emits `associated_data_salt_id=<id>` and `associated_data_salt_version=<n>` after proving wrong associated data is rejected, and `tools/verify-journal-crypto.mjs` rejects drift that removes those implementation or smoke-test markers before emitting `associated_data_salt_binding=true`. This tightens local and future native-device evidence that journal ciphertext is authenticated to server-issued salt metadata; real EAS/native-device execution remains pending under `CLIENT-MOBILE-001`.

2026-06-30 update: web and mobile journal encryption now reject stale raw key references after a disposable key handle is disposed, and reject keys that were not derived by the journal crypto module. `tools/verify-journal-crypto.mjs` emits `revoked_key_rejected=true`, mobile/web crypto smoke tests prove stale raw-key revocation and untracked-key rejection, and local gate report validation inherits the new proof marker through the journal crypto verifier. Native-device/EAS execution remains pending under `CLIENT-MOBILE-001`.

2026-06-30 update: `CLIENT-MOBILE-001` native AES-GCM evidence must now preserve those concrete associated-data salt markers. `tools/mobileprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation require `associated_data_salt_id=<id>` and `associated_data_salt_version=<n>` on the `mobile-native-crypto-smoke` segment, preventing generic associated-data wording from satisfying mobile journal salt-binding proof. Real EAS/native-device execution remains pending under `CLIENT-MOBILE-001`.

2026-06-30 update: `CLIENT-MOBILE-001` native mobile crypto evidence now explicitly rejects `provider=webcrypto-fallback`, `provider status webcrypto-fallback`, `webcrypto-fallback`, `native_required=false`, and `native-required false` markers. `tools/record-staging-evidence.mjs`, strict-release validation, and the staging evidence contract now keep native-device/EAS evidence bound to native-required `react-native-quick-crypto` AES-GCM proof rather than local WebCrypto-compatible smoke behavior.

2026-06-30 update: `tools/mobileprobe` now emits structured `provider=react-native-quick-crypto` and `native_required=true` JSON fields on passing `mobile-native-crypto-smoke` probes, giving `CLIENT-MOBILE-001` native AES-GCM evidence a machine-readable native provider assertion in addition to marker text. Actual native-device/EAS execution remains pending external proof under `CLIENT-MOBILE-001`.

2026-06-30 update: `CLIENT-MOBILE-001` associated-data salt proof is now machine-checkable. `tools/mobileprobe` emits structured `associated_data_salt_id` and `associated_data_salt_version` fields for passing native crypto smoke probes, rejects empty or zero salt values, and the staging evidence recorder plus strict-release validation require concrete salt markers before mobile native AES-GCM evidence can be recorded. Real EAS/native-device execution remains pending external proof.

2026-06-30 update: `CLIENT-MOBILE-001` native crypto artifacts now must carry exact `provider=react-native-quick-crypto` and `native_required=true` markers in the `mobile-native-crypto-smoke` segment before `tools/mobileprobe`, the staging evidence recorder, or strict-release validation will accept them. This closes the path where broad provider-status prose could satisfy native AES-GCM evidence without the machine-readable provider/native-required assertion; real EAS/native-device execution remains pending external proof.

2026-07-01 update: `tools/mobileprobe` now rejects dev-client/debug-client, Expo Go, simulator/emulator, and remote-debug markers directly in the `mobile-native-crypto-smoke` artifact, not only in the EAS/native-device run segment. Regression coverage proves an otherwise marker-complete native crypto artifact that includes `remote debug` fails before `CLIENT-MOBILE-001` can pass; real EAS/native-device execution remains pending external proof.

- F-AUD-005: Locally proved for fail-closed AI runtime, verification, handler-level audit persistence, and tenant RLS audit visibility. AI startup no longer panics on missing API keys, model/endpoint/timeout settings are environment-driven, and the LLM adapter now fails closed for missing `OPENAI_API_KEY` even under `GO_ENV=testing` instead of returning a synthetic success. `internal/adapters/llm/client_test.go` proves missing-key failure, bounded HTTP timeout failure, citation-free rejection, hallucinated-citation rejection, and successful verified-citation responses using `httptest.Server` with no external network dependency. `internal/ports/ai_audit_integration_test.go` proves direct successful/failed AI audit logs, persisted `citation_trails`, and cross-tenant invisibility under tenant RLS, and now also drives `GenerateCurriculumHandler` through the real `/api/v1/ai/generate/study` handler with a fake vector DB plus local LLM server to prove tenant A generation persists `ai_request_logs` and `citation_trails` while tenant B cannot see those rows under RLS. Evidence: against disposable `pgvector/pgvector:pg16` Postgres on 2026-06-25, `set DATABASE_URL=postgres://postgres:scriptureforge@localhost:55433/scriptureforge?sslmode=disable&& set JWT_SECRET_KEY=test-secret-for-db-backed-auth-tests&& set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tests/integration ./internal/ports -count=1 -timeout=90s -v` passed; in the current shell without `DATABASE_URL`, `cmd /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& go test ./internal/ports -count=1 -timeout=90s -v"` compiles the new handler-level RLS test and skips DB-backed cases as expected. `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./internal/adapters/llm -count=1 -timeout=60s -v`, full Go test, and Go vet passed on 2026-06-25. Remaining production work is staging validation against real provider configuration, outage/degradation behavior, and telemetry.
- 2026-06-30 F-AUD-005 AI evidence binding hardening: `tools/aiprobe`, the staging evidence recorder, and strict-release validation now require the deployed `ai-generation-route` proof and `ai-audit-persistence` proof to carry the same concrete `request_id`, preventing a real generation artifact from being paired with an unrelated `ai_request_logs` row. This is local evidence-contract hardening; real staging AI provider configuration, degradation behavior, citation verification, and audit persistence proof remain pending under `EXT-AI-001`.
- F-AUD-006: Locally proved. `services/scripture-engine` now vendors `protoc` through `protoc-bin-vendored`, `build.rs` sets `PROTOC` before `tonic_build`, CI no longer needs a separate `setup-protoc` step, `Cargo.lock` pins the Rust dependency graph including vendored platform `protoc` packages, Rust unit tests instantiate generated embedding, vector-search, and gRPC client/server protobuf types, the gRPC server defaults to `0.0.0.0:50051` instead of loopback-only binding so it can be reached by a Kubernetes service, and the server exposes the standard `tonic-health` gRPC health checking service required by Kubernetes gRPC probes. `tools/verify-rust-protobuf.mjs` now also enforces the `proto/scripture.proto` tenant/RPC contract, health-service registration, SERVING state wiring for `ScriptureEngineServer`, Kubernetes-safe gRPC/metrics fallback bind addresses (`0.0.0.0:50051` and `0.0.0.0:9102`), generated vector-search and gRPC client/server type coverage, and bounded vector-search input validation so those requirements cannot drift behind the Cargo tests. The Rust dependency lane now uses `pgvector 0.4.2` with `sqlx-postgres 0.9.0`, removing the prior `sqlx-postgres v0.7.4` future-incompatibility warning, and the vector search query now passes a literal SQL string with bind parameters instead of dynamic SQL. Evidence: `cmd.exe /c "call tools\use-project-path.cmd >nul && cd services\scripture-engine && rtk cargo test --locked"` passed 12 tests on 2026-06-30; `cmd.exe /c "call tools\use-project-path.cmd >nul && rtk node --test --test-reporter=dot tools\verify-rust-protobuf.test.mjs"` passed 19 tests; `cmd.exe /c "call tools\use-project-path.cmd >nul && rtk node tools\verify-rust-protobuf.mjs"` passed with `vendored_protoc=true`, `ambient_protoc_not_required=true`, `generated_types_covered=true`, `generated_grpc_client_server_covered=true`, `proto_contract_covered=true`, `lockfile_platform_protoc_covered=true`, `health_service_covered=true`, `bounded_vector_search_inputs=true`, and `rust_runtime_observability_covered=true`. `cargo tree -i sqlx-postgres@0.9.0` showed a single SQLx 0.9 lane through both `pgvector` and `scripture-engine`. Remaining production work is deployed Rust gRPC health/metrics proof under `RUST-GRPC-001`.
- F-AUD-007: Locally proved. Web now runs on the patched Next/React line (`next@16.2.9`, `react@19.2.3`) with a PostCSS override for the advisory-free tree. Evidence: `npm audit --audit-level=moderate`, `npm run typecheck`, and `npm run build` in `web` passed on 2026-06-25.
- F-AUD-008: Locally proved with repo-local Go and repo activation helpers. CI targets Go `1.24.3`, local validation uses `.tools\go\bin\go.exe` with repo-local `GOCACHE`, and `tools\use-project-path.cmd` / `tools\use-project-path.ps1` activate the repo-local Go, Cargo/Rust, and Terraform toolchain for fresh shells. The activation helpers now also add common Windows install paths for Go tools, AWS CLI v2, and PostgreSQL clients when present, and support `--strict-staging` / `-StrictStaging` to run staging PATH verification immediately after activation. `tools/verify-project-path.mjs` now fail-closes missing required local tools (`rtk`, `git`, `go`, `node`, `npm`, `cargo`, `rustc`, `terraform`) and is part of the full local gate matrix. It also has `--strict-staging` / `--staging-evidence` mode for production evidence collection shells, where missing or version-check-broken `gopls`, `psql`, `kubectl`, `aws`, or `gh` fail the report while standalone `protoc` remains optional because `services/scripture-engine` vendors it through Cargo. Missing strict staging tools now print remediation text, and broken strict staging shims are reported as version-check failures, so an evidence-collection shell says whether PostgreSQL client tools or AWS CLI v2 need installation/PATH exposure or a broken PATH shim needs repair. Evidence: `cmd /c "call tools\use-project-path.cmd >nul && node tools\verify-project-path.mjs"` reports required tools `8/8`; `cmd /c "call tools\use-project-path.cmd --strict-staging"` exits 0 and resolves `gopls`, `psql`, `kubectl`, `aws`, and `gh`; `rtk powershell -NoProfile -ExecutionPolicy Bypass -File tools\install-project-path.ps1 -WhatIf` reports the Windows user PATH already includes all discovered project tool directories; `cmd /c "call tools\use-project-path.cmd >nul && node tools\verify-project-path.test.mjs"` covers both default local and strict staging modes; `cmd /c "call tools\use-project-path.cmd >nul && rtk node tools\run-local-gates.mjs --continue-on-failure --report artifacts\local-gate-report-current.json"` passed the earlier 31-gate local matrix in the dirty development worktree on 2026-06-29T12:33:40Z, including Docker-backed RLS DB proof; current focused proof adds the 32nd `staging-evidence-gap-report` gate and validates its generated subset report. Remaining production work is clean pushed CI evidence for the exact release SHA plus real staging evidence collection.
- 2026-06-28 F-AUD-008 PATH hardening update: `tools\use-project-path.cmd`, `tools\use-project-path.ps1`, and `tools\verify-project-path.mjs` now also search common WinGet link, Chocolatey bin, Scoop shim, `Program Files (x86)\Amazon\AWSCLIV2`, and `Program Files (x86)\PostgreSQL\*\bin` locations. Strict staging failures and `tools\report-staging-evidence-gaps.mjs` now print the fallback paths searched for missing required staging tools, distinguish broken shims whose version check fails, and the Windows activation helpers preserve `verify-project-path --strict-staging` failures while avoiding redundant `where` checks after the verifier succeeds. Current evidence: `cmd /c "call tools\use-project-path.cmd >nul && rtk node --test tools\verify-project-path.test.mjs"` covers strict staging PATH version-check failures; `cmd /c "call tools\use-project-path.cmd >nul && rtk node tools\verify-project-path.mjs"` passes local PATH readiness; `cmd /c "call tools\use-project-path.cmd --strict-staging"` now passes and resolves `psql (PostgreSQL) 18.4` plus `aws-cli/2.35.11`.
- F-AUD-009: Locally validated skeleton with explicit TLS ingress, observability inputs, workload secret-injection wiring, health/readiness probes, resource controls, topology spread constraints, pod disruption budgets, horizontal pod autoscalers, database backup controls, and rollout/rollback settings; staging deployment proof remains. `build/terraform` now separates providers, variables, IAM, network security groups, EKS cluster/node group, Aurora PostgreSQL cluster/instances, Redis replication group, ECR repositories, Kubernetes deployments/services/ingress, remote S3 backend configuration, example backend/tfvars files, Secrets Manager ARN inputs, service version inputs, OTLP collector endpoint inputs, IRSA workload identity, Secrets Store CSI `SecretProviderClass`, and outputs. It removes production-hostile snapshot defaults and hardcoded AWS role/subnet IDs. Aurora PostgreSQL now has encrypted storage, deletion protection, named final snapshot, automated backup retention, preferred backup/maintenance windows, PostgreSQL log export, and tag copying to snapshots. Public API and web ALB ingresses now require an ACM certificate ARN, configure an explicit AWS SSL policy, listen on HTTP/HTTPS, redirect HTTP to HTTPS, and health-check the API on `/ready` instead of `/metrics`. The API exposes `/live` and `/ready`, and Terraform maps those to Kubernetes liveness/readiness probes. The Rust engine deployment sets `RUST_ENGINE_BIND_ADDRESS=0.0.0.0:50051`, the Rust server exposes `tonic-health`, and Terraform includes gRPC liveness/readiness probes on port `50051`. The Go API still defaults `GRPC_ENGINE_ADDRESS` to `localhost:50051` for local development, but `cmd/platform-engine` now rejects missing `GRPC_ENGINE_ADDRESS` under `DEPLOYMENT_ENVIRONMENT=staging|production|prod`, and Terraform injects `scriptureforge-rust-engine:50051` into the API pod. API, Rust, and web deployments now declare variable-driven CPU/memory requests and limits, zone topology spread constraints, PodDisruptionBudgets with at least one pod available during voluntary disruption, HPA v2 CPU/memory autoscaling with at least two replicas, rollout history retention, and rolling updates with zero unavailable pods. The API Kubernetes deployment now receives `SERVICE_VERSION`, `DEPLOYMENT_ENVIRONMENT`, `OTEL_SERVICE_NAME`, `OTEL_EXPORTER_OTLP_ENDPOINT`, and `OTEL_EXPORTER_OTLP_INSECURE` from Terraform variables so deployed API traces can reach a collector once one is provided. API and Rust pods now reference synced runtime secret keys for `DATABASE_URL`, `JWT_SECRET_KEY`, `OPENAI_API_KEY`, `ZOOM_ACCOUNT_ID`, `ZOOM_CLIENT_ID`, `ZOOM_CLIENT_SECRET`, and `ZOOM_WEBHOOK_SECRET_TOKEN` without committing plaintext values or constructing a root-user database URL in manifests. `tools/validate-deployment-skeleton.mjs` now guards these deployment invariants in CI, including remote state shape, TLS ingress annotations, IRSA/Secrets Store CSI wiring, runtime secret references, API `/live` and `/ready` probes, Rust gRPC probes, OTLP env wiring, resource requests/limits, PDBs, topology spread constraints, HPAs, database backup/final-snapshot controls, rolling update settings, and the absence of inline/root database URL construction in workload manifests. Evidence: `.tools\terraform\terraform.exe -chdir=build\terraform fmt -check -recursive`, `.tools\terraform\terraform.exe -chdir=build\terraform validate`, and `node tools\validate-deployment-skeleton.mjs` passed on 2026-06-25; prior `.tools\terraform\terraform.exe init -backend=false` also passed. Remaining production work is supplying real staging inputs, validating ACM/DNS/OTLP collector ownership, proving Secrets Store CSI/IRSA in staging, proving the database URL secret uses a scoped app user, running a staging plan/apply, probing deployed TLS/readiness/trace export, tuning resource/autoscaling values from staging load data, and proving rollback/degradation plus backup/restore paths.
- 2026-06-29 F-AUD-009 CI Terraform parity update: `.github/workflows/security.yml` and `tools/validate-ci-workflow.mjs` now require `terraform fmt -check -recursive`, matching the local Terraform gate so nested Terraform files cannot bypass formatting coverage. This is local/CI gate hardening only; live staging plan/apply proof remains tracked under `DEPLOY-TF-001`.
- 2026-06-29 F-AUD-009 Terraform init wrapper hardening: `tools/run-terraform-init.mjs` is now import-safe and covered by `tools/run-terraform-init.test.mjs`, which proves the local init wrapper keeps the safe `-backend=false` default, preserves explicit init args, classifies Terraform registry/network outages as local blockers, and propagates real Terraform failures. The shared local tooling-test gate, GitHub Actions tooling-test command, and `tools/validate-ci-workflow.mjs` all require this wrapper coverage. This remains local/CI gate hardening only; live remote-state init, staging plan/apply, and deployment proof remain tracked under `DEPLOY-TF-001`.
- 2026-06-30 F-AUD-009 Terraform apply safety hardening: `tools/deploymentprobe`, the staging evidence recorder, and strict-release validation now require real Terraform apply evidence to preserve `Apply complete`, `Resources:`, and `0 destroyed` markers before `DEPLOY-TF-001` can pass from an apply artifact. The deployment-approval fallback remains available only with structured matching `change_ticket=<ticket-id>` proof. This is local evidence-contract hardening; live remote-state init, staging plan/apply or approved change ticket, and Kubernetes rollout proof remain pending under `DEPLOY-TF-001` and `DEPLOY-K8S-001`.
- 2026-07-01 F-AUD-009 Terraform apply count hardening: `tools/deploymentprobe` now parses Terraform apply resource counts from `Resources: <n> added, <n> changed, <n> destroyed`, emits structured `terraform_apply_added`, `terraform_apply_changed`, and `terraform_apply_destroyed` JSON fields, and appends matching summary markers. The staging evidence recorder and strict-release validator now require `terraform_apply_destroyed=0` plus added/changed count markers for apply-path evidence; deployment-approval fallback remains ticket-based. This is still evidence-contract hardening only; real staging apply or approved change-ticket evidence remains pending external proof.
- 2026-07-01 F-AUD-009 Terraform secret-input hardening: `tools/validate-deployment-skeleton.mjs` now emits `database_root_secret_no_default=true` and `workload_secret_inputs_are_arns=true`, and its regression tests reject Terraform changes that add a default database root passphrase, remove Secrets Manager ARN validation for workload secrets, or reintroduce root-password workload URL construction. `build/terraform/variables.tf` also clarifies that API/JWT/OpenAI/Zoom workload inputs must be AWS Secrets Manager ARNs. This is local skeleton/CI hardening only; real remote-state, Secrets Store CSI/IRSA, scoped database URL, and staging plan/apply evidence remain pending under `DEPLOY-TF-001`, `DEPLOY-K8S-001`, `SEC-SECRETS-001`, and `SEC-DBUSER-001`.
- 2026-07-01 F-AUD-009 Terraform immutable-image hardening: `build/terraform/variables.tf` now rejects API, web, and Rust workload image inputs unless they are pinned to `@sha256:<64 hex>` digests, `terraform.tfvars.example` documents digest-pinned ECR values, and `tools/validate-deployment-skeleton.mjs` plus its regression tests emit `immutable_workload_image_digests=true` only when the digest validations and workload image references remain wired. This improves release-bound deployment input safety; real remote-state init, staging plan/apply, Kubernetes rollout, and image-digest deployment evidence remain pending under `DEPLOY-TF-001` and `DEPLOY-K8S-001`.
- 2026-07-01 F-AUD-009 staging input guard hardening: Terraform now rejects weak production/staging input values before plan/apply. `service_version` no longer has an `unversioned` default and rejects `unversioned`, `latest`, or `replace-with` placeholders; `allowed_ws_origins` requires comma-separated HTTPS origins and rejects localhost, wildcard, example, test, and invalid hosts; `api_hostname` and `web_hostname` require real DNS-shaped hostnames and reject localhost/example/test/invalid hosts. `tools/validate-deployment-skeleton.mjs` emits `staging_input_placeholders_rejected=true` only while these guards remain wired. Real AWS account inputs, remote-state init, staging plan/apply, and rollout evidence remain pending external proof.
- F-AUD-010: Locally proved for adapter resilience and webhook safety. Zoom uses bounded HTTP calls, environment-configurable retry attempts, circuit-breaker state, offline meeting fallback, signature verification with a bounded timestamp freshness window, signed `endpoint.url_validation` challenge responses, duplicate-safe webhook state updates, and fail-closed meeting-to-room lookup. `internal/adapters/integration_zoom/zoom_client_test.go` proves credential/OAuth timeout fallback, provider meeting-create timeout fallback after OAuth succeeds, circuit-open fallback without further provider calls, missing-credential offline fallback, and transient Zoom 5xx retry recovery without opening the circuit. `internal/adapters/integration_zoom/zoom_webhook_test.go` proves invalid signature denial, stale signed delivery replay denial without state mutation, signed URL-validation response generation without state mutation, malformed URL-validation denial, duplicate webhook idempotency, bounded delivery cache eviction, distinct tracked delivery processing, meeting-to-room mapping, no Redis state mutation when a Zoom meeting cannot be mapped to an internal room, no fallback mutation using the external Zoom meeting ID when mapping fails, and ended-event inactive room updates. Evidence: `cmd /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& go test ./internal/adapters/integration_zoom -count=1 -timeout=60s -v"` passed on 2026-06-27; focused current evidence `.tools\go\bin\go.exe test .\internal\adapters\integration_zoom -count=1` passed on 2026-06-27. Full Go test and Go vet evidence remains in the local gate matrix. Remaining production work is staging validation against real Zoom credentials/webhook delivery, endpoint URL validation, and captured timeout/circuit/fallback artifacts.
- F-AUD-011: Locally proved for compact mobile product wiring and Node-backed crypto verification. Mobile now uses environment-driven API config, register/login/MFA/refresh/logout helpers, bearer-backed logout revocation, authenticated AES-GCM journal saves, explicit journal list/save/load helpers, room list/create/select screens, and encoded room socket URL construction. The Expo structural crypto placeholder has been replaced with native-preferred `react-native-quick-crypto`/WebCrypto AES-GCM plumbing, and the dependency line is coherent on Expo `56.0.12`, React Native `0.86.0`, React `19.2.7`, and `@types/react` `19.2.17`. Mobile journal key lifecycle now uses disposable key handles in `SecureJournalContainer`, wipes derivation byte buffers, clears replaced/unmounted key references, disposes async-derived handles that arrive after cleanup, clears passphrase/plaintext/key state on sign-out, clears plaintext after encrypted save, prefers the native quick-crypto provider before fallback WebCrypto, and rejects disposed-handle encryption through `tools/verify-journal-crypto.mjs`. `mobile/src/lib/api.smoke.mts` mocks API calls and proves canonical auth, organization scoping, MFA code payloads, refresh/logout, encrypted-only journal fields, room list/create, and socket URL setup. `security/dependency_risk_register.md` tracks the remaining moderate Expo tooling `uuid <11.1.1` advisory as DRR-001 because the forced audit fix proposes a breaking Expo downgrade. Evidence: `cmd /c "call tools\use-project-path.cmd >nul && node tools\verify-journal-crypto.mjs"`, `cmd /c "call tools\use-project-path.cmd >nul && cd mobile && npm run smoke"`, and `cmd /c "call tools\use-project-path.cmd >nul && cd mobile && npm run build:check"` passed on 2026-06-27; prior high-severity `npm audit` evidence remains from the full gate matrix. Remaining product-readiness work is EAS/native-device evidence against the native crypto binding and deployed mobile config smoke against staging endpoints.
- F-AUD-012: Locally proved for compact product wiring. Web now has minimal auth bootstrap, backend-backed logout revocation, room create/list/select, environment-driven WS config, journal persistence integration, reusable API client helpers for canonical auth/journal/room routes, mocked smoke coverage for login, register, refresh, logout, journal list/save/load, room list/create, encoded socket setup, and strict runtime config that rejects local or insecure API/WS endpoints when `NEXT_PUBLIC_DEPLOYMENT_ENVIRONMENT=staging|production|prod`. Web typecheck/build pass, and zero web `npm audit --audit-level=moderate` findings remain after the Next 16/PostCSS upgrade. 2026-06-28 update: `CLIENT-WEB-001` evidence now requires distinct login/register, encrypted journal, and room/WebSocket browser-smoke artifact URLs, and each browser-smoke segment must carry `distinct_web_artifacts=true` so one deployed artifact cannot satisfy multiple web proof roles. Evidence: `npm.cmd run smoke`, `npm.cmd run typecheck`, and `npm.cmd run build` in `web` passed on 2026-06-25; current smoke coverage also exercises strict staging HTTPS/WSS endpoint rejection. Remaining production work is deployed browser smoke against staging web/API/WS endpoints.
- 2026-06-30 F-AUD-012 web smoke identity update: `tools/stagingprobe`, the staging evidence recorder, and strict-release validation now require deployed auth, journal, and room browser-smoke artifacts to bind to the same concrete `user_id` and `organization_id`, with created-resource `journal_id=<id>` and `room_id=<id>` markers for the journal save/load and room/WebSocket flows. This is local evidence-tool hardening; real deployed browser smoke remains pending under `CLIENT-WEB-001`.
- 2026-06-30 F-AUD-012 web staging endpoint update: `tools/stagingprobe`, the staging evidence recorder, and strict-release validation now reject `CLIENT-WEB-001` browser-smoke evidence that contains hardcoded production API or WebSocket endpoints such as `https://api.scriptureforge.com` or `wss://api.scriptureforge.com`. This prevents deployed staging web smoke proof from passing while the browser is pointed at production traffic. Real deployed browser smoke remains pending under `CLIENT-WEB-001`.
- Cross-cutting abuse controls: Locally proved for backend HTTP/WebSocket entrypoints and mounted auth routes. `internal/domain/abuse` adds configurable fixed-window limits for auth, AI, journal, rooms, and WebSocket profiles; protected paths limit by verified tenant/user claims and public auth paths limit by client IP. Limiter state now prunes expired buckets and honors `ABUSE_LIMIT_MAX_BUCKETS`, sending identity-spray overflow into a shared per-profile bucket instead of growing active buckets without bound. `cmd/platform-engine/routes_test.go` proves the actual router wiring enforces auth limits and legacy aliases cannot bypass the canonical auth bucket. Evidence: `.tools\go\bin\go.exe test ./internal/domain/abuse -count=1 -v`, `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./cmd/platform-engine -count=1 -timeout=60s -v`, `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./... -count=1 -timeout=60s`, and `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe vet ./...` passed on 2026-06-25. Remaining production work is staging observation of auth, AI, journal, room, and socket limits under real ingress/client identity behavior.
- Cross-cutting observability: Locally proved for backend baseline telemetry, trace propagation, env-driven OpenTelemetry exporter bootstrap, tenant-aware structured API access logs, Rust gRPC correlated logs and Prometheus metrics, Terraform API/Rust env wiring, and deployable monitoring artifacts. `internal/domain/observability` adds trace ID propagation via `X-Trace-ID`, W3C `traceparent` extraction/response propagation, JSON access logs with trace/status/duration/service/component/version/environment fields, verified JWT-derived tenant/user/role log enrichment for protected routes, `GET`/`HEAD`-only `/metrics` with `405` denial for unsupported methods, request counters, duration sums, Prometheus route-latency histogram buckets/counts for P99 queries, high-cardinality path normalization, `/metrics` scrape exclusion from application access logs/request counters, request spans, W3C propagators, OTLP/HTTP trace exporter initialization from `OTEL_EXPORTER_OTLP_ENDPOINT`, resource attributes for service/version/environment, and graceful tracer-provider shutdown. `services/scripture-engine` now emits JSON startup/request/error logs, extracts W3C `traceparent` metadata from gRPC requests into `trace_id` log fields, exposes `GET`/`HEAD`-only Prometheus text metrics on `RUST_ENGINE_METRICS_ADDRESS` defaulting to `0.0.0.0:9102`, returns `405` for unsupported metrics methods, reads `OTEL_SERVICE_NAME`, `SERVICE_VERSION`, `DEPLOYMENT_ENVIRONMENT`, and `OTEL_EXPORTER_OTLP_ENDPOINT`, and includes unit tests for observability defaults, metrics binding/rendering, metrics method restrictions, traceparent parsing, and JSON escaping. `build/terraform` exposes service version and OTLP collector variables, maps them into both API and Rust Kubernetes deployments, and exposes the Rust metrics service port. `observability/grafana-scriptureforge-overview.json` adds a Grafana dashboard for request rate, 5xx rate, route status mix, route P99 latency, Rust gRPC activity/failures, and trace-correlated logs. `observability/prometheus-alerts.yaml` adds alert rules for high error rate, absent traffic, auth failures, abuse-limit spikes, P99 route latency, dependency failures, encrypted journal write failures, live room stream failures, and Rust engine failures. `observability/README.md` documents scrape/log shipping, `/metrics` method restrictions and scrape exclusion from app traffic metrics, OTLP env vars, structured log fields, retention targets, incident triage, Rust gRPC log/metric correlation, and staging proof requirements. `tools/validate-observability.mjs` validates required metric/dashboard/alert/runbook coverage, API structured log fields, `/metrics` scrape exclusion, auth claim log enrichment, Rust gRPC observability hooks, and Terraform Rust env wiring, and is wired into CI. Evidence: `cmd /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& go test ./internal/domain/observability ./internal/domain/auth -count=1 -timeout=60s -v"` passed on 2026-06-27; earlier `node tools/validate-observability.mjs`, Rust, Terraform, full Go test, and Go vet evidence remains in the local gate matrix. Remaining production observability work includes deployed OTLP collector/backend proof, Rust span export or collector-side gRPC correlation proof, dashboard import/screenshot proof, alert delivery proof, backend retention proof, and staging telemetry across Go API, Rust gRPC, Postgres, Redis, Zoom, and AI provider calls.
- Cross-cutting secret hygiene: Locally proved for high-confidence static secret hygiene, configuration guardrails, and Terraform workload secret-injection shape. `.gitignore` now excludes local `.env`, `.env.*`, Terraform state, local tool caches, and build outputs while still allowing example env files. `security/secret_handling_review.md` documents runtime secret sources, Terraform Secrets Manager ARN handling, IRSA/Secrets Store CSI wiring for database/JWT/OpenAI/Zoom runtime values, accepted test-only values, and remaining release-time proof. `tools/validate-secret-hygiene.mjs` scans tracked and untracked repo text files for high-confidence OpenAI, AWS, GitHub, Slack, and private-key material; validates required ignore rules; and validates Terraform example placeholder markers. The validator is wired into CI before SAST/TruffleHog. Evidence: `node tools/validate-secret-hygiene.mjs` passed on 2026-06-25 across 113 text files; `.tools\terraform\terraform.exe -chdir=build\terraform fmt -check -recursive` and `.tools\terraform\terraform.exe -chdir=build\terraform validate` also passed. Remaining production work is clean pushed CI TruffleHog output, cloud secret-manager access review, encrypted remote Terraform state proof, staging IRSA/CSI secret-sync proof, scoped database credential proof, and staging credential rotation evidence.
- Cross-cutting threat model and security artifacts: Locally proved for current repo-aligned security documentation. `security/threat_model.md` now maps trust boundaries, STRIDE threats, current mitigations, local evidence, and residual production blockers across auth, tenant RLS, journal crypto, WebSockets, AI, Zoom, observability, and deployment secrets. `security/crypto_iam_audit.md`, `security/sast_sca_report.md`, `security/domain_specific_audit.md`, and `security/fuzzing_report.md` were refreshed to remove stale claims about missing MFA, deleted Terraform files, legacy hardcoded database URLs, and nonexistent exception models. `security/dependency_risk_register.md` now records the exact locked Expo/uuid versions plus owner, accepted-by, review-due, and expiry metadata for DRR-001. `tools/validate-security-artifacts.mjs` requires the current threat/security coverage and rejects those stale claim patterns, while `tools/validate-security-artifacts.test.mjs` proves current artifacts pass, stale claims are rejected, required coverage markers cannot be removed, and required security files must be loaded. `tools/validate-dependency-risk.mjs` compares DRR-001 against `mobile/package-lock.json` and fails if the accepted-risk record becomes stale after `uuid >=11.1.1`, if the review date is after expiry, or if the acceptance has expired. Evidence: `node tools\validate-security-artifacts.mjs`, `node tools\validate-dependency-risk.mjs`, and `node --test tools\validate-dependency-risk.test.mjs` passed on 2026-06-25. Current hardening also adds `tools/validate-security-artifacts.test.mjs` to the local/CI tooling-test suite and `tools/validate-ci-workflow.test.mjs` rejects workflows that drop it. Remaining production work is owner/security review signoff plus clean pushed CI, staging IAM/secret evidence, staging abuse telemetry, and dependency-risk closure for DRR-001.
- Cross-cutting performance harness: Locally proved for harness correctness only. `tools/loadtest` now provides a standard-library HTTP load harness with JSON RPS/P50/P95/P99/failure/threshold reporting, configurable target, duration, concurrency, timeout, expected status, minimum RPS, maximum P99, release candidate, and service version. Reports now include `evidence_profile`, production target RPS/P99 metadata for non-local HTTPS/WSS staging targets, explicit `threshold_failures` such as weak configured thresholds, low RPS, high P99, request failures, and WebSocket sequence gaps, plus `result_summary` marker lists tying the staging profile, target scheme, configured thresholds, observed RPS/P99, release metadata, replica/telemetry artifacts, reconnect/polling artifacts, and WebSocket/Redis sequence fields together. It also provides a WebSocket room-stream self-test that runs the production room handler in-process, sends concurrent client events, waits for accepted broadcasts, and reports event throughput and latency through the same JSON report shape. The harness supports external `-websocket` runs against deployed room streams with bearer auth and allowed origin headers, enabling staging evidence for real WebSocket upgrade/auth/broadcast behavior. Only non-local HTTPS HTTP load reports emit `PERF-HTTP-001`; only non-local WSS WebSocket load reports emit `PERF-WS-001` and `DATA-REDIS-001`; local/self-test reports intentionally omit `evidence_items` so they cannot be recorded as staging proof. The harness and staging evidence recorder now reject weak configured thresholds for both HTTP and WebSocket evidence: HTTP must prove at least 5,000 RPS with P99 <= 200ms, while WebSocket/Redis evidence must prove at least 500 accepted events/sec with P99 <= 200ms plus contiguous Redis-backed sequencing. For HTTPS staging evidence, `tools/loadtest` now fetches the HTTP API replica distribution and dependency telemetry artifacts before emitting `PERF-HTTP-001`, rejects weak or mock artifact content, requires `release_candidate` and `service_version` markers inside those artifacts, and the recorder requires matching release metadata fields plus `http_replica_artifact_verified` and `dependency_telemetry_artifact_verified` markers so URL-only or stale-build side artifacts cannot satisfy HTTP performance proof. For WSS staging evidence, `tools/loadtest` fetches the API replica distribution, reconnect, HTTP polling fallback, and Redis telemetry artifacts before emitting evidence, rejects weak or mock artifact content, requires the same release metadata markers in each artifact, and the recorder requires release metadata plus `*_artifact_verified` markers so URL-only or wrong-release side artifacts cannot satisfy `PERF-WS-001` or `DATA-REDIS-001`. CI is wired to run both the HTTP `/health` self-test and the WebSocket room self-test. Evidence: `cmd /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& go test ./tools/loadtest -count=1 -timeout=60s -v"` and `cmd /c "call tools\use-project-path.cmd >nul && node --test tools\record-staging-evidence.test.mjs"` passed on 2026-06-27. This does not prove the production 5,000 req/s target because it uses in-process local endpoints, not staging ingress/API/dependencies.
- Cross-cutting staging evidence contract: Locally proved as a manifest/schema guard, strict release gate, final claim verifier, release-manifest bootstrap path, Obsidian snapshot guard, and evidence-gap reporter, not as staging proof. `production-readiness/staging-evidence.example.json` enumerates the required production-readiness artifacts for source-control/CI, Terraform plan/apply, TLS, Kubernetes workloads, IRSA/CSI secrets, scoped database credentials, RLS, Redis/WebSocket sequencing, Rust gRPC, observability, web/mobile client smokes, Zoom, AI, load tests, rollback, backup/restore, and owner/security signoff. `tools/bootstrap-staging-evidence.mjs` creates a git-ignored environment-specific manifest for the current `git rev-parse HEAD` release candidate and resets every item to `pending_external` until real artifacts are recorded. `tools/report-staging-evidence-gaps.mjs` validates a manifest and prints text or JSON counts plus the exact items and required evidence that still block strict release readiness; its CLI also runs strict staging PATH readiness and emits `STAGING-PATH-TOOLS` when evidence-collection tools such as `psql` or `aws` are missing, treats `SEC-SIGNOFF-001` accepted risk as non-blocking only when it references `security/dependency_risk_register.md#DRR-001` with non-overdue review/expiry metadata, and `tools/sync-obsidian-readiness.mjs` embeds the same strict PATH result into the generated Obsidian snapshot. `tools/verify-production-readiness.mjs` remains fail-fast for release claims, now loads the staging evidence contract, requires the same gap-summary engine used by Obsidian to report `strict_release_ready=true`, separately rejects any remaining `accepted_risk` item before a 100% production-readiness claim can pass, runs `git fetch --dry-run` before reading current Git status so ahead/behind counts depend on fresh upstream metadata, binds the local gate report to the current SHA, branch, and upstream, and prints a matching gap-report command on failure that preserves both the staging evidence manifest path and staging evidence contract path so operators can inspect the complete blocker list against the same contract. `tools/validate-staging-evidence.mjs` rejects missing evidence categories, invalid statuses, `passed` entries without artifact records, `passed` evidence observed after manifest generation, probe-backed strict-release evidence without HTTPS non-local JSON report artifacts, blocked/failed entries without owner/blocker details, generic passed `SEC-SIGNOFF-001` summaries that omit threat-model approval, DRR-001 dependency-risk decision, residual-risk review, owner/security approval, and release-risk signoff markers, and accepted-risk entries without decision, owner, accepted-by, review-due, and expiry details that remain unexpired at validation time. Its `--strict-release` mode rejects placeholder release candidates plus any pending, blocked, or failed item, enforces probe-family summary markers for Terraform remote-state/release linkage, Kubernetes rollout/workload safety, TLS/readiness/browser-smoke proof, tenant API/database RLS proof, secret handling/scoped DB-user proof, rollback/degradation/backup/restore proof, Rust gRPC health/API metrics proof, mobile native AES-GCM/EAS/staging-config proof, Zoom OAuth/webhook/fallback/idempotency/room-mapping proof, AI provider/degradation/citation/audit proof, observability OTEL metrics/trace/log proof, dashboard/alert/retention proof, abuse account-scoped login/config proof, HTTP load thresholds/release linkage/artifact verification, WebSocket load thresholds/authenticated origin/reconnect/polling proof, and Redis contiguous sequence telemetry, and allows `accepted_risk` only for `SEC-SIGNOFF-001` when it references `security/dependency_risk_register.md#DRR-001`, the review date is on or before the expiry date, and the expiry has not passed. Evidence: `node tools\validate-staging-evidence.mjs` passed on 2026-06-25 against the checked-in pending-external example, `node tools\bootstrap-staging-evidence.mjs --out production-readiness\staging-evidence.staging.json --environment staging --force` produced a local manifest for release candidate `7b68fe93650ed9606a0e54ba90d4c398423db07d`, `node tools\validate-staging-evidence.mjs --manifest production-readiness\staging-evidence.staging.json` passed, `node tools\report-staging-evidence-gaps.mjs --manifest production-readiness\staging-evidence.staging.json` reports the current strict-release blockers, `node --test tools\bootstrap-staging-evidence.test.mjs tools\report-staging-evidence-gaps.test.mjs tools\run-local-gates.test.mjs tools\validate-ci-workflow.test.mjs` passed, `node --test tools\validate-staging-evidence.test.mjs` passed for strict-mode behavior, including the guard that every probe-backed evidence family declares semantic summary-marker checks, `node --test tools\verify-production-readiness.test.mjs` passed for clean/synced/SHA-match after remote tracking refresh, strict PATH readiness, local-gate-report branch/upstream binding, Obsidian snapshot, stale contract/gap-report rejection, and accepted-risk final-claim rejection cases, and `node tools\validate-local-gate-report.mjs --report artifacts\local-gate-report.json` validated a full 23-gate non-dry-run local report on 2026-06-25. Remaining production work is filling the environment-specific evidence manifest with real staging artifacts, rerunning or preserving the full local gate report from the clean release SHA, pushing/syncing that exact SHA, closing accepted risks with passed signoff evidence, and passing the final claim verifier against those artifacts.

2026-07-01 update: pending `SEC-SIGNOFF-001` manifest rows now fail validation if their `required_evidence` text omits content-verified repo `security/*.md` signoff/approval wording or `signoff_artifact_verified=true`. `tools/validate-staging-evidence.test.mjs` covers this regression, so checked-in and environment-specific contracts cannot silently weaken owner/security approval back to summary-only evidence before real signoff is recorded.
2026-07-01 update: the local environment-specific staging manifest was refreshed against the checked-in evidence contract with `tools/sync-staging-evidence-contract.mjs --apply`, removing stale required-evidence drift for `DEPLOY-TF-001`, `DATA-RLS-001`, and `EXT-ZOOM-001`. `tools/sync-staging-evidence-contract.mjs --check` now emits `contract_drift_zero=true`, and the Obsidian readiness snapshot now shows only the release-candidate SHA mismatch plus pending external staging proof items. This is contract hygiene only; deployed artifacts remain pending.
2026-07-01 update: the local and CI staging-evidence contract gates now pass explicit checked-in example manifest inputs to `tools/sync-staging-evidence-contract.mjs`, and the Obsidian snapshot gate now validates the checked-in evidence contract snapshot rather than relying on ignored `production-readiness/staging-evidence.staging.json`. This keeps clean GitHub Actions checkouts from depending on local environment-specific evidence files while final production readiness still requires `STAGING_EVIDENCE_FILE` with real deployed artifacts.
2026-06-29 update: `tools/report-staging-evidence-gaps.mjs` now emits `STAGING-EVIDENCE-STRICT-VALIDATION` when an otherwise unblocked release-target manifest fails strict release evidence validation, so the gap report and Obsidian snapshot cannot claim strict readiness for weak `passed` evidence summaries.

- Cross-cutting staging evidence recorder: Locally proved as artifact workflow tooling, not as staging proof. `tools/record-staging-evidence.mjs` reads a passing probe JSON report, records its artifact path/URL and command into only the manifest IDs listed in `evidence_items`, marks those items `passed`, preserves the rest of the manifest, and handles Windows UTF-8 BOM JSON artifacts. It also supports explicit `--item-id` recording for human-reviewed artifacts such as Terraform plan/apply logs, Kubernetes rollout output, Secrets Store CSI/IRSA proof, scoped database user proof, observability proof, native mobile proof, rollback drills, backup/restore drills, and owner/security signoff; manual `SEC-SIGNOFF-001` summaries now must include threat-model approval, `security/dependency_risk_register.md#DRR-001`, dependency-risk decision, residual-risk review, owner/security approval, release-risk signoff, signoff artifact verification, and exact release-candidate markers. Repo-local `security/*.md` signoff artifacts must also contain those same markers before recording. For honest release tracking, it can also record `blocked` and `failed` statuses with required owner/blocker details and `accepted_risk` statuses with required decision, owner, accepted-by, review-due, and expiry details that are not already expired on the recording date. Evidence: `node --test tools\record-staging-evidence.test.mjs` passed, a probe-report temporary manifest update followed by `STAGING_EVIDENCE_FILE=<temp>\manifest.json node tools\validate-staging-evidence.mjs` passed, an explicit `--item-id DEPLOY-TF-001` temporary manifest update followed by the same validator passed, and temporary `blocked` plus `accepted_risk` recordings followed by the same validator passed on 2026-06-25. Remaining production work is using the recorder with real staging probe, release, blocker, and risk-decision artifacts.
- Cross-cutting strict evidence marker guard: Locally proved as validation hardening, not as staging proof. `tools/validate-staging-evidence.mjs` now rejects strict-release probe summaries or artifact fields marked private-network, private network, private IPv6, IPv4-mapped, link-local, link local, or unspecified, in addition to localhost, loopback, local-only, mock, placeholder, synthetic, stubbed, test-only, and dry-run markers. The checked-in and local staging evidence manifests now name those exclusions so generated Obsidian readiness snapshots do not understate the release-evidence host boundary. Remaining production work is still real non-local HTTPS staging artifacts for every pending evidence item.
- Cross-cutting CI release evidence probe tooling: Locally proved as probe-tool behavior, not as a pushed CI run. `tools/write-ci-release-evidence.mjs` writes the `ci-release-evidence` GitHub Actions artifact after required CI gates, TruffleHog, and a tracked-file cleanliness check complete, and the workflow validates the local artifact file with `tools/ciprobe` before upload. `tools/validate-ci-workflow.mjs` now also enforces the release-evidence step order so TruffleHog and tracked-file cleanliness must appear before evidence writing, and local `ciprobe` validation must appear before artifact upload; a workflow that only preserves marker strings in the wrong order fails validation. `tools/ciprobe` emits JSON evidence for `SRC-CI-001` by validating either a local downloaded artifact file or HTTPS non-local artifact URL for the exact 40-character release commit SHA, the `Security Pipeline Verification` workflow, the `security-audit` job, completed/successful status, GitHub repository/ref/event provenance, clean source-control marker, release evidence scope marker, TruffleHog, and every current local gate marker, including project PATH readiness, Go test/vet, evidence probes, web/mobile npm audit/smoke/typecheck/build checks, Rust protobuf plus cargo test, Terraform fmt/init/validate, observability validation, RLS schema validation, deployment skeleton validation, staging evidence validation, CI workflow validation, CI evidence gate drift validation, security/dependency-risk/secret/journal crypto validation, Serena/Obsidian validation, Obsidian readiness snapshot validation, and tooling tests. `tools/validate-ci-evidence-gates.mjs` now compares `tools/run-local-gates.mjs`, `tools/write-ci-release-evidence.mjs`, and `tools/ciprobe` so the release artifact marker list cannot drift from the local matrix. It rejects failed, cancelled, timed-out, dirty-worktree, stale-SHA, missing RLS schema gate, missing CI evidence gate marker, missing source-control-provenance markers, and local/insecure remote artifact URLs. Evidence: `cmd /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& go test ./tools/ciprobe -count=1 -v"`, `cmd /c "call tools\use-project-path.cmd >nul && node --test tools\write-ci-release-evidence.test.mjs"`, and `cmd /c "call tools\use-project-path.cmd >nul && node tools\validate-ci-evidence-gates.mjs"` passed on 2026-06-27; focused current hardening evidence `.tools\go\bin\go.exe test .\tools\ciprobe -count=1` passed on 2026-06-27. Current CI-order hardening evidence: `cmd /c "call tools\use-project-path.cmd >nul && node --test tools\validate-ci-workflow.test.mjs"` passed on 2026-06-27 and includes reordering regressions for evidence writing before clean source-control proof and upload before validation. Remaining production work is committing and pushing the release branch, downloading or exposing the real `ci-release-evidence` artifact for that exact SHA, running `tools/ciprobe` against it, and recording the passing report into the environment-specific evidence manifest.

2026-06-29 update: CI release evidence now requires untracked source cleanliness, not just tracked/cached diffs. `.github/workflows/security.yml` fails before writing the release artifact if `git status --short` emits any non-ignored untracked or modified file; `tools/write-ci-release-evidence.mjs` records `source_control_untracked_status: clean`, the concrete `git diff --quiet`, `git diff --cached --quiet`, and `git status --short` command markers, and `source_control_untracked_clean_verified=true`; `tools/ciprobe` requires those markers before emitting `SRC-CI-001`. Evidence: `node --test tools/write-ci-release-evidence.test.mjs tools/validate-ci-evidence-gates.test.mjs tools/validate-ci-workflow.test.mjs`, `node tools/validate-ci-evidence-gates.mjs`, `node tools/validate-ci-workflow.mjs`, and `go test ./tools/ciprobe -count=1 -timeout=90s` passed locally on 2026-06-29. This remains local/CI-tooling proof until a clean pushed GitHub Actions run uploads a real `ci-release-evidence` artifact for the exact release SHA.
- Cross-cutting CI tooling-test parity: Locally proved as workflow coverage hardening, not as a pushed CI run. `.github/workflows/security.yml` now runs the same tooling-test file list as the local `tooling-tests` gate, including npm audit wrapper tests, RLS DB runner tests, local gate report validation, CI workflow and evidence drift tests, deployment/RLS/dependency/staging/security validators, production-readiness verifier, staging evidence recorder/bootstrap/gap reporter, CI release evidence writer, Rust protobuf verifier, and project PATH verifier. It also runs the same checked-in staging evidence gap-report command used by the local `staging-evidence-gap-report` gate with explicit `--allow-blockers`, so CI proves the not-ready blocker list still renders before release evidence is written. `tools/validate-ci-workflow.mjs` now requires that full tooling-test command and the gap-report gate, emits distinct proof markers for both staging evidence contract sync and staging evidence gap-report coverage, and `tools/validate-ci-workflow.test.mjs` rejects a workflow that drops `tools/run-npm-audit.test.mjs`, drops `tools/run-rls-db-integration-docker.test.mjs`, drops `tools/validate-security-artifacts.test.mjs`, or weakens the gap-report command. Remaining production work is still a clean pushed GitHub Actions run for the exact release SHA.
- Cross-cutting deployment staging probe tooling: Locally proved as probe-tool behavior, not as deployed proof. `tools/deploymentprobe` emits JSON evidence for Terraform and Kubernetes deployment artifacts. `-probe-terraform` requires HTTPS non-local remote S3 backend init output with bucket/key, backend encryption, and DynamoDB lock-table markers, a staging Terraform plan containing EKS/node group/RDS/Redis/ECR, Kubernetes deployment/ingress/HPA/PDB/SecretProviderClass, and IAM role markers, and apply evidence with `0 destroyed`, `release_candidate`, and `service_version` linkage or deployment-approval evidence with `release_candidate` and `service_version` linkage before emitting `DEPLOY-TF-001`; approval fallback evidence must also include a structured `change_ticket=<ticket-id>` marker so a bare "approved" note cannot replace actual apply output. It rejects non-HTTPS URLs and localhost/loopback hosts before fetching, rejects local `-backend=false` evidence plus mock, placeholder, synthetic, stubbed, test-only, dry-run, localhost, or loopback artifact content, and emits verified marker summaries with a `staging artifact` provenance marker that the staging evidence recorder now requires. `-probe-kubernetes` requires HTTPS non-local rollout status and workload resource artifacts proving staging namespace, API, web, and Rust deployments plus service, TLS ingress, HPA min/max and target settings, PDB min availability, readiness/liveness probes, rolling-update `maxUnavailable=0`, SecretProviderClass wiring, immutable image digest, `release_candidate`, and `service_version` linkage before emitting `DEPLOY-K8S-001`; the recorder rejects markerless summaries and summaries without `staging artifact` provenance, namespace, image-digest, and release-linkage markers. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/deploymentprobe -count=1 -v` passed on 2026-06-25; focused current hardening evidence `.tools\go\bin\go.exe test .\tools\deploymentprobe -count=1` passed on 2026-06-27. Remaining production work is running the probe against real staging Terraform/Kubernetes artifacts and pairing it with `tools/stagingprobe` TLS/readiness output.

2026-06-28 update: `DEPLOY-TLS-001` is now exact-release-bound. `tools/stagingprobe` requires `-release-candidate`/`RELEASE_CANDIDATE` and `-service-version`/`SERVICE_VERSION` for TLS/readiness evidence, appends those markers to API live/readiness, API/web TLS, and HTTP-to-HTTPS redirect summaries, and the staging evidence recorder rejects reports whose release candidate differs from the manifest. Strict staging manifest validation now includes `DEPLOY-TLS-001` in the exact release-candidate guard list and requires release markers on each TLS/readiness/redirect segment. Remaining production work is real DNS/ACM/ALB/TLS evidence from staging.
- Cross-cutting staging probe tooling: Locally proved as probe-tool behavior, not as deployed proof. `tools/stagingprobe` emits JSON evidence for deployed API/web HTTPS readiness: API `/live`, API `/ready`, web root status, TLS version/certificate expiry, and HTTP-to-HTTPS redirect checks, with successful API/web/TLS/redirect summaries carrying verified marker lists that the staging evidence recorder now requires for `DEPLOY-TLS-001` and `CLIENT-WEB-001`. Strict-release validation now segment-guards those stagingprobe summaries so API health, API/web TLS, redirect, web root, browser auth, encrypted journal, and room/WebSocket proof markers must appear in their own segments instead of borrowing broad summary text from another check. It rejects non-HTTPS, reserved documentation/sample hosts, localhost, loopback, private-network, unspecified, and link-local API/web targets plus TLS/browser-smoke artifact URLs before issuing release-evidence probes, rejects reused auth/journal/room browser-smoke artifact URLs, and web evidence now requires exact `release_candidate=<manifest release_candidate>`, `service_version`, and per-segment `distinct_web_artifacts=true` markers in deployed browser auth, journal, and room/WebSocket smoke artifacts. Optional `-probe-zoom` coverage verifies invalid Zoom webhook signature denial and, with `-zoom-webhook-secret`, signed no-op webhook acceptance plus signed `endpoint.url_validation` challenge response. Optional `-probe-ai` coverage calls authenticated study generation with a supplied bearer token so provider readiness can be captured deliberately. The reports map to `DEPLOY-TLS-001`, `CLIENT-WEB-001`, `EXT-ZOOM-001`, and `EXT-AI-001` in the staging evidence manifest; Kubernetes evidence remains owned by `tools/deploymentprobe -probe-kubernetes`. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/stagingprobe -count=1 -v` passed on 2026-06-25; focused current hardening evidence `.tools\go\bin\go.exe test .\tools\stagingprobe -count=1` passed on 2026-06-27. Remaining production work is running the probes against real staging API/web hostnames with staging secrets/tokens and attaching their JSON output to an environment-specific evidence manifest.
- Cross-cutting Zoom staging probe tooling: Locally proved as probe-tool behavior, not as deployed proof. `tools/zoomprobe` emits JSON evidence for `EXT-ZOOM-001` by validating captured artifacts for Zoom OAuth readiness, meeting creation or offline fallback, provider-timeout/circuit-open fallback, webhook signature headers, stale signed delivery replay denial, delivery checks, endpoint URL validation, duplicate webhook idempotency with one state mutation, and Zoom meeting-to-internal-`live_rooms` mapping with unknown-meeting/no-external-ID-fallback safeguards, and successful probes emit verified marker summaries that the staging evidence recorder now requires. It rejects non-HTTPS artifact URLs and localhost/loopback artifact hosts before fetching, rejects artifacts that expose obvious Zoom secret values, and also rejects mock, placeholder, synthetic, stubbed, test-only, dry-run, localhost, or loopback artifact content; successful summaries include a `staging artifact` marker that the recorder requires. Current hardening also binds every Zoom OAuth, meeting/fallback, resilience, webhook, URL-validation, duplicate, and mapping artifact to explicit `release_candidate` and `service_version` markers; `tools/record-staging-evidence.mjs` rejects `EXT-ZOOM-001` reports whose `release_candidate` differs from the manifest, strict staging validation now rejects stale Zoom evidence that lacks `release_candidate=<manifest release_candidate>`, strict-release validation requires the `zoom-meeting-create-or-fallback` segment itself to prove either a Zoom `join_url` or `offline://in-person` fallback so timeout fallback markers cannot substitute for meeting evidence, and duplicate webhook idempotency evidence must include both `x-zm-trackingid` and `delivery_id=<id>` inside the `zoom-duplicate-webhook-idempotency` segment. `zoomprobe` also emits the parsed duplicate webhook `delivery_id` as a structured JSON field, and `tools/record-staging-evidence.mjs` rejects missing or mismatched structured `delivery_id` values so release evidence cannot rely on prose-only delivery identity. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/zoomprobe -count=1 -v` passed on 2026-06-25; focused current hardening evidence `.tools\go\bin\go.exe test .\tools\zoomprobe -count=1` passed on 2026-06-27. Remaining production work is capturing real staging Zoom OAuth, meeting/fallback, webhook delivery, URL-validation, duplicate, and mapping artifacts for the exact release SHA, and pairing the report with `tools/stagingprobe -probe-zoom`.
2026-06-28 update: local Zoom webhook handling no longer consumes a delivery ID before meeting-to-room mapping succeeds. `internal/adapters/integration_zoom/zoom_webhook_test.go` proves a signed webhook whose room lookup temporarily fails can retry with the same `x-zm-trackingid` and mutate the recovered internal room exactly once. Real staging Zoom delivery and retry evidence remains pending under `EXT-ZOOM-001`.
- Cross-cutting AI staging probe tooling: Locally proved as probe-tool behavior, not as deployed proof. `tools/aiprobe` emits JSON evidence for `EXT-AI-001` by validating captured staging artifacts for redacted provider/model/endpoint/timeout/retry configuration, authenticated `/api/v1/ai/generate/study` generation with JWT-derived `organization_id` and `user_id` proof, provider-timeout/retry-exhausted fail-closed degradation, no-citation and hallucinated-citation rejection plus verified-citation acceptance, and persisted `ai_request_logs` plus `citation_trails` with tenant RLS/cross-tenant-hidden proof, and successful probes emit verified marker summaries that the staging evidence recorder now requires. It rejects non-HTTPS artifact URLs and localhost/loopback artifact hosts before fetching, rejects obvious provider API key or bearer-token leaks, and also rejects mock, placeholder, synthetic, stubbed, test-only, dry-run, localhost, or loopback artifact content; successful summaries include a `staging artifact` marker that the recorder requires. Current hardening also binds every AI provider, generation, degradation, citation, and audit artifact to explicit `release_candidate` and `service_version` markers; `tools/record-staging-evidence.mjs` rejects `EXT-AI-001` reports whose `release_candidate` differs from the manifest, strict staging validation now rejects stale AI evidence that lacks `release_candidate=<manifest release_candidate>`, and strict-release validation requires the `ai-citation-verification` and `ai-audit-persistence` segments themselves to carry their citation rejection/acceptance and tenant-RLS audit markers so one AI artifact cannot satisfy another. `tools/aiprobe` now also emits a structured JSON `request_id` for `ai-audit-persistence`, and the staging evidence recorder rejects missing or mismatched audit request IDs so release evidence cannot rely on prose-only audit identity. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/aiprobe -count=1 -v` passed on 2026-06-25; focused current hardening evidence `.tools\go\bin\go.exe test .\tools\aiprobe -count=1` passed on 2026-06-27. Remaining production work is capturing real staging AI provider/degradation/citation/audit artifacts for the exact release SHA and pairing the report with `tools/stagingprobe -probe-ai`.
- Cross-cutting abuse staging probe tooling: Locally proved as probe-tool behavior, not as deployed proof. `tools/abuseprobe` emits JSON evidence for `ABUSE-LIMIT-001` by repeatedly exercising auth login, auth refresh, account-scoped login, AI, journal, room, and WebSocket route profiles through HTTPS staging ingress until each returns `429` with `Retry-After`, `X-RateLimit-Limit`, `X-RateLimit-Remaining`, and `X-RateLimit-Reset` headers, and successful probes emit verified marker summaries that the staging evidence recorder now requires. It fails first-attempt `429` responses because they do not prove rate limiting after repeated attempts, and successful profile summaries include `repeated_attempts_verified=true`. It requires a staging bearer token for protected routes, requires an HTTPS non-local allowed staging origin for the room-stream profile, requires HTTPS non-local API/config-artifact hosts, rejects reserved documentation/sample hosts before fetching abuse evidence, fetches and verifies the redacted config artifact for concrete `ABUSE_LIMIT_*` request/window assignments, concrete `ABUSE_LIMIT_AUTH_ACCOUNT_*` request/window assignments, `ABUSE_LIMIT_MAX_BUCKETS=`, `TRUST_PROXY_HEADERS=true`, `X-Forwarded-For`, `X-Real-IP`, `redacted`, and `staging artifact` markers, rejects mock/dry-run/local-only or obvious secret-leaking config artifacts, sends rotating forwarded client IP headers for the account-scoped login profile, sends a stable opaque refresh token body for refresh-token proof, and sends a real WebSocket upgrade request so socket evidence cannot be satisfied by a plain HTTP GET. The staging evidence recorder and strict-release manifest validator now reject `ABUSE-LIMIT-001` reports that omit `config_artifact_verified=true`, concrete config assignment marker summaries, `account-scoped login`, `rotating forwarded client IP`, `refresh token`, `websocket upgrade`, structured `attempts >= 2`, or repeated-attempt marker proofs, and the recorder rejects HTTPS loopback reports as local/self-test evidence. Evidence: `cmd /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& go test ./tools/abuseprobe -count=1 -timeout=60s -v"` and `node --test tools\record-staging-evidence.test.mjs` passed on 2026-06-27. Remaining production work is running the probe against real staging ingress with known `ABUSE_LIMIT_*`, `ABUSE_LIMIT_AUTH_ACCOUNT_*`, and trusted proxy/header settings and attaching the JSON report to an environment-specific evidence manifest.
- Cross-cutting tenant staging probe tooling: Locally proved as probe-tool behavior, not as deployed proof. `tools/tenantprobe` emits JSON evidence for deployed API tenant isolation through encrypted journal and room endpoints. With an owner bearer token and a second blocked bearer token, it creates an encrypted journal entry and a live room, proves tenant/user override write attempts are denied for journal and room create routes, proves the owner can directly read the created journal, proves the owner journal list contains the created encrypted entry, proves the owner can list/state-check the room, proves the blocked principal receives `404` for the direct journal read, proves the blocked principal's journal and active-room lists exclude the created resources, proves blocked room-state access is denied, and requires a redacted database/RLS artifact proving staging artifact provenance, non-superuser app role, `superuser=false`, `bypassrls=false`, `app.current_org_id`, `current_setting('app.current_org_id')`, `row_security=on`, forced row security across all nine tenant-scoped tables, same-tenant read visibility, cross-tenant read invisibility, cross-tenant write denial, exact `release_candidate`, and `service_version` markers. Tenant API probe summaries now carry verified same-tenant/cross-tenant markers plus exact release/service markers for journal writes, tenant-override write denial, direct journal reads, journal list visibility, journal list exclusion, room writes, room list visibility, and room-state denial, with structured `journal_id` and `room_id` fields that must match exact `journal_id=<id>` and `room_id=<id>` summary markers so every read/list/state proof binds to the created resource. The staging evidence recorder rejects `DATA-RLS-001` reports that only preserve HTTP status codes, omit owner journal-list/API write-denial proof, or omit/mismatch those structured resource ID markers. The probe rejects non-HTTPS API/artifact URLs and localhost/loopback API/artifact hosts before issuing requests; the DB/RLS artifact validator rejects obvious DSN/password/private-key leaks and artifacts marked mock, placeholder, synthetic, stubbed, test-only, dry-run, localhost, or loopback. The report maps to `DATA-RLS-001` in the staging evidence manifest, and the staging evidence recorder now rejects reports that omit the owner journal-list probe, omit tenant-override write-denial probes, omit the room probes, omit the `staging artifact` marker, omit tenant API proof summaries, omit release identity, omit concrete DB bypass/row-security markers, or omit the verified DB/RLS marker list from the database proof summary. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/tenantprobe -count=1 -v` passed on 2026-06-25; focused current hardening evidence `.tools\go\bin\go.exe test .\tools\tenantprobe -count=1` passed on 2026-06-27; current recorder hardening evidence `node --test tools\record-staging-evidence.test.mjs` passed 76/76 on 2026-06-27 including API-proof-summary rejection. Remaining production work is running the probe against real staging API hostnames with two valid staging principals and attaching its JSON output to an environment-specific evidence manifest.
- Cross-cutting Rust staging probe tooling: Locally proved as probe-tool behavior, not as deployed proof. `tools/rustprobe` emits JSON evidence for deployed Rust gRPC health, Rust metrics reachability, and Go API-to-Rust integration metrics. The report maps to `RUST-GRPC-001` in the staging evidence manifest, verifies the standard gRPC health service reports `SERVING`, requires the Rust metrics endpoint to expose concrete Prometheus samples for embedding/vector-search request and failure counters, requires deployed Go API `/metrics` to show positive successful `rust_engine` `vector_search` dependency count and duration samples after a staging API flow invokes Rust, rejects local/loopback gRPC and metrics targets before issuing release-evidence probes, and successful probes emit verified marker summaries that the staging evidence recorder now requires. Current hardening also binds the Rust gRPC health summary, Rust metrics proof, and Go API Rust-integration metrics proof to explicit `release_candidate` and `service_version` markers; `tools/record-staging-evidence.mjs` rejects `RUST-GRPC-001` reports whose `release_candidate` differs from the manifest, and strict staging validation now rejects stale Rust evidence that lacks `release_candidate=<manifest release_candidate>`. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/rustprobe -count=1 -v` passed on 2026-06-25; focused current hardening evidence `.tools\go\bin\go.exe test .\tools\rustprobe -count=1` passed on 2026-06-27. Remaining production work is running the probe for the exact release SHA from a staging network context that can reach `scriptureforge-rust-engine:50051`, its metrics endpoint, and deployed API `/metrics` after an API flow invokes the Rust engine, then attaching the JSON output to an environment-specific evidence manifest.

2026-06-28 update: strict-release validation now segment-guards `RUST-GRPC-001` rustprobe summaries. `rust-grpc-health`, `rust-metrics`, and `api-rust-integration-metrics` must each carry their own health, Rust metric, Go API integration, release candidate, and service version markers, so a broad Rust summary cannot borrow proof across health, metrics, and API integration segments. Current Rust hardening also requires `rust_metrics_samples_verified=true` and `api_rust_metrics_samples_verified=true`, so comment-only or name-only metric scrapes cannot satisfy `RUST-GRPC-001`. Deployed Rust gRPC evidence remains pending external staging proof.

2026-06-28 update: `tools/rustprobe` now rejects non-HTTPS Go API metrics URLs and canonical duplicate Rust/API metrics target aliases, including host-case and default-port variants, before it can emit `RUST-GRPC-001`. The staging evidence contract now calls for canonical-distinct Rust service metrics and HTTPS Go API metrics proof with `distinct_metrics_targets=true`; deployed Rust gRPC evidence remains pending external staging proof.

2026-06-28 update: Rust metrics target separation now canonicalizes query order and fragments in addition to scheme/host/default-port normalization, and `tools/record-staging-evidence.mjs` mirrors the same canonical distinctness check for manually recorded `RUST-GRPC-001` reports. Deployed Rust gRPC, Rust metrics, and Go API integration metrics proof remains pending external staging evidence.

2026-06-29 update: Rust vector-search requests now fail fast before the Postgres/pgvector query path unless `organization_id` is present, `query_vector` has exactly 1536 finite dimensions, `top_k_results` is between 1 and 100, and `minimum_similarity_threshold` is finite and within 0..1. `cargo test --locked --manifest-path services\scripture-engine\Cargo.toml` now passes 11 tests including `vector_search_request_rejects_unbounded_or_invalid_inputs`, and `tools/verify-rust-protobuf.mjs` plus local gate report validation require that bounded-input proof marker. Local gate report validation also now rejects stale `ci-evidence-gate-validation` summaries unless they include both required-entry and proof-marker counts. Deployed Rust gRPC evidence remains pending external staging proof.

2026-06-29 update: `tools/rustprobe` now rejects reserved documentation/sample hosts such as `.example`, `example.com`, `.test`, and `.invalid` for gRPC, Rust metrics, and API metrics targets before it emits `RUST-GRPC-001`. This aligns the source probe with the staging evidence recorder and strict manifest validation, so sample-domain Rust evidence fails closed before report generation. Deployed Rust gRPC, Rust metrics, and Go API integration metrics proof remains pending external staging evidence.

2026-06-29 update: `tools/rustprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation now require `staging artifact` provenance on every `RUST-GRPC-001` health, Rust metrics, and Go API Rust-integration metrics proof segment. Current local evidence: `cmd.exe /c "call tools\use-project-path.cmd >nul && rtk go test ./tools/rustprobe -count=1 -timeout=60s"` passed 28 tests, and `cmd.exe /c "call tools\use-project-path.cmd >nul && rtk node --test tools\record-staging-evidence.test.mjs tools\validate-staging-evidence.test.mjs"` passed 392 tests. Deployed Rust gRPC evidence remains pending external staging proof.

2026-06-29 update: `tools/verify-rust-protobuf.mjs` now emits `rust_runtime_observability_covered=true` and fails if the Rust runtime loses Prometheus embedding/vector-search request/failure counters, W3C `traceparent` extraction tests, observability default tests, or `SERVICE_VERSION`/`DEPLOYMENT_ENVIRONMENT`/`OTEL_EXPORTER_OTLP_ENDPOINT` configuration reads. This keeps the local Rust protobuf/build gate aligned with the deployed observability evidence contract while staging `RUST-GRPC-001` proof remains pending external evidence.

2026-06-30 update: `tools/verify-rust-protobuf.mjs` now requires the full vendored `protoc` platform package set currently pinned in `services/scripture-engine/Cargo.lock`: Linux aarch64, ppcle64, s390x, x86_32, x86_64, macOS aarch64, macOS x86_64, and win32. The verifier regression suite rejects missing non-host packages, so `lockfile_platform_protoc_covered=true` cannot be satisfied by only the current developer or CI host platform. Current focused evidence: `cmd.exe /c "call tools\use-project-path.cmd >nul && rtk node --test --test-reporter=dot tools\verify-rust-protobuf.test.mjs"`, `cmd.exe /c "call tools\use-project-path.cmd >nul && rtk node tools\verify-rust-protobuf.mjs"`, and `cmd.exe /c "call tools\use-project-path.cmd >nul && cd services\scripture-engine && rtk cargo test --locked"` passed. Deployed Rust gRPC health/metrics evidence remains pending under `RUST-GRPC-001`.

2026-06-30 update: local and CI Rust Cargo evidence now runs through `tools/run-rust-cargo-gate.mjs` instead of accepting raw marker-free `cargo test` output. The wrapper runs `cargo test --locked --manifest-path services/scripture-engine/Cargo.toml`, requires concrete `test tests::<name> ... ok` lines for generated protobuf request/response types, generated vector-search results, generated gRPC client/server types, bounded vector-search inputs, gRPC health-service naming, Kubernetes-safe gRPC/metrics bind defaults, and Prometheus metric rendering, then emits `rust-cargo-test validated:` proof markers. `tools/run-local-gates.mjs`, `.github/workflows/security.yml`, `tools/validate-local-gate-report.mjs`, and `tools/validate-ci-workflow.mjs` now require the wrapper and its regression test. Current focused evidence: `tools/verify-rust-protobuf.mjs` passed, `tools/run-rust-cargo-gate.mjs --bin .\.tools\cargo\bin\cargo.exe` passed with 13 Rust tests and proof markers, and the affected local-gate/CI/readiness tests passed. Deployed Rust gRPC health/metrics evidence remains pending under `RUST-GRPC-001`.

2026-07-01 update: the Rust Cargo proof wrapper now requires the full current 13-test Rust suite, not just the earlier protobuf/vector/health/bind/metrics subset. `tools/run-rust-cargo-gate.mjs` now fails if observability defaults, W3C traceparent extraction/rejection, metrics HTTP method behavior, or JSON escaping tests disappear, and emits dedicated proof markers for those runtime-safety checks. Current focused evidence: `tools/run-rust-cargo-gate.mjs --bin .\.tools\cargo\bin\cargo.exe`, `tools/verify-rust-protobuf.mjs`, and the affected Node proof tests passed. Deployed Rust gRPC health/metrics evidence remains pending under `RUST-GRPC-001`.

2026-07-01 update: the Rust Cargo proof wrapper now parses Cargo test summaries and rejects failed, ignored, measured, or filtered tests before emitting `rust-cargo-test validated:`. It also requires a unit-test summary with at least the full 13 required Rust tests, so a partial `cargo test --locked` run or a suite with skipped Rust checks cannot satisfy local/CI Rust proof. Deployed Rust gRPC health/metrics evidence remains pending under `RUST-GRPC-001`.

2026-07-01 update: the Rust Cargo proof wrapper now poisons ambient `PROTOC` with `__scriptureforge_invalid_ambient_protoc__` before running `cargo test --locked`, and `services/scripture-engine/build.rs` declares `cargo:rerun-if-env-changed=PROTOC` before overriding `PROTOC` with `protoc_bin_vendored::protoc_bin_path()`. `tools/verify-rust-protobuf.mjs` emits `ambient_protoc_rerun_bound=true`, and the cargo gate emits `rust_cargo_ambient_protoc_poisoned=true` only after the 13-test Rust suite passes. This strengthens local/CI proof that protobuf generation does not depend on a machine-level `protoc`; deployed Rust gRPC health/metrics evidence remains pending under `RUST-GRPC-001`.

2026-07-01 update: `RUST-GRPC-001` final-release validation now requires the recorded staging manifest to retain `structured_report.rust_grpc_runtime_proof` with deployment environment, gRPC/Rust metrics/API metrics targets, run identity, health/service identity, positive Rust metric counts, and positive Go API Rust-integration metric counts bound back to the `rustprobe` summary markers. This prevents marker-only or hand-edited Rust runtime evidence from satisfying deployed gRPC readiness after raw probe JSON is no longer attached locally. Real deployed Rust gRPC health/metrics/API integration proof remains pending external staging evidence.

2026-07-01 update: strict-release validation now revalidates retained `RUST-GRPC-001` structured targets. `grpc_target` must remain a non-local host:port service target, `metrics_target` must remain an HTTP(S) non-local artifact URL, `api_metrics_target` must remain an HTTPS non-local artifact URL, and the Rust metrics/API metrics targets must remain distinct after canonicalization. This prevents a final manifest from preserving Rust health/metrics summaries while drifting structured targets to local, malformed, non-HTTPS API, or duplicate metrics endpoints. Real deployed Rust gRPC health/metrics/API integration proof remains pending external staging evidence.

2026-06-30 update: `tools/rustprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation now bind `RUST-GRPC-001` to `deployment_environment` in addition to the exact release candidate and Rust service version. Successful Rust gRPC health, Rust metrics, and Go API Rust-integration metrics proof segments must each carry `deployment_environment=<environment>` markers, and the recorder rejects reports that omit the top-level `deployment_environment` field. This prevents exact-release Rust evidence from a wrong environment from satisfying final readiness; deployed Rust gRPC health/metrics evidence remains pending under `RUST-GRPC-001`.

2026-06-30 update: `RUST-GRPC-001` Rust metrics proof now rejects idle all-zero Rust request counters. `tools/rustprobe` requires positive Prometheus samples for `scriptureforge_rust_engine_embedding_requests_total` and `scriptureforge_rust_engine_vector_search_requests_total` before emitting `rust_embedding_requests_positive=true` and `rust_vector_search_requests_positive=true`; the staging evidence recorder and strict-release validation require those markers on the `rust-metrics` segment. This keeps deployed Rust evidence tied to an exercised service instead of a merely scrapeable metrics page. Deployed Rust gRPC health/metrics evidence remains pending under `RUST-GRPC-001`.

2026-06-30 update: `RUST-GRPC-001` Rust metrics proof now preserves positive sample counts as structured JSON fields. `tools/rustprobe` emits `embedding_requests`, `vector_search_requests`, `api_rust_vector_search_ops`, and `api_rust_vector_search_seconds`, and the staging evidence recorder rejects reports where those fields are missing or non-positive. This prevents marker-only Rust metrics summaries from satisfying deployed gRPC/API integration evidence. Deployed Rust gRPC health/metrics evidence remains pending under `RUST-GRPC-001`.

2026-06-30 update: deployed Rust gRPC evidence is now bound to one concrete staging run. `tools/rustprobe` requires `-load-run-id` or `STAGING_LOAD_RUN_ID`, persists `load_run_id`, and emits matching `load_run_id=<same run>` markers on Rust health, Rust metrics, and Go API Rust-integration metrics proof. The staging evidence recorder and strict-release validation reject missing or mixed Rust run IDs, preventing health, metrics, and API integration proof from different staged runs from satisfying `RUST-GRPC-001`. Real deployed Rust gRPC health/metrics evidence remains pending external proof.
- Cross-cutting mobile staging probe tooling: Locally proved as probe-tool behavior, not as native-device proof. `tools/mobileprobe` emits JSON evidence for `CLIENT-MOBILE-001` only when supplied EAS/native-device run output with `staging artifact` provenance, Android/iOS completion, native-device validation, installed-app proof, `release channel staging`, and `expo profile staging`; native `react-native-quick-crypto` AES-GCM smoke output with `staging artifact` provenance, explicit native-provider/native-module loading, provider-status, native-required, provider-bound key, fallback-derived key rejection, round-trip, tamper-rejection, non-extractable key, key-disposal, disposed-handle rejection, passphrase wipe, salt wipe, and plaintext-cleared markers; plus staging API/WS config proof with `staging artifact` provenance that sets `EXPO_PUBLIC_REQUIRE_NATIVE_CRYPTO=true` and `EXPO_PUBLIC_DEPLOYMENT_ENVIRONMENT=staging` while avoiding localhost plus old hardcoded `https://api.scriptureforge.com` and `wss://api.scriptureforge.com` endpoint values. Successful probes emit verified marker summaries that the staging evidence recorder now requires. It rejects non-HTTPS artifact URLs and localhost/loopback artifact hosts before fetching, rejects dry-run, Expo Go, dev-client/debug-client, simulator/emulator/remote-debug EAS output, Node/WebCrypto fallback, Expo crypto, placeholder, mock artifacts, missing staging provenance, and configs that disable native-required crypto or point staging mobile config at production API/WS endpoints so local-compatible crypto checks cannot satisfy native mobile readiness. Local crypto hardening now binds derived journal keys to the selected crypto provider, exposes provider status for smoke evidence, and rejects fallback-derived keys when `EXPO_PUBLIC_REQUIRE_NATIVE_CRYPTO=true`, so provider selection cannot silently switch from native to WebCrypto between derive/encrypt/decrypt operations. Evidence: `cmd /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& go test ./tools/mobileprobe -count=1 -v"` passed on 2026-06-27; focused current hardening evidence `.tools\go\bin\go.exe test .\tools\mobileprobe -count=1` passed on 2026-06-27; current provider-binding evidence `cmd /c "call tools\use-project-path.cmd >nul && cd mobile && rtk npm run build:check"` and `cmd /c "call tools\use-project-path.cmd >nul && rtk node tools\verify-journal-crypto.mjs"` passed on 2026-06-28. Remaining production work is running the mobile native/EAS validation and attaching the probe JSON report to an environment-specific evidence manifest.

2026-06-30 update: mobile staging config evidence now rejects hardcoded production API endpoints as well as the old production WebSocket endpoint. `tools/mobileprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation reject `https://api.scriptureforge.com` in `mobile-staging-config` proof so `CLIENT-MOBILE-001` cannot pass with a staging app pointed at production API traffic. Native-device/EAS validation remains pending under `CLIENT-MOBILE-001`.
- Cross-cutting observability staging probe tooling: Locally proved as probe-tool behavior, not as deployed proof. `tools/observabilityprobe` emits JSON evidence for deployed telemetry. `-probe-otel` requires collector config proof, API HTTP metrics plus API-side successful `rust_engine` `vector_search` dependency count/duration markers, architecture-named `websocket_active_connections_count` and `ai_inference_duration_seconds_*` profile metrics, Rust embedding/vector-search request and failure counters, trace backend search, log backend search with the shared trace ID plus route/method, service/version/environment, and tenant principal markers, and exact `release_candidate`/`service_version` markers before emitting `OBS-OTEL-001`; it rejects reserved documentation/sample hosts, local/loopback/private/link-local telemetry targets, broad trace/log query URLs that do not carry the supplied trace ID, artifacts whose release identity does not match the manifest, and artifacts marked mock, placeholder, synthetic, stubbed, test-only, dry-run, or local-only. It persists that `trace_id`, `observed_route`, `http_method`, and release identity in the JSON report, and the staging evidence recorder now rejects `OBS-OTEL-001` reports whose trace/log backend targets do not carry the persisted report trace ID, whose trace/log summaries do not include exact `route=<observed_route>` and `method=<http_method>` markers, whose API metrics proof omits Rust dependency markers or WebSocket/AI profile markers, whose Rust metrics proof omits the concrete embedding/vector-search counters, or whose release candidate differs from the manifest. `-probe-alerts` requires non-local dashboard, alert-rules, alertmanager/status, retention proof targets, architecture metric profile dashboard proof, the `ScriptureForgeAIInferenceLatencyElevated` alert, and exact `release_candidate`/`service_version` markers before emitting `OBS-ALERT-001`; it rejects reserved documentation/sample hosts, mock/dry-run alert artifacts, and stale-release evidence. Successful probes emit verified marker summaries that the staging evidence recorder and strict staging manifest validator now require across collector, metrics, trace/log, dashboard, alert, and retention probes. Evidence remains local/probe-only until rerun after this hardening. Remaining production work is running the probe against real staging observability backends after dashboard import, alert wiring, log shipping, trace export, retention policy configuration, and exact-release deployment.

2026-06-29 update: `tools/observabilityprobe`, `tools/record-staging-evidence.mjs`, and strict staging manifest validation now bind `OBS-OTEL-001` trace/log proof to a concrete deployed route and HTTP method. Probe reports must persist `observed_route` and `http_method`, and both `trace-backend-search` and `log-backend-trace-correlation` summaries must include exact `route=<observed_route>` and `method=<http_method>` markers before evidence can be accepted. This is local evidence-tool hardening only; real route-specific trace/log proof still requires staging observability backends.
- Cross-cutting security staging probe tooling: Locally proved as probe-tool behavior, not as deployed proof. `tools/securityprobe` emits JSON evidence for staged secret handling and scoped database credentials. `-probe-secrets` requires HTTPS non-local IRSA service-account plus trust-policy proof, Secrets Store CSI `SecretProviderClass` proof with AWS provider, Secrets Manager `objects`, `objectName`, `objectType=secretsmanager`, `objectAlias`, `jmesPath`, `secretObjects`, and `type=Opaque` markers, redacted synced-secret metadata for runtime keys with `stringData absent` and Secrets Store CSI management markers, scoped IAM policy with no wildcard resources, and scoped allow/deny access-test artifacts. It rejects non-HTTPS URLs, reserved documentation/sample hosts, and localhost/loopback/private/link-local hosts before fetching, rejects obvious plaintext DSNs/API keys/private keys in metadata artifacts, rejects wildcard IAM policy resource proof, and emits the verified marker list for each secret artifact probe before `SEC-SECRETS-001` can be recorded. The staging evidence recorder now also requires IRSA trust-policy markers, AWS SecretProviderClass object-sync markers, synced-secret redaction markers, and scoped/no-wildcard IAM markers before accepting `SEC-SECRETS-001`. `-probe-db-user` connects with the staged app `DATABASE_URL` without echoing it, rejects root/admin/reserved principals, verifies the connected role is not superuser, cannot bypass RLS, lacks `CREATEROLE` and `CREATEDB`, denies a privileged `CREATE ROLE` probe, and proves `SELECT,INSERT,UPDATE,DELETE` application grants across nine runtime tables before emitting `SEC-DBUSER-001`. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/securityprobe -count=1 -v` passed on 2026-06-25; focused current hardening evidence `cmd /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& go test ./tools/securityprobe -count=1 -timeout=60s -v"` passed on 2026-06-27; `cmd /c "call tools\use-project-path.cmd >nul && node --test tools\record-staging-evidence.test.mjs"` passed 77/77 with scoped IAM marker rejection on 2026-06-27. Remaining production work is running the probe against real staging artifacts and credentials, plus clean pushed TruffleHog/SAST/SCA output and owner cloud-access review.
- Cross-cutting resilience staging probe tooling: Locally proved as probe-tool behavior, not as deployed proof. `tools/resilienceprobe` emits JSON evidence for rollback/degradation and backup/restore drills. `-probe-rollback` requires before/after API readiness evidence with explicit pre/post rollback version linkage, rollout undo/status artifact evidence naming previous and target revisions for `scriptureforge-api`, and AI/Zoom degradation fallback drill evidence proving `AI_ORCHESTRATION_ENGINE_FAULT`, `offline://in-person`, `zoom circuit open`, and `non-AI routes healthy` with structured `ai_fault=true`, `zoom_offline_fallback=true`, `non_ai_routes_healthy=true`, and `zoom_circuit_open=true` JSON/summary fields before emitting `DR-ROLLBACK-001`; passing probe summaries include `staging artifact` provenance, the probe rejects non-HTTPS URLs, reserved documentation/sample hosts, localhost/loopback/private/link-local hosts before fetching, and artifact content marked mock, placeholder, synthetic, stubbed, test-only, dry-run, or local-only is rejected. `-probe-backup` requires RDS snapshot/backup creation evidence with structured JSON `snapshot_id` matching `snapshot_id=<id>`, encryption with structured kms_key_id matching kms_key_id=<id>, retention, automated-backup, source-cluster, and structured positive `rpo_minutes` matching `rpo_minutes=<minutes>`; restore drill evidence must include structured JSON `restore_job_id`, `source_snapshot_id`, `rto_minutes`, and `restore_duration_minutes` fields matching the summary markers, with `source_snapshot_id` matching the backup `snapshot_id`, plus checksum and isolated-restore proof; and an application smoke/readiness proof against the restored database must cover auth, tenant/RLS, migration version, journal behavior, and no plaintext journal persistence before emitting `DR-BACKUP-001`. The recorder now rejects markerless backup/restore smoke summaries, version-linkage-free rollback summaries, backup/restore summaries without recovery objective markers, missing or mismatched structured rollback/degradation/backup/restore IDs and recovery objectives, rollback after-readiness versions that do not point back to the pre-rollback version and forward to the post-rollback version, restore source snapshots that do not match the backup snapshot, restored-smoke summaries without RLS/no-plaintext markers, and resilience reports without the `staging artifact` marker. Evidence: `set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& .tools\go\bin\go.exe test ./tools/resilienceprobe -count=1 -v` passed on 2026-06-25, `.tools\go\bin\go.exe test .\tools\resilienceprobe -count=1` passed on 2026-06-27, and focused rollback-linkage tests passed on 2026-07-01. Remaining production work is actually running rollback, degradation, backup, and restore drills in staging and attaching the probe JSON reports to an environment-specific evidence manifest.
- Cross-cutting local gate matrix: Locally proved for project PATH/toolchain readiness, Go, Rust, web audit/smoke/typecheck/build, mobile high-severity audit/smoke/build-compatible checks, Terraform/TLS skeleton validation, Docker-backed DB/RLS integration, RLS schema validation, deployment skeleton invariant validation, staging evidence manifest validation, staging evidence gap reporting, staging evidence contract synchronization, observability artifacts, CI evidence gate drift validation, secret hygiene, security artifact validation, dependency-risk validation, CI workflow gate coverage, repeatable local gate reporting, and the local load harness on 2026-06-29. The staging web smoke proof contract now requires `tools/stagingprobe` to fetch distinct login/register, encrypted journal save/load, and room create/select/WebSocket artifact URLs and emit verified marker summaries, including `distinct_web_artifacts=true`, before `CLIENT-WEB-001` can be recorded. `tools/stagingprobe -probe-ai/-probe-zoom` remains useful for live staging smoke but no longer emits `EXT-AI-001` or `EXT-ZOOM-001`; those production evidence items must come from `tools/aiprobe` and `tools/zoomprobe` with the full artifact marker sets. The current matrix has 32 gates after adding `rls-db-integration`, `staging-evidence-contract-check`, and `staging-evidence-gap-report`; the RLS gate runs `tools/run-rls-db-integration-docker.mjs` with `REQUIRE_DATABASE_URL=true`, applies migrations into a disposable `pgvector/pgvector:pg16` container, and runs the tenant table, handler, room/socket, and AI audit RLS tests so missing DB-backed proof fails instead of skipping. `node tools\run-local-gates.mjs --continue-on-failure --report artifacts\local-gate-report-current.json` reported the earlier 31/31 gates passing on 2026-06-29T12:33:40Z in the dirty but upstream-synced `codex/production-readiness-remediation` worktree after correcting the auth observability test fixture to use a valid W3C-compatible generated trace ID, aligning local-gate report validation with current CI evidence proof-marker output, and serializing shared RLS test-role setup with a transaction-scoped Postgres advisory lock; that report remains development evidence until rerun from a clean release SHA with the current 32-gate matrix. The full report includes project PATH readiness, Go test/vet, DB-backed RLS integration, evidence probe tests, web moderate audit/smoke/typecheck/build, mobile high-severity audit/smoke/build-compatible checks, Rust protobuf validation, Rust cargo test, Terraform fmt/init/validate, observability validation, RLS schema validation, deployment skeleton validation, staging evidence validation, staging evidence gap reporting, CI workflow validation, CI evidence gate drift validation, security artifact validation, dependency-risk validation, secret hygiene validation, journal crypto validation, Serena/Obsidian validation, staging evidence contract synchronization, Obsidian readiness snapshot validation, and tooling tests, and is stamped with `git_head`, branch, upstream, ahead/behind counts, and `git status --short` cleanliness so release verification can reject stale, dirty, unsynced, skipped, or weakened RLS DB/gap-report evidence. `tools/verify-production-readiness.mjs` now also requires the Obsidian readiness snapshot to be in sync with the same strict staging manifest and release candidate, and rejects local gate reports observed before the strict staging manifest was generated, before a production-readiness claim can pass. `tools/run-local-gates.mjs` centralizes the local matrix, including project PATH readiness, Docker-backed DB/RLS integration, web and mobile npm audit gates, RLS schema validation, CI evidence gate drift validation, staging evidence contract synchronization, staging evidence gap reporting, and dependency-risk validation, handles Windows npm command shims without `spawn EINVAL`, includes env overrides such as `REQUIRE_DATABASE_URL=true` in report command strings, and writes JSON reports for full or selected gate runs, while `tools/validate-local-gate-report.mjs` rejects failed, partial, malformed, skipped, dry-run, dirty, unsynced, unknown-gate, future-dated, missing git metadata, weakened `rls-db-integration` rows that do not call either `tools/run-rls-db-integration.mjs` or `tools/run-rls-db-integration-docker.mjs` with `REQUIRE_DATABASE_URL=true`, and weakened `staging-evidence-gap-report` rows that omit `--allow-blockers`, the not-ready proof footer, rendered blocker rows, or gap-report proof markers unless relaxed development flags are explicitly used where available. The tooling test gate now covers the local gate runner, RLS DB integration runners, local gate report validator, CI workflow validator, CI evidence gate validator, deployment skeleton validator, RLS schema validator, dependency-risk validator, staging evidence validator, staging evidence contract sync, production readiness verifier, staging evidence recorder, staging evidence bootstrapper, staging evidence gap reporter, CI release evidence writer, Rust protobuf verifier, and project PATH verifier. `.github/workflows/security.yml` now provisions a `pgvector/pgvector:pg16` Postgres service, applies migrations, runs the DB-backed RLS/auth/AI/journal integration tests with `DATABASE_URL` and `REQUIRE_DATABASE_URL=true` set instead of relying only on skip-capable `CI=true` Go tests, runs the load harness self-test, validates observability artifacts, validates RLS schema invariants, validates secret hygiene, validates deployment skeleton invariants, validates the staging evidence manifest, runs the staging evidence gap-report gate with `--allow-blockers`, validates the staging evidence manifest contract, validates CI workflow gate coverage and release-evidence ordering, validates CI evidence gate drift, validates security/dependency-risk artifacts, tests the staging evidence bootstrapper and gap reporter, tests the project PATH verifier, runs web smoke before web typecheck/build, runs mobile high-severity audit plus smoke before mobile build-compatible checks, and verifies tracked-file cleanliness before writing the CI release evidence artifact. Remaining blockers: no real staging deployment/load test has been run, the environment-specific staging evidence manifest still contains pending external items, and the current local/CI matrix has not yet been proven by a clean GitHub Actions run from the current uncommitted worktree.

2026-07-01 update: local gate reports now require fresh upstream metadata before their Git sync fields can support a readiness claim. `tools/run-local-gates.mjs` runs `git fetch --dry-run` before computing branch/upstream ahead-behind state and records `git_remote_refreshed=true`; `tools/validate-local-gate-report.mjs` rejects reports missing that marker, so old or hand-edited reports cannot rely on stale tracking refs. Real clean local gates and clean pushed CI evidence for the exact release SHA remain pending.

2026-06-30 update: project PATH readiness now proves the standalone-`protoc` exception instead of only documenting it. `tools/verify-project-path.mjs` fails closed if `services/scripture-engine/Cargo.toml` or `Cargo.lock` loses `protoc-bin-vendored` coverage while `protoc` is absent from PATH, emits `vendored_protoc_covered=true` in local, CI, and strict-staging proof markers, and reports the concrete Rust manifest/lockfile coverage in both local and staging-evidence modes. Focused evidence: `cmd.exe /c "call tools\use-project-path.cmd >nul && rtk node --test tools\verify-project-path.test.mjs"` passed 19 tests; `rtk node tools\verify-project-path.mjs` and `rtk node tools\verify-project-path.mjs --strict-staging` both passed with required toolchains resolved and the vendored-protoc proof present.

2026-06-29 update: current focused local tenant-isolation evidence passed with `cmd /c "call tools\use-project-path.cmd >nul && rtk node tools\run-rls-db-integration-docker.mjs --container-name scriptureforge-rls-db-codex-proof"` after escalating for local Docker pipe access. The run applied migrations to disposable `pgvector/pgvector:pg16` and passed `TenantRLSCoversAllTenantTables`, `TenantScopedJournalHandlersEnforceRLS`, `TenantScopedRoomActiveHandlerEnforcesRLS`, `JournalHandlersHonorTenantIsolation`, `RoomHandlersHonorTenantIsolation`, `SocketStreamIsTenantScoped`, `AIRequestLogPersistsCitationsAndHonorsTenantRLS`, `GenerateCurriculumHandlerPersistsAuditRowsWithTenantRLS`, `AuthRegisterLoginRefreshRotationAndLogout`, `PrivilegedLoginRequiresAndVerifiesMFA`, `WorkspaceSwitchRequiresAuthenticatedOrgMatch`, and `MFAEnrollAndVerifyFlowForPrivilegedUsers`. This is local/container proof only; deployed `DATA-RLS-001` API/database tenantprobe artifacts remain pending.

2026-06-28 update: the tooling test gate now also covers `tools/sync-obsidian-readiness.test.mjs`, so local and CI gate drift checks include Obsidian readiness snapshot strict PATH and release-candidate behavior.

2026-06-29 update: `tools/run-npm-audit.mjs` is now import-safe and covered by `tools/run-npm-audit.test.mjs`, which proves web/mobile npm audit gates preserve configured severity levels, propagate vulnerability failures, classify registry/network outages separately from vulnerability results, and fall back through `cmd.exe` for Windows npm shims. The shared local tooling-test gate, GitHub Actions tooling-test command, and `tools/validate-ci-workflow.mjs` all require this wrapper coverage. Clean pushed CI dependency-audit evidence for the exact release SHA remains pending.

2026-07-01 update: npm audit local gate evidence now distinguishes completed registry audits from network-blocked local placeholder rows. `tools/run-npm-audit.mjs` emits `npm_audit_completed=true` only after a completed zero-exit audit and emits `npm_audit_network_blocked=true` when the registry is unreachable; `tools/validate-local-gate-report.mjs` rejects network-blocked web/mobile audit rows, so final local reports cannot satisfy dependency audit proof with an unreachable registry. Clean pushed CI dependency-audit evidence for the exact release SHA remains pending.

2026-06-28 update: `tools/mobileprobe` now rejects broader non-native mobile proof artifacts for `CLIENT-MOBILE-001`, including Expo Go, dev-client/debug-client, simulator/emulator/remote-debug, browser/global WebCrypto, Node crypto, Expo crypto, and JavaScript fallback wording. Focused current evidence: `cmd /c "call tools\use-project-path.cmd --strict-staging >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& rtk go test ./tools/mobileprobe -count=1"` passed with regression coverage for those weak-proof cases.
2026-06-28 update: mobile crypto production evidence now requires explicit buffer-zeroization markers. `tools/mobileprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation require native AES-GCM smoke artifacts to prove passphrase, salt, and plaintext buffers were zeroized in addition to generic wipe/clear markers, and `tools/verify-journal-crypto.mjs` rejects removal of the mobile `bytes.fill(0)` wipe helper. Native-device/EAS validation remains pending under `CLIENT-MOBILE-001`.

2026-06-29 update: mobile journal crypto now exports `runJournalCryptoSelfTest()` for native/EAS evidence capture from inside the app runtime. The self-test reports provider/native-required status and proves AES-GCM round-trip, tamper rejection, associated-data rejection, non-extractable key behavior, disposed-key rejection, and passphrase/salt/plaintext lifecycle markers; `mobile/src/lib/crypto.smoke.mts` covers the local fallback behavior, and `tools/verify-journal-crypto.mjs` now emits `native_device_self_test_export=true` plus `native_device_self_test_markers=true`. Focused evidence: `cmd /c "call tools\use-project-path.cmd >nul && cd mobile && rtk npm run build:check"` passed typecheck, API/crypto smoke, and journal crypto verification with the new marker. This adds a concrete native evidence hook but does not replace the still-pending EAS/native-device artifact required for `CLIENT-MOBILE-001`.
2026-06-29 update: `tools/verify-journal-crypto.mjs` now proves runtime AES-GCM buffer zeroization in its native quick-crypto harness by capturing IV and associated-data buffers passed to encrypt/decrypt and requiring them to be zeroed after use before emitting `runtime_buffer_zeroization=true`. Native-device/EAS validation remains pending under `CLIENT-MOBILE-001`.
2026-06-28 update: mobile journal crypto now wipes passphrase and salt byte buffers even when PBKDF2 key-material import fails, and wipes temporary base64 decode buffers after copying ciphertext/IV bytes for decrypt. `tools/verify-journal-crypto.mjs` emits `import_failure_zeroization=true` and `base64_decode_buffer_zeroization=true` proof markers so local gate reports cannot drop those cleanup paths. Native-device/EAS validation remains pending under `CLIENT-MOBILE-001`.
2026-06-28 update: mobile production evidence is now exact-release-bound. `tools/mobileprobe` requires `-release-candidate`/`-service-version` (or staging env equivalents), verifies those markers in EAS/native-device, native crypto, and staging-config artifact content, emits them in the JSON report, and the staging evidence recorder plus strict-release validation reject `CLIENT-MOBILE-001` evidence that does not match the manifest release candidate. Native-device/EAS validation remains pending under `CLIENT-MOBILE-001`.

2026-06-29 update: `tools/mobileprobe` now rejects reserved documentation/sample artifact hosts such as `.example`, `example.com`, `.test`, and `.invalid` before emitting `CLIENT-MOBILE-001`, matching strict staging validation and preventing sample HTTPS hosts from standing in for native-device/EAS, native AES-GCM, or staging-config proof. Web and mobile strict runtime config now reject the same reserved documentation/sample hosts for API and WebSocket endpoints, so staging/production client builds cannot be configured with sample endpoint URLs that production evidence would reject. Native-device/EAS validation remains pending under `CLIENT-MOBILE-001`.

2026-06-29 update: mobile staging config evidence now rejects contradictory native-crypto and environment markers. `tools/mobileprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation reject mobile staging config summaries or artifacts that include disabled native crypto or development/local deployment markers even if they also contain the required `EXPO_PUBLIC_REQUIRE_NATIVE_CRYPTO=true` and `EXPO_PUBLIC_DEPLOYMENT_ENVIRONMENT=staging` proof. Native-device/EAS validation remains pending under `CLIENT-MOBILE-001`.

2026-06-29 update: mobile production evidence now requires `staging artifact` provenance on each `CLIENT-MOBILE-001` EAS/native-device, native AES-GCM smoke, and staging-config segment. `tools/mobileprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation reject mobile evidence that has the right native-crypto or endpoint markers but omits staging artifact provenance, preventing generic local/native logs from standing in for release-bound mobile proof. Native-device/EAS validation remains pending under `CLIENT-MOBILE-001`.

2026-06-29 update: web journal crypto now mirrors the mobile disposable-key lifecycle locally. `web/src/lib/crypto.ts` exposes disposable key handles, `web/src/components/JournalEditor.tsx` disposes replaced, unmounted, async-stale, and post-save key handles, `web/src/lib/crypto.smoke.mts` proves disposed handles cannot encrypt, and `tools/verify-journal-crypto.mjs` emits `web_key_disposal=true`. Deployed web/mobile staging smoke and native-device/EAS validation remain pending external proof.

2026-06-29 update: web and mobile journal key derivation now rejects blank passphrases and blank server-issued salt material before PBKDF2, preventing accidental weak local key derivation when UI/bootstrap state is incomplete. `web/src/lib/crypto.smoke.mts`, `mobile/src/lib/crypto.smoke.mts`, and `tools/verify-journal-crypto.mjs` cover this behavior and the verifier emits `blank_key_input_rejected=true`. Native-device/EAS validation remains pending under `CLIENT-MOBILE-001`.

2026-06-30 update: backend journal persistence now rejects malformed encrypted envelopes before opening the tenant DB transaction. `POST /api/v1/journal_entries` requires strict base64 ciphertext, strict base64 12-byte AES-GCM IVs, versioned `journal:v...` salt IDs, and positive salt versions; plaintext-shaped `ciphertext` values now fail with `400` instead of being stored. Handler-level Postgres/RLS tests prove unknown plaintext/passphrase fields and plaintext-in-ciphertext payloads persist zero rows, and `tools/validate-rls-schema.mjs` requires those source markers. Focused current evidence: `cmd.exe /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& rtk go test ./internal/ports -run \"TestJournal\" -count=1 -timeout=90s"`, `cmd.exe /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& rtk go test ./internal/ports ./tests/integration -run \"TestTenantScopedJournalHandlersEnforceRLS|TestJournalHandlersHonorTenantIsolation|TestJournal\" -count=1 -timeout=120s"`, `cmd.exe /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& rtk go test ./tools/abuseprobe ./tools/tenantprobe -count=1 -timeout=90s"`, and `cmd.exe /c "call tools\use-project-path.cmd >nul && rtk node --test --test-reporter=dot tools\validate-rls-schema.test.mjs && rtk node tools\validate-rls-schema.mjs"` passed. Native-device/EAS and deployed staging journal evidence remain pending external proof.

2026-06-30 update: deployed tenant/RLS evidence now also has to prove the journal plaintext-denial behavior. `tools/tenantprobe` sends a plaintext-shaped `ciphertext` payload to `/api/v1/journal_entries` before the valid owner create; `owner-create-encrypted-journal` only passes if the plaintext-shaped payload returns `400` and the subsequent encrypted envelope creates successfully. The staging evidence recorder and strict-release validator require `plaintext-shaped journal payload denied` plus `malformed encrypted envelope rejected` markers on the owner-create segment, so a deployed `DATA-RLS-001` report cannot prove tenant journal writes while skipping no-plaintext persistence behavior. Focused current evidence: `cmd.exe /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& rtk go test ./tools/tenantprobe -count=1 -timeout=90s"` and `cmd.exe /c "call tools\use-project-path.cmd >nul && rtk node --test --test-reporter=dot tools\record-staging-evidence.test.mjs tools\validate-staging-evidence.test.mjs tools\verify-production-readiness.test.mjs"` passed. Real deployed API/database RLS evidence remains pending external proof.

2026-06-28 update: `tools/securityprobe` now rejects synced-secret artifacts that expose common base64-encoded Kubernetes Secret values for Postgres DSNs or OpenAI-style API keys, and rejects any `stringData:` field in `SEC-SECRETS-001` proof artifacts. Focused current evidence: `cmd /c "call tools\use-project-path.cmd --strict-staging >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& rtk go test ./tools/securityprobe -count=1"` passed with regression coverage for base64 secret data and `stringData` leakage.

2026-06-28 update: `tools/abuseprobe` and `tools/record-staging-evidence.mjs` now require `ABUSE-LIMIT-001` evidence to prove semantically exhausted rate-limit headers: positive `Retry-After`, positive `X-RateLimit-Limit`, `X-RateLimit-Remaining=0`, and positive `X-RateLimit-Reset`. Current hardening also rejects first-attempt `429` evidence and requires `repeated_attempts_verified=true` plus structured `attempts >= 2`, so static-deny behavior cannot masquerade as rate limiting. Strict-release validation now also segment-guards `ABUSE-LIMIT-001` so auth, account-scoped login, refresh-token, AI, journal, rooms, WebSocket upgrade, and config-artifact markers must appear in their own abuseprobe segments instead of borrowing broad summary text from another route. Focused current evidence: `cmd /c "call tools\use-project-path.cmd --strict-staging >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& rtk go test ./tools/abuseprobe -count=1"` and `cmd /c "call tools\use-project-path.cmd --strict-staging >nul && rtk node --test tools\record-staging-evidence.test.mjs"` passed with weak-header rejection coverage.

2026-06-30 update: strict-release validation now parses concrete `ABUSE-LIMIT-001` rate-limit header values in each abuse profile segment instead of accepting decorative header names. Auth login, account-scoped login, refresh-token, AI, journal, rooms, and WebSocket profile summaries must include numeric `Retry-After=<seconds>`, `X-RateLimit-Limit=<n>`, `X-RateLimit-Remaining=0`, and `X-RateLimit-Reset=<epoch-or-seconds>` markers before abuse evidence can support final readiness. Focused current evidence: `cmd.exe /c "call tools\use-project-path.cmd >nul && rtk node --test --test-reporter=dot tools\validate-staging-evidence.test.mjs tools\verify-production-readiness.test.mjs tools\record-staging-evidence.test.mjs"` and `cmd.exe /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& rtk go test ./tools/abuseprobe -count=1 -timeout=90s"` passed. Real staging ingress evidence remains pending external proof.

2026-06-28 update: `ABUSE-LIMIT-001` is now exact-release-bound. `tools/abuseprobe` requires `-release-candidate`/`STAGING_RELEASE_CANDIDATE` and `-service-version`/`STAGING_ABUSE_SERVICE_VERSION`, verifies those markers in the redacted config artifact, and emits the same markers in every successful abuse profile summary. The staging evidence recorder rejects missing or mismatched report release identity and requires exact `release_candidate=<manifest release_candidate>` plus `service_version` markers on every profile and config-artifact summary. Strict staging manifest validation now includes `ABUSE-LIMIT-001` in the exact release-candidate guard list. Remaining production work is still real staging ingress evidence attached to the environment-specific manifest.

2026-06-28 update: local WebSocket fan-out now evicts lagging room subscribers and records `dependency="websocket", operation="room_broadcast", status="dropped"` metrics instead of silently dropping participant broadcasts. `internal/ports/room_hub_test.go` proves lagging subscriber eviction and `internal/ports/rooms_realtime_test.go` proves accepted senders still receive broadcasts while dropped fan-out is observable. Staging WebSocket/Redis load evidence now must also include `room_broadcast_drops=0`, so contiguous sequence proof cannot hide fan-out drops during `PERF-WS-001` or `DATA-REDIS-001` collection.

2026-06-28 update: `tools/loadtest` and `tools/record-staging-evidence.mjs` now require WebSocket replica distribution, reconnect behavior, HTTP polling fallback, and Redis telemetry proof to use distinct HTTPS artifact URLs before `PERF-WS-001`/`DATA-REDIS-001` evidence can be accepted. This prevents one broad artifact from satisfying every WebSocket load proof role while external staging load evidence remains pending.

2026-06-28 update: `tools/loadtest` now emits the structured `ws_polling_latest_sequence` JSON field for WebSocket/Redis load reports, and `tools/record-staging-evidence.mjs` requires it to equal `ws_max_sequence`. A load report can no longer rely only on a summary string that claims the polling fallback reached the final sequence while the JSON evidence shows stale room state.

2026-06-29 update: current focused local WebSocket evidence passed for room-stream behavior, Redis/Lua sequencing, and the load harness. `cmd /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& rtk go test ./internal/ports -run TestLiveRoom -count=1 -timeout=90s -v"` passed 8 tests after adding `TestLiveRoomFanOutDeliversEveryAcceptedEventToEverySubscriber`, which proves every active subscriber receives every accepted event in contiguous sequence order and no dropped fan-out metric is emitted. The handler now serializes append-plus-broadcast so concurrent senders cannot produce accepted sequence order that differs from fan-out order; focused stress checks for the fan-out and concurrent-sender tests each passed 10 consecutive runs. `rtk go test ./internal/domain/room -count=1 -timeout=90s -v` passed, and `rtk go test ./tools/loadtest -count=1 -timeout=90s -v` passed 53 tests. This is local correctness/probe coverage only; deployed multi-replica WSS/Redis load evidence remains pending under `PERF-WS-001` and `DATA-REDIS-001`.

2026-06-30 update: local WebSocket event envelopes now reject non-low-cardinality event types before Redis append or broadcast. `internal/ports/driving_wss.go` allows only non-empty lowercase letters, digits, `_`, `-`, and `.` up to 64 bytes for `type`; whitespace, uppercase, punctuation, and overlong values close the stream with a policy-violation fault and do not persist events. `TestLiveRoomRejectsInvalidEventTypesWithoutPersisting` and `TestValidRoomEventTypeKeepsLowCardinalityEnvelopeTypes` prove the guard, and `tools/run-go-core-gate.mjs --mode test` now requires the invalid-event-type PASS line before emitting local Go proof markers. This is local envelope hardening only; real multi-instance WebSocket/load and Redis sequencing evidence remains pending under `PERF-WS-001` and `DATA-REDIS-001`.

2026-06-28 update: local Go API dependency observability now emits low-cardinality OpenTelemetry child spans from every `ObserveDependencyFromContext` call while preserving existing dependency metrics. `internal/domain/observability/observability_test.go` proves dependency spans carry `scriptureforge.dependency`, `scriptureforge.dependency.operation`, `scriptureforge.dependency.status`, and `scriptureforge.dependency.duration_ms` attributes, error-like statuses mark the span as an OpenTelemetry error, and the Prometheus dependency metric still records the same operation. `tools/validate-observability.mjs` now guards this span surface. This is local trace instrumentation proof; deployed OTLP collector/backend proof remains under `OBS-OTEL-001`.

2026-07-01 update: dependency-span error classification now treats dropped, rejected, and unavailable dependency statuses as OpenTelemetry error spans, including WebSocket room broadcast drops and unavailable provider outcomes. `TestDroppedRejectedAndUnavailableDependencyStatusesAreErrors` and `tools/validate-observability.mjs` guard the tokens so local span semantics cannot drift silently. This is local instrumentation proof only; deployed collector/backend proof remains under `OBS-OTEL-001`.

2026-06-28 update: `tools/observabilityprobe` and `tools/record-staging-evidence.mjs` now reject `OBS-OTEL-001` evidence with placeholder trace IDs. The persisted `trace_id` must be a 32-character non-zero lowercase hex OpenTelemetry trace ID before trace/log backend correlation evidence can be accepted, and the recorder now rejects trace/log summaries that do not include that exact structured trace ID value. Strict-release validation also requires `trace-backend-search` itself to prove API/Rust trace correlation, `log-backend-trace-correlation` itself to prove trace ID plus tenant/user/role log markers, and both segments to carry the same concrete non-zero 32-character trace ID. The observability probe now also requires Rust metrics evidence to come from an external, non-local, non-private staging artifact URL instead of an internal scrape address. Deployed observability proof remains pending external evidence.

2026-06-28 update: local Go API trace propagation now rejects invalid client-supplied `X-Trace-ID` and malformed `Traceparent` trace IDs before they reach response headers, access logs, request context, or OpenTelemetry span context. The middleware emits a 32-character non-zero hex trace ID even when a custom generator returns invalid data, binds generated or accepted `X-Trace-ID` values into the request span trace ID when no valid W3C parent exists, preserves streaming response-writer behavior through `Flush` and `Unwrap`, tracks implicit status commits so late status changes do not corrupt metrics, uses a nonzero response span ID in `Traceparent`, and `tools/validate-observability.mjs` guards the code/test markers for that behavior. This closes local placeholder/correlation-string and streaming-wrapper ingress gaps; deployed OpenTelemetry backend proof remains pending under `OBS-OTEL-001`.

2026-06-28 update: strict-release validation now also segment-guards `OBS-ALERT-001`. `dashboard-import`, `alert-rules-loaded`, `alert-delivery-status`, and `telemetry-retention-policy` must each carry their own required dashboard metric, alert rule, Alertmanager delivery, and retention markers; broad summaries can no longer borrow proof from another observability segment. Deployed dashboard, alert, delivery, and retention evidence remains pending external staging proof.

2026-06-28 update: strict-release validation now segment-guards `DATA-RLS-001` tenantprobe summaries. Each journal, room, and `database-rls-context-proof` segment must carry its own same-tenant success, cross-tenant denial, tenant override rejection, or database RLS marker set, so a broad tenant summary cannot borrow proof across API and database segments. Tenant probe evidence also requires structured `journal_id` and `room_id` fields matching exact `journal_id=<id>` and `room_id=<id>` summary markers for created-resource read/list/state proofs. 2026-06-30 hardening: `tools/tenantprobe` now verifies owner direct journal read and owner room-state responses contain the created resource ID, and the staging evidence recorder plus strict-release validator reject `DATA-RLS-001` evidence unless all journal proof segments share the created `journal_id` and all room proof segments share the created `room_id`. Deployed API/database RLS evidence remains pending external staging proof.

2026-06-28 update: strict-release validation now segment-guards `SEC-SECRETS-001` securityprobe summaries. `irsa-service-account`, `secret-provider-class`, `synced-secret-metadata-redacted`, `iam-secrets-policy`, and `scoped-secrets-access-test` must each carry their own IRSA, CSI, redaction, scoped IAM, or access-test markers, so a broad secret-handling summary cannot borrow proof across security segments. Deployed IRSA/CSI/secret-manager evidence remains pending external staging proof.

2026-06-29 update: `tools/securityprobe`, the staging evidence recorder, and strict-release validation now segment-guard `SEC-DBUSER-001` scoped database-user summaries. `database-scoped-user` itself must prove a live connection plus `superuser=false`, `bypassrls=false`, `createrole=false`, `createdb=false`, `privileged_operation_denied=true`, `app_grants_verified=true`, `app_grant_tables=9`, and `app_grants=SELECT,INSERT,UPDATE,DELETE`, so generic scoped-role text or non-admin-only proof outside the DB-user probe segment cannot satisfy production database-user proof. Deployed scoped database credential and grant evidence remains pending external staging proof.

2026-06-28 update: security staging evidence is now exact-release-bound. `tools/securityprobe` requires `-release-candidate`/`-service-version` (or staging env equivalents), verifies those markers in IRSA, SecretProviderClass, synced-secret, IAM policy, scoped access-test, and database-scoped-user proof, emits the release identity in JSON reports, and the staging evidence recorder plus strict-release validator reject `SEC-SECRETS-001` or `SEC-DBUSER-001` evidence that does not match the manifest release candidate. Deployed security evidence remains pending external staging proof.

2026-06-30 update: security staging evidence is now bound to one concrete run. `tools/securityprobe` requires `-load-run-id` or `STAGING_LOAD_RUN_ID`, persists `load_run_id`, and emits matching `load_run_id=<same run>` markers across IRSA, SecretProviderClass, synced-secret metadata, IAM policy, scoped access-test, and database-scoped-user proof. The staging evidence recorder and strict-release validation reject missing or mixed security run IDs, preventing secret-delivery and scoped DB-user artifacts from different staged runs from satisfying `SEC-SECRETS-001` or `SEC-DBUSER-001`. Real staged IRSA/CSI/scoped-database evidence remains pending external proof.

2026-06-28 update: accepted dependency risk handling now fails closed when the DRR-001 review date is overdue, not only when the accepted risk expires. `tools/validate-dependency-risk.mjs`, `tools/record-staging-evidence.mjs`, and `tools/validate-staging-evidence.mjs` reject overdue accepted-risk decisions, and `security/dependency_risk_register.md` records that final production-readiness validation must fail if the accepted risk is expired or past review due without a refreshed decision.

2026-07-01 update: DRR-001 accepted dependency risk now fails closed if it becomes mobile-runtime reachable. `tools/validate-dependency-risk.mjs` scans `mobile/App.tsx` and `mobile/src` and emits `drr001_mobile_runtime_not_imported=true` only when the accepted `uuid <11.1.1` advisory remains limited to Expo tooling; regression tests reject static and dynamic `uuid` imports from mobile runtime code while the risk is accepted. This does not remediate the transitive advisory; native/mobile production closure still requires dependency-risk closure or refreshed approval.

2026-06-28 update: deployment evidence is now release-bound before it can support `DEPLOY-TF-001` or `DEPLOY-K8S-001`. `tools/deploymentprobe` requires `-release-candidate` and `-service-version`, rejects Terraform apply/approval and Kubernetes workload artifacts unless they include exact `release_candidate=<expected SHA>` and `service_version=<expected version>` markers, and the staging evidence recorder rejects deployment probe reports whose `release_candidate` differs from the manifest. Strict staging manifest validation also rejects deployment evidence summaries that omit the exact manifest release candidate.

2026-06-28 update: strict-release validation now segment-guards deploymentprobe summaries. Terraform remote backend init, Terraform staging plan, Terraform apply-or-approval, Kubernetes rollout status, and Kubernetes workload resource segments must each carry their own required markers, so a broad deployment summary cannot borrow proof across Terraform and Kubernetes segments. Real staging plan/apply and Kubernetes rollout evidence remains pending external proof.

2026-06-29 update: `tools/deploymentprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation now parse Kubernetes workload image digest proof for `DEPLOY-K8S-001` and require at least three concrete `sha256:<64 hex>` digests bound to the API/web/Rust workload names in `kubernetes-workload-resources`. The deployment probe rejects generic or unbound digest markers before report emission, the staging evidence recorder rejects weak probe reports before manifest updates, and passing workload-resource summaries record `concrete_image_digests=3` plus workload digest markers, preventing generic `sha256` labels, unbound digest lists, or a single image proof from satisfying API/web/Rust deployment readiness. Real staging Kubernetes workload evidence remains pending external proof.

2026-06-29 update: deployment production evidence now rejects failure contradiction markers. `tools/deploymentprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation reject Terraform/Kubernetes artifacts or summaries that admit init, plan, apply, rollout, zero ready/available replicas, CrashLoopBackOff, or ImagePullBackOff failures even if required success markers also appear. Real staging plan/apply and Kubernetes rollout evidence remains pending external proof.

2026-06-30 update: Terraform deployment evidence now release-binds the remote backend and plan artifacts, not only apply/approval. `tools/deploymentprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation require `terraform-remote-backend-init` and `terraform-staging-plan` summaries to carry exact `release_candidate=<manifest release_candidate>` plus `service_version` markers, preventing stale backend-init or plan artifacts from being paired with a current deployment approval. Focused current evidence: `cmd.exe /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& rtk go test ./tools/deploymentprobe -count=1 -timeout=90s"` passed, and the staging evidence recorder/validator/production-readiness tests were updated to reject missing init/plan release linkage. Real staging plan/apply evidence remains pending external proof.

2026-07-01 update: Terraform remote-state evidence now requires KMS-backed state encryption and enabled state bucket versioning markers in addition to the existing S3 backend, key, and DynamoDB lock proof. `tools/deploymentprobe`, `tools/record-staging-evidence.mjs`, strict-release validation, and the staging evidence contract all require `kms_key_id=` plus `versioning=enabled` on the `terraform-remote-backend-init` segment, so a backend initialized with only generic `encrypt=true` can no longer satisfy deployment readiness. Real remote backend/apply proof remains pending external staging artifacts.

2026-07-01 update: Terraform-managed RDS and Redis storage now require customer-managed AWS KMS key inputs. `build/terraform` wires `database_kms_key_arn` into the Aurora cluster and `redis_kms_key_arn` into the Redis replication group, `tools/validate-deployment-skeleton.mjs` guards the variables, examples, and resource wiring, and deployment evidence now requires Terraform plan markers for `kms_key_id`, `database_kms_key_arn`, and `redis_kms_key_arn`. Real AWS KMS/key-policy/apply proof remains pending external staging artifacts.

2026-06-28 update: mobile staging evidence is now segment-bound for distinct artifact proof. `tools/record-staging-evidence.mjs` and strict-release validation require `distinct_mobile_artifacts=true` on each EAS/native-device, native AES-GCM smoke, and staging config segment, preventing one mobile proof segment from lending artifact-distinctness to another `CLIENT-MOBILE-001` segment.

2026-06-28 update: Terraform approval fallback evidence now requires a structured `change_ticket` JSON field plus a matching `change_ticket=<ticket-id>` marker before it can stand in for real apply output. `tools/deploymentprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation reject `DEPLOY-TF-001` approval artifacts that only say approved plus release identity without an auditable ticket ID; real staging apply/approval evidence remains pending external proof.

2026-06-28 update: load/performance evidence is now release-bound before it can support `PERF-HTTP-001`, `PERF-WS-001`, or `DATA-REDIS-001`. `tools/loadtest` rejects HTTP/WebSocket side artifacts unless they include exact `release_candidate=<expected SHA>` and `service_version=<expected version>` markers, the staging evidence recorder rejects load reports whose `release_candidate` differs from the manifest, and strict staging manifest validation rejects load evidence summaries that omit the exact manifest release candidate.

2026-06-28 update: strict-release validation now segment-guards load/performance summaries. `PERF-HTTP-001` must keep HTTP threshold, observed RPS/P99, release, and service markers in the measurement segment while keeping replica/dependency telemetry verification in the verified-artifacts segment. `PERF-WS-001` must keep WebSocket threshold, observed latency, origin/auth, contiguous sequence, sequence cardinality/range, release, and service markers in the measurement segment while keeping replica, reconnect, polling, Redis telemetry, and no-drop markers in the verified-artifacts segment. `DATA-REDIS-001` must keep sequence cardinality/range and Redis telemetry URL markers in its Redis sequence segment while keeping Redis telemetry verification and `room_broadcast_drops=0` in the verified-artifacts segment. The staging evidence recorder now also rejects WebSocket/Redis reports whose structured `ws_room_id` does not match the exact `ws_room_id=<id>` summary marker, preventing one staged room's sequence counts from being paired with another room's summary. External staging load evidence remains pending.

2026-06-28 update: strict-release validation now parses load/performance numbers instead of accepting marker labels alone. `PERF-HTTP-001` evidence fails if `observed_rps` is below 5,000 or `observed_p99_ms` is above 200, `PERF-WS-001` evidence fails if `observed_rps` is below 500 accepted events/sec or `observed_p99_ms` is above 200, and `DATA-REDIS-001` evidence fails if unique sequence count, max sequence, or HTTP polling latest sequence do not match the expected event count. External staging load evidence remains pending.

2026-06-28 update: staging evidence validation now emits `strict_numeric_thresholds_checked=true`, and `tools/validate-local-gate-report.mjs` rejects local gate reports whose `staging-evidence-validation` output omits that marker. This keeps the new performance numeric checks visible in local/CI proof output rather than relying only on source inspection.

2026-06-29 update: local and CI client build/typecheck evidence now uses `tools/run-client-command.mjs` for `web-typecheck`, `web-build`, and `mobile-build-check`. The wrapper runs the configured npm script and emits stable success markers such as `client_script_command=true`, `web_typescript_no_emit=true`, `web_production_bundle=true`, and `mobile_crypto_verification=true`; `tools/validate-local-gate-report.mjs` and `tools/validate-ci-workflow.mjs` reject raw npm build/typecheck rows or marker-free output. This strengthens local/CI evidence capture only; deployed web/mobile smoke evidence remains pending under `CLIENT-WEB-001` and `CLIENT-MOBILE-001`.

2026-06-29 update: local and CI Go core gates now use `tools/run-go-core-gate.mjs` for all-package `go test ./... -count=1 -timeout=90s` and `go vet ./...`. The wrapper emits stable proof markers for all-package coverage, uncached test execution, timeout enforcement, and static analysis, and both `tools/validate-local-gate-report.mjs` and `tools/validate-ci-workflow.mjs` reject raw Go test/vet rows or marker-free output. This strengthens required gate evidence capture; it does not replace the pending clean pushed CI artifact or staging evidence manifest.

2026-06-29 update: the production evidence probe test gate now uses `tools/run-go-probe-tests.mjs` instead of a raw multi-package `go test` command. The wrapper runs the required Go probe packages for CI/source, staging, tenant/RLS, Rust gRPC, observability, security, resilience, mobile, deployment, abuse, Zoom, AI, and load evidence tooling with `-count=1`, then emits per-probe-family proof markers. `tools/validate-local-gate-report.mjs`, `tools/validate-ci-workflow.mjs`, local gates, and GitHub Actions now reject marker-free or raw probe-test evidence. This makes local/CI evidence-tool coverage explicit; it does not replace the pending external staging artifacts those probes are designed to verify.

2026-06-29 update: `tools/verify-production-readiness.mjs` now rejects same-SHA local gate reports older than 24 hours at verification time and emits the `local_gate_freshness_checked=true` proof marker. This closes a final-claim evidence gap where a clean, synced, matching-HEAD local gate report could otherwise be too stale to support a current production-readiness claim. Full production readiness still requires a clean current gate run plus the external staging evidence manifest blockers listed above.

2026-06-29 update: a real non-dry local gate run completed with `node tools/run-local-gates.mjs --continue-on-failure --report artifacts/local-gate-report-current.json`: 33/33 gates run, 0 failed, `threshold_pass=true`, `dry_run=false`, branch `codex/production-readiness-remediation`, upstream `origin/codex/production-readiness-remediation`, ahead/behind `0/0`, observed at 2026-06-29T21:27:33Z. `node tools/validate-local-gate-report.mjs --report artifacts/local-gate-report-current.json --allow-dirty` passed. This is current development evidence only, not release evidence, because the report captured `git_status_clean=false`; `tools/verify-production-readiness.mjs --local-gate-report artifacts/local-gate-report-current.json` still rejects the claim on dirty-worktree evidence before the 21 pending external staging evidence items can support readiness.

2026-06-29 update: failed `tools/verify-production-readiness.mjs` runs now also emit staging evidence blocker diagnostics from the manifest/contract pair. Even when the first hard failure is local, such as a dirty local-gate report, the CLI prints blocker IDs, status counts, strict staging PATH readiness, and strict release readiness before the replayable gap-report command. Running it against `artifacts/local-gate-report-current.json` now surfaces the dirty-worktree local evidence rejection and the 21 pending external blocker IDs in one output.

2026-06-29 update: local and CI Terraform formatting/validation gates now use `tools/run-terraform-command.mjs` for recursive `fmt -check` and `validate` against `build/terraform`, while `terraform init -backend=false` remains routed through the existing network-aware init wrapper. The new wrapper emits `terraform-fmt-gate` and `terraform-validate-gate` proof summaries, and the local gate report plus CI workflow validators reject raw or marker-free Terraform fmt/validate evidence. This improves local/CI gate evidence quality; real remote backend, staging plan/apply, and Kubernetes rollout proof remain pending under `DEPLOY-TF-001` and `DEPLOY-K8S-001`.

2026-07-01 update: Terraform validate proof now requires Terraform's own success text in addition to wrapper markers. `tools/run-terraform-command.mjs --mode validate` fails if a zero-exit validate run does not include `Success! The configuration is valid.`, and `tools/validate-local-gate-report.mjs` rejects stale `terraform-validate` rows that omit that Terraform output. This tightens local/CI deployment-skeleton evidence only; real remote backend, staging plan/apply, and Kubernetes rollout proof remain pending under `DEPLOY-TF-001` and `DEPLOY-K8S-001`.

2026-06-28 update: load/performance production evidence now also requires `threshold_pass=true` in HTTP and WebSocket load summary segments. `tools/record-staging-evidence.mjs` rejects performance reports whose summary says `threshold_pass=false` even when raw numeric fields are present, and strict-release validation requires the same marker before `PERF-HTTP-001` or `PERF-WS-001` can support readiness.

2026-06-29 update: load/performance production evidence now rejects threshold contradiction markers. `tools/loadtest`, `tools/record-staging-evidence.mjs`, and strict-release validation reject load artifacts or summaries that admit `threshold failed`, `threshold_failures`, RPS-below-threshold, or P99-above-threshold conditions even if required `threshold_pass=true` and numeric markers also appear. Real staging performance/load evidence remains pending external proof.

2026-06-30 update: HTTP load dependency telemetry evidence now has to preserve concrete dependency latency proof. `tools/loadtest` rejects `PERF-HTTP-001` dependency telemetry artifacts unless they carry `postgres_p99_ms=<n>`, `redis_p99_ms=<n>`, and `dependency_threshold_pass=true`, and the staging evidence recorder plus strict-release validation require `dependency_latency_artifact_verified=true` in the load summary. This prevents generic "postgres redis p99 below threshold" prose from satisfying the architecture's 5,000 RPS/P99 evidence path. Real staging HTTP load evidence remains pending external proof.

2026-06-29 update: load/performance evidence now requires meaningful measurement volume before it can support production readiness. `tools/loadtest`, `tools/record-staging-evidence.mjs`, and strict staging validation require HTTP staging evidence to include `production_min_duration_ms=60000` plus `duration_ms >= 60000`, and WebSocket/Redis evidence to include `production_min_ws_events=30000` plus at least 30,000 expected/unique/max/polled events, matching 500 accepted events/sec for 60 seconds. This prevents tiny successful bursts from satisfying `PERF-HTTP-001`, `PERF-WS-001`, or `DATA-REDIS-001`; external staging load evidence remains pending.

2026-06-30 update: WebSocket polling fallback evidence now has to match the exact load run sequence, not merely exceed the production minimum. `tools/loadtest` rejects `ws-polling-artifact-url` evidence whose `latest_sequence` differs from `concurrency * ws-events-per-client`, emits `ws_polling_artifact_latest_sequence_matches_run=true`, and the staging evidence recorder plus strict-release validation require that marker alongside the existing `ws_polling_latest_sequence=ws_max_sequence` structured proof. Real staging WebSocket/Redis load evidence remains pending external proof.

2026-06-28 update: load/performance evidence summaries must preserve `production_target_rps` and `production_target_p99_ms` markers in addition to configured and observed thresholds. `tools/record-staging-evidence.mjs` and strict-release validation reject HTTP/WebSocket load summaries that omit those production-target markers, keeping staging proof tied to the architecture target instead of local threshold labels only.

2026-06-28 update: strict-release validation now parses `ABUSE-LIMIT-001` abuse profile attempt counts instead of accepting marker words alone. Auth login, account-scoped login, refresh-token, AI, journal, rooms, and WebSocket profile segments must each include a concrete `after <n> attempts` or `attempts=<n>` count with `n >= 2`, so first-attempt deny/static-block evidence cannot support production abuse-control readiness.

2026-06-28 update: `tools/abuseprobe` now emits the normalized HTTPS `web_origin` used for the WebSocket upgrade profile, and `tools/record-staging-evidence.mjs` rejects `ABUSE-LIMIT-001` reports when the redacted config artifact host matches either the staging API target or the reported web origin. This preserves the existing local-only boundary: deployed staging ingress evidence is still required before abuse-control readiness can close.

2026-06-29 update: `tools/abuseprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation now require staging artifact provenance on every `ABUSE-LIMIT-001` auth, account-scoped login, refresh-token, AI, journal, rooms, and WebSocket route-profile segment, not only on the redacted config artifact summary. Current local evidence: `cmd.exe /c "call tools\use-project-path.cmd >nul && rtk go test ./tools/abuseprobe -count=1 -timeout=60s"` passed 36 tests, and `cmd.exe /c "call tools\use-project-path.cmd >nul && rtk node --test tools\record-staging-evidence.test.mjs tools\validate-staging-evidence.test.mjs"` passed 390 tests. Real staging ingress evidence remains pending external proof.

2026-06-28 update: strict-release validation now mirrors the security probe's high-confidence secret leak guard for `SEC-SECRETS-001` result summaries. Even if a manifest is edited manually, summaries containing plaintext Postgres DSNs, OpenAI-style API keys, common base64 secret prefixes, Kubernetes `stringData:`, client/webhook secrets, password fields, or PEM headers fail before they can support production secret-handling readiness.

2026-06-29 update: `tools/validate-secret-hygiene.mjs` is now import-safe and covered by `tools/validate-secret-hygiene.test.mjs`, which proves the current repository passes local secret hygiene validation and rejects local `.env` files, high-confidence provider tokens/private keys, missing `.gitignore` env/Terraform-state protections, and Terraform examples without placeholder markers. GitHub Actions runs this test directly and through the shared tooling-test command, and `tools/validate-ci-workflow.mjs` rejects workflows that drop either path. Real cloud secret-manager, IRSA/CSI, scoped secret access, and clean pushed CI evidence remain pending external proof.

2026-06-28 update: local gate reports now fail closed on command drift across the full matrix. `tools/validate-local-gate-report.mjs` requires every recorded gate command to match the canonical `tools/run-local-gates.mjs` command display, so stale or weakened report rows such as a narrowed `go-test` command cannot support final readiness.

2026-06-29 update: local gate reports now also fail closed on weak PATH/toolchain evidence. `tools/run-local-gates.mjs` runs both local PATH readiness and `tools/verify-project-path.mjs --strict-staging`, `tools/verify-project-path.mjs` emits proof markers for required command resolution, version checks, strict staging tool resolution, strict staging version checks, and broken-shim rejection, and `tools/validate-local-gate-report.mjs` rejects gate reports whose PATH rows omit those markers. This prevents a local gate report from supporting readiness when staging evidence CLIs are missing or only indirectly assumed; external staging evidence remains pending.

2026-06-29 update: local gate reports now fail closed on weak npm-audit and Terraform-init output. `tools/run-npm-audit.mjs` emits `npm audit gate validated` proof markers for command execution, audit-level enforcement, network-blocker classification, and Windows command-shim fallback coverage; `tools/run-terraform-init.mjs` emits `terraform init gate validated` proof markers for command execution, `-backend=false` defaulting, and registry-blocker classification. `tools/validate-local-gate-report.mjs` rejects web/mobile audit rows or Terraform init rows that omit these summaries or markers, so a bare exit-zero wrapper row cannot support final readiness. 2026-07-01 update: the npm audit wrapper now separates `npm_audit_completed=true` from `npm_audit_network_blocked=true`, and local gate report validation rejects network-blocked audit rows for release-grade dependency evidence. Clean pushed CI and live staging Terraform evidence remain pending.

2026-06-29 update: local gate reports now fail closed on weak web/mobile smoke output. `web/src/lib/api.smoke.mts`, `web/src/lib/crypto.smoke.mts`, `mobile/src/lib/api.smoke.mts`, and `mobile/src/lib/crypto.smoke.mts` emit proof summaries for canonical auth, encrypted journal-only payloads, room/WebSocket setup, strict endpoint guards, AES-GCM, associated-data rejection, PBKDF2 work factor, key disposal, and native-required mobile fail-closed behavior. `tools/validate-local-gate-report.mjs` rejects `web-smoke` or `mobile-smoke` rows that omit the API/crypto smoke summaries or markers, so local client-flow evidence cannot be satisfied by a generic passing npm script. Deployed browser/mobile staging smoke and native-device/EAS evidence remain pending external proof.

2026-06-29 update: local gate reports now fail closed on weak staging-evidence gap-report output. `tools/report-staging-evidence-gaps.mjs` writes status counts and blocking-item IDs into the proof footer, and `tools/validate-local-gate-report.mjs` requires the `staging-evidence-gap-report` gate to render the not-ready proof footer, footer status counts, footer blocking IDs, blocker rows, explicit `--allow-blockers`, gap-report proof markers, and a proof-footer `blocking_items=<n>` count that exactly matches the footer ID list. This prevents a truncated or stale gap report from supporting final readiness while external staging evidence remains pending.

2026-06-29 update: CI release evidence now carries the strengthened gap-report contract forward into `SRC-CI-001` proof. `tools/write-ci-release-evidence.mjs` emits `staging_gap_report_footer_contract_required=true`, `tools/ciprobe` requires that proof marker before accepting a CI release artifact, and `tools/validate-ci-evidence-gates.mjs` verifies the writer/probe marker lists remain mirrored. Real clean pushed CI evidence for the exact release SHA remains pending.

2026-06-29 update: CI release evidence now also carries `staging_gap_report_required_evidence_contract_required=true`, and `tools/ciprobe` requires it before accepting `SRC-CI-001`. This binds clean CI proof to the local gate contract that staging gap reports must preserve rendered `required:` evidence details, not only footer blocker IDs and counts. Real clean pushed CI evidence for the exact release SHA remains pending.

2026-06-29 update: final production-readiness verification now emits `staging_gap_report_footer_contract_validated=true` after accepting the strict staging manifest, current Obsidian snapshot, and local gate report. This ties the final claim proof marker set to the strengthened gap-report footer contract for blocker IDs and status counts, rather than only asserting that the gap report was clear.

2026-06-29 update: WebSocket load evidence now rejects stale HTTP polling fallback artifacts. `tools/loadtest` parses the polling artifact's `latest_sequence=<n>` marker and requires it to be at least the 30,000-event production minimum before `PERF-WS-001`/`DATA-REDIS-001` probe evidence can be emitted, closing a gap where a polling artifact could contain any numeric sequence while the JSON report claimed the full staged room sequence.

2026-06-29 update: the WebSocket polling artifact sequence check now survives evidence recording. `tools/loadtest` emits `ws_polling_artifact_latest_sequence_validated=true`, while `tools/record-staging-evidence.mjs` and `tools/validate-staging-evidence.mjs` require that marker for `PERF-WS-001` and `DATA-REDIS-001`, preventing manually weakened or stale load reports from dropping the artifact-body validation proof.

2026-06-29 update: WebSocket/Redis load evidence now preserves staging artifact provenance in recorded summaries, not only in side artifact bodies. `tools/loadtest`, `tools/record-staging-evidence.mjs`, and strict-release validation require `staging artifact` markers before `PERF-WS-001` or `DATA-REDIS-001` can support readiness. Current local evidence: `cmd.exe /c "call tools\use-project-path.cmd >nul && rtk go test ./tools/loadtest -count=1 -timeout=90s"` passed 54 tests, and `cmd.exe /c "call tools\use-project-path.cmd >nul && rtk node --test tools\record-staging-evidence.test.mjs tools\validate-staging-evidence.test.mjs"` passed 393 tests. Real staging WebSocket/Redis performance evidence remains pending external proof.

2026-06-29 update: CI release evidence now mirrors that strict PATH gate. `tools/ciprobe` requires the GitHub Actions release artifact to list `gate: strict-staging-path-readiness`, and `tools/validate-ci-evidence-gates.mjs` derives the expected CI gate set from `tools/run-local-gates.mjs` while emitting `strict_staging_path_gate_mirrored=true`, so stale CI artifacts or probe code cannot omit staging PATH/toolchain verification while still satisfying `SRC-CI-001`.

2026-06-29 update: the GitHub Actions workflow now runs an explicit CI runtime PATH check. `node tools/verify-project-path.mjs --ci` executes after `postgresql-client` is installed and verifies the CI toolchain (`git`, Go, Node/npm, Rust/Cargo, Terraform, and `psql`) without requiring local-only `rtk`; `tools/validate-ci-workflow.mjs` requires that step and emits `ci_path_readiness_gate=true`. This strengthens CI/local gate execution evidence, while the clean pushed Actions run for the exact release SHA remains pending.

2026-06-29 update: the GitHub Actions workflow now also executes the strict staging PATH gate it claims in release evidence. The workflow installs AWS CLI v2 from the AWS distribution zip, exports `$(go env GOPATH)/bin` through `GITHUB_PATH` so the later strict verifier can resolve `gopls`, installs `gopls` and `kubectl`, then runs `node tools/verify-project-path.mjs --ci --strict-staging`; this combined mode verifies CI-required tools plus staging evidence CLIs without requiring local-only `rtk`, and `tools/verify-project-path.mjs` now rejects AWS CLI v1 output when strict staging evidence tooling is required. `tools/validate-ci-workflow.mjs` rejects removal of those installs or the strict PATH step and emits `strict_staging_path_readiness_gate=true`. This closes the local mismatch where `SRC-CI-001` release evidence could list `gate: strict-staging-path-readiness` without a matching workflow execution step or with the wrong AWS CLI major version; actual clean pushed Actions evidence remains pending.

2026-06-28 update: resilience evidence is now release-bound before it can support `DR-ROLLBACK-001` or `DR-BACKUP-001`. `tools/resilienceprobe` requires `-release-candidate` and `-service-version`, its rollback/degradation/backup/restore/restored-smoke artifacts must contain exact release markers, the staging evidence recorder rejects resilience reports whose `release_candidate` differs from the manifest, and strict staging manifest validation rejects DR summaries that omit the exact manifest release candidate. This is local/probe-tool hardening; the actual staging rollback and backup/restore drills remain pending external evidence.

2026-06-28 update: strict-release validation now segment-guards `DR-ROLLBACK-001` and `DR-BACKUP-001` resilienceprobe summaries. Rollback readiness, rollout undo, degradation fallback, backup snapshot, restore drill, and restored-database smoke segments must each carry their own required markers, so a broad resilience summary cannot borrow proof across rollback, degradation, backup, restore, and smoke segments. The actual staging rollback and backup/restore drills remain pending external evidence.

2026-06-30 update: backup/restore evidence now fails closed when the restore drill misses its declared recovery-time objective. `tools/resilienceprobe` rejects `restore-drill-artifact` evidence when structured `restore_duration_minutes` is greater than `rto_minutes`, `tools/record-staging-evidence.mjs` rejects manually weakened resilience reports with the same contradiction, and strict-release validation rejects recorded `DR-BACKUP-001` summaries whose restore segment admits `restore_duration_minutes > rto_minutes`. Focused local evidence: `cmd.exe /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& rtk go test ./tools/resilienceprobe -count=1 -timeout=90s"` passed 30 tests, and `cmd.exe /c "call tools\use-project-path.cmd >nul && rtk node --test --test-reporter=dot tools\record-staging-evidence.test.mjs tools\validate-staging-evidence.test.mjs tools\verify-production-readiness.test.mjs"` passed. Real rollback, degradation, backup, restore, and restored-database staging evidence remains pending external proof.

2026-06-30 update: rollback degradation evidence now fails closed unless AI/Zoom fallback proof includes structured `ai_fault=true`, `zoom_offline_fallback=true`, `non_ai_routes_healthy=true`, and `zoom_circuit_open=true` markers in both probe JSON and summary text. `tools/resilienceprobe` parses and emits those fields for `degradation-drill-artifact`, `tools/record-staging-evidence.mjs` rejects resilience reports whose degradation probe lacks them, and strict-release validation requires the same markers in recorded `DR-ROLLBACK-001` summaries. Focused local evidence: `cmd.exe /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& rtk go test ./tools/resilienceprobe -count=1 -timeout=90s"` passed 30 tests, and `cmd.exe /c "call tools\use-project-path.cmd >nul && rtk node --test --test-reporter=dot tools\record-staging-evidence.test.mjs tools\validate-staging-evidence.test.mjs tools\verify-production-readiness.test.mjs"` passed. Real rollback/degradation staging evidence remains pending external proof.

2026-06-29 update: rollback/backup resilience evidence now rejects failed-drill contradiction markers. `tools/resilienceprobe`, `tools/record-staging-evidence.mjs`, strict-release validation, and the staging evidence contract reject resilience artifacts or summaries that admit failed rollback, rollout undo, degradation, backup, restore, smoke, RPO, or RTO conditions even if required success markers also appear. Real staging rollback/degradation and backup/restore drills remain pending external proof.

2026-06-28 update: `tools/loadtest` now canonicalizes performance artifact URLs before distinctness checks, so default HTTPS ports, host-case differences, or reordered query strings cannot let one artifact masquerade as separate HTTP replica/dependency telemetry or WebSocket replica/reconnect/polling/Redis telemetry proof. External staging load evidence remains pending.

2026-06-28 update: `tools/record-staging-evidence.mjs` now canonicalizes artifact URLs before every shared distinct-artifact check, so manually recorded or probe-backed staging evidence cannot reuse one URL through default-port, host-case, query-order, or fragment aliases across deployment, security, mobile, Zoom, AI, resilience, observability, or performance proof roles.

2026-06-28 update: `tools/deploymentprobe` and `tools/securityprobe` now also canonicalize their own artifact URLs before distinctness checks, so Terraform backend/plan/apply-or-approval evidence and IRSA/SecretProviderClass/synced-secret/IAM/access-test evidence fail before report emission when one artifact is reused through URL aliases. Real Terraform, Kubernetes, IRSA, CSI, and scoped-secret staging evidence remains pending external proof.

2026-06-28 update: `tools/aiprobe` and `tools/zoomprobe` now canonicalize their own artifact URLs before distinctness checks, so AI provider/generation/degradation/citation/audit evidence and Zoom OAuth/meeting/resilience/webhook/URL-validation/duplicate/mapping evidence fail before report emission when one artifact is reused through URL aliases. Real AI provider and Zoom staging evidence remains pending external proof.

2026-06-29 update: `tools/aiprobe` now rejects reserved documentation/sample artifact hosts such as `.example`, `example.com`, `.test`, and `.invalid` before emitting `EXT-AI-001`, matching strict staging validation and preventing sample HTTPS hosts from standing in for AI provider, generation, degradation, citation, or audit evidence. Real AI provider staging evidence remains pending external proof.

2026-06-29 update: AI release evidence now rejects citation/audit contradiction markers. `tools/aiprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation reject AI artifacts or summaries that state citation verification, citations, audit logging, audit persistence, `ai_request_logs`, or `citation_trails` were disabled even when the same text also contains the required citation rejection/acceptance or tenant-RLS audit markers. Real AI provider staging evidence remains pending external proof.

2026-06-29 update: strict staging validation now requires `staging artifact` provenance plus exact release/service markers on every `EXT-AI-001` provider-config, generation-route, timeout/degradation, citation-verification, and audit-persistence proof segment instead of allowing broad aiprobe summary text to lend markers across segments. `tools/validate-staging-evidence.test.mjs` pins provider provenance, generation-proof borrowing, and timeout-proof borrowing regressions. Real AI provider, degradation, citation, and audit persistence staging artifacts remain pending external proof.

2026-06-30 update: AI citation verification evidence now binds to the persisted audit row with a concrete citation trail ID. `tools/aiprobe` parses and emits structured `citation_id` fields for both `ai-citation-verification` and `ai-audit-persistence`, rejects missing IDs, and fails when the audit citation ID differs from the citation-verification artifact. `tools/record-staging-evidence.mjs` and strict-release validation mirror the same requirement, so manually edited `EXT-AI-001` evidence cannot pair one verified citation artifact with an unrelated audit/citation-trail row. Focused current evidence: `cmd.exe /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& rtk go test ./tools/aiprobe -count=1 -timeout=90s"` passed 27 tests, and `cmd.exe /c "call tools\use-project-path.cmd >nul && rtk node --test --test-reporter=dot tools\record-staging-evidence.test.mjs tools\validate-staging-evidence.test.mjs tools\verify-production-readiness.test.mjs"` passed. Real AI provider, degradation, citation, and audit persistence staging artifacts remain pending external proof.

2026-06-30 update: AI generation and audit persistence evidence now bind to the same concrete tenant principal. `tools/aiprobe` parses and emits structured `organization_id` and `user_id` fields for both `ai-generation-route` and `ai-audit-persistence`, requires exact `organization_id=<id>` and `user_id=<id>` summary markers, and fails when the audit row differs from the generation artifact. `tools/record-staging-evidence.mjs` and strict-release validation mirror the same requirement, so manually edited `EXT-AI-001` evidence cannot pair one tenant/user generation proof with an unrelated audit row. Real AI provider, degradation, citation, and audit persistence staging artifacts remain pending external proof.

2026-06-29 update: `tools/deploymentprobe` now rejects reserved documentation/sample artifact hosts such as `.example`, `example.com`, `.test`, and `.invalid` before emitting `DEPLOY-TF-001` or `DEPLOY-K8S-001`, matching strict staging validation and preventing sample HTTPS hosts from standing in for Terraform backend, plan, apply/approval, Kubernetes rollout, or workload-resource evidence. Real Terraform and Kubernetes staging evidence remains pending external proof.

2026-06-30 update: `tools/deploymentprobe`, the staging evidence recorder, and strict-release validation now require `DEPLOY-K8S-001` rollout and workload-resource proof to use canonical-distinct HTTPS artifact URLs and emit `distinct_kubernetes_artifacts=true` in `kubernetes-workload-resources`. This prevents one broad `kubectl` artifact or URL alias from satisfying both Kubernetes rollout and resource proof roles. This is local evidence-contract hardening only; real staging Kubernetes rollout/resource proof remains pending external proof.

2026-07-01 update: Kubernetes workload image evidence is now structured end-to-end. `tools/deploymentprobe` emits `concrete_image_digests`, `workload_image_digests`, and per-workload `image_digests` for API/web/Rust workload resources, while the staging evidence recorder rejects hand-recorded `DEPLOY-K8S-001` reports whose structured digest fields are missing or do not match the summary markers. Strict-release validation also requires `concrete_image_digests=3` and `workload_image_digests=3`. Real staging Kubernetes rollout/resource proof remains pending external proof.

2026-06-29 update: `tools/zoomprobe` now rejects reserved documentation/sample artifact hosts such as `.example`, `example.com`, `.test`, and `.invalid` before emitting `EXT-ZOOM-001`, matching strict staging validation and preventing sample HTTPS hosts from standing in for Zoom OAuth, meeting/fallback, resilience, webhook, URL-validation, duplicate, or mapping evidence. Real Zoom staging evidence remains pending external proof.

2026-06-29 update: Zoom webhook release evidence now rejects signature-verification contradiction markers. `tools/zoomprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation reject Zoom webhook artifacts or summaries that state signature verification was disabled, bypassed, or skipped even when the same text also contains the required signed/invalid/replay markers. Real Zoom staging evidence remains pending external proof.

2026-06-29 update: current focused local Zoom evidence passed for bounded client resilience, webhook signature/idempotency/mapping behavior, retryable state-mutation failure handling, and release-evidence artifact validation. Zoom webhook delivery IDs are now reserved while in flight, marked processed only after successful state mutation, and released on Redis/state-manager failure so a retried signed delivery can still update the mapped room. `cmd /c "call tools\use-project-path.cmd >nul && set GOCACHE=C:\dev\ScriptureForgeAI\.gocache&& rtk go test ./internal/adapters/integration_zoom ./tools/zoomprobe -count=1 -timeout=90s -v"` passed 43 tests across two packages. This is local adapter/probe coverage only; live staging Zoom OAuth, webhook delivery, fallback, duplicate, and room-mapping proof remains pending under `EXT-ZOOM-001`.

2026-06-29 update: strict staging validation now requires staging artifact provenance plus exact release/service markers on every `EXT-ZOOM-001` OAuth, meeting/fallback, timeout/circuit fallback, webhook signature, URL-validation, duplicate, and mapping proof segment instead of allowing a broad zoomprobe summary to lend markers across segments. `tools/validate-staging-evidence.test.mjs` pins the OAuth provenance regression. This is contract/tooling hardening only; real staging Zoom OAuth, webhook, fallback, duplicate, and room-mapping artifacts remain pending external proof.

2026-06-30 update: Zoom room-mapping release evidence now requires concrete `meeting_external_id=<id>` and `internal_room_id=<id>` values. `tools/zoomprobe` extracts both identifiers into structured JSON fields, its summaries include the exact IDs, `tools/record-staging-evidence.mjs` rejects missing or mismatched structured mapping IDs, and strict staging validation rejects label-only room-mapping summaries. This is local evidence-contract hardening only; real staging Zoom OAuth, webhook, fallback, duplicate, and room-mapping artifacts remain pending external proof.

2026-06-30 update: Zoom webhook signature release evidence now requires concrete signature header values, not only header names. `tools/zoomprobe` extracts `x-zm-signature=<v0 signature>` and `x-zm-request-timestamp=<epoch>` into structured JSON fields, emits the exact markers in the webhook signature summary, and the staging evidence recorder plus strict-release validation reject label-only or mismatched webhook signature proof. Real staging Zoom OAuth, webhook, fallback, duplicate, and room-mapping artifacts remain pending external proof.

2026-06-30 update: Zoom duplicate webhook release evidence now requires a concrete Zoom tracking header value, not only the header name. `tools/zoomprobe` extracts `x-zm-trackingid=<id>` into structured JSON `tracking_id`, emits the exact marker in the duplicate-idempotency summary, and the staging evidence recorder plus strict-release validation reject missing, label-only, or mismatched tracking ID proof. Real staging Zoom OAuth, webhook, fallback, duplicate, and room-mapping artifacts remain pending external proof.

2026-06-29 update: `tools/observabilityprobe` now rejects reserved documentation/sample artifact hosts such as `.example`, `example.com`, `.test`, and `.invalid` before emitting `OBS-OTEL-001` or `OBS-ALERT-001`, matching strict staging validation and preventing sample HTTPS hosts from standing in for collector, API/Rust metrics, trace, log, dashboard, alert-rule, alert-delivery, or retention evidence. Real observability backend, dashboard, alert-delivery, and retention evidence remains pending external proof.

2026-06-29 update: `tools/observabilityprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation now require `staging artifact` provenance on every `OBS-OTEL-001` collector/API metrics/Rust metrics/trace/log segment and every `OBS-ALERT-001` dashboard/rules/delivery/retention segment. Strict validation now segment-guards collector, API metrics, and Rust metrics proofs in addition to trace/log and alert proofs, so a broad observability summary cannot borrow staging provenance or metric markers from another segment. Real observability backend, dashboard, alert-delivery, and retention evidence remains pending external proof.

2026-06-29 update: `tools/validate-observability.mjs` is now import-safe and covered by `tools/validate-observability.test.mjs`, which proves the current observability artifacts pass and rejects drift that drops AI inference dashboard metrics, room broadcast-drop alerts, API trace ID guards, Rust traceparent observability, or external Rust metrics staging proof. The local tooling-test gate, GitHub Actions tooling-test command, and `tools/validate-ci-workflow.mjs` all require this regression test. Real observability backend, dashboard, alert-delivery, and retention evidence remains pending external proof.

2026-06-29 update: `OBS-OTEL-001` tenant-aware log evidence now requires concrete normalized principal markers in the deployed log-correlation segment. `tools/observabilityprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation require `tenant_id=`, `user_id=`, and `role=` markers on `log-backend-trace-correlation`, preventing bare schema field names from satisfying tenant-aware telemetry proof. Focused local evidence passed for `tools/observabilityprobe`, staging evidence recorder/validator regressions, production-readiness verifier regressions, and `tools/validate-observability.mjs`. Real deployed collector/log backend evidence remains pending external proof.

2026-06-30 update: `OBS-OTEL-001` tenant-aware log evidence now binds to exact structured principal values. `tools/observabilityprobe` requires `--tenant-id`, `--user-id`, and `--role` inputs for OTEL evidence, persists them in the probe JSON report, and requires matching `tenant_id=<id>`, `user_id=<id>`, and `role=<role>` markers in `log-backend-trace-correlation`. `tools/record-staging-evidence.mjs` and strict-release validation reject reports or summaries that only carry bare principal field names or mismatched values. This is evidence-tool hardening only; deployed collector/log backend proof remains pending under `OBS-OTEL-001`.

2026-07-01 update: Go API access logs now emit canonical architecture fields `timestamp` and `severity` alongside legacy-compatible `at` and `level` aliases, and local tests prove the canonical fields match the emitted access-log event. `tools/observabilityprobe`, the staging evidence recorder, strict-release validation, and `tools/validate-observability.mjs` now require `timestamp=` and `severity=` markers on `log-backend-trace-correlation` evidence. This closes a local structured-log schema gap; real deployed log backend and collector proof remain pending under `OBS-OTEL-001`.

2026-06-30 update: `OBS-ALERT-001` alert delivery evidence now has to name the actual staged alert delivery. `tools/observabilityprobe` requires `-alert-name`/`STAGING_ALERT_NAME` and `-alert-receiver`/`STAGING_ALERT_RECEIVER`, persists them as `alert_name` and `alert_receiver`, and requires the Alertmanager delivery artifact to include `alertname=<alert_name>`, `receiver=<alert_receiver>`, and `delivery_id=<id>`. `tools/record-staging-evidence.mjs` cross-checks the report fields against the delivery segment, and strict-release validation rejects generic `success receiver delivered test alert` summaries without concrete alert identity. Real deployed Alertmanager delivery evidence remains pending external proof.

2026-06-30 update: `tools/observabilityprobe` now compares the extracted trace/log/alert identity fields against the configured probe inputs before emitting passing `OBS-OTEL-001` or `OBS-ALERT-001` JSON. Trace and log proof must extract the same `trace_id`, `route`, and `method` as the report, log proof must extract the same `tenant_id`, `user_id`, and `role`, and alert delivery proof must extract the same `alertname` and `receiver`; later marker text cannot mask a different first event. This is evidence-tool hardening only; real staging trace/log backend and Alertmanager delivery proof remains pending external evidence.

2026-07-01 update: `OBS-OTEL-001` API and Rust metrics evidence now rejects idle all-zero metric scrapes. `tools/observabilityprobe` requires positive Prometheus samples for API request/duration/AI/dependency counters and Rust embedding/vector-search request counters before emitting `api_metrics_samples_positive=true` and `rust_metrics_samples_positive=true`; the staging evidence recorder, strict-release validation, and staging evidence contract require those markers too. This keeps observability evidence tied to exercised staging traffic instead of a scrapeable metrics page; real deployed collector, metrics, trace/log, dashboard, alert, and retention evidence remains pending external proof.

2026-06-30 update: `OBS-ALERT-001` alert-delivery evidence now rejects contradiction markers that indicate a notification was silenced, muted, inhibited, suppressed, not delivered, failed, or dry-run. `tools/observabilityprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation all enforce the same exclusion so Alertmanager evidence cannot satisfy production readiness by combining delivery markers with non-delivery status text. Real deployed Alertmanager delivery evidence remains pending external proof.

2026-06-29 update: `tools/stagingprobe` now rejects reserved documentation/sample API/web base hosts and TLS/browser-smoke artifact hosts such as `.example`, `example.com`, `.test`, and `.invalid` before emitting `DEPLOY-TLS-001` or `CLIENT-WEB-001`, matching strict staging validation and preventing sample HTTPS hosts from standing in for deployed API readiness, web root, TLS, redirect, browser auth, encrypted journal, or room/WebSocket smoke evidence. Real DNS, ACM, ALB/TLS, API/web, and deployed browser-smoke evidence remains pending external proof.

2026-06-29 update: `tools/securityprobe` now rejects reserved documentation/sample artifact hosts such as `.example`, `example.com`, `.test`, and `.invalid` before emitting `SEC-SECRETS-001`, matching strict staging validation and preventing sample HTTPS hosts from standing in for IRSA service-account, SecretProviderClass, synced-secret, IAM-policy, or scoped access-test evidence. Real IRSA, CSI, synced-secret, scoped IAM, access-test, and DB-user staging evidence remains pending external proof.

2026-06-30 update: `SEC-SECRETS-001` evidence is now workload-identity bound. `tools/securityprobe`, the staging evidence recorder, and strict-release validation require `namespace=staging`, `service_account=scriptureforge-api`, and concrete `role_arn=arn:aws:iam::...:role/...` markers on the relevant IRSA, SecretProviderClass, IAM policy, and scoped access-test segments, and strict validation now requires every security segment label to be present with its own markers instead of accepting borrowed broad summaries. Real IRSA/CSI/scoped-secret staging artifacts remain pending external proof.

2026-06-30 update: secret workload identity evidence now rejects prefix-only IAM role markers. `tools/securityprobe` extracts a concrete `role_arn=arn:aws:iam::<12-digit-account>:role/<role>` from IRSA, SecretProviderClass, IAM policy, and scoped access-test artifacts and emits the exact ARN in the JSON report summary; `tools/record-staging-evidence.mjs` and strict-release validation require the same ARN shape before accepting `SEC-SECRETS-001`. Real staged AWS account/role proof remains pending external evidence.

2026-06-30 update: scoped database-user evidence now preserves structured role and grant fields instead of relying only on summary text. `tools/securityprobe` emits `current_user`, false-valued privilege flags, `privileged_operation_denied`, `app_grants_verified`, `app_grant_tables`, and `app_grants` in the `database-scoped-user` JSON report, while the staging evidence recorder rejects reports whose structured values do not prove `scriptureforge_app` with no superuser/RLS-bypass/role/DB-create privilege and full app grants. Strict-release validation also binds `connected as` and `current_user` to `scriptureforge_app` in the same segment. Real staged scoped `DATABASE_URL` proof remains pending under `SEC-DBUSER-001`.

2026-06-30 update: synced-secret evidence now requires concrete Secrets Store CSI ownership proof. `tools/securityprobe`, the staging evidence recorder, and strict-release validation require the redacted synced-secret metadata segment to include `managed by secrets-store.csi.k8s.io`, `ownerReferences`, and `secrets-store.csi.k8s.io/managed=true` before `SEC-SECRETS-001` can support final readiness. Real staged synced-secret/SecretProviderClass ownership evidence remains pending external proof.

2026-06-30 update: resilience backup/restore probe output now preserves exact recovery identifiers and objective numbers in the report summaries. `tools/resilienceprobe` emits `snapshot_id=<id>`, `kms_key_id=<id>`, `rpo_minutes=<n>`, `restore_job_id=<id>`, `source snapshot_id=<id>`, `rto_minutes=<n>`, and `restore_duration_minutes=<n>` markers matching its structured JSON fields, aligning the producer with the staging evidence recorder and strict-release `DR-BACKUP-001` validation. Real staged RDS backup, restore drill, and restored-database smoke artifacts remain pending external proof.

2026-06-30 update: resilience rollback/degradation and backup/restore evidence now has producer-side run-identity binding. `tools/resilienceprobe` requires `-load-run-id` or `STAGING_LOAD_RUN_ID`, persists `load_run_id` in the JSON report, and rejects rollback readiness, rollout, degradation, backup snapshot, restore drill, or restored-database smoke artifacts that omit the matching `load_run_id=<same run>` marker. This aligns the producer with recorder and strict-release `DR-ROLLBACK-001`/`DR-BACKUP-001` load-run validation so same-release but different-run DR artifacts cannot satisfy readiness. Real staging rollback, degradation, backup, restore, and restored-database smoke evidence remains pending external proof.

2026-07-01 update: rollback readiness evidence now binds concrete rollback version values across producer, recorder, and strict-release validation. `tools/resilienceprobe` emits `pre_rollback_version=<version>`, `post_rollback_version=<version>`, `rolled_back_from=<version>`, and `rolled_back_to=<version>` markers plus matching JSON fields, then rejects rollback evidence where the after-rollback readiness artifact does not point back to the before-rollback version or forward to the post-rollback version. `tools/record-staging-evidence.mjs` and `tools/validate-staging-evidence.mjs` reject the same inconsistency for manually recorded or strict-release evidence. Real staging rollback/degradation evidence remains pending external proof.

2026-07-01 update: resilience drill artifacts now require provenance in the fetched artifact content itself. `tools/resilienceprobe` rejects rollback readiness, rollout, degradation, backup snapshot, restore drill, and restored-database smoke artifacts that omit the `staging artifact` marker instead of adding that marker only to passing summaries. Focused regression coverage proves an otherwise complete backup snapshot artifact without staging provenance fails before `DR-BACKUP-001` can pass; real staging rollback, degradation, backup, restore, and restored-database smoke evidence remains pending external proof.

2026-07-01 update: backup snapshot evidence now requires a concrete customer-managed KMS key marker. `tools/resilienceprobe` extracts structured `kms_key_id`, emits the matching `kms_key_id=<id>` summary marker, and fails `backup-snapshot-artifact` if the snapshot proof only says generic encryption/KMS. The staging evidence recorder and strict manifest validator also reject `DR-BACKUP-001` evidence without structured and summary-level `kms_key_id` proof. Real staged RDS backup, restore drill, and restored-database smoke artifacts remain pending external proof.

2026-07-01 update: rollback/degradation and backup/restore evidence now persists structured recovery proof into the staging manifest. `tools/record-staging-evidence.mjs` records `structured_report.rollback_degradation_proof` for `DR-ROLLBACK-001` and `structured_report.backup_restore_proof` for `DR-BACKUP-001`; strict-release validation rejects drift in release/run identity, artifact targets, rollback version linkage, degradation booleans, snapshot/KMS IDs, RPO/RTO, restore job/source snapshot, or restore duration. Real staging rollback, degradation, backup, restore, and restored-database smoke evidence remains pending external proof.

2026-07-01 update: strict-release validation now revalidates retained resilience structured targets. `DR-ROLLBACK-001` readiness, rollout, after-rollback, and degradation targets and `DR-BACKUP-001` backup, restore, and restored-smoke targets must remain HTTPS, non-local, and distinct after canonicalization. This prevents final manifests from retaining strong rollback/backup summaries while drifting structured recovery targets to local or duplicated artifacts. Real staging rollback, degradation, backup, restore, and restored-database smoke evidence remains pending external proof.

2026-06-30 update: secret workload identity evidence now binds all secret-delivery artifacts to one workload role. `tools/securityprobe` emits structured `role_arn` fields and fails reports with `role_arn_mismatch=true` when IRSA, SecretProviderClass, IAM policy, and scoped access-test artifacts name different roles; `tools/record-staging-evidence.mjs` and strict-release validation reject mismatched role ARN summaries as well. Real staged AWS account/role proof remains pending external evidence.

2026-06-29 update: `tools/resilienceprobe` now rejects reserved documentation/sample artifact hosts such as `.example`, `example.com`, `.test`, and `.invalid` before emitting `DR-ROLLBACK-001` or `DR-BACKUP-001`, matching strict staging validation and preventing sample HTTPS hosts from standing in for rollback readiness, rollout undo, degradation fallback, backup snapshot, restore drill, or restored-database smoke evidence. Real rollback, degradation, backup, restore, and restored-database staging evidence remains pending external proof.

2026-06-29 update: `tools/abuseprobe` now rejects reserved documentation/sample API base, web origin, and config artifact hosts such as `.example`, `example.com`, `.test`, and `.invalid` before emitting `ABUSE-LIMIT-001`, matching strict staging validation and preventing sample HTTPS hosts from standing in for auth, refresh, account-scoped login, AI, journal, room, WebSocket, or redacted abuse-config evidence. Real staging ingress abuse/rate-limit evidence remains pending external proof.

2026-06-29 update: WebSocket origin validation now rejects staging/production upgrades when the configured or requested origin is insecure, local/private, or a reserved documentation/sample host such as `.example`, `example.com`, `.test`, or `.invalid`, even if `ALLOWED_WS_ORIGINS` exactly matches it. Localhost/no-origin fallback remains limited to local development/test shells. Real deployed WebSocket ingress evidence remains pending external proof.

2026-06-29 update: `tools/loadtest` now rejects reserved documentation/sample HTTP/WSS load targets, WebSocket origins, and load artifact hosts such as `.example`, `example.com`, `.test`, and `.invalid` before producing staging HTTP/WebSocket load evidence. This aligns deployed load/performance proof with the stricter WebSocket origin guard and prevents sample domains from satisfying `PERF-HTTP-001`, `PERF-WS-001`, or `DATA-REDIS-001`. Real staging performance/load evidence remains pending external proof.

2026-06-29 update: HTTP and WebSocket performance evidence now must preserve the explicit production-duration assertion, not only declare the target duration. `tools/loadtest`, `tools/record-staging-evidence.mjs`, and strict-release validation require `duration_ms>=60000` alongside numeric `duration_ms` checks and `production_min_duration_ms=60000`, preventing short load runs from satisfying `PERF-HTTP-001` or `PERF-WS-001`. Real staging performance/load evidence remains pending external proof.

2026-06-30 update: WebSocket reconnect evidence now must prove sequence continuity, not only reconnect acceptance. `tools/loadtest` requires the reconnect artifact to include `ws_reconnect_sequence_continues=true`, its result summary emits the same marker, and the staging evidence recorder plus strict-release validation reject `PERF-WS-001` evidence without that marker. Real staging WebSocket reconnect/load evidence remains pending external proof.
2026-07-01 update: WebSocket reconnect continuity is now structured in load evidence, not summary-only. `tools/loadtest` emits `ws_reconnect_sequence_continues=true` as a JSON boolean after validating the reconnect artifact, and the staging evidence recorder rejects `PERF-WS-001`/`DATA-REDIS-001` reports that omit or falsify that field even if the summary text still carries the marker. Real staging WebSocket/Redis load evidence remains pending external proof.

2026-06-30 update: WebSocket/Redis load evidence summaries now preserve concrete side-artifact URL provenance. `tools/loadtest` emits HTTPS `ws_replica_artifact_url`, `ws_reconnect_artifact_url`, `ws_polling_artifact_url`, and `redis_telemetry_artifact_url` markers in the WebSocket result summary, and the staging evidence recorder plus strict-release validation require those URL markers before `PERF-WS-001` or `DATA-REDIS-001` can support readiness. This prevents generic verified labels from standing in for the actual fetched staging artifacts; real staging WebSocket/Redis load evidence remains pending external proof.

2026-06-30 update: load side-artifact evidence now preserves parsed concrete values instead of marker-only proof. `tools/loadtest` parses and persists `http_replica_count`, `ws_replica_count`, `dependency_postgres_p99_ms`, `dependency_redis_p99_ms`, and `room_broadcast_drops`; `tools/record-staging-evidence.mjs` and strict-release validation reject single-replica artifacts, dependency p99 values above the 200ms production target, and nonzero room broadcast drops before `PERF-HTTP-001`, `PERF-WS-001`, or `DATA-REDIS-001` can support readiness. This remains evidence-contract hardening only; real staging HTTP/WebSocket/Redis load proof remains pending external evidence.

2026-07-01 update: WebSocket side-artifact room binding is now structured, not prose-only. `tools/loadtest` parses `room_id=<id>` from reconnect, HTTP polling fallback, and Redis telemetry artifacts, emits `ws_reconnect_room_id`, `ws_polling_room_id`, and `redis_telemetry_room_id`, and rejects any value that differs from the run's `ws_room_id`. The staging evidence recorder and strict-release validator also require those structured fields and summary markers to equal `ws_room_id` before `PERF-WS-001` or `DATA-REDIS-001` can support readiness. This is local evidence-contract hardening only; real staging WSS/Redis load proof remains pending external evidence.

2026-07-01 update: WebSocket performance evidence now requires a full structured sequence range before either recorder or strict-release validation accepts it. `PERF-WS-001` must carry `ws_expected_events`, `ws_unique_sequences`, `ws_min_sequence=1`, `ws_max_sequence`, and `ws_polling_latest_sequence`, with unique/max/polling values matching the expected event count. This mirrors the Redis evidence range guard and prevents a WebSocket throughput pass from omitting or weakening contiguous-room sequence proof. Real staging WSS/Redis load proof remains pending external evidence.

2026-07-01 update: WebSocket polling fallback proof now persists the deployed polling artifact's parsed `latest_sequence` as structured `ws_polling_artifact_latest_sequence`. `tools/loadtest`, `tools/record-staging-evidence.mjs`, and strict-release validation require that value to equal `ws_max_sequence` in addition to `ws_polling_latest_sequence=ws_max_sequence`, preventing a hand-edited load report from pairing a fresh in-run sequence range with a stale polling endpoint artifact. Real staging WSS/Redis load proof remains pending external evidence.

2026-06-30 update: Kubernetes rollout evidence is now release-bound at the rollout segment, not only at the workload-resource segment. `tools/deploymentprobe`, `tools/record-staging-evidence.mjs`, and strict-release validation require `kubernetes-rollout-status` evidence to carry exact `release_candidate=<manifest release_candidate>` and `service_version` markers before `DEPLOY-K8S-001` can support readiness. Real staged Kubernetes rollout/resource artifacts remain pending external proof.

2026-06-30 update: TLS readiness evidence now preserves concrete certificate identity instead of only `cert_not_after`. `tools/stagingprobe` emits `cert_hostname=<hostname>` and `cert_issuer=<issuer>` in structured JSON plus summaries, while `tools/record-staging-evidence.mjs` and strict-release validation reject TLS evidence where the certificate hostname is missing, mismatched to the target, or has an empty issuer. This is evidence-contract hardening only; real DNS, ACM, ingress, HTTPS redirect, and TLS policy proof remains pending under `DEPLOY-TLS-001`.

2026-06-30 update: TLS and web staging evidence is now load-run-bound. `tools/stagingprobe` requires `-load-run-id`/`STAGING_LOAD_RUN_ID`, emits `load_run_id` in the JSON report, appends `load_run_id=<id>` to API live/readiness, API/web TLS, redirects, web root, and browser-smoke summaries, and `tools/record-staging-evidence.mjs` plus strict-release validation reject missing or mixed run IDs for `DEPLOY-TLS-001` and `CLIENT-WEB-001`. This is evidence-tool hardening only; real deployed staging TLS and browser-smoke proof remains pending external evidence.

2026-06-29 update: `tools/tenantprobe` now rejects reserved documentation/sample API base and DB/RLS artifact hosts such as `.example`, `example.com`, `.test`, and `.invalid` before emitting `DATA-RLS-001`, matching strict staging validation and preventing sample HTTPS hosts from standing in for deployed tenant API or database RLS proof. Real deployed API/database RLS evidence remains pending external proof.

2026-06-29 update: deployed tenant DB/RLS evidence must now identify the actual application role. `tools/tenantprobe`, the staging evidence recorder, and strict-release validation require `current_user=scriptureforge_app` in the `database-rls-context-proof` segment, so generic `current_user` or unnamed non-superuser proof cannot satisfy `DATA-RLS-001`. Real deployed API/database RLS evidence remains pending external proof.

2026-06-30 update: deployed tenant DB/RLS evidence must also include explicit all-table policy coverage markers. `tools/tenantprobe`, the staging evidence recorder, and strict-release validation now require `rls_tables_verified=9`, `rls_forced_tables=9`, and `rls_policy_scope=app.current_org_id` in the `database-rls-context-proof` segment, so a DB proof cannot rely on a loose list of table names without confirming count, FORCE RLS coverage, and tenant policy scope. Real deployed API/database RLS evidence remains pending external proof.

2026-06-30 update: deployed tenant DB/RLS evidence must bind to UUID-shaped tenant organization IDs. `tools/tenantprobe` now rejects non-UUID `owner_org_id`/`blocked_org_id` inputs, the staging evidence recorder rejects DATA-RLS reports without UUID-shaped structured tenant IDs, and strict-release validation requires the `database-rls-context-proof` segment to carry UUID `app.current_org_id=<owner_org_id>` and `blocked_org_id=<blocked_org_id>` markers for distinct organizations. Real deployed API/database RLS evidence remains pending external proof.

2026-06-30 update: deployed tenant DB/RLS evidence now has structured report fields for the database-session proof. `tools/tenantprobe` emits `application_role=scriptureforge_app`, `row_security=on`, `rls_tables_verified=9`, `rls_forced_tables=9`, and `rls_policy_scope=app.current_org_id` on passing `database-rls-context-proof` probes, and the staging evidence recorder rejects DATA-RLS reports where those fields are missing or mismatched. Real deployed API/database RLS evidence remains pending external proof.

2026-06-30 update: local/container tenant RLS proof markers now depend on actual verbose `go test` PASS lines. `tools/run-rls-db-integration.mjs` parses the DB-backed test output and only emits `rls-db-integration proof:` after every required all-table RLS, handler-level journal/room, WebSocket, AI audit, auth/session, MFA, and workspace-switch tenant test reports `--- PASS`; missing or skipped tests now fail the wrapper instead of receiving semantic proof markers. Real deployed API/database RLS evidence remains pending external proof.

2026-06-30 update: local/container tenant RLS proof summaries now include explicit semantic markers beyond test names. `tools/run-rls-db-integration.mjs` emits table markers for all nine tenant-scoped tables plus all-table same-tenant read success, all-table same-tenant write success, all-table cross-tenant read denial, all-table cross-tenant insert denial, all-table cross-tenant update hiding, all-table cross-tenant delete hiding, journal plaintext rejection, room override denial, WebSocket tenant scoping, AI audit RLS, auth refresh/session RLS, MFA, and workspace-switch tenant matching. `tools/validate-local-gate-report.mjs` rejects local gate reports that omit those markers. Real deployed API/database RLS evidence remains pending external proof under `DATA-RLS-001`.

2026-06-30 update: local/container handler-level tenant RLS proof summaries now name journal and room read/write outcomes explicitly. `tools/run-rls-db-integration.mjs` emits `journal_handler_same_tenant_write_pass=true`, `journal_handler_same_tenant_read_pass=true`, `journal_handler_cross_tenant_read_denied=true`, `journal_handler_cross_tenant_write_denied=true`, `room_handler_same_tenant_write_pass=true`, `room_handler_same_tenant_read_pass=true`, `room_handler_cross_tenant_read_denied=true`, and `room_handler_cross_tenant_write_denied=true` only after the required DB-backed handler tests report concrete `--- PASS` lines. `tests/integration/tenant_handler_rls_test.go` also verifies that a mismatched tenant/user journal create leaves the owner row unchanged and persists zero rows for the blocked tenant. Real deployed API/database RLS evidence remains pending under `DATA-RLS-001`.

2026-07-01 update: handler-level RLS proof now covers the HTTP room-state polling endpoint in the DB-backed integration suite. `TestTenantScopedRoomStateHandlerEnforcesRLS` proves a cross-tenant request to `/api/v1/rooms/state/{room_id}` is denied before the polling state store is called, while the same tenant receives the state for the owned room. `tools/validate-rls-schema.mjs` and `tools/run-rls-db-integration.mjs` now require this test and emit room-state-specific proof markers. This is local/container evidence only; deployed API/database tenantprobe artifacts remain pending under `DATA-RLS-001`.

2026-06-30 update: local/container all-table tenant RLS coverage now proves positive write paths as well as denial paths. `TestTenantRLSCoversAllTenantTables` performs rollback-only same-tenant inserts across `organizations`, `users`, `scripture_texts`, `refresh_tokens`, `journal_entries`, `live_rooms`, `room_participants`, `ai_request_logs`, and `citation_trails`, and the RLS wrapper emits `rls_same_tenant_writes_pass_all_tables=true` only after the DB-backed test reports a concrete `--- PASS`. This is local/container evidence only; deployed API/database tenantprobe artifacts remain pending under `DATA-RLS-001`.

2026-07-01 update: local/container all-table tenant RLS coverage now proves same-tenant update and delete paths as distinct positive mutations. `TestTenantRLSCoversAllTenantTables` performs rollback-only same-tenant updates and deletes across `organizations`, `users`, `scripture_texts`, `refresh_tokens`, `journal_entries`, `live_rooms`, `room_participants`, `ai_request_logs`, and `citation_trails`, failing unless each operation affects exactly one row. The RLS wrapper emits `rls_same_tenant_updates_pass_all_tables=true` and `rls_same_tenant_deletes_pass_all_tables=true`, and local gate/schema validation require those markers. This is local/container evidence only; deployed API/database tenantprobe artifacts remain pending under `DATA-RLS-001`.

2026-07-01 update: local tenant/RLS drift validation now also guards production room and WebSocket membership wiring. `tools/validate-rls-schema.mjs` reads `cmd/platform-engine/main.go`, `internal/ports/rooms_http.go`, and `internal/ports/driving_wss.go` and rejects production `RoomHandler` or `SocketConnection` construction that installs a `MembershipValidator` override instead of using the default Postgres/RLS membership path. It also requires both membership implementations to call `auth.SetTenantContext` and query `room_participants` by `organization_id`, `room_id`, and `user_id`. The RLS DB proof marker contract now emits `production_room_membership_uses_db_rls=true` and `production_socket_membership_uses_db_rls=true` after the DB-backed tests pass. This is local production-wiring guard evidence only; deployed API/database tenantprobe artifacts remain pending under `DATA-RLS-001`.

2026-06-30 update: local Go backend test proof now depends on actual verbose WebSocket production-behavior test output. `tools/run-go-core-gate.mjs --mode test` runs `go test ./... -count=1 -timeout=90s -v` and refuses to emit `go-test-gate` markers unless live-room invalid-event, oversized-event, reconnect/polling, missing-state-manager, concurrent sequence, fan-out, dropped-broadcast metric, origin, and polling-fallback tests all report `--- PASS`. This is local gate hardening only; real multi-instance WebSocket/load and Redis sequencing evidence remains pending under `PERF-WS-001` and `DATA-REDIS-001`.

2026-06-30 update: focused disposable DB proof refreshed with the semantic marker guard. `cmd.exe /c "call tools\use-project-path.cmd >nul && rtk node tools\run-rls-db-integration-docker.mjs --container-name scriptureforge-rls-semantic-proof"` passed against disposable `pgvector/pgvector:pg16`, applied migrations, passed the required DB-backed tenant/RLS tests, and emitted the new semantic `rls-db-integration proof:` marker list. This is local/container evidence only; deployed API/database tenantprobe artifacts remain pending under `DATA-RLS-001`.

2026-06-30 update: CI Postgres RLS integration now runs `node tools/run-rls-db-integration.mjs --bin go` instead of raw `go test`, so GitHub Actions must verify the same required `--- PASS` lines and semantic `rls-db-integration proof:` marker contract used by local gate reports. `tools/validate-ci-workflow.mjs` rejects workflow drift back to raw RLS test execution. This strengthens clean pushed CI evidence, but deployed tenantprobe evidence remains pending under `DATA-RLS-001`.

2026-06-30 update: scoped database-user production evidence now has to name the exact application grant table set, not only the count. `tools/securityprobe` emits `app_grant_table_names=organizations,users,scripture_texts,refresh_tokens,journal_entries,live_rooms,room_participants,ai_request_logs,citation_trails` alongside `app_grant_tables=9` and `app_grants=SELECT,INSERT,UPDATE,DELETE`; `tools/validate-staging-evidence.mjs` rejects `SEC-DBUSER-001` strict-release evidence that omits the table-name binding. Real scoped staging `DATABASE_URL` proof remains pending under `SEC-DBUSER-001`.

2026-06-30 update: `tools/record-staging-evidence.mjs` now enforces the same `SEC-DBUSER-001` table-name binding before recording securityprobe reports into a staging manifest. Hand-recorded reports must carry both structured `app_grant_table_names` and the matching `app_grant_table_names=organizations,users,scripture_texts,refresh_tokens,journal_entries,live_rooms,room_participants,ai_request_logs,citation_trails` summary marker, preventing a count-only scoped-user proof from entering release evidence. Real scoped staging `DATABASE_URL` proof remains pending under `SEC-DBUSER-001`.

2026-06-29 update: `tools/record-staging-evidence.mjs` now rejects reserved documentation/sample hosts such as `.example`, `example.com`, `.test`, and `.invalid` through its shared probe-backed target/artifact validator before recording evidence into the staging manifest. This closes the stale or manually edited probe-report path where sample-domain targets could bypass individual probe fail-closed checks. Real staging evidence remains pending external proof.

2026-06-29 update: `tools/ciprobe` now rejects reserved documentation/sample remote CI run artifact hosts such as `.example`, `example.com`, `.test`, and `.invalid` before emitting `SRC-CI-001`, matching strict staging validation and preventing sample HTTPS hosts from standing in for clean pushed GitHub Actions proof. Real exact-release GitHub Actions evidence remains pending external proof.

2026-06-30 update: `tools/record-staging-evidence.mjs` now rejects `SRC-CI-001` reports whose `github-actions-release-run` target is a local artifact file, non-HTTPS URL, private/local network URL, or reserved placeholder host. `tools/ciprobe` can still validate a downloaded artifact file for debugging, but production readiness recording requires the uploaded HTTPS `ci-release-evidence` artifact URL for the exact release SHA. Real exact-release GitHub Actions evidence remains pending external proof.

2026-06-30 update: strict staging validation now also requires `SRC-CI-001` evidence to show `tools/ciprobe -run-artifact-url https://...ci-release-evidence...` and rejects strict release manifests recorded from `-run-artifact-file artifacts/ci-release-evidence.txt`, so hand-edited or stale manifests cannot bypass the uploaded-artifact requirement.

2026-06-28 update: `tools/observabilityprobe` and `tools/resilienceprobe` now canonicalize their own artifact URLs before distinctness checks, so OTEL/alert evidence and rollback/backup evidence fail before report emission when one artifact is reused through URL aliases. Real observability backend, alert delivery, rollback, and backup/restore staging evidence remains pending external proof.

2026-06-29 update: strict staging evidence validation now rejects reserved placeholder artifact hosts such as `.example`, `example.com`, `.test`, and `.invalid` before a final release claim can pass. This prevents documentation/sample domains from satisfying the HTTPS non-local artifact requirement. Real staging evidence remains pending external proof.

2026-06-28 update: `tools/stagingprobe` now canonicalizes TLS and web browser-smoke artifact URLs before distinctness checks, and it requires DNS and ACM/TLS artifacts to be distinct before emitting `DEPLOY-TLS-001` evidence. Default HTTPS ports, host-case differences, reordered query strings, and fragments can no longer let one DNS/ACM/auth/journal/room artifact satisfy separate TLS or web smoke proof roles. Real DNS, ACM, ingress, HTTPS redirect, and deployed browser smoke evidence remains pending external proof.

2026-06-28 update: the staging evidence contract and recorder now mirror the stagingprobe alias guard. `DEPLOY-TLS-001` requires canonical-distinct DNS and ACM/TLS artifact URLs, `CLIENT-WEB-001` requires canonical-distinct auth, journal, and room browser-smoke artifact URLs, and `tools/record-staging-evidence.mjs` rejects canonical duplicate DNS/ACM report fields even if a report is manually edited or produced by an older probe.

2026-06-28 update: `tools/mobileprobe` and `tools/record-staging-evidence.mjs` now canonicalize mobile EAS/native-device, native AES-GCM, and staging config artifact URLs before distinctness checks, so one mobile proof artifact cannot satisfy separate `CLIENT-MOBILE-001` proof roles through default-port, host-case, query-order, or fragment aliases. Native-device/EAS staging evidence remains pending external proof.

2026-06-30 update: `tools/mobileprobe` now extracts the native crypto artifact's first `provider=` and `native_required=` values and requires them to be `provider=react-native-quick-crypto` and `native_required=true` before emitting passing `CLIENT-MOBILE-001` JSON. Later native markers can no longer mask an earlier fallback/provider line in the same artifact, and focused regressions prove mismatched provider/native-required bindings fail. This is evidence-tool hardening only; EAS/native-device AES-GCM proof remains pending external validation.

2026-06-30 update: mobile native-provider binding is now enforced at every staging evidence ingestion layer. `tools/record-staging-evidence.mjs` requires `mobile-native-crypto-smoke` reports to include structured or extracted `provider=react-native-quick-crypto` and `native_required=true` values, and strict release validation rejects passed `CLIENT-MOBILE-001` summaries whose first `provider=` marker is not `react-native-quick-crypto`. This prevents hand-recorded or stale mobile evidence from bypassing the stronger `tools/mobileprobe` binding check; native-device/EAS proof remains pending external evidence.

2026-06-30 update: mobile native crypto evidence now has to prove stale raw key references are revoked after handle disposal, not only that the disposed handle itself rejects access. `runJournalCryptoSelfTest` attempts encryption with the captured pre-disposal key after `disposeJournalCryptoKey(handle)` and emits `revoked_key_rejected=true` plus `stale raw key rejected` only when that operation fails. `tools/mobileprobe`, `tools/record-staging-evidence.mjs`, strict staging validation, `tools/verify-journal-crypto.mjs`, and mobile smoke tests all require the new markers, preventing native/EAS crypto artifacts from satisfying `CLIENT-MOBILE-001` without key lifecycle proof. Current focused evidence passed: `mobile` smoke, `tools/verify-journal-crypto.mjs`, `go test ./tools/mobileprobe`, and staging evidence validation tests. Native-device/EAS proof remains pending external evidence.

2026-06-30 update: mobile production evidence is now bound to a concrete staging load run. `tools/mobileprobe` requires `-load-run-id`/`STAGING_LOAD_RUN_ID`, emits `load_run_id` in the JSON report, appends `load_run_id=<id>` to each EAS/native-device, native-crypto, and staging-config probe summary, and `tools/record-staging-evidence.mjs` plus strict-release validation reject missing or mixed run IDs for `CLIENT-MOBILE-001`. This is evidence-tool hardening only; real native-device/EAS proof remains pending external evidence.

2026-07-01 update: mobile production evidence now binds EAS/native-device, native AES-GCM smoke, and staging config proof to one concrete app build. `tools/mobileprobe` extracts and emits `mobile_build_id=<id>` for all three `CLIENT-MOBILE-001` probes, rejects mismatched build IDs before report emission, and `tools/record-staging-evidence.mjs` plus strict-release validation reject missing or mixed build IDs for hand-recorded or stale mobile evidence. This is evidence-tool hardening only; real native-device/EAS proof remains pending external evidence.

2026-07-01 update: mobile native crypto release evidence is now persisted as structured manifest proof, not summary-only text. `tools/record-staging-evidence.mjs` writes `structured_report.mobile_native_crypto_proof` for `CLIENT-MOBILE-001`, and strict-release validation rejects drift in provider/native-required mode, AES-GCM round-trip/tamper/associated-data rejection booleans, unique IV proof, lifecycle and zeroization booleans, associated-data salt values, installed device/runtime binding, staging API/WS config, `mobile_build_id`, or `load_run_id`. This is evidence-contract hardening only; real installed staging app/EAS native-device artifacts remain pending external proof.

2026-07-01 update: web and mobile journal clients now validate AES-GCM associated-data salt inputs before encryption/decryption use. `journalAssociatedData()` rejects blank salt IDs plus non-positive or non-integer salt versions, client smoke tests emit `web_crypto_associated_data_input_guard=true` and `mobile_crypto_associated_data_input_guard=true`, and `tools/verify-journal-crypto.mjs` requires the guard in both clients. This closes a local client crypto lifecycle gap; real native-device/EAS proof remains pending external evidence.

2026-07-01 update: web journal crypto smoke now proves AES-GCM IV freshness for repeated same-plaintext encryptions. `web/src/lib/crypto.smoke.mts` encrypts the same journal payload twice with the same key and associated data, fails if either the IV or ciphertext repeats, emits `web_crypto_unique_iv=true`, and `tools/verify-journal-crypto.mjs` plus `tools/validate-local-gate-report.mjs` require the marker. This is local web-client crypto evidence hardening; deployed browser smoke evidence remains pending external proof.

2026-07-01 update: local web/mobile smoke gate validation now requires parity with the current crypto smoke marker set. `tools/validate-local-gate-report.mjs` rejects stale web smoke output that omits associated-data input guards, revoked-key rejection, or import-failure cleanup markers, and rejects stale mobile smoke output that omits associated-data input guards, IV freshness, runtime buffer zeroization, native-required self-test fail-closed, or revoked-key rejection markers. This is local gate evidence hardening; deployed browser smoke and native-device/EAS artifacts remain pending external proof.

2026-07-01 update: mobile build-compatible gate proof now requires the journal crypto verifier output, not only a successful npm wrapper exit. `tools/run-client-command.mjs` supports `--require-output`, local and CI mobile `build:check` gates require `journal crypto verification passed:`, `tools/validate-ci-workflow.mjs` rejects workflow drift that drops the requirement, and `tools/validate-local-gate-report.mjs` rejects stale local reports whose `mobile-build-check` command or output omits the verifier proof. This strengthens local/CI mobile crypto evidence only; native-device/EAS proof remains pending under `CLIENT-MOBILE-001`.

2026-07-01 update: local Go gate proof now pins mounted abuse/rate-limit route coverage. `tools/run-go-core-gate.mjs --mode test` requires verbose PASS lines for auth route throttling, legacy auth alias shared buckets, and mounted auth, AI, journal, rooms, and WebSocket rate-limit route subtests before it emits `go-test-gate` markers. This prevents local release evidence from passing if sensitive-route abuse tests are skipped or removed; staging ingress abuse evidence remains pending under `ABUSE-LIMIT-001`.

2026-07-01 update: local Go gate proof now also pins observability behavior coverage. `tools/run-go-core-gate.mjs --mode test` requires verbose PASS lines for trace ID propagation/normalization, OpenTelemetry span binding, `/metrics` serving and method restrictions, metrics-scrape exclusion, dependency span/metric recording, mock dependency status handling, and architecture profile metrics for WebSockets plus AI inference before it emits `go-test-gate` markers. This strengthens local observability evidence only; deployed collector/backend, dashboard import, alert delivery, and retention evidence remain pending under `OBS-OTEL-001` and `OBS-ALERT-001`.

2026-07-01 update: local WebSocket lifecycle proof now covers disconnect cleanup. `internal/ports/rooms_realtime_test.go` proves a room stream client unsubscribe removes the room hub subscriber and decrements `websocket_active_connections_count` back to zero, and `tools/run-go-core-gate.mjs` requires `TestLiveRoomDisconnectCleansSubscriberAndActiveConnectionMetric` before emitting `go-test-gate` proof. This is local handler evidence; multi-instance staging load and reconnect artifacts remain pending under `PERF-WS-001` and `DATA-REDIS-001`.

2026-07-01 update: release evidence now has a release-wide staging run identity guard. `tools/record-staging-evidence.mjs` rejects recording probe-backed evidence when its `load_run_id` differs from already recorded staging evidence, and strict-release validation rejects completed manifests whose non-CI/non-signoff staging evidence summaries reference more than one `load_run_id`. This prevents deployment, RLS, Rust, client, AI, Zoom, observability, security, resilience, abuse, and performance proof from being stitched together across unrelated staging captures; real staging evidence remains pending external proof.
2026-07-01 update: tenant RLS production evidence now carries exact structured table-set binding, not just count and prose markers. `tools/tenantprobe` emits `rls_table_names` for the database proof, and `tools/record-staging-evidence.mjs` rejects `DATA-RLS-001` reports unless that array exactly matches `organizations,users,scripture_texts,refresh_tokens,journal_entries,live_rooms,room_participants,ai_request_logs,citation_trails`. This strengthens the "every tenant-scoped table" proof path; live staging RLS evidence remains pending external proof.

2026-07-01 update: local/CI RLS integration evidence now requires package-level Go success lines. `tools/run-rls-db-integration.mjs` refuses to emit its tenant/RLS proof markers unless the verbose DB-backed run includes both every required tenant test PASS line and `ok scriptureforge/tests/integration` plus `ok scriptureforge/internal/ports`, preventing stale or pasted PASS snippets from satisfying handler-level tenant isolation proof without both packages actually reporting success. Live staging RLS evidence remains pending external proof.

2026-07-01 update: Terraform and Kubernetes deployment evidence recording now requires a report-level run identity. `tools/record-staging-evidence.mjs` rejects release-bound `DEPLOY-TF-001` and `DEPLOY-K8S-001` reports that omit top-level `load_run_id`, even if individual probe summaries carry matching run markers, and the staging evidence contract now names that report-level requirement. This is evidence-contract hardening only; real Terraform apply/approval and deployed Kubernetes proof remain pending external evidence.

2026-07-01 update: Rust gRPC staging evidence now has the same report-envelope run identity guard. `tools/record-staging-evidence.mjs` rejects `RUST-GRPC-001` reports that omit top-level `load_run_id`, even if `rust-grpc-health`, `rust-metrics`, or API integration summaries include matching run markers, and the staging evidence contract now names the report-level requirement. This is evidence-contract hardening only; deployed Rust health, metrics, and Go API integration proof remain pending external evidence.

2026-07-01 update: Zoom staging evidence now has a report-envelope run identity guard. `tools/record-staging-evidence.mjs` rejects release-bound `EXT-ZOOM-001` reports that omit top-level `load_run_id`, even if OAuth, meeting/fallback, webhook signature, URL validation, duplicate idempotency, and mapping summaries include matching run markers, and the staging evidence contract now names the report-level requirement. This is evidence-contract hardening only; real Zoom OAuth/webhook/fallback/mapping staging proof remains pending external evidence.

2026-07-01 update: local Go gate proof now pins Zoom resilience and webhook coverage. `tools/run-go-core-gate.mjs --mode test` requires verbose PASS lines for bounded timeout fallback, transient retry, circuit fallback, offline fallback without ambient test-mode mocks, dependency metrics, webhook signature/replay denial, duplicate idempotency, retry-safe delivery handling, URL validation, delivery-cache bounds, ended-event handling, and meeting-to-room mapping before it emits `go-test-gate` markers. This strengthens local Zoom evidence only; real staging Zoom OAuth, webhook delivery, fallback, duplicate, and room-mapping proof remains pending under `EXT-ZOOM-001`.

2026-07-01 update: AI staging evidence now has a report-envelope run identity guard. `tools/record-staging-evidence.mjs` rejects release-bound `EXT-AI-001` reports that omit top-level `load_run_id`, even if provider config, authenticated generation, timeout/degradation, citation verification, and audit persistence summaries include matching run markers, and the staging evidence contract now names the report-level requirement. This is evidence-contract hardening only; real AI provider/degradation/citation/audit staging proof remains pending external evidence.

2026-07-01 update: security staging evidence now has a report-envelope run identity guard. `tools/record-staging-evidence.mjs` rejects `SEC-SECRETS-001` and `SEC-DBUSER-001` reports that omit top-level `load_run_id`, even if secret-sync, IRSA, scoped access-test, or database-user summaries include matching run markers, and the staging evidence contract now names the report-level requirement. This is evidence-contract hardening only; real secret-manager/IRSA sync and scoped database-user staging proof remain pending external evidence.

2026-06-28 update: `tools/abuseprobe` and `tools/record-staging-evidence.mjs` now canonicalize abuse config artifact hosts before distinctness checks, so host-case or trailing-dot aliases cannot let the staging API target or web origin masquerade as a separate redacted abuse-config evidence host. Real ingress abuse/rate-limit evidence remains pending external proof.

2026-06-29 update: `tools/securityprobe`, `tools/record-staging-evidence.mjs`, and strict staging validation now require `staging artifact` provenance on every `SEC-SECRETS-001` IRSA service-account, SecretProviderClass, synced-secret metadata, IAM policy, scoped access-test segment, and on the direct `SEC-DBUSER-001` database-scoped-user segment. This prevents generic cloud/security snippets or direct DB checks without explicit staging provenance from satisfying final security evidence. Real staging secret-sync, scoped database user, and cloud access proof remains pending external evidence.

2026-06-29 update: `tools/report-staging-evidence-gaps.mjs` now emits `blocking_item_required_evidence_listed=true` and regression tests prove text/Obsidian gap reports preserve concrete required-evidence lines for each blocker. This keeps the external staging evidence collection checklist machine-checkable instead of only listing blocker IDs. Real staging evidence remains pending external proof.

2026-06-29 update: `tools/validate-local-gate-report.mjs` now rejects `staging-evidence-gap-report` local gate rows that omit rendered `required:` evidence details, even if the proof footer, blocker IDs, and proof markers are present. This prevents a truncated gap report from passing local release evidence while external staging artifacts remain pending.

2026-07-01 update: production evidence probe gate proof now requires every probe package to appear in the uncached Go test output. `tools/run-go-probe-tests.mjs` refuses to emit its source, tenant/RLS, Rust, observability, security, resilience, mobile, deployment, abuse, Zoom, AI, and load proof markers unless `go test` reports an `ok scriptureforge/tools/...` result line for each package in the batch. This strengthens local/CI probe-tool coverage only; deployed staging evidence for those systems remains pending external proof.

2026-08-10 dependency pin refresh: web `next` is now exact-pinned at `16.2.12` and mobile `expo` is exact-pinned at `56.0.17` in both manifests and lockfile roots. Current repo context and dependency-risk fixtures are aligned to those versions; prior dated audit evidence remains historical. This is local reproducibility hardening and does not close the remaining external staging gates.

2026-08-10 gRPC trust-boundary remediation: `services/scripture-engine` now requires mTLS and a 32-byte shared secret in staging/production, binds verified `x-scriptureforge-organization-id` metadata instead of trusting the request organization field, and bounds frames, organization IDs, books, text, vectors, and coordinates. The Go client loads CA/client certificate/client key/server-name configuration and sends the shared secret plus tenant metadata. Terraform models the Secrets Manager inputs and workload env wiring, Rust exposes HTTP `/healthz` for mTLS-incompatible Kubernetes probes, and `tools/rustprobe` requires mTLS files and proves a generated in-test handshake. Local proof passed through `go test ./tools/rustprobe`, `tools/verify-rust-protobuf.mjs`, and `tools/validate-deployment-skeleton.mjs`; deployed certificate issuance, CSI/IRSA injection, rotation, cross-namespace traffic, and `RUST-GRPC-001` health/metrics/API integration evidence remain pending.

2026-08-10 dependency hardening: web PostCSS is updated to `8.5.26`, resolving the `nanoid <3.3.17` high finding; mobile overrides resolve `brace-expansion`, `js-yaml`, `nanoid`, `postcss`, and `uuid@11.1.1`, closing DRR-001 after mobile build/check and xcode CommonJS compatibility proof. The mobile `npm audit --audit-level=high` gate still reports 11 high findings through Metro's `image-size@1.2.1` path; npm's forced fix proposes the breaking Expo `53.0.27` downgrade, so DRR-002 remains an explicit production blocker rather than an unsafe forced migration.

2026-08-10 mobile build-chain hardening: `mobile/metro.config.js` now disables the vulnerable `image-size` HEIF/ICNS/JXL parsers and removes HEIC/AVIF asset extensions from Metro's accepted set; `tools/validate-dependency-risk.mjs` and regression tests verify the controls. The npm audit findings remain open until a patched Expo 56-compatible dependency line is available.

2026-08-10 mobile audit closure: the registry has no unaffected `image-size` release for the Expo 56 Metro lane, so `mobile/vendor/image-size` now provides a dependency-free, bounded compatibility implementation for Metro's safe asset formats and omits HEIF/ICNS/JXL/JXL-stream parsers. The lockfile, `tools/verify-mobile-image-size.test.mjs`, dependency-risk validator, Metro loading smoke, and mobile build/check prove the replacement; `npm audit --audit-level=high` now reports 0 high, 0 critical, and 0 total findings. DRR-002 is closed locally and must be re-evaluated on every Expo/Metro refresh.

2026-08-10 CI runtime hardening: `.github/workflows/security.yml` now pins current Node24-compatible action majors for checkout, Go, Node, Terraform, and artifact upload using immutable commit SHAs. `tools/validate-ci-workflow.mjs` and its regression tests reject mutable references and legacy Node20 action majors; the exact pushed run after this update remains required as fresh release evidence.

2026-08-10 client lifecycle hardening: web and mobile API clients now perform single-flight refresh-token rotation after expired access tokens, preserve typed privileged MFA challenges instead of treating them as generic login failures, and reconnect canonical room streams with bounded backoff plus authenticated `GET /api/v1/rooms/state/{room_id}` polling fallback. Web smoke/typecheck and mobile smoke/typecheck pass locally; deployed browser/native staging smoke and recorded `SRC-CI-001` release evidence remain external readiness requirements.
2026-08-10 browser refresh custody hardening: web auth responses now set an HttpOnly SameSite=Strict refresh cookie scoped to `/api` and omit the refresh token from JSON; web requests send credentials and refresh without a token body, while mobile and compatibility callers retain bounded body-token support. Backend auth tests plus 18 web smoke tests cover cookie attributes, cookie fallback, body precedence, rotation, and no-body refresh. Deployed browser CORS/cookie/CSRF behavior and exact-release `SRC-CI-001` evidence remain external readiness requirements.
2026-08-10 WebSocket credential transport hardening: room stream clients no longer place access JWTs in `?ticket=` URLs. Web and mobile negotiate `scriptureforge-bearer` with the JWT as a subprotocol; backend middleware accepts that header only on room streams, the upgrader negotiates the named protocol, and query-token regression coverage now fails closed. Deployed WSS proxy/header behavior remains external staging evidence.
2026-08-10 browser access-token custody hardening: the web store no longer reads or writes `auth_token` in localStorage; access JWTs remain in memory, while user/org identifiers support a single-flight reload bootstrap through the HttpOnly refresh cookie. Cookie-only web logout now sends the authenticated organization context and clears the server cookie in the backend path. Web store smoke coverage proves memory-only custody, bootstrap request semantics, rejected-cookie cleanup, and cookie-only logout payload behavior. Deployed browser CORS/cookie/CSRF behavior remains external staging evidence.
2026-08-10 AI provider resilience hardening: chat and embedding calls now share bounded timeout/retry configuration, rebuild request bodies for each transient retry, stop retry backoff when the request context is canceled, retry only 429/5xx responses, and cap external provider responses at 1 MiB without returning provider body content in application errors. Focused tests prove chat/embedding request replay, cancellation, retry exhaustion, and oversized-response rejection. Real provider/degradation/citation/audit staging evidence remains external under `EXT-AI-001`.

2026-08-10 RAG fail-closed hardening: embedding, Rust gRPC, and vector-search failures now return sanitized typed `503` faults without provider or transport details. Production clients no longer synthesize embeddings when `OPENAI_API_KEY` is absent; offline tests inject an explicit local embedding function. Focused tests cover missing-key failure, provider-error redaction, typed-fault preservation, null gRPC responses, and context compilation status. Real provider/degradation/citation/audit staging evidence remains external under `EXT-AI-001`.

2026-08-11 AI audit fail-closed hardening: AI generation now requires its DB, RAG, verification, LLM, and MapReduce dependencies and returns typed `503` faults rather than serving output when `ai_request_logs` or `citation_trails` persistence fails; missing-dependency and missing-audit-DB unit coverage proves no panic or unaudited success path. Live provider and audit telemetry remains external under `EXT-AI-001`.

2026-08-11 Zoom response-boundary hardening: outbound Zoom OAuth, meeting, and status JSON responses now stop at a 1 MiB cap before decoding; transient retries no longer drain unbounded bodies, provider error bodies are excluded from application errors, and missing tokens or incomplete meeting responses fail closed with offline fallback. Focused adapter tests cover oversized responses and missing access tokens; live Zoom evidence remains external under `EXT-ZOOM-001`.

2026-08-11 LLM error-redaction hardening: network, malformed-response, and empty-response failures now return sanitized typed `503` faults without provider URLs, transport details, or raw response content. Focused tests cover timeout/detail redaction and normalized response faults; live provider evidence remains external under `EXT-AI-001`.

2026-08-11 MapReduce resource-boundary hardening: AI chunking now guarantees UTF-8-safe `MaxChunkSize` bounds, and processing uses a capped cancellation-aware worker pool with nil-input guards instead of unbounded goroutine fan-out. Focused tests cover chunk limits, ordering, concurrency bounds, cancellation, and processor failures.

2026-08-10 runtime secret hardening: JWT generation/validation now rejects keys shorter than 32 bytes; staging/production startup rejects missing, weak, or reused `JWT_SECRET_KEY` and `JOURNAL_SALT_SECRET`; journal bootstrap no longer falls back to the JWT key; and Terraform/CSI/IAM now projects a dedicated journal salt secret. Local auth, journal, deployment-skeleton, Terraform, and documentation checks remain required; cloud secret-sync, scoped access, and rotation evidence remain external under `SEC-SECRETS-001`.

2026-08-16 JWT validation contract hardening: `internal/domain/auth` now accepts only HS256 tokens with issuer `scriptureforge-platform` and rejects tokens missing non-empty user, organization, or role identity claims. Regression coverage rejects alternate HMAC algorithms, issuer substitution, and missing identity claims. This closes a local JWT contract gap within `F-AUD-002`; deployed key rotation and identity-provider evidence remain external.

2026-08-10 security evidence alignment: `tools/securityprobe` now requires `JOURNAL_SALT_SECRET` in SecretProviderClass and redacted synced-secret markers, with regression fixtures covering the complete runtime-secret set. Cloud secret-sync, scoped access, and rotation evidence remain external under `SEC-SECRETS-001`.

2026-08-10 client request deadline hardening: web and mobile API clients now enforce a configurable 1-120 second request timeout (`NEXT_PUBLIC_API_REQUEST_TIMEOUT_MS` / `EXPO_PUBLIC_API_REQUEST_TIMEOUT_MS`, default 15 seconds), preserve the deadline across refresh/retry calls, propagate caller cancellation, and return typed `NETWORK_FAULT` timeout errors. Web/mobile smoke coverage proves stalled fetches abort and the default remains bounded; deployed latency behavior remains external staging evidence.

2026-08-10 readiness dependency hardening: strict staging/production `/ready` now checks PostgreSQL and Redis plus a bounded standard gRPC health response for `scriptureforge.engine.ScriptureEngine`, so the non-blocking gRPC dial cannot make the API report ready while the AI/RAG engine is unreachable or non-serving. Local in-process tests cover serving and non-serving health states; deployed Rust health/mTLS/readiness proof remains external under `RUST-GRPC-001`.

2026-08-10 HTTP transport hardening: the Go API server now applies validated bounded read-header (5s), read (30s), write (30s), and idle (60s) deadlines plus a 1 MiB header cap by default, with bounded `HTTP_*` environment overrides. Terraform projects the same validated settings into the API workload. WebSocket handlers retain their own ping/read/write deadlines after upgrade; configuration and server-construction tests cover defaults, invalid values, and applied limits.

2026-08-10 startup dependency hardening: PostgreSQL and Redis startup pings now use a bounded dependency context instead of the process lifetime context. API database pool size/minimum/lifetime/idle settings and Redis pool size/max-active/wait/dial/read/write settings are explicit, range-validated environment values with bounded defaults and Terraform projections; pgx connection establishment also receives the startup bound. Platform tests cover defaults, invalid limits and relationships, malformed URLs, and applied client settings. Staging saturation/tuning and deployed dependency-failure evidence remain external production gates.

2026-08-10 browser boundary hardening: the API now validates the existing `ALLOWED_WS_ORIGINS` list at strict-environment startup and reuses it as the credentialed CORS allowlist. Foreign origins, invalid preflight methods/headers, and missing strict origin configuration fail closed; API responses apply `nosniff`, frame, referrer, permissions, CSP, HSTS, and no-store controls. Platform tests cover allowed requests, credentialed preflight, foreign-origin rejection, unknown-header rejection, strict origin validation, and native requests without `Origin`. Deployed browser CORS/cookie/CSRF behavior remains external staging evidence.

2026-08-10 API request lifecycle hardening: ordinary `/api/` handlers now inherit a validated `API_REQUEST_TIMEOUT_MS` context deadline (15 seconds by default, 1-120 seconds allowed), preserve any earlier client cancellation, and leave long-lived `/api/v1/rooms/stream/{room_id}` connections to their handler-owned deadlines after upgrade. Focused platform tests cover cancellation, earlier-deadline preservation, and stream exemption; Terraform projects the setting into the API workload. Deployed timeout tuning and telemetry remain external readiness evidence.

2026-08-11 graceful shutdown hardening: the API now marks `/ready` unready before termination, closes tracked room WebSockets with a going-away signal, rejects new room upgrades while draining, handles unexpected listener failure without `log.Fatal` cleanup bypass, and uses a validated `SHUTDOWN_TIMEOUT_MS` budget (10 seconds by default, 1-120 seconds allowed) projected through Terraform. Platform tests (66) and the room shutdown regression pass under `-race`; deployed pod termination-grace, ingress drain, and multi-replica evidence remain external.

2026-08-11 browser CSRF hardening: credentialed web mutations now bootstrap `GET /api/v1/auth/csrf`, receive a browser-readable SameSite=Strict token cookie, and submit a matching `X-CSRF-Token`; the API rejects missing, malformed, or mismatched browser tokens before handler execution while native callers retain compatibility. Backend security tests cover issuance, reuse, pre-handler rejection, and native compatibility; web smoke covers browser bootstrap. Deployed cookie/CORS behavior remains external under the browser staging gate.

2026-08-11 room ingress hardening: `POST /api/v1/rooms/create` now uses the shared strict JSON decoder, rejects concatenated or unknown-field payloads, caps bodies at 16 KiB, and rejects room titles over 256 bytes before database or Redis work. Focused tests cover oversized, concatenated, and overlong inputs; deployed gateway/body-limit telemetry remains external.

2026-08-16 AI nested-readiness hardening: the generation handler now rejects incomplete RAG and LLM dependency graphs before work begins, including missing vector storage, API key, endpoint, model, or bounded HTTP client. Direct RAG and LLM calls also return typed `503` faults for nil/partial wiring. Focused handler/domain tests passed; deployed provider, audit, and degradation evidence remains pending under `EXT-AI-001`.

2026-08-16 AI aggregate-output hardening: generated curriculum assembly now uses amortized buffering and an 8 MiB aggregate envelope across the bounded chunk set. Overflow is recorded as a failed AI audit attempt and returns a sanitized typed `503` without partial output; focused exact-limit, overflow, and nil-builder tests passed. Deployed provider, audit, and degradation evidence remains pending under `EXT-AI-001`.

2026-08-16 tenant list cursor hardening: active-room and journal list handlers now check `pgx.Rows.Err()` after iteration and fail with generic `500` responses plus dependency telemetry if a database cursor ends with an error, preventing truncated successful lists. Existing RLS transaction boundaries remain unchanged; deployed database failure telemetry remains pending.

2026-08-16 room state initialization hardening: `POST /api/v1/rooms/create` now returns a sanitized `503` when Redis active-state initialization fails after PostgreSQL commit and performs a tenant-RLS-scoped compensation update marking the durable room inactive. Focused handler coverage is local; the DB-backed compensation regression runs in the Postgres/RLS gate, while deployed multi-store failure telemetry remains pending.
2026-08-16 Zoom webhook RLS mapping hardening: signed Zoom callbacks now resolve `live_rooms` through a transaction-local non-tenant sentinel plus `app.webhook_lookup_verified=true` and the exact `meeting_external_id`, guarded by a dedicated FORCE-RLS SELECT policy. Mapping failures return `503` without consuming the delivery ID for provider retry; the Docker-backed RLS gate proves unverified mapping is hidden and verified exact-ID mapping succeeds. Live Zoom delivery evidence remains pending under `EXT-ZOOM-001`.

2026-08-16 room replica fan-out hardening: accepted room events now sequence, persist latest state, and publish an origin-tagged envelope in one Redis Lua transaction. Redis-backed room hubs subscribe while local clients are present, suppress their own publication before local fallback broadcast, and close subscriptions on teardown. Miniredis tests prove atomic publication, cross-replica delivery, and cleanup; deployed multi-replica WSS/Redis evidence remains pending under `PERF-WS-001` and `DATA-REDIS-001`.

2026-08-16 room connection-cap hardening: active WebSocket limits now use a Redis-backed leased semaphore across global, tenant, and user scopes instead of process-local counters in production wiring. Lua acquire/renew/release operations clean expired leases atomically; sockets renew every 30 seconds, crashed leases expire after two minutes, and Redis outages return sanitized `503` capacity faults. Miniredis tests prove cross-instance caps, renewal, expiry recovery, and fail-closed backend behavior; deployed multi-replica socket-load evidence remains pending under `PERF-WS-001`.

2026-08-16 Rust scripture-ingestion hardening: `ProcessTextEmbedding` no longer returns success without persistence. It requires a provider-generated finite 1536-dimensional vector, rejects empty or oversized text, atomically upserts the runtime `scripture_texts` schema under transaction-local tenant RLS, and returns the stored row identity. Rust vector search now uses the same RLS transaction boundary, and startup fails closed when `DATABASE_URL` is absent instead of using a placeholder. Local Rust/protobuf and migration checks cover the source contract; deployed provider-backed ingestion, semantic quality, and Rust tenant evidence remain pending under `RUST-GRPC-001` and `DATA-RLS-001`.

2026-08-16 MFA enrollment lifecycle hardening: privileged enrollment now stages the TOTP seed with `mfa_enabled=false`, returns it with no-store cache headers, and lets `/api/v1/auth/mfa/verify` activate the factor only after a valid code. Re-enrollment is rejected once a factor is active. Postgres-backed integration coverage proves the staged and activated states, wrong-code denial, cache policy, and active-factor replacement denial; deployed MFA/rate-limit observation remains pending under the existing auth/session closure evidence.

2026-08-16 mobile journal load-flow hardening: the mobile journal container now lists the authenticated user's encrypted entries, fetches a selected ciphertext record through `GET /api/v1/journal_entries/{id}`, and decrypts it only in local memory using the server-provided salt ID/version. Identity changes clear prior entries, plaintext, passphrase, and key handles, while ordinary access-token rotation preserves the active user's local work. Mobile API smoke coverage proves encrypted list/save/load transport; native-device and staging browser/mobile evidence remain pending under `CLIENT-MOBILE-001` and the existing client smoke closure.

2026-08-16 shared server crypto boundary hardening: the original Phase 02 `/pkg/crypto_utils` package now owns Argon2id password hashing and verification. It uses bounded work factors, strict stored-hash parsing, secure salts, constant-time comparison, and transient byte cleanup; `internal/domain/auth` retains compatibility aliases for existing handlers. Focused crypto/auth tests pass, and this closes the local shared-utility gap within `F-AUD-002`; production password-policy tuning and staging evidence remain external.

2026-08-16 Zoom provider budget hardening: the PR7 Zoom adapter now reads `ZOOM_HTTP_TIMEOUT_MS` and `ZOOM_MAX_RETRIES` with finite 100-30000ms and 0-3 bounds, projects those settings through Terraform, closes unexpected response bodies on retry errors, and uses a finite fallback client when direct wiring omits an HTTP client. Focused adapter tests cover defaults, configured values, invalid/excessive values, and nil-client safety. This closes the local configurable transport-budget gap within `F-AUD-010`; live Zoom timeout/circuit/fallback and webhook evidence remains external under `EXT-ZOOM-001`.

2026-08-16 auth transaction and journal salt binding hardening: successful login now inserts and commits its first opaque refresh token in the existing tenant-scoped credential transaction, avoiding nested pool acquisition while preserving refresh issuance telemetry. Encrypted journal creation now derives the current server-side per-user `journal:v1` salt ID and rejects mismatched salt IDs/versions before database work. Focused backend tests plus the disposable Postgres/RLS auth, journal, and tenant suites pass; staging auth observation and native/deployed journal evidence remain external.

2026-08-17 auth dependency fail-closed hardening: registration, login, refresh, logout, and privileged MFA handlers now return typed HTTP 503 dependency faults when the authentication database pool is absent instead of dereferencing a nil pool. Focused coverage proves every auth handler path fails closed before transaction work; deployed dependency-failure telemetry remains external.

2026-08-17 MFA assurance, encrypted seed, and deployment boundary hardening: refresh tokens now persist `mfa_verified_at`, privileged refreshes require active-factor state plus prior MFA assurance, and a member refresh token cannot mint a privileged JWT after role elevation. TOTP seeds now persist only as versioned AES-GCM envelopes under an API-only `MFA_ENCRYPTION_KEY`; missing, wrong-key, malformed, or legacy plaintext material fails closed until re-enrollment. Unknown deployment names now fail closed to Go/Rust gRPC security requirements, and Terraform separates API/Rust service accounts, IRSA policies, SecretProviderClasses, and secret mounts so the Rust engine excludes API-only credentials. Focused Go/Rust/Terraform validation passes; live MFA rotation/migration, IAM/CSI enforcement, and network evidence remain external.

2026-08-17 independent source security review retained these open boundaries for later in-scope remediation or explicit owner decision: public registration accepts a caller-selected organization UUID without invitation proof; existing deployed plaintext TOTP rows require a controlled re-enrollment migration; and live Redis ACL/network-policy/rotation proof remains external even though non-local startup now requires an API-injected password. The dedicated public Zoom webhook abuse profile is now locally implemented and CI-verified; live ingress enforcement remains external under `ABUSE-LIMIT-001`. These are not claimed closed by this change.

2026-08-17 security hardening closure: the current remediation set adds refresh-token family containment and current-MFA assurance checks, strict AI role and citation-like-claim validation, exact AI provider-host allowlisting for chat and embeddings, trusted-proxy peer-CIDR enforcement, Zoom retry idempotency plus meeting URL/ID validation and Redis-backed signed-delivery dedupe, identity-bound web/mobile journal cleanup, WebSocket event/byte budgets, Rust vector-search tenant transaction binding, immutable CI integrity inputs, and API/Rust SecretProviderClass separation so the API does not receive Rust server TLS private material. The standard Codex Security baseline was run against the pre-hardening snapshot and reported 16 findings; the four residual code findings identified there are implemented in this remediation set and require fresh post-change CI/security evidence. Public registration organization proof, legacy plaintext TOTP re-enrollment, live Redis ACL/network policy/rotation, and all deployed staging evidence remain explicitly open rather than being represented as production-ready.

2026-08-27 workload image definition hardening: the legacy API Dockerfile now builds `cmd/platform-engine` with the current Go toolchain, the Rust Dockerfile now builds the real gRPC service with the shared protobuf input, and a production Next.js web Dockerfile plus root `.dockerignore` are present. `tools/validate-deployment-skeleton.mjs` and its regression tests now reject the obsolete in-memory API and placeholder sleep entrypoints. This closes the local workload-definition gap; Docker image build/push, immutable ECR digest capture, and staging rollout evidence remain external.

2026-08-28 database transport hardening: Go and Rust startup now fail closed in staging/production unless `DATABASE_URL` parses as PostgreSQL with exactly one secure `sslmode` of `require`, `verify-ca`, or `verify-full`; local development keeps its existing relaxed URL behavior. Focused Go/Rust tests and the deployment validator cover the contract. This closes the local database-TLS configuration gap; live secret contents and deployed TLS negotiation remain external evidence.

2026-08-28 registration and room-session hardening: public registration now rejects caller-selected `organization_id` fields through strict JSON decoding, generates a fresh organization UUID server-side, and creates the organization, forced-member user, and initial refresh token in one RLS-scoped transaction. Web and mobile callers submit only a bounded `organization_name`; login, refresh, and logout continue to use the returned tenant ID. Validated JWTs now require an `exp` claim, and active room WebSockets close when that access token expires. Focused Go auth/realtime tests plus web/mobile smoke and build gates pass. This closes the local public-registration tenant-selection and active-socket token-lifetime gaps; invitation enrollment, deployed reconnect behavior, and staging evidence remain external.

2026-08-28 MFA verification abuse hardening: privileged `/api/v1/auth/mfa/verify` attempts now use a hashed organization/user identity in the distributed `auth_account` limiter in addition to the route limiter, so rotating client IPs cannot bypass the TOTP guess budget. Focused Go handler tests pass and prove the second attempt is rejected before database work without leaking tenant or user identifiers in metrics. This closes the local MFA verification brute-force gap; deployed limiter behavior and telemetry remain external.

2026-08-28 successor security hardening: refresh-token replay containment now commits descendant revocation, all unknown non-local deployment labels fail closed for secure cookies/HSTS/origin checks and web/mobile endpoint validation, production web images receive explicit API/WS endpoints, room creation requires moderator/admin authorization, provider HTTP clients reject redirects, and the legacy MFA cleanup migration requires a superuser or BYPASSRLS role. Focused Go, web, mobile, Rust, Serena/Obsidian, deployment, CI, and whitespace checks pass for this worktree successor. The sealed Codex Security scan `522240c7-4bb8-410d-b41a-ef7e8dd65d93` targeted the pre-fix revision `431e328b90ac320902b37b53fd410cb952b4d538`; remaining open/deferred boundaries include live-room session revocation, EKS secret encryption, Terraform state protection, NetworkPolicy, metrics exposure, AI retention/budget controls, registration enumeration, trusted-proxy contract evidence, and live staging/provider proof. This entry does not claim production readiness.

2026-08-28 EKS Secret protection hardening: the Terraform deployment skeleton now requires a customer-managed `eks_secrets_kms_key_arn` input and wires it to `aws_eks_cluster.encryption_config` for Kubernetes Secret envelope encryption. Deployment validation and regression tests require the variable, documented input, `resources = ["secrets"]`, and KMS key binding. This closes the repository IaC omission; staging must still prove the KMS key policy, rotation, cluster behavior, and deployed SecretProvider/IRSA path.

2026-08-28 Rust observability transport hardening: the Rust `/metrics` and `/healthz` listener now bounds the first request read with `RUST_ENGINE_METRICS_READ_TIMEOUT_MS`, clamped to 100ms-30s with a 5s default, so idle or slow clients cannot retain spawned tasks indefinitely. Rust tests pass; deployed listener and ingress behavior remain external.
2026-08-28 API metrics exposure hardening: the Go API `/metrics` endpoint now requires a constant-time `Authorization: Bearer <METRICS_AUTH_TOKEN>` comparison in non-local environments, returns a sanitized `503` when the required token is missing, and preserves explicit local/development/test scrape behavior. Terraform injects the API-only secret, deployment/observability validators guard the boundary, and Rust/observability staging probes accept `STAGING_METRICS_AUTH_TOKEN`; live secret delivery and protected Prometheus scrape evidence remain external.

2026-08-28 documentation closure: `audit_report.txt` is now explicitly labeled as a historical snapshot and directs readers to this briefing plus current local, CI, and staging evidence gates. This removes a stale report from the current readiness evidence path.

2026-08-28 local-gate resilience hardening: the disposable Docker/RLS runner now verifies the Docker daemon with `docker info` and bounds every Docker subprocess, using a longer bounded budget for image startup. Docker daemon outages now fail the local gate quickly with an explicit environment blocker instead of allowing the full matrix to hang; this preserves the fail-closed RLS evidence requirement while making local diagnostics deterministic.

2026-08-28 Terraform RDS credential-state hardening: the deployment skeleton no longer accepts or passes a root database password through Terraform variables or tfvars. Aurora now manages the RDS master password in Secrets Manager with the customer-managed database KMS key, and deployment validation plus regression coverage reject legacy root-password inputs and Terraform-managed `master_password`; the current proof marker is `database_root_secret_managed=true`. This reduces local plaintext-equivalent credential exposure in state; encrypted remote-state access, managed-secret rotation, and scoped application-user staging proof remain external.

2026-08-28 live-room session revocation hardening: logout now resolves the refresh-token owner, revokes its family, and persists `users.sessions_revoked_at` in the same tenant-scoped transaction. Default RLS-backed room streams require JWT issued-at claims and re-check room membership plus the cutoff during the heartbeat, so logout and membership removal close existing streams before access-token expiry. Focused membership-revocation, JWT-contract, migration-runner, and Postgres auth/session coverage protect the path; deployed multi-replica timing and ingress behavior remain external.

2026-08-27 MFA enrollment bootstrap hardening: privileged users without an enrolled factor now receive a short-lived `mfa_enrollment_token` only after password verification. The token is purpose-bound and middleware accepts it only for `/api/v1/auth/mfa/enroll` and `/api/v1/auth/mfa/verify`; it has no refresh token or normal application access and is rejected after activation. Enrollment and verification re-check the current database role, and web/mobile expose setup helpers plus compact memory-only setup UI. Focused auth, route, integration, client smoke, and web/mobile typecheck gates pass. This closes the local MFA enrollment deadlock and setup-token replay window; deployed MFA/rate-limit observation and native/staging evidence remain external.

2026-08-28 migration and WebSocket-origin hardening: `000005_mfa_legacy_plaintext_cleanup.up.sql` now clears legacy plaintext TOTP values and disables those factors so existing deployments can force safe re-enrollment without attempting secret conversion; its down migration fails closed because deleted secrets are not restorable. CI and the Docker RLS runner now apply the complete ordered `migrations/*.up.sql` set, including `000003`. The development WebSocket fallback now parses origins and accepts only exact `http://localhost` or `http://127.0.0.1` hosts, rejecting lookalike domains and paths. Focused Go and migration-runner tests pass; live migration execution, hosted CI, and staging evidence remain external.

## 100% Production Readiness Evidence Still Required

- Source control and CI: Commit and push the current remediation set, then prove GitHub Actions passes on the exact branch intended for release and record a passing `tools/ciprobe` report for `SRC-CI-001`.

2026-06-28 update: `SRC-CI-001` is now exact-release-bound. `tools/record-staging-evidence.mjs` rejects `ciprobe` reports whose `commit_sha` differs from the manifest `release_candidate`, and recorded CI summaries include `release_candidate=<commit_sha>` so strict staging validation can reject stale CI artifacts. Strict release validation now requires that marker on `SRC-CI-001` evidence in addition to the uploaded `ci-release-evidence` artifact and `tools/ciprobe` JSON report. Remaining production work is still a clean pushed GitHub Actions run for the exact release SHA.
- Auth/session closure: Local tests now cover registration role forcing, canonical and legacy route behavior, refresh rotation, expired/revoked refresh denial, privileged-role MFA denial/verification, logout revocation, and abusive login/refresh controls. Remaining closure is clean pushed CI evidence plus staging observation of auth/rate-limit behavior behind real ingress/client identity.
- AI runtime closure: Local tests now cover missing-key readiness behavior, bounded timeout paths, bounded retry/body replay and cancellation, oversized provider response rejection, hallucinated-citation rejection, no-citation rejection, successful `ai_request_logs` and `citation_trails` persistence, handler-level `/api/v1/ai/generate/study` audit persistence under tenant RLS, and no unit-test dependency on live provider calls. Remaining closure is staging validation against real provider configuration, timeout/retry/degradation behavior, audit persistence, and telemetry.
- Mobile/native closure: Run EAS or equivalent native-device validation proving the native AES-GCM binding works outside Node/WebCrypto test shims; keep the repository-owned Metro image-size compatibility package under review on every Expo/Metro refresh.
- Web/product closure: Mocked route smoke coverage is locally proved and wired into CI config; remaining web product closure is deployed browser smoke against staging API/WS endpoints.
- Observability closure: Local dashboard, full alert-rule suite, retention-runbook, trace/log/metrics, dependency spans, abuse limiter allow/limited profile metrics, auth/session Postgres dependency operation metrics for register/login/refresh/logout/MFA paths, journal and room Postgres/Redis dependency operation metrics, architecture-named `websocket_active_connections_count` and `ai_inference_duration_seconds` profile metrics, AI provider outcome metrics, Zoom operation/fallback metrics, artifact validation coverage plus regression tests, Go API OTLP exporter bootstrap, protected Go API `/metrics` authentication, and Terraform API env wiring exist; remaining closure is deployed OpenTelemetry collector/backend proof, production log/metric/trace retention proof, dashboard import evidence, alert delivery proof, protected staging Prometheus scrape proof, and staging telemetry proof for exported traces, request metrics, dependency spans across real services, room/socket counters, AI faults, auth abuse under real ingress identity, and dependency failures.
- Deployment closure: TLS-capable ingress skeleton, app env wiring, observability env wiring, workload secret injection skeleton, API health routes, Kubernetes probes, Rust service bind wiring, standard gRPC health service support, RDS backup/final-snapshot controls, and Kubernetes rolling-update rollback settings now exist; remaining closure is supplying real staging Terraform inputs, initializing remote state, validating ACM/DNS/collector ownership, running staging `plan`/`apply`, proving Kubernetes app wiring, Secrets Store CSI/IRSA sync, scoped `DATABASE_URL` secret, deployed ingress/TLS/readiness probes, trace export, RDS/Redis connectivity, Rust gRPC reachability, rollback exercise, backup creation, and restore drill.
- External service closure: Prove staging Zoom OAuth/webhook delivery, AI provider configuration/degradation, Redis sequencing, Postgres RLS, web deployment, and mobile app configuration using real service endpoints.
- Security closure: RLS integration tests now cover every tenant-scoped table locally, local secret hygiene validation/review plus regression tests exist, current threat/security artifacts are validated, Terraform includes IRSA/Secrets Store CSI workload secret wiring, mounted auth abuse/session tests cover refresh/MFA/logout plus route-level auth rate limiting with low-cardinality limiter metrics, DRR-001 is closed by the locked uuid 11.1.1 compatibility proof, and DRR-002 is closed by the tested local Metro image-size package. Remaining security closure is clean pushed CI TruffleHog/SAST/SCA/dependency audit evidence, deployed TLS/SSL probe checks, owner/security threat-model signoff, staging abuse telemetry under real ingress behavior, cloud secret-manager/remote-state access proof, and staging secret-sync proof.
- Performance closure: The reproducible HTTP and WebSocket harnesses exist, self-test locally, include an external authenticated HTTPS-origin WebSocket mode for deployed room streams, reject local/loopback performance artifact URLs before emitting staging evidence reports, and now require release_candidate/service_version plus shared `load_run_id` linkage across HTTP, WebSocket, and Redis staging load artifacts; remaining performance closure is staging evidence for HTTP, WebSocket fan-out across multiple API instances, Redis Lua sequence ordering under real Redis, DB/RLS query behavior, AI timeout/circuit behavior, and the architecture's stated 5,000 req/s with P99 under 200ms target or an architecture update with measured production limits.

## Current Production Readiness Verdict

Not production-ready. The codebase has useful foundations and several security-aware components, and the local Go/Rust/web/Terraform/mobile-build-compatible/load-harness/observability-artifact/secret-hygiene gate matrix is much stronger. The latest validated release `e07f423b519566277bf5f9d72681309c79d8cb00` passed hosted Security Pipeline Verification run `33184095128`, including PostgreSQL/RLS integration, the synchronized Obsidian snapshot, secret scanning, and CI release-evidence upload. The environment-specific manifest still lacks the required HTTPS `SRC-CI-001` recording plus EAS/native mobile evidence, actual production observability collector/backend/import/alert delivery proof, protected staging metrics scrape proof, staging performance/load proof, staging deployment proof, external service readiness evidence, and cloud secret-manager/remote-state proof. DRR-002 is closed locally through the repository-owned bounded Metro image-size package, but it remains a maintenance re-evaluation point for future Expo/Metro upgrades.
