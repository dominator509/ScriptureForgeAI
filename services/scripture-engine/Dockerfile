FROM rust:1.76

WORKDIR /usr/src/scripture-engine
COPY . .

# Since it's a dummy setup for now, we'll just run a simple sleep loop
# Normally this would be `cargo run` or building a binary
CMD ["sleep", "infinity"]
